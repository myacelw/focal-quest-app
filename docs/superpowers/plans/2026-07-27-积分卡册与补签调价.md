# 积分卡册 + 补签调价 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给积分开一个永久的、不花真钱的出口——花 500 分开卡包抽卡收进卡册；同时把补签从「50 分 / 每月 2 次」改成「500 分 / 每月 3 次」，让主闸门从次数换成价格。

**Architecture:** 卡片是纯数据（`src/cards/card-defs.ts` 两套各 32 张，图用两张 4×4 宫格 webp + `background-position` 切格，与勋章同一套做法）。抽卡逻辑从既有 `pickCapture` 里抽出泛型纯函数 `pickWeighted` 供图鉴与卡包共用。持久化新增 Dexie v7 的 `cards` 表并接入云同步（同步类别从 7 类变 8 类，合并取最早 `obtainedAt`）。开包在一个 Dexie 事务里同时给卡与扣分。

**Tech Stack:** TypeScript + React + Vite；Dexie.js（IndexedDB）；vitest + fake-indexeddb；Cloudflare Pages Functions + D1（同步后端）。

## Global Constraints

- **卡 id 不得含冒号。** 同步 uuid 形如 `card:default:pony-07`，`naturalKeyFromUuid` 靠「自然键侧不含冒号」取最后一个 `:` 之后来反解。
- **`card` 合并取最早 `obtainedAt`，不用 LWW。** 与 `monster.capturedAt` 同理：「首次获得时刻」才是正确语义。
- **pack 兑换记录恒为 `status: 'fulfilled'`，永不进 pending、永不可撤销。** 撤销会退分而不收回卡。
- **卡册入口不进底部导航。** 手机断点 560px 是按「5 项英文导航单行所需 531px」算出来的，第 6 项会让英文导航折行。
- **前后端两份 `KINDS` 必须逐字一致**（`src/sync/sync-keys.ts` 与 `functions/lib/sync-validate.ts`）。服务端 kind 白名单是整批全或无，一条未知 kind 让整批 400，而 400 被判永久错误。
- **定价常量集中，一处可调**：`PACK_COST` / `REPAIR_COST` / `REPAIR_MONTHLY_MAX`。
- **中英文案必须成对。** `src/i18n.test.ts` 用 `hasKey('zh'|'en', k)` 逐键校验，只加一种语言会红。
- **前置条件：必须在「训练完成门槛」分支合并进 master 之后，从 master 起新分支。** 本计划要改 `src/rewards/ledger.ts`、`src/rewards/rewards-service.ts`、`src/i18n.tsx`，门槛迭代此刻正在改同样这几个文件。
- **开工第一步先跑 `npm test` 记下真实基线条数**（spec 里写的 555 个 / 58 文件是 2026-07-27 快照，门槛迭代正在往里加）。

---

## File Structure

**新建**
- `src/data/pick-weighted.ts` — 泛型加权抽取纯函数（图鉴与卡包共用）
- `src/data/pick-weighted.test.ts`
- `src/cards/card-defs.ts` — 两套卡的纯数据 + 宫格定位派生
- `src/cards/card-defs.test.ts`
- `src/cards/pack.ts` — `PACK_COST`、权重、`pickCard`
- `src/cards/pack.test.ts`
- `src/cards/cards-service.ts` — `openPack`（事务）、进度查询
- `src/cards/cards-service.test.ts`
- `src/cards/CardImage.tsx` — 宫格切格 + 缺图回落
- `src/cards/CardAlbum.tsx` — 卡册页
- `src/sync/kinds-parity.test.ts` — 前后端 KINDS 一致性闸门
- `src/nav.test.ts` — 导航仍是 5 项的回归锚

**修改**
- `src/rewards/ledger.ts` — 涨价、上限常量化、注释更新
- `src/rewards/ledger.test.ts` — 跟改断言
- `src/rewards/rewards-service.ts` — `RepairStatus.monthlyMax`、`doRepair` 包事务
- `src/rewards/rewards-service.test.ts`（若不存在则新建）— 事务原子性
- `src/HomePage.tsx` — 补签文案传 `max`、新增卡册入口卡
- `src/i18n.tsx` — 15 条卡册文案（中英）+ `repair.monthLimit` 参数化
- `src/i18n.test.ts` — `CARD_KEYS`
- `src/data/db.ts` — `CardRow`、`RedemptionRow.kind` 加 `'pack'`、Dexie v7
- `src/data/db-migrate.test.ts` — v6→v7
- `src/data/api.ts` — `TABLE_OF`、`pushCards`、`pushAll`
- `src/sync/sync-keys.ts` — `KINDS`、`KEYED_KINDS`、`naturalKeyOf`
- `src/sync/sync-keys.test.ts`
- `src/sync/merge.ts` — `EARLIEST_FIELD`
- `src/sync/engine.ts` — `TABLE_OF`（**第二份，别漏**）
- `src/sync/migrate-fields.ts` — `TIME_FIELD`
- `src/dex/capture.ts` — `pickCapture` 改调 `pickWeighted`
- `src/training/sfx.ts` — 新增 `'shiny'`
- `src/App.tsx` — `View` 加 `'cards'`、导出 `NAV`、接线
- `functions/lib/sync-validate.ts` — `KINDS`
- `functions/lib/sync-validate.test.ts`

**用户交付物（不在代码任务里）**
- `public/cards/pony/sheet1.webp`、`sheet2.webp`
- `public/cards/deep/sheet1.webp`、`sheet2.webp`

各 1536×1536 / webp q85（单卡 384×384）。**每套第 29-32 张（即 sheet2 最后一行 4 格）是闪卡，出图时要画得最华丽。** 图没到之前 `CardImage` 会回落到「渐变 + 编号」，功能可完整开发与测试。

---

# 阶段一 · 补签调价

## Task 1: 补签涨价与月上限常量化

**Files:**
- Modify: `src/rewards/ledger.ts:4-5`（常量）、`:82-112`（注释与 `canRepair`）
- Modify: `src/rewards/rewards-service.ts:77-104`（`RepairStatus` 加 `monthlyMax`）
- Modify: `src/HomePage.tsx:133-135`（文案传参）
- Modify: `src/i18n.tsx:364`（zh）、`:865`（en）
- Test: `src/rewards/ledger.test.ts`

**Interfaces:**
- Consumes: 无（本任务是起点）
- Produces: `REPAIR_COST: number`（=500）、`REPAIR_MONTHLY_MAX: number`（=3）从 `src/rewards/ledger.ts` 导出；`RepairStatus` 新增字段 `monthlyMax: number`

- [ ] **Step 1: 先记基线**

Run: `npm test`
把「N passed / M files」记在提交信息里，后续每个任务都对着它看有没有意外变红。

- [ ] **Step 2: 写失败的测试**

在 `src/rewards/ledger.test.ts` 顶部 import 里加上 `REPAIR_MONTHLY_MAX`，然后追加：

```ts
describe('补签定价（改这两个常量即可调价 / 调松紧）', () => {
  it('补签 500 分、每月上限 3 次', () => {
    expect(REPAIR_COST).toBe(500)
    expect(REPAIR_MONTHLY_MAX).toBe(3)
  })

  it('本月已补 3 次 → month-limit（判据用常量，不是裸数字）', () => {
    const target = { missedDate: '2026-07-19', phantomStreak: 3, phantomTotal: 100 }
    expect(canRepair({ target, monthRepairCount: REPAIR_MONTHLY_MAX, available: 999, cost: REPAIR_COST }))
      .toEqual({ ok: false, reason: 'month-limit' })
  })

  it('本月已补 2 次仍可补 —— 上限从 2 放宽到 3 的回归锚', () => {
    const target = { missedDate: '2026-07-19', phantomStreak: 3, phantomTotal: 100 }
    expect(canRepair({ target, monthRepairCount: 2, available: 999, cost: REPAIR_COST }))
      .toEqual({ ok: true })
  })
})
```

同时把既有那条 `it('本月已补 2 次：month-limit', …)`（在 `describe('canRepair')` 里，约 `ledger.test.ts:104-106`）改成：

```ts
  it('本月已补 3 次：month-limit', () => {
    expect(canRepair({ ...base, monthRepairCount: 3 })).toEqual({ ok: false, reason: 'month-limit' })
  })
```

**不要动**那些显式传 `cost: 50` 的 `attempted` 排序测试（约 `:132-157`）——它们验的是原因码优先级而不是价格，显式传参照样成立，改它们只会跟门槛迭代抢同一片代码。

- [ ] **Step 3: 跑测试确认它红**

Run: `npx vitest run src/rewards/ledger.test.ts`
Expected: FAIL —— `REPAIR_MONTHLY_MAX` 未导出（import 报错）、`REPAIR_COST` 是 50 而非 500、`monthRepairCount: 2` 那条期望 `ok: true` 但实际 `month-limit`。

- [ ] **Step 4: 改常量与 `canRepair`**

`src/rewards/ledger.ts` 顶部：

```ts
/** 补签价（≈ 孩子练一天所得，真有痛感但救得起；改这一处即可调价） */
export const REPAIR_COST = 500

/**
 * 每月补签上限。3 次 = 一个月最多漏 3 天 ≈ 90% 依从率。
 * 价格才是主闸门，这条只是兜底——防止「攒够分就无限买连续」。
 */
export const REPAIR_MONTHLY_MAX = 3
```

`canRepair` 里把裸 `2` 换掉：

```ts
  if (p.monthRepairCount >= REPAIR_MONTHLY_MAX) return { ok: false, reason: 'month-limit' }
```

- [ ] **Step 5: 更新 `canRepair` 的注释（它现在的论据已失效）**

`src/rewards/ledger.ts` 里 `targetAttempted` 那段注释目前写的是「而 50 分只相当于 1-2 天打卡分，比认真练一轮还便宜」。涨到 500 后这句不成立了。替换成：

```
 * `targetAttempted`（训练完成门槛的配套闸门，spec §5.6）：缺口日若「练了但没练够」，
 * 不可补。否则「每天挂机 30 秒 + 花分补签」就能把门槛彻底架空。
 *
 * ⚠️ 这条闸门的理由是**语义**而不是价格：补签卡是为"当天根本没机会练"（生病/外出）
 * 设计的，而"练了没练够"是当天可挽回的（结算页就有「再练一轮」）。
 * 补签价从 50 涨到 500 之后，挂机买连续在经济上已经很不划算，但闸门照旧保留——
 * 别因为"现在贵了"就把它删掉。
```

- [ ] **Step 6: `RepairStatus` 带上 `monthlyMax`，让 UI 不必 import 常量**

`src/rewards/rewards-service.ts`：

```ts
export interface RepairStatus extends RepairEligibility {
  streak: number       // 补签后可保住/达到的连续天数
  cost: number
  monthlyMax: number   // 每月上限，供文案显示（UI 不直接 import 常量）
  missedDate: string   // 漏掉的那天（= 昨天）
}
```

`getRepairStatus` 的返回对象里加一行 `monthlyMax: REPAIR_MONTHLY_MAX,`，并把 import 从 `REPAIR_COST` 扩成 `REPAIR_COST, REPAIR_MONTHLY_MAX`。

- [ ] **Step 7: i18n 参数化**

`src/i18n.tsx` 两处：

```ts
  'repair.monthLimit': '本月补签次数已用完（每月 {max} 次）',
```
```ts
  'repair.monthLimit': 'No repairs left this month ({max}/month)',
```

`src/HomePage.tsx` 调用处：

```tsx
        {repair && !repair.ok && repair.reason === 'month-limit' && (
          <div className="fq-card" style={{ color: 'var(--muted)', fontSize: 13 }}>{t('repair.monthLimit', { max: repair.monthlyMax })}</div>
        )}
```

- [ ] **Step 8: 跑测试确认全绿**

Run: `npm test`
Expected: PASS，且总数 = Step 1 基线 + 3。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 9: 提交**

```bash
git add src/rewards/ledger.ts src/rewards/ledger.test.ts src/rewards/rewards-service.ts src/HomePage.tsx src/i18n.tsx
git commit -m "feat(补签): 50 分/月2次 → 500 分/月3次，主闸门从次数换成价格"
```

---

## Task 2: `doRepair` 包进事务

**Files:**
- Modify: `src/rewards/rewards-service.ts:110-144`
- Test: `src/rewards/rewards-service.test.ts`（新建）

**Interfaces:**
- Consumes: `REPAIR_COST` / `REPAIR_MONTHLY_MAX`（Task 1）
- Produces: `doRepair(today: string): Promise<boolean>` 行为不变，但两张表的写入变成原子

为什么现在做：补签从 50 涨到 500，「扣了分却没补上签」从小毛病变成大事故。

- [ ] **Step 1: 写失败的测试**

新建 `src/rewards/rewards-service.test.ts`：

```ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest'
import { db } from '../data/db'
import { doRepair } from './rewards-service'

/** 造出「前天打过卡、昨天漏了、今天还没打」的可补局面，并给足积分 */
async function seedRepairable(): Promise<void> {
  await db.checkins.clear()
  await db.redemptions.clear()
  // 前天：streak 3、累计 900 分（够付 500）
  await db.checkins.put({ date: '2026-07-25', streak: 3, dailyPoints: 300, totalPoints: 900 })
}

describe('doRepair 的原子性', () => {
  beforeEach(async () => { await seedRepairable() })
  afterEach(() => { vi.restoreAllMocks() })

  it('成功时两张表都写：兑换记录 + 补插的打卡行', async () => {
    expect(await doRepair('2026-07-27')).toBe(true)
    expect(await db.redemptions.count()).toBe(1)
    const phantom = await db.checkins.get('2026-07-26')
    expect(phantom?.streak).toBe(4)
    expect(phantom?.dailyPoints).toBe(0)     // 补插行不虚涨积分
    expect(phantom?.totalPoints).toBe(900)   // 累计沿用缺口前一天
  })

  it('补插打卡失败时兑换记录一起回滚 —— 绝不出现"扣了 500 分却没补上签"', async () => {
    vi.spyOn(db.checkins, 'put').mockRejectedValueOnce(new Error('boom'))
    await expect(doRepair('2026-07-27')).rejects.toThrow()
    expect(await db.redemptions.count()).toBe(0)
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/rewards/rewards-service.test.ts`
Expected: 第二条 FAIL —— 当前实现先 `redemptions.add` 再 `checkins.put`，两者不在同一事务，`redemptions.count()` 会是 1。

- [ ] **Step 3: 改成事务**

把 `src/rewards/rewards-service.ts` 里 `doRepair` 从 `const now = Date.now()` 到 `return true` 的整段替换成：

```ts
  const now = Date.now()
  const redemption: RedemptionRow = {
    kind: 'repair', title: 'repair', cost: REPAIR_COST,
    createdAt: now, createdDate: toDateStr(new Date(now)),
    status: 'fulfilled', fulfilledAt: now, repairDate: target.missedDate,
  }
  const phantom: CheckinRow = {
    date: target.missedDate, streak: target.phantomStreak, dailyPoints: 0, totalPoints: target.phantomTotal,
  }
  // 路径 B：今天已打卡被重置，把今天行接续（只改 streak，保留已赚 dailyPoints/totalPoints）。
  // 事务外先算好，事务里只写、事务后统一 push。
  const todayRow = target.fixTodayStreak !== undefined ? checkins.find((c) => c.date === today) : undefined
  const fixedToday: CheckinRow | undefined =
    todayRow !== undefined ? { ...todayRow, streak: target.fixTodayStreak! } : undefined

  // 一个事务包住"记消耗 + 补插打卡"。不包事务时中间失败会扣了分却没补上签——
  // 补签价 500 之后这是真事故，而且账本对不上没法自动修。
  let id = 0
  await db.transaction('rw', db.redemptions, db.checkins, async () => {
    id = await db.redemptions.add(redemption)
    await db.checkins.put(phantom)
    if (fixedToday !== undefined) await db.checkins.put(fixedToday)
  })

  pushRedemptions([{ ...redemption, id }])
  pushCheckin(phantom)
  if (fixedToday !== undefined) pushCheckin(fixedToday)
  return true
```

- [ ] **Step 4: 跑测试确认全绿**

Run: `npm test`
Expected: PASS，比 Task 1 结束时多 2 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 5: 提交**

```bash
git add src/rewards/rewards-service.ts src/rewards/rewards-service.test.ts
git commit -m "fix(补签): doRepair 的记消耗与补插打卡包进一个事务"
```

---

# 阶段二 · 卡册

## Task 3: 把加权抽取抽成共用纯函数

**Files:**
- Create: `src/data/pick-weighted.ts`
- Create: `src/data/pick-weighted.test.ts`
- Modify: `src/dex/capture.ts:21-51`

**Interfaces:**
- Consumes: 无
- Produces: `pickWeighted<T, K extends string>(remaining: readonly T[], tierOf: (item: T) => K, weights: Record<K, number>, tierOrder: readonly K[], rand: number): T | null`

为什么要抽：`pickCapture` 把 `MONSTER_DEFS` 写死在函数体里，卡包要按「套」抽，没法直接调用。而里面那套「某档抽空归一化 + 用区段余量算第二个分数」带着一条修过 bug 的注释，照抄一份必然漂移。

- [ ] **Step 1: 写失败的测试**

新建 `src/data/pick-weighted.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { pickWeighted } from './pick-weighted'

type Tier = 'a' | 'b'
const ORDER: readonly Tier[] = ['a', 'b']
const W: Record<Tier, number> = { a: 80, b: 20 }

interface Item { id: string; tier: Tier }
const tierOf = (i: Item) => i.tier

describe('pickWeighted', () => {
  it('空池返回 null', () => {
    expect(pickWeighted<Item, Tier>([], tierOf, W, ORDER, 0.5)).toBeNull()
  })

  it('rand 落在第一档权重区段内就抽第一档', () => {
    const pool: Item[] = [{ id: 'a1', tier: 'a' }, { id: 'b1', tier: 'b' }]
    // total=100，rand=0.5 → target=50 < 80 → 档 a
    expect(pickWeighted(pool, tierOf, W, ORDER, 0.5)?.id).toBe('a1')
    // rand=0.9 → target=90 ≥ 80 → 档 b
    expect(pickWeighted(pool, tierOf, W, ORDER, 0.9)?.id).toBe('b1')
  })

  it('某档池空时权重归一化到剩余档（不会白占概率而返回 null）', () => {
    const onlyB: Item[] = [{ id: 'b1', tier: 'b' }]
    // 档 a 为空 → total 只剩 20，任何 rand 都必须抽到 b
    for (const r of [0, 0.1, 0.5, 0.99]) {
      expect(pickWeighted(onlyB, tierOf, W, ORDER, r)?.id).toBe('b1')
    }
  })

  it('池内位置不偏置 —— 用区段余量算第二个分数，不复用 rand 本身', () => {
    // 这是 capture.ts 里修过的那个 bug 的回归锚：直接拿 rand 映射下标的话，
    // 低权重档（rand 只可能落在区段尾部）永远只抽到池尾那个。
    const pool: Item[] = [
      { id: 'a1', tier: 'a' }, { id: 'a2', tier: 'a' },
      { id: 'b1', tier: 'b' }, { id: 'b2', tier: 'b' },
    ]
    // 档 b 的区段是 [80,100)。区段内前半 → b1，后半 → b2
    expect(pickWeighted(pool, tierOf, W, ORDER, 0.85)?.id).toBe('b1')
    expect(pickWeighted(pool, tierOf, W, ORDER, 0.95)?.id).toBe('b2')
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/data/pick-weighted.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `pick-weighted.ts`**

```ts
/**
 * 按档位权重从剩余池里抽 1 个。原本内联在 `src/dex/capture.ts` 的 `pickCapture` 里，
 * 卡包要按「套」抽、复用不了那份写死 MONSTER_DEFS 的实现，故抽成共用纯函数
 * （`key-map.ts` 从 TrainingPage 抽出来是同一个理由：两处各写一份必然漂移）。
 *
 * 档位类型泛型化，**不能 import `Rarity`**——它住在 `src/dex/`，
 * 让 `src/data/` 依赖 `src/dex/` 是反向依赖。
 *
 * 两条不可动的性质：
 *  ① 某档池空后，其权重归一化到剩余非空档。否则空档白占概率，抽到它时只能返回 null。
 *  ② 池内位置**必须用区段余量重新算一个分数**，不能复用 `rand` 本身——
 *    `rand` 已被约束在被选档的权重区段内，直接拿它映射下标会让低权重档只抽到池尾。
 *    （这一条是既有实现里修过的 bug，`pick-weighted.test.ts` 有回归锚。）
 *
 * `tierOrder` 决定档位的遍历顺序，故同一个 `rand` 恒定映射到同一个结果（可确定性测试）。
 */
export function pickWeighted<T, K extends string>(
  remaining: readonly T[],
  tierOf: (item: T) => K,
  weights: Record<K, number>,
  tierOrder: readonly K[],
  rand: number,
): T | null {
  if (remaining.length === 0) return null

  const buckets = new Map<K, T[]>()
  for (const tier of tierOrder) buckets.set(tier, [])
  for (const item of remaining) buckets.get(tierOf(item))?.push(item)

  const nonEmpty = tierOrder.filter((tier) => (buckets.get(tier) ?? []).length > 0)
  if (nonEmpty.length === 0) return null

  // 归一化：池空的档权重不计
  const totalWeight = nonEmpty.reduce((sum, tier) => sum + weights[tier], 0)
  const target = rand * totalWeight

  // 选档，并记录 target 落在被选档权重区段内的起点
  let acc = 0
  let chosen: K = nonEmpty[nonEmpty.length - 1] // 兜底：rand 接近 1 时取最后一档
  let segStart = totalWeight - weights[chosen]
  for (const tier of nonEmpty) {
    if (target < acc + weights[tier]) { chosen = tier; segStart = acc; break }
    acc += weights[tier]
  }

  const pool = buckets.get(chosen)!
  const frac = (target - segStart) / weights[chosen]
  return pool[Math.min(pool.length - 1, Math.floor(frac * pool.length))]
}
```

- [ ] **Step 4: 跑新测试确认绿**

Run: `npx vitest run src/data/pick-weighted.test.ts`
Expected: PASS（4 条）。

- [ ] **Step 5: 让 `pickCapture` 改调它**

`src/dex/capture.ts`：把 `RARITY_LIST` 的声明改成带类型的只读数组，并把 `pickCapture` 整个函数体替换掉。保留原有的文档注释与 `WEIGHTS`。

```ts
import { pickWeighted } from '../data/pick-weighted'

const RARITY_LIST: readonly Rarity[] = ['common', 'rare', 'epic']

/**
 * 从未拥有的怪兽中按 source 对应权重抽 1 只；全集返回 null。
 * 抽取算法（含"某档抽空归一化"与"池内位置不偏置"两条性质）已抽到
 * `src/data/pick-weighted.ts`，与卡包共用。
 */
export function pickCapture(ownedIds: string[], source: CaptureSource, rand: number): MonsterDef | null {
  const owned = new Set(ownedIds)
  const remaining = MONSTER_DEFS.filter((m) => !owned.has(m.id))
  return pickWeighted(remaining, (m) => m.rarity, WEIGHTS[source], RARITY_LIST, rand)
}
```

- [ ] **Step 6: 跑全量测试 —— dex 既有测试必须全绿**

Run: `npm test`
Expected: PASS，比 Task 2 结束时多 4 条，**且 `src/dex/capture.test.ts` 一条不红**。这是「重构没改变行为」的唯一证据；若有红，是重构错了，不要改测试去迁就。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 7: 提交**

```bash
git add src/data/pick-weighted.ts src/data/pick-weighted.test.ts src/dex/capture.ts
git commit -m "refactor(抽卡): 加权抽取抽成 pickWeighted 纯函数，图鉴与卡包共用"
```

---

## Task 4: 卡套数据层

**Files:**
- Create: `src/cards/card-defs.ts`
- Create: `src/cards/card-defs.test.ts`

**Interfaces:**
- Consumes: `asset(path: string): string`（`src/data/asset.ts`）
- Produces:
  - `type CardRarity = 'common' | 'rare' | 'shiny'`
  - `CARD_RARITY_ORDER: readonly CardRarity[]`
  - `interface CardDef { id: string; setId: string; rarity: CardRarity; nameKey: string; sheet: 1 | 2; index: number }`
  - `interface CardSet { id: string; nameKey: string; cards: CardDef[] }`
  - `CARD_SETS: CardSet[]`、`PER_SET: number`（=32）
  - `cardSetById(setId: string): CardSet | undefined`
  - `getCardDef(id: string): CardDef | undefined`
  - `cardSheetPos(def: CardDef): { row: number; col: number }`
  - `cardSheetUrl(def: CardDef): string`

- [ ] **Step 1: 写失败的测试**

新建 `src/cards/card-defs.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { CARD_SETS, PER_SET, cardSetById, getCardDef, cardSheetPos, type CardRarity } from './card-defs'

describe('卡套数据', () => {
  it('两套，每套 32 张', () => {
    expect(CARD_SETS.map((s) => s.id)).toEqual(['pony', 'deep'])
    expect(PER_SET).toBe(32)
    for (const s of CARD_SETS) expect(s.cards.length).toBe(32)
  })

  it('卡 id 全局唯一', () => {
    const ids = CARD_SETS.flatMap((s) => s.cards.map((c) => c.id))
    expect(new Set(ids).size).toBe(ids.length)
  })

  it('卡 id 不含冒号 —— 同步 uuid 反解靠"自然键侧不含冒号"（sync-keys.naturalKeyFromUuid）', () => {
    for (const s of CARD_SETS) {
      for (const c of s.cards) expect(c.id.includes(':'), c.id).toBe(false)
    }
  })

  it('每套稀有度分配 = 普通 18 / 稀有 10 / 闪卡 4', () => {
    for (const s of CARD_SETS) {
      const n: Record<CardRarity, number> = { common: 0, rare: 0, shiny: 0 }
      for (const c of s.cards) n[c.rarity]++
      expect(n).toEqual({ common: 18, rare: 10, shiny: 4 })
    }
  })

  it('闪卡恰好是每套最后 4 张，即 sheet2 的最后一行 —— 出图时那 4 格要画得最华丽', () => {
    for (const s of CARD_SETS) {
      const shiny = s.cards.filter((c) => c.rarity === 'shiny')
      expect(shiny.map((c) => c.id)).toEqual([29, 30, 31, 32].map((n) => `${s.id}-${n}`))
      expect(shiny.map((c) => c.sheet)).toEqual([2, 2, 2, 2])
      expect(shiny.map((c) => c.index)).toEqual([12, 13, 14, 15])
    }
  })

  it('(sheet, index) 恰好覆盖两张 4×4 宫格的 0..15，无重无漏', () => {
    for (const s of CARD_SETS) {
      for (const sheet of [1, 2] as const) {
        const idx = s.cards.filter((c) => c.sheet === sheet).map((c) => c.index).sort((a, b) => a - b)
        expect(idx).toEqual([...Array(16).keys()])
      }
    }
  })

  it('cardSheetPos 用 4×4 行列（与 BadgeCard.spritePos 同一套约定）', () => {
    const set = cardSetById('pony')!
    expect(cardSheetPos(set.cards[0])).toEqual({ row: 0, col: 0 })   // 第 1 张
    expect(cardSheetPos(set.cards[5])).toEqual({ row: 1, col: 1 })   // 第 6 张 → index 5
    expect(cardSheetPos(set.cards[31])).toEqual({ row: 3, col: 3 })  // 第 32 张 → sheet2 index 15
  })

  it('nameKey 形如 card.<setId>.<nn>，套名形如 card.set.<setId>', () => {
    const set = cardSetById('deep')!
    expect(set.nameKey).toBe('card.set.deep')
    expect(set.cards[6].nameKey).toBe('card.deep.07')
  })

  it('按 id 查得到 def，未知 id 得 undefined', () => {
    expect(getCardDef('pony-07')?.setId).toBe('pony')
    expect(getCardDef('nope-99')).toBeUndefined()
    expect(cardSetById('nope')).toBeUndefined()
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/cards/card-defs.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `card-defs.ts`**

```ts
import { asset } from '../data/asset'

/**
 * 卡册的纯数据。**加一套 = 往 CARD_SETS 加一条 + 放两张宫格图 + 加 i18n 文案，
 * 不动任何逻辑代码。**
 *
 * 稀有度用 'shiny'（闪卡）而不是图鉴那套 'epic'：一是贴合集卡语境，二是刻意
 * 不 import `src/dex/monster-defs.ts` 的 `Rarity`——卡册与图鉴是两个独立系统，
 * 共用类型只会让将来任一边加档位时牵连另一边。
 */
export type CardRarity = 'common' | 'rare' | 'shiny'

/** 档位遍历顺序，喂给 pickWeighted；顺序固定才能确定性测试 */
export const CARD_RARITY_ORDER: readonly CardRarity[] = ['common', 'rare', 'shiny']

export interface CardDef {
  /** 主键，如 'pony-07'。**不得含冒号**（同步 uuid 反解依赖，见 sync-keys.naturalKeyFromUuid） */
  id: string
  setId: string
  rarity: CardRarity
  /** 完整 i18n key，渲染时 t(nameKey)。缺失时 UI 回落到「套名 #编号」（见 CardAlbum.cardName） */
  nameKey: string
  /** 在哪张宫格图：第 1-16 张在 sheet1，第 17-32 张在 sheet2 */
  sheet: 1 | 2
  /** 宫格图里的格子，0..15（行优先） */
  index: number
}

export interface CardSet {
  id: string
  nameKey: string
  cards: CardDef[]
}

/** 每套张数 = 两张 4×4 宫格图 */
export const PER_SET = 32

/**
 * 稀有度按位置分配：第 1-18 张普通、19-28 稀有、29-32 闪卡。
 * 位置固定是刻意的——闪卡恒落在 sheet2 最后一行，出图时那 4 格画得最华丽即可，
 * 不必在代码与素材之间来回对照编号。
 */
const RARITY_BY_POSITION: readonly CardRarity[] = [
  ...Array<CardRarity>(18).fill('common'),
  ...Array<CardRarity>(10).fill('rare'),
  ...Array<CardRarity>(4).fill('shiny'),
]

function makeSet(setId: string): CardSet {
  const cards: CardDef[] = RARITY_BY_POSITION.map((rarity, i) => {
    const n = i + 1
    const nn = String(n).padStart(2, '0')
    return {
      id: `${setId}-${n}`,
      setId,
      rarity,
      nameKey: `card.${setId}.${nn}`,
      sheet: (n <= 16 ? 1 : 2) as 1 | 2,
      index: (n - 1) % 16,
    }
  })
  return { id: setId, nameKey: `card.set.${setId}`, cards }
}

/** 首批两套：魔法小马、深海精灵 */
export const CARD_SETS: CardSet[] = [makeSet('pony'), makeSet('deep')]

export const TOTAL_CARDS = CARD_SETS.reduce((sum, s) => sum + s.cards.length, 0)

export function cardSetById(setId: string): CardSet | undefined {
  return CARD_SETS.find((s) => s.id === setId)
}

export function getCardDef(id: string): CardDef | undefined {
  for (const s of CARD_SETS) {
    const found = s.cards.find((c) => c.id === id)
    if (found) return found
  }
  return undefined
}

/** 宫格图里的行列（与 BadgeCard.spritePos 同一套 4×4 约定） */
export function cardSheetPos(def: CardDef): { row: number; col: number } {
  return { row: Math.floor(def.index / 4), col: def.index % 4 }
}

/** 宫格图 URL。路径不写进数据，避免 64 条重复字符串 */
export function cardSheetUrl(def: CardDef): string {
  return asset(`/cards/${def.setId}/sheet${def.sheet}.webp`)
}
```

⚠️ 注意 `id` 用 `${setId}-${n}` 而 `nameKey` 用补零的 `nn`：测试里 `pony-7` 与 `card.pony.07` 是刻意不同的形状，别改成一致——id 短、key 对齐好读。若你更喜欢统一，改测试和实现两处一起改。

- [ ] **Step 4: 跑测试确认绿**

Run: `npx vitest run src/cards/card-defs.test.ts`
Expected: PASS（9 条）。

- [ ] **Step 5: 提交**

```bash
git add src/cards/card-defs.ts src/cards/card-defs.test.ts
git commit -m "feat(卡册): 两套 32 张的卡套数据层（宫格定位 + 稀有度分配）"
```

---

## Task 5: 抽卡层

**Files:**
- Create: `src/cards/pack.ts`
- Create: `src/cards/pack.test.ts`

**Interfaces:**
- Consumes: `pickWeighted`（Task 3）、`CARD_SETS` / `CARD_RARITY_ORDER` / `CardDef` / `CardRarity` / `CardSet` / `cardSetById`（Task 4）
- Produces: `PACK_COST: number`（=500）、`PACK_WEIGHTS: Record<CardRarity, number>`、`pickCard(ownedIds: readonly string[], set: CardSet, rand: number): CardDef | null`

- [ ] **Step 1: 写失败的测试**

新建 `src/cards/pack.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { PACK_COST, PACK_WEIGHTS, pickCard } from './pack'
import { cardSetById } from './card-defs'

/** mulberry32：确定性伪随机，避免统计性断言 flaky */
function prng(seed: number): () => number {
  let a = seed >>> 0
  return () => {
    a = (a + 0x6d2b79f5) >>> 0
    let t = a
    t = Math.imul(t ^ (t >>> 15), t | 1)
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61)
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296
  }
}

const pony = cardSetById('pony')!
const deep = cardSetById('deep')!

describe('卡包', () => {
  it('单价 500 分，权重闪卡最低', () => {
    expect(PACK_COST).toBe(500)
    expect(PACK_WEIGHTS).toEqual({ common: 65, rare: 28, shiny: 7 })
  })

  it('只从该套未拥有的卡里抽', () => {
    const owned = pony.cards.slice(0, 31).map((c) => c.id)
    for (const r of [0, 0.3, 0.7, 0.99]) {
      expect(pickCard(owned, pony, r)?.id).toBe(pony.cards[31].id)
    }
  })

  it('该套集齐返回 null', () => {
    expect(pickCard(pony.cards.map((c) => c.id), pony, 0.5)).toBeNull()
  })

  it('跨套互不影响：拥有整套 pony 不妨碍抽 deep', () => {
    const got = pickCard(pony.cards.map((c) => c.id), deep, 0.5)
    expect(got?.setId).toBe('deep')
  })

  it('闪卡在抽取顺序上系统性偏后 —— 集齐路上后段更有惊喜', () => {
    // 这条对冲"一套要练两个月、中途会腻"的风险，是 spec §4.4 唯一可验证的形式。
    // ⚠️ 不要写成"最后 4 张必是闪卡"：权重归一化只保证前两档抽空后剩闪卡，
    //    闪卡本身也可能被早早抽到。
    let commonPosSum = 0, commonN = 0, shinyPosSum = 0, shinyN = 0
    for (let seed = 1; seed <= 200; seed++) {
      const rnd = prng(seed)
      const owned: string[] = []
      for (let pos = 1; pos <= pony.cards.length; pos++) {
        const c = pickCard(owned, pony, rnd())!
        owned.push(c.id)
        if (c.rarity === 'common') { commonPosSum += pos; commonN++ }
        if (c.rarity === 'shiny') { shinyPosSum += pos; shinyN++ }
      }
    }
    const commonAvg = commonPosSum / commonN
    const shinyAvg = shinyPosSum / shinyN
    // 留足余量而非贴着实测值写死：这是方向性性质，不是精确数字
    expect(shinyAvg).toBeGreaterThan(commonAvg + 5)
  })

  it('抽满一整套不会重复，且第 33 次返回 null', () => {
    const rnd = prng(42)
    const owned: string[] = []
    for (let i = 0; i < pony.cards.length; i++) {
      const c = pickCard(owned, pony, rnd())
      expect(c).not.toBeNull()
      expect(owned).not.toContain(c!.id)
      owned.push(c!.id)
    }
    expect(pickCard(owned, pony, rnd())).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/cards/pack.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `pack.ts`**

```ts
import { pickWeighted } from '../data/pick-weighted'
import { CARD_RARITY_ORDER, type CardDef, type CardRarity, type CardSet } from './card-defs'

/**
 * 卡包单价。≈ 稳定期 1.4 天 / 新手 2.8 天所得（一天进账 180~360 分）。
 * 定得高于"每天都能开"是刻意的：即时反馈已经被怪兽图鉴占住（打卡必得一只），
 * 卡包的定位是"攒几天的大奖"。改这一处即可调价。
 */
export const PACK_COST = 500

/**
 * 抽卡权重。闪卡权重最低 → 闪卡在抽取顺序上系统性偏后，集齐路上后段惊喜更密
 * （`pack.test.ts` 有方向性回归锚）。
 */
export const PACK_WEIGHTS: Record<CardRarity, number> = { common: 65, rare: 28, shiny: 7 }

/**
 * 从该套**未拥有**的卡里抽 1 张；该套已集齐返回 null。跨套互不影响。
 * 不出重复是产品决策：孩子练一整天换来一张重复的挫败会直接反噬训练动机。
 */
export function pickCard(ownedIds: readonly string[], set: CardSet, rand: number): CardDef | null {
  const owned = new Set(ownedIds)
  const remaining = set.cards.filter((c) => !owned.has(c.id))
  return pickWeighted(remaining, (c) => c.rarity, PACK_WEIGHTS, CARD_RARITY_ORDER, rand)
}
```

- [ ] **Step 4: 跑测试确认绿**

Run: `npx vitest run src/cards/pack.test.ts`
Expected: PASS（6 条）。

如果「闪卡偏后」那条红了：先打印 `commonAvg` / `shinyAvg` 看实际差值。差值应该在 10 以上；若确实只有 6-8，把断言里的 `+ 5` 保持不动（它已经留了余量）。**不要把它降到 +2 以下**——那就不再验证任何东西了。若差值是负的，说明权重传错了档位顺序，回去查 `CARD_RARITY_ORDER`。

- [ ] **Step 5: 提交**

```bash
git add src/cards/pack.ts src/cards/pack.test.ts
git commit -m "feat(卡册): 卡包抽卡层（500 分/包，不出重复，闪卡偏后）"
```

---

## Task 6: Dexie v7 + `cards` 表

**Files:**
- Modify: `src/data/db.ts`（新增 `CardRow`、`RedemptionRow.kind` 加 `'pack'`、`cards` 表声明、`version(7)`）
- Modify: `src/data/db-migrate.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `interface CardRow extends SyncableRow { id: string; obtainedAt: number }`；`db.cards: Table<CardRow, string>`；`RedemptionRow['kind']` 扩为 `'reward' | 'repair' | 'pack'`

- [ ] **Step 1: 写失败的测试**

在 `src/data/db-migrate.test.ts` 里，把既有 `describe('全新安装')` 整段替换成：

```ts
/** v6 的 schema（与 db.ts 里 version(6) 逐字一致），用来模拟"已经升到 v6 的真机" */
const V6_SCHEMA = {
  sessions: '++id, date, uuid',
  checkins: 'date, uuid',
  badges: 'id, uuid',
  monsters: 'id, uuid',
  rewards: '++id, uuid',
  redemptions: '++id, kind, status, uuid',
  exams: '++id, date, uuid',
  outbox: '++id, uuid, kind',
  syncMeta: 'key',
}

describe('v6 → v7 加 cards 空表', () => {
  it('既有数据一行不丢，cards 表存在且为空', async () => {
    const name = 'focalquest-mig-v6'
    const legacy = new Dexie(name)
    legacy.version(6).stores(V6_SCHEMA)
    await legacy.open()
    await legacy.table('checkins').bulkAdd([
      { date: '2026-07-20', streak: 2, dailyPoints: 200, totalPoints: 400, uuid: 'checkin:default:2026-07-20', updatedAt: 111, profileId: 'default' },
    ])
    legacy.close()

    const up = new FocalQuestDB(name)
    await up.open()
    expect(up.verno).toBe(7)
    expect(up.tables.map((t) => t.name)).toContain('cards')
    expect(await up.cards.count()).toBe(0)
    // 纯加表，既有行的业务字段与同步字段都不动
    const ck = await up.checkins.get('2026-07-20')
    expect(ck?.totalPoints).toBe(400)
    expect(ck?.updatedAt).toBe(111)
    up.close()
  })
})

describe('全新安装', () => {
  it('没有历史数据时直接建到 v7，10 张表齐全且全空（Dexie 不跑 upgrade）', async () => {
    const fresh = new FocalQuestDB('focalquest-fresh-v7')
    await fresh.open()
    expect(fresh.verno).toBe(7)
    expect(fresh.tables.length).toBe(10)
    expect(await fresh.sessions.count()).toBe(0)
    expect(await fresh.cards.count()).toBe(0)
    expect(await fresh.outbox.count()).toBe(0)
    fresh.close()
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/data/db-migrate.test.ts`
Expected: FAIL —— `up.cards` 不存在、`verno` 是 6 而非 7、`tables.length` 是 9 而非 10。

- [ ] **Step 3: 改 `db.ts`**

在 `ExamRow` 之后加类型：

```ts
/**
 * 卡册（v7 新增）。setId / rarity 都从 `src/cards/card-defs.ts` 派生，不落库——
 * 存进来就会和数据层产生两份真相。
 */
export interface CardRow extends SyncableRow {
  /** 主键 = CardDef.id，如 'pony-7'。**不含冒号**（同步 uuid 反解依赖） */
  id: string
  /** 首次获得时刻。合并**取最早**（语义同 monster.capturedAt），不走 LWW */
  obtainedAt: number
}
```

把 `RedemptionRow` 的 `kind` 与其注释改成：

```ts
/** 积分消耗账本：兑换奖励 / 补签 / 开卡包（v4 新增，v7 加 'pack'） */
export interface RedemptionRow extends SyncableRow {
  id?: number           // ++id 自增
  kind: 'reward' | 'repair' | 'pack'
```

类声明里加表字段：

```ts
  cards!: Table<CardRow, string>
```

在 `version(6)` 之后追加：

```ts
    // v7：纯加 cards 空表。没有存量数据要回填，故**不需要 .upgrade()**——
    // 也就不碰 v6 那条"逐行兜底防白屏"的老伤。
    this.version(7).stores({
      sessions: '++id, date, uuid',
      checkins: 'date, uuid',
      badges: 'id, uuid',
      monsters: 'id, uuid',
      rewards: '++id, uuid',
      redemptions: '++id, kind, status, uuid',
      exams: '++id, date, uuid',
      cards: 'id, uuid',
      outbox: '++id, uuid, kind',
      syncMeta: 'key',
    })
```

- [ ] **Step 4: 跑测试确认绿**

Run: `npm test`
Expected: PASS，比 Task 5 结束时多 **1** 条（新增 v6→v7 那个 describe 有 1 条，「全新安装」原有 1 条被替换掉不算新增）。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 5: 提交**

```bash
git add src/data/db.ts src/data/db-migrate.test.ts
git commit -m "feat(卡册): Dexie v7 新增 cards 表，redemption 支持 pack 类型"
```

---

## Task 7: 接入云同步（7 类 → 8 类）

**Files:**
- Modify: `src/sync/sync-keys.ts:17`（`KINDS`）、`:24`（`KEYED_KINDS`）、`:40-62`（`naturalKeyOf`）
- Modify: `functions/lib/sync-validate.ts:5`（`KINDS`）
- Modify: `src/sync/merge.ts:16-19`（`EARLIEST_FIELD`）
- Modify: `src/sync/engine.ts:89-97`（`TABLE_OF`）
- Modify: `src/data/api.ts:26-34`（**第二份 `TABLE_OF`**）、`:98-109`（`pushCards`）、`:134-157`（`pushAll`）
- Modify: `src/sync/migrate-fields.ts:7-15`（`TIME_FIELD`）
- Modify: `src/sync/sync-keys.test.ts:10-20`
- Modify: `functions/lib/sync-validate.test.ts:8-12`
- Create: `src/sync/kinds-parity.test.ts`

**Interfaces:**
- Consumes: `CardRow`（Task 6）
- Produces: `pushCards(rows: CardRow[]): void`（`src/data/api.ts`）；`SyncKind` 扩为 8 类

⚠️ **`TABLE_OF` 有两份**（`src/data/api.ts:26` 与 `src/sync/engine.ts:89`），两处都要加，漏一处会在运行时报 `db.table('undefined')`。

- [ ] **Step 1: 写失败的测试**

改 `src/sync/sync-keys.test.ts` 里那两个 describe：

```ts
describe('KINDS', () => {
  it('恰为 8 类，且与服务端白名单逐字一致（functions/lib/sync-validate.ts）', () => {
    expect([...KINDS].sort()).toEqual(['badge', 'card', 'checkin', 'exam', 'monster', 'redemption', 'reward', 'session'])
  })
})

describe('KEYED_KINDS — 本地主键就是自然键的四类', () => {
  it('checkin / badge / monster / card；其余四类是 ++id 自增', () => {
    expect([...KEYED_KINDS]).toEqual(['checkin', 'badge', 'monster', 'card'])
    expect(isKeyedKind('card')).toBe(true)
    expect(isKeyedKind('session')).toBe(false)
  })
})

describe('card 的同步身份', () => {
  it('自然键取行内 id，uuid 形如 card:default:pony-7', () => {
    expect(naturalKeyOf('card', { id: 'pony-7' })).toBe('pony-7')
    expect(naturalKeyOf('card', {})).toBeNull()
    expect(recordUuid('card', { id: 'pony-7' }, P)).toBe('card:default:pony-7')
  })

  it('uuid 能反解回卡 id（墓碑找本地行靠它）', () => {
    expect(naturalKeyFromUuid('card:default:pony-7')).toBe('pony-7')
  })
})
```

改 `functions/lib/sync-validate.test.ts`：

```ts
describe('KINDS', () => {
  it('覆盖全部 8 类业务记录（与 Dexie 八张业务表一一对应）', () => {
    expect([...KINDS].sort()).toEqual(['badge', 'card', 'checkin', 'exam', 'monster', 'redemption', 'reward', 'session'])
  })
})
```

新建 `src/sync/kinds-parity.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { KINDS as CLIENT_KINDS } from './sync-keys'
import { KINDS as SERVER_KINDS } from '../../functions/lib/sync-validate'

/**
 * 这条闸门补的是一个真实缺口：此前两份 KINDS 各自被一条硬编码断言看着，
 * 谁只改一边、只更新那一边的断言，两边的测试都还是绿的，而线上会整批 400
 * （服务端 kind 白名单是全或无，400 被判永久错误 → 那台设备再也同步不了任何数据）。
 */
describe('前后端 KINDS 必须逐字一致', () => {
  it('客户端 src/sync/sync-keys.ts 与服务端 functions/lib/sync-validate.ts 完全相同', () => {
    expect([...CLIENT_KINDS]).toEqual([...SERVER_KINDS])
  })
})
```

新增 `src/sync/merge.test.ts` 的断言（该文件已存在，追加一个 it）：

```ts
  it('card 取最早 obtainedAt —— "首次获得时刻"才是正确语义，LWW 会留下较晚的值', () => {
    const local = { obtainedAt: 500, updatedAt: 100 }
    // 远端更早：应覆盖本地
    expect(mergeRecord('card', local, { payload: { obtainedAt: 300 }, updatedAt: 999 })).toEqual({ op: 'put' })
    // 远端更晚：即使 updatedAt 更大也不动本地
    expect(mergeRecord('card', local, { payload: { obtainedAt: 900 }, updatedAt: 999 })).toEqual({ op: 'skip' })
  })
```

- [ ] **Step 2: 跑测试确认它们红**

Run: `npx vitest run src/sync functions/lib/sync-validate.test.ts`
Expected: FAIL —— `KINDS` 还是 7 类；`naturalKeyOf('card', …)` 类型报错；parity 测试因两边都是 7 类而"意外通过"（这正常，它是防将来漂移的）。

- [ ] **Step 3: 客户端 `sync-keys.ts`**

```ts
export const KINDS = ['session', 'checkin', 'badge', 'monster', 'reward', 'redemption', 'exam', 'card'] as const
```
```ts
export const KEYED_KINDS = ['checkin', 'badge', 'monster', 'card'] as const
```

`naturalKeyOf` 里把 `card` 并入 badge/monster 那一支：

```ts
    case 'badge':
    case 'monster':
    case 'card':
      return typeof row.id === 'string' && row.id.length > 0 ? row.id : null
```

同时把文件头注释里的「**7 类全部派生确定性 uuid**」改成「**8 类全部派生确定性 uuid**」，并在括号说明里加上 `card 取 id`。

- [ ] **Step 4: 服务端 `sync-validate.ts`**

```ts
export const KINDS = ['session', 'checkin', 'badge', 'monster', 'reward', 'redemption', 'exam', 'card'] as const
```

- [ ] **Step 5: `merge.ts` 的取最早清单**

```ts
/**
 * badge / monster / card 的"首次达成/获得时刻"字段——合并取更早者。
 * ⚠️ 这一条是本文件存在的理由：两台设备各自解开同一枚勋章（或开出同一张卡）时，
 * LWW 会留下**较晚**的时刻，而勋章墙 / 卡册都按"首次"呈现，取晚的就是错的。
 */
const EARLIEST_FIELD: Partial<Record<SyncKind, string>> = {
  badge: 'unlockedAt',
  monster: 'capturedAt',
  card: 'obtainedAt',
}
```

- [ ] **Step 6: 两份 `TABLE_OF` + `TIME_FIELD`**

`src/sync/engine.ts` 与 `src/data/api.ts` 的 `TABLE_OF` 各加一行：

```ts
  card: 'cards',
```

`src/sync/migrate-fields.ts` 的 `TIME_FIELD` 加一行（注释里的「7 张表」也改成「8 张表」）：

```ts
  card: 'obtainedAt',
```

- [ ] **Step 7: `pushCards` 与全量补推**

`src/data/api.ts`：import 里加 `type CardRow`，在 `pushExams` 之后加：

```ts
export function pushCards(rows: CardRow[]): void {
  fire('card', rows)
}
```

`pushAll` 里加进去（三处：`Promise.all` 的解构、数组、`enqueuePut`）：

```ts
    const [sessions, checkins, badges, monsters, rewards, redemptions, exams, cards] = await Promise.all([
      db.sessions.toArray(),
      db.checkins.toArray(),
      db.badges.toArray(),
      db.monsters.toArray(),
      db.rewards.toArray(),
      db.redemptions.toArray(),
      db.exams.toArray(),
      db.cards.toArray(),
    ])
```
```ts
    await enqueuePut('card', cards, keep)
```

同时把该函数注释里的「本地 7 表」改成「本地 8 表」，文件头注释里的「下面 7 个 pushXxx」改成「8 个」。

- [ ] **Step 8: 跑全量测试与两份 typecheck**

Run: `npm test`
Expected: PASS，比 Task 6 结束时多 4 条。

Run: `npm run typecheck`
Expected: 无输出。

⚠️ 若 `typecheck` 抱怨 `src/sync/kinds-parity.test.ts` 跨项目 import（`tsconfig.json` 的 `exclude` 里有 `functions`）：把该测试移到 `functions/lib/kinds-parity.test.ts`，方向反过来 import `../../src/sync/sync-keys`，断言内容不变。`vitest.config.ts` 的 `include` 同时覆盖 `src/**` 与 `functions/**`，两处都会被跑到。

Run: `npm run typecheck:functions`
Expected: 无输出。

- [ ] **Step 9: 提交**

```bash
git add src/sync/sync-keys.ts src/sync/sync-keys.test.ts src/sync/merge.ts src/sync/merge.test.ts src/sync/engine.ts src/sync/migrate-fields.ts src/sync/kinds-parity.test.ts src/data/api.ts functions/lib/sync-validate.ts functions/lib/sync-validate.test.ts
git commit -m "feat(卡册): 同步接入 card kind（8 类），合并取最早 obtainedAt"
```

---

## Task 8: 开包服务（事务 + 账本）

**Files:**
- Create: `src/cards/cards-service.ts`
- Create: `src/cards/cards-service.test.ts`

**Interfaces:**
- Consumes: `db.cards` / `CardRow` / `RedemptionRow`（Task 6）、`PACK_COST` / `pickCard`（Task 5）、`CARD_SETS` / `cardSetById` / `CardDef`（Task 4）、`pushCards`（Task 7）、`getAvailablePoints`（既有 `src/rewards/rewards-service.ts`）
- Produces:
  - `getOwnedCardIds(): Promise<Set<string>>`
  - `interface SetProgress { setId: string; owned: number; total: number; complete: boolean }`
  - `getSetProgress(): Promise<SetProgress[]>`
  - `type OpenPackResult = { ok: true; card: CardDef } | { ok: false; reason: 'no-points' | 'complete' }`
  - `openPack(setId: string, now: number, rand?: number): Promise<OpenPackResult>`

- [ ] **Step 1: 写失败的测试**

新建 `src/cards/cards-service.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'
import { db } from '../data/db'
import { toDateStr } from '../data/date-utils'
import { monthRepairCount } from '../rewards/ledger'
import { cardSetById } from './card-defs'
import { PACK_COST } from './pack'
import { openPack, getSetProgress } from './cards-service'

const pony = cardSetById('pony')!

/** 可用积分来自 checkins 链最后一行的 totalPoints */
async function seedPoints(total: number): Promise<void> {
  await db.checkins.clear()
  await db.redemptions.clear()
  await db.cards.clear()
  await db.checkins.put({ date: '2026-07-26', streak: 5, dailyPoints: 300, totalPoints: total })
}

describe('openPack', () => {
  beforeEach(async () => { await seedPoints(1000) })
  afterEach(() => { vi.restoreAllMocks() })

  it('余额够 → 给一张卡 + 记一条 pack 消耗', async () => {
    const res = await openPack('pony', 1_700_000_000_000, 0.5)
    expect(res.ok).toBe(true)
    expect(await db.cards.count()).toBe(1)
    const red = (await db.redemptions.toArray())[0]
    expect(red.kind).toBe('pack')
    expect(red.title).toBe('pack')
    expect(red.cost).toBe(PACK_COST)
    // createdDate 是必填字段（漏写会让将来按月看开包量出错）。
    // 用 toDateStr 算而不是写死字符串——它按**本地**时区折算，写死会在别的时区红。
    expect(red.createdDate).toBe(toDateStr(new Date(1_700_000_000_000)))
  })

  it('开包不占补签的每月次数 —— monthRepairCount 只数 kind=repair', async () => {
    await openPack('pony', 1_700_000_000_000, 0.5)
    await openPack('pony', 1_700_000_001_000, 0.6)
    const month = toDateStr(new Date(1_700_000_000_000)).slice(0, 7)
    expect(monthRepairCount(await db.redemptions.toArray(), month)).toBe(0)
  })

  it('pack 记录恒为 fulfilled —— 撤销会退分而不收回卡，等于白得一张', async () => {
    await openPack('pony', 1_700_000_000_000, 0.5)
    const red = (await db.redemptions.toArray())[0]
    expect(red.status).toBe('fulfilled')
    expect(red.fulfilledAt).toBe(1_700_000_000_000)
  })

  it('开包扣可用分：1000 分开两包后不够第三包', async () => {
    expect((await openPack('pony', 1, 0.1)).ok).toBe(true)
    expect((await openPack('pony', 2, 0.2)).ok).toBe(true)
    const third = await openPack('pony', 3, 0.3)
    expect(third).toEqual({ ok: false, reason: 'no-points' })
    expect(await db.cards.count()).toBe(2)      // 失败不给卡
    expect(await db.redemptions.count()).toBe(2) // 也不扣分
  })

  it('该套集齐 → complete，不再扣分', async () => {
    await seedPoints(999_999)
    await db.cards.bulkPut(pony.cards.map((c) => ({ id: c.id, obtainedAt: 1 })))
    expect(await openPack('pony', 5, 0.5)).toEqual({ ok: false, reason: 'complete' })
    expect(await db.redemptions.count()).toBe(0)
  })

  it('跨套独立：pony 集齐后仍能开 deep', async () => {
    await seedPoints(999_999)
    await db.cards.bulkPut(pony.cards.map((c) => ({ id: c.id, obtainedAt: 1 })))
    const res = await openPack('deep', 6, 0.5)
    expect(res.ok).toBe(true)
    if (res.ok) expect(res.card.setId).toBe('deep')
  })

  it('记账失败时卡一起回滚 —— 绝不出现"扣了分没拿到卡"或"拿了卡没扣分"', async () => {
    vi.spyOn(db.redemptions, 'add').mockRejectedValueOnce(new Error('boom'))
    await expect(openPack('pony', 7, 0.5)).rejects.toThrow()
    expect(await db.cards.count()).toBe(0)
  })

  it('未知套 id 当已集齐处理，不扣分', async () => {
    expect(await openPack('nope', 8, 0.5)).toEqual({ ok: false, reason: 'complete' })
    expect(await db.redemptions.count()).toBe(0)
  })
})

describe('getSetProgress', () => {
  it('按套统计已拥有数与是否集齐', async () => {
    await seedPoints(0)
    await db.cards.bulkPut(pony.cards.slice(0, 3).map((c) => ({ id: c.id, obtainedAt: 1 })))
    const prog = await getSetProgress()
    expect(prog).toEqual([
      { setId: 'pony', owned: 3, total: 32, complete: false },
      { setId: 'deep', owned: 0, total: 32, complete: false },
    ])
  })
})
```

⚠️ 上面 `createdDate` 那条断言写的是 `1_700_000_000_000` 对应的**本地**日期。跑之前先确认：在 UTC+8 下 `toDateStr(new Date(1700000000000))` 是 `'2026-11-14'`。若你的机器时区不同导致这条红，把断言改成 `expect(red.createdDate).toBe(toDateStr(new Date(1_700_000_000_000)))` 并 import `toDateStr`——不要删掉这条，`createdDate` 是必填字段、漏写会让将来按月统计出错。

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/cards/cards-service.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `cards-service.ts`**

```ts
import { db, type CardRow, type RedemptionRow } from '../data/db'
import { CARD_SETS, cardSetById, type CardDef } from './card-defs'
import { PACK_COST, pickCard } from './pack'
import { getAvailablePoints } from '../rewards/rewards-service'
import { pushCards, pushRedemptions } from '../data/api'
import { toDateStr } from '../data/date-utils'

export async function getOwnedCardIds(): Promise<Set<string>> {
  return new Set((await db.cards.toArray()).map((c) => c.id))
}

export async function getOwnedCards(): Promise<CardRow[]> {
  return (await db.cards.toArray()).sort((a, b) => a.obtainedAt - b.obtainedAt)
}

export interface SetProgress {
  setId: string
  owned: number
  total: number
  complete: boolean
}

export async function getSetProgress(): Promise<SetProgress[]> {
  const owned = await getOwnedCardIds()
  return CARD_SETS.map((s) => {
    const n = s.cards.filter((c) => owned.has(c.id)).length
    return { setId: s.id, owned: n, total: s.cards.length, complete: n >= s.cards.length }
  })
}

export type OpenPackResult =
  | { ok: true; card: CardDef }
  | { ok: false; reason: 'no-points' | 'complete' }

/**
 * 开一包：抽一张该套未拥有的卡，同时记一条积分消耗。
 *
 * **两张表的写入必须在同一个事务里。** 不包事务时中间失败会出现"扣了 500 分却没拿到卡"
 * （孩子会哭，且账本对不上没法自动修）或"拿了卡没扣分"（白得）。push 放事务之后，
 * 与项目既有"先落库再 push、失败靠 outbox 补扫 + rescanOrphans 兜底"的模式一致。
 *
 * `rand` 可注入，供确定性测试；生产走 Math.random()。
 */
export async function openPack(setId: string, now: number, rand = Math.random()): Promise<OpenPackResult> {
  const set = cardSetById(setId)
  // 未知套：当已集齐处理。UI 只从 CARD_SETS 渲染按钮，正常路径到不了这里
  if (!set) return { ok: false, reason: 'complete' }

  const owned = await getOwnedCardIds()
  const ownedInSet = set.cards.filter((c) => owned.has(c.id)).map((c) => c.id)
  if (ownedInSet.length >= set.cards.length) return { ok: false, reason: 'complete' }

  if ((await getAvailablePoints()) < PACK_COST) return { ok: false, reason: 'no-points' }

  const card = pickCard(ownedInSet, set, rand)
  if (!card) return { ok: false, reason: 'complete' }

  const row: CardRow = { id: card.id, obtainedAt: now }
  // status 恒为 'fulfilled'：卡当场到手，没有"待家长确认"的中间态，
  // 也**永不可撤销**——退分而不收回卡等于白得一张（RewardsPage 的撤销按钮只对 pending 渲染）。
  const redemption: RedemptionRow = {
    kind: 'pack', title: 'pack', cost: PACK_COST,
    createdAt: now, createdDate: toDateStr(new Date(now)),
    status: 'fulfilled', fulfilledAt: now,
  }

  let id = 0
  await db.transaction('rw', db.cards, db.redemptions, async () => {
    await db.cards.put(row)
    id = await db.redemptions.add(redemption)
  })

  pushCards([row])
  pushRedemptions([{ ...redemption, id }])
  return { ok: true, card }
}
```

- [ ] **Step 4: 跑测试确认绿**

Run: `npm test`
Expected: PASS，比 Task 7 结束时多 9 条（openPack 8 条 + getSetProgress 1 条）。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 5: 提交**

```bash
git add src/cards/cards-service.ts src/cards/cards-service.test.ts
git commit -m "feat(卡册): openPack 在一个事务里给卡并扣分，pack 恒 fulfilled"
```

---

## Task 9: 卡片图片组件 + 闪卡音效

**Files:**
- Create: `src/cards/CardImage.tsx`
- Modify: `src/training/sfx.ts:1`（`SfxKind`）、`:30-62`（新增 case）

**Interfaces:**
- Consumes: `CardDef` / `cardSheetPos` / `cardSheetUrl`（Task 4）
- Produces: `CardImage({ def, size }: { def: CardDef; size: number })`；`playSfx('shiny')`

- [ ] **Step 1: 加闪卡音效**

`src/training/sfx.ts` 第 1 行的联合类型加 `'shiny'`：

```ts
type SfxKind = 'correct' | 'wrong' | 'flip' | 'finish' | 'checkin' | 'badge' | 'egg' | 'timeout' | 'shiny'
```

在 `case 'egg':` 之后加：

```ts
    // 闪卡 = 抽到最稀有的一档。**必须与普通/稀有的庆祝音区分开**：
    // 界面刚用金边分出的稀有度，若耳朵里是同一个声音就等于没分
    // （与 'timeout' 不能沿用 'wrong' 是同一个理由）。更高、更亮、多一段上冲。
    case 'shiny':
      ;[1047, 1568, 2093, 2637, 3136].forEach((f, i) => tone(f, i * 0.07, 0.24, 0.16, 'triangle'))
      break
```

- [ ] **Step 2: 实现 `CardImage.tsx`**

```tsx
import { useState } from 'react'
import { cardSheetPos, cardSheetUrl, type CardDef } from './card-defs'

/**
 * 从 4×4 宫格图切一格显示（与 `BadgeCard` 同一套 background-position 约定：
 * backgroundSize 400%，位置按 col/3、row/3 的百分比）。
 *
 * 图缺失时回落到「渐变 + 编号」，两个理由：
 *  ① 正式卡图是用户后补的资源，图还没到之前功能要能完整开发与验收；
 *  ② 线上万一某张 sheet 没进 SW 预缓存，也不该给孩子看一个破图图标。
 * 探测用一个隐藏的 <img onError>，真正显示仍走 background-position 切片。
 */
export function CardImage({ def, size }: { def: CardDef; size: number }) {
  const [failed, setFailed] = useState(false)
  const { row, col } = cardSheetPos(def)
  const url = cardSheetUrl(def)

  if (failed) {
    return (
      <div
        style={{
          width: size, height: size, borderRadius: 12,
          display: 'grid', placeItems: 'center',
          background: 'linear-gradient(135deg, #f4effe, #ffe9f3)',
          color: '#a89adf', fontSize: Math.round(size / 3), fontWeight: 800,
        }}
      >
        {def.id.replace(`${def.setId}-`, '')}
      </div>
    )
  }

  return (
    <>
      <img src={url} alt="" onError={() => setFailed(true)} style={{ display: 'none' }} />
      <div
        style={{
          width: size, height: size, borderRadius: 12,
          backgroundImage: `url(${url})`,
          backgroundSize: '400% 400%',
          backgroundPosition: `${(col / 3) * 100}% ${(row / 3) * 100}%`,
          backgroundRepeat: 'no-repeat',
        }}
      />
    </>
  )
}
```

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: 无输出。

Run: `npm test`
Expected: PASS，条数与 Task 8 结束时一致（本任务不加测试——`CardImage` 是纯展示、`playSfx` 需要 AudioContext，两者都不适合在 node 环境单测；它们的验收在 Task 10 的真机/浏览器检查里）。

- [ ] **Step 4: 提交**

```bash
git add src/cards/CardImage.tsx src/training/sfx.ts
git commit -m "feat(卡册): 宫格切片图片组件（缺图回落）+ 闪卡专属音效"
```

---

## Task 10: 卡册页 + 入口 + 文案

**Files:**
- Create: `src/cards/CardAlbum.tsx`
- Create: `src/nav.test.ts`
- Modify: `src/App.tsx:31`（`View`）、`:33`（导出 `NAV`）、`:82-98`（接线）
- Modify: `src/HomePage.tsx:147-174`（入口卡）
- Modify: `src/i18n.tsx`（zh 与 en 各 15 条）
- Modify: `src/i18n.test.ts`（`CARD_KEYS`）

**Interfaces:**
- Consumes: `getSetProgress` / `getOwnedCards` / `openPack` / `SetProgress`（Task 8）、`CardImage`（Task 9）、`CARD_SETS` / `CardDef` / `CardRarity`（Task 4）、`PACK_COST`（Task 5）、`getAvailablePoints`（既有 `src/rewards/rewards-service.ts`）、`playSfx`（Task 9 扩过的 `src/training/sfx.ts`）
- Produces: `CardAlbum()`（无 props，返回顶部由页面自己管，与 `RewardsPage` 同构）；`NAV: { key: View; icon: string }[]` 从 `src/App.tsx` 导出

- [ ] **Step 1: 写失败的测试**

`src/i18n.test.ts` 顶部加常量，并在 describe 里加一条 it：

```ts
/** 卡册用到的全部文案键（64 个卡名不在此列——它们随正式卡图一起补，缺失时 UI 回落到「套名 #编号」） */
const CARD_KEYS = [
  'card.pageTitle', 'card.homeCard', 'card.open', 'card.complete', 'card.allComplete',
  'card.notEnough', 'card.locked', 'card.progress', 'card.obtainedAt', 'card.got',
  'card.rarity.common', 'card.rarity.rare', 'card.rarity.shiny',
  'card.set.pony', 'card.set.deep',
]
```
```ts
  it('卡册 15 个文案键在 zh 与 en 都存在（漏一份就会在另一种语言下回退成中文）', () => {
    expect(CARD_KEYS.length).toBe(15)
    for (const k of CARD_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```

新建 `src/nav.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { NAV } from './App'

/**
 * 手机竖屏断点 560px 是按「5 项英文导航单行所需 531px」算出来的
 * （见 src/layout/layout-budget.ts 与 css-contract.test.ts）。
 * 第 6 项会让英文导航折行，把 3d 迭代刚修好的手机布局重新弄坏——
 * 那次的症状是训练页整体比首屏高 42px、↓ 方向键有一截点不到。
 * 所以卡册、限时挑战、隐私政策、管理后台一律走"页面 + 页内入口"，不进 NAV。
 */
describe('底部导航', () => {
  it('恒为 5 项 —— 新功能加入口卡，不许加导航项', () => {
    expect(NAV.length).toBe(5)
  })
})
```

- [ ] **Step 2: 跑测试确认它们红**

Run: `npx vitest run src/i18n.test.ts src/nav.test.ts`
Expected: FAIL —— 15 个键都不存在；`NAV` 未从 `App.tsx` 导出。

- [ ] **Step 3: 加文案**

`src/i18n.tsx` 的 zh 字典里（放在 `repair.*` 附近）：

```ts
  'card.pageTitle': '🃏 卡册',
  'card.homeCard': '🃏 卡册',
  'card.open': '开卡包 · {cost} 分',
  'card.complete': '✨ 已集齐',
  'card.allComplete': '🎉 全部卡册都集齐啦！',
  'card.notEnough': '再练 {n} 分',
  'card.locked': '？？？',
  'card.progress': '{n}/{total} 张',
  'card.obtainedAt': '{date} 获得',
  'card.got': '🎉 获得新卡！',
  'card.rarity.common': '普通',
  'card.rarity.rare': '稀有',
  'card.rarity.shiny': '闪卡',
  'card.set.pony': '魔法小马',
  'card.set.deep': '深海精灵',
```

en 字典里：

```ts
  'card.pageTitle': '🃏 Card Album',
  'card.homeCard': '🃏 Cards',
  'card.open': 'Open pack · {cost} pts',
  'card.complete': '✨ Complete',
  'card.allComplete': '🎉 Every album is complete!',
  'card.notEnough': '{n} more pts',
  'card.locked': '???',
  'card.progress': '{n}/{total}',
  'card.obtainedAt': 'Got {date}',
  'card.got': '🎉 New card!',
  'card.rarity.common': 'Common',
  'card.rarity.rare': 'Rare',
  'card.rarity.shiny': 'Shiny',
  'card.set.pony': 'Magic Ponies',
  'card.set.deep': 'Deep Sea Spirits',
```

- [ ] **Step 4: 实现 `CardAlbum.tsx`**

```tsx
import { useEffect, useState } from 'react'
import { CARD_SETS, type CardDef, type CardRarity } from './card-defs'
import { PACK_COST } from './pack'
import { CardImage } from './CardImage'
import { getOwnedCards, getSetProgress, openPack, type SetProgress } from './cards-service'
import { getAvailablePoints } from '../rewards/rewards-service'
import { playSfx } from '../training/sfx'
import { toDateStr } from '../data/date-utils'
import { useT } from '../i18n'

/** 稀有度 → 边框色 + 光晕（沿用图鉴/勋章那套：普通银灰、稀有紫、闪卡金） */
const RARITY_BORDER: Record<CardRarity, string> = {
  common: '#c7c0db',
  rare: '#7c6cf0',
  shiny: '#ffb400',
}
const RARITY_GLOW: Record<CardRarity, string> = {
  common: 'rgba(199,192,219,0.35)',
  rare: 'rgba(124,108,240,0.42)',
  shiny: 'rgba(255,180,0,0.48)',
}

type T = (key: string, params?: Record<string, string | number>) => string

/**
 * 卡名。正式卡名随卡图一起补，缺失时 `translate` 会原样返回 key，
 * 此时回落到「套名 #编号」，界面上不会出现 'card.pony.07' 这种字样。
 */
function cardName(def: CardDef, t: T): string {
  const s = t(def.nameKey)
  if (s !== def.nameKey) return s
  return `${t(`card.set.${def.setId}`)} #${def.id.replace(`${def.setId}-`, '')}`
}

export function CardAlbum() {
  const t = useT()
  const [progress, setProgress] = useState<SetProgress[] | null>(null)
  const [obtainedMap, setObtainedMap] = useState<Record<string, number>>({})
  const [available, setAvailable] = useState(0)
  const [zoom, setZoom] = useState<CardDef | null>(null)
  const [got, setGot] = useState<CardDef | null>(null)
  const [busy, setBusy] = useState(false)

  async function refresh(): Promise<void> {
    const [prog, owned, av] = await Promise.all([getSetProgress(), getOwnedCards(), getAvailablePoints()])
    const m: Record<string, number> = {}
    for (const r of owned) m[r.id] = r.obtainedAt
    setProgress(prog)
    setObtainedMap(m)
    setAvailable(av)
  }
  useEffect(() => { void refresh() }, [])

  // 放大卡 / 开包结果都用 ESC 关
  useEffect(() => {
    if (!zoom && !got) return
    const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') { setZoom(null); setGot(null) } }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [zoom, got])

  // busy 期间按钮禁用：孩子连点两次会并发读到同一个余额（余额校验纯在本地做）
  async function onOpen(setId: string): Promise<void> {
    if (busy) return
    setBusy(true)
    try {
      const res = await openPack(setId, Date.now())
      if (res.ok) {
        playSfx(res.card.rarity === 'shiny' ? 'shiny' : 'badge')
        setGot(res.card)
        await refresh()
      }
    } finally {
      setBusy(false)
    }
  }

  if (progress === null) return <div className="fq-page">{t('home.loading')}</div>

  const allComplete = progress.every((p) => p.complete)

  return (
    <div className="fq-page fq-rise">
      <h2 className="fq-h2">{t('card.pageTitle')}</h2>

      <div className="fq-card" style={{ marginTop: 14, textAlign: 'center' }}>
        <div style={{ fontSize: 34, fontWeight: 800, color: 'var(--violet)' }}>{available}</div>
        <div style={{ fontSize: 12, color: 'var(--muted)', marginTop: 4 }}>{t('reward.available')}</div>
      </div>

      {allComplete && (
        <div className="fq-card" style={{ marginTop: 12, textAlign: 'center', fontSize: 15, fontWeight: 800, color: 'var(--lemon)' }}>
          {t('card.allComplete')}
        </div>
      )}

      {CARD_SETS.map((set) => {
        const prog = progress.find((p) => p.setId === set.id)!
        const pct = Math.round((prog.owned / prog.total) * 100)
        const affordable = available >= PACK_COST
        return (
          <section key={set.id} style={{ marginTop: 22 }}>
            <div className="fq-card-title" style={{ fontSize: 15 }}>
              <span>{t(set.nameKey)}</span>
              <span style={{ marginLeft: 'auto', fontSize: 12, color: 'var(--muted)', fontWeight: 600 }}>
                {t('card.progress', { n: prog.owned, total: prog.total })}
              </span>
            </div>

            <div className="fq-bar" style={{ marginTop: 8 }}><i style={{ width: `${pct}%` }} /></div>

            <button
              className="fq-cta"
              style={{ width: '100%', marginTop: 12, opacity: prog.complete || !affordable || busy ? 0.55 : 1 }}
              disabled={prog.complete || !affordable || busy}
              onClick={() => void onOpen(set.id)}
            >
              {prog.complete
                ? t('card.complete')
                : affordable
                  ? t('card.open', { cost: PACK_COST })
                  : t('card.notEnough', { n: PACK_COST - available })}
            </button>

            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 10, marginTop: 12 }}>
              {set.cards.map((def) => {
                const at = obtainedMap[def.id]
                const owned = at !== undefined
                return (
                  <div
                    key={def.id}
                    onClick={() => owned && setZoom(def)}
                    title={owned ? cardName(def, t) : t('card.locked')}
                    style={{
                      flex: '1 1 96px',
                      maxWidth: 120,
                      padding: '10px 8px',
                      borderRadius: 16,
                      textAlign: 'center',
                      background: '#fff',
                      border: owned ? `1.5px solid ${RARITY_BORDER[def.rarity]}` : '1px solid var(--line)',
                      boxShadow: owned ? `0 8px 16px -8px ${RARITY_GLOW[def.rarity]}` : 'none',
                      cursor: owned ? 'pointer' : 'default',
                    }}
                  >
                    {owned ? (
                      <div style={{ width: 80, height: 80, margin: '0 auto', overflow: 'hidden', borderRadius: 12, background: '#fafaff' }}>
                        <CardImage def={def} size={80} />
                      </div>
                    ) : (
                      <div style={{
                        width: 80, height: 80, margin: '0 auto', borderRadius: 12,
                        display: 'grid', placeItems: 'center',
                        background: 'linear-gradient(135deg, #f4effe, #ece4fb)',
                        border: '2px dashed #d8ccf3',
                        color: '#c0b2ea', fontSize: 34, fontWeight: 800,
                      }}>?</div>
                    )}
                    <div style={{
                      fontSize: 12, marginTop: 6, fontWeight: 700,
                      color: owned ? 'var(--ink)' : 'var(--muted)',
                      whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis',
                    }}>
                      {owned ? cardName(def, t) : t('card.locked')}
                    </div>
                    <div style={{
                      fontSize: 10, marginTop: 2, fontWeight: owned ? 700 : 400,
                      color: owned ? RARITY_BORDER[def.rarity] : 'var(--muted)',
                    }}>
                      {owned ? t(`card.rarity.${def.rarity}`) : ''}
                    </div>
                  </div>
                )
              })}
            </div>
          </section>
        )
      })}

      {/* 开包结果 */}
      {got && <CardModal def={got} caption={t('card.got')} onClose={() => setGot(null)} />}

      {/* 放大看已拥有的卡 */}
      {zoom && (
        <CardModal
          def={zoom}
          caption={t('card.obtainedAt', { date: toDateStr(new Date(obtainedMap[zoom.id]!)) })}
          onClose={() => setZoom(null)}
        />
      )}
    </div>
  )
}

/** 开包结果与放大看卡共用同一个 modal，只有底部那行说明文字不同 */
function CardModal({ def, caption, onClose }: { def: CardDef; caption: string; onClose: () => void }) {
  const t = useT()
  return (
    <div
      onClick={onClose}
      style={{
        position: 'fixed', inset: 0, zIndex: 50,
        background: 'rgba(51,40,90,0.55)', backdropFilter: 'blur(6px)',
        display: 'grid', placeItems: 'center', padding: 20,
      }}
    >
      <div
        className="fq-card fq-rise"
        onClick={(e) => e.stopPropagation()}
        style={{
          textAlign: 'center', padding: 24, maxWidth: 320,
          border: `2px solid ${RARITY_BORDER[def.rarity]}`,
          boxShadow: `0 18px 36px -12px ${RARITY_GLOW[def.rarity]}`,
        }}
      >
        <div style={{
          width: 240, height: 240, margin: '0 auto', maxWidth: '100%',
          borderRadius: 18, overflow: 'hidden',
          border: `2px solid ${RARITY_BORDER[def.rarity]}`, background: '#fff',
        }}>
          <CardImage def={def} size={240} />
        </div>
        <div style={{ fontSize: 20, fontWeight: 800, marginTop: 14, color: 'var(--ink)' }}>
          {cardName(def, t)}
        </div>
        <div style={{
          display: 'inline-block', marginTop: 8, padding: '3px 12px',
          fontSize: 12, fontWeight: 700, borderRadius: 99,
          color: '#fff', background: RARITY_BORDER[def.rarity],
        }}>
          {t(`card.rarity.${def.rarity}`)}
        </div>
        <div style={{ fontSize: 12, color: 'var(--muted)', marginTop: 12 }}>{caption}</div>
        <button className="fq-btn" style={{ marginTop: 16, width: '100%' }} onClick={onClose}>✓</button>
      </div>
    </div>
  )
}
```

- [ ] **Step 5: 接线 `App.tsx`**

第 31 行 `View` 加 `'cards'`：

```ts
type View = 'home' | 'train' | 'stats' | 'badges' | 'calib' | 'speech' | 'settings' | 'rewards' | 'privacy' | 'admin' | 'challenge' | 'cards'
```

第 33 行 `NAV` 改成导出（**内容一项都不加**）：

```ts
/**
 * 底部导航恒 5 项。加第 6 项会让英文导航在手机竖屏折行，破掉 560px 断点的前提
 * （见 src/nav.test.ts 与 src/layout/layout-budget.ts）。
 * 新功能一律走"页面 + 页内入口卡"，与 challenge / privacy / admin 同构。
 */
export const NAV: { key: View; icon: string }[] = [
```

import `CardAlbum`，给 `HomePage` 传 `onOpenCards`，并加视图分支（放在 `challenge` 那一行旁边）：

```tsx
            onOpenCards={() => setView('cards')}
```
```tsx
        {/* 卡册不进 NAV：入口在首页与图鉴并排（spec §9.2），与 challenge 同构 */}
        {view === 'cards' && <CardAlbum />}
```

- [ ] **Step 6: 首页入口卡**

`src/HomePage.tsx`：props 加 `onOpenCards: () => void`（同时改函数签名里那个内联的类型注解），加一行 import：

```ts
import { getSetProgress, type SetProgress } from './cards/cards-service'
```

再加状态与加载：

```tsx
  const [cardProg, setCardProg] = useState<SetProgress[] | null>(null)
```
```tsx
    void getSetProgress().then(setCardProg)
```

在既有「图鉴 | 奖励」两列容器里、奖励卡**之后**再加一张（容器已经是 `flexWrap`，`flex: '1 1 150px'` 会在 iPad 上排一行、手机上自动折行）：

```tsx
          <button
            onClick={onOpenCards}
            className="fq-card"
            style={{ flex: '1 1 150px', textAlign: 'left', cursor: 'pointer', border: '1.5px solid var(--lemon)', background: 'linear-gradient(160deg, #fffbe8, #fff)', padding: 14 }}
          >
            <div style={{ fontSize: 20 }}>🃏</div>
            <div style={{ fontSize: 14, fontWeight: 800, marginTop: 4 }}>{t('card.homeCard').replace('🃏 ', '')}</div>
            <div style={{ fontSize: 12, color: 'var(--violet)', fontWeight: 700, fontVariantNumeric: 'tabular-nums', marginTop: 2 }}>
              {cardProg ? `${cardProg.reduce((s, p) => s + p.owned, 0)}/${cardProg.reduce((s, p) => s + p.total, 0)}` : '—'}
            </div>
            <div className="fq-bar" style={{ marginTop: 8 }}>
              <i style={{ width: `${cardProg ? Math.round((cardProg.reduce((s, p) => s + p.owned, 0) / cardProg.reduce((s, p) => s + p.total, 0)) * 100) : 0}%` }} />
            </div>
          </button>
```

- [ ] **Step 7: 跑全量测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 9 结束时多 2 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 8: 浏览器里真验一遍**

Run: `npm run dev`，然后在预览里：
1. 首页出现「🃏 卡册」入口卡，进度显示 `0/64`
2. 进卡册页，两套各 32 个「？」格子，两个按钮都因余额不足而禁用并显示「再练 N 分」
3. 手动给自己加分（浏览器控制台）：
   ```js
   const db = await (await import('/src/data/db.ts')).db
   await db.checkins.put({ date: '2026-07-27', streak: 5, dailyPoints: 300, totalPoints: 3000 })
   ```
   刷新页面 → 按钮变可点
4. 点「开卡包」→ 弹出获得新卡的 modal（图未到时显示「渐变 + 编号」回落）、有音效、格子点亮、进度 +1、可用分 −500
5. 点已拥有的卡 → 放大 modal，ESC 可关
6. 底部导航仍是 5 项
7. 打开 DevTools Console 确认无 error

- [ ] **Step 9: 提交**

```bash
git add src/cards/CardAlbum.tsx src/nav.test.ts src/App.tsx src/HomePage.tsx src/i18n.tsx src/i18n.test.ts
git commit -m "feat(卡册): 卡册页 + 首页入口 + 中英文案（导航仍 5 项）"
```

---

## Task 11: 接入正式卡图

**Files:**
- Create: `public/cards/pony/sheet1.webp`、`public/cards/pony/sheet2.webp`
- Create: `public/cards/deep/sheet1.webp`、`public/cards/deep/sheet2.webp`
- Modify: `src/i18n.tsx`（64 个卡名 × 2 语言，可选）
- Modify: `src/i18n.test.ts`（若加了卡名，把它们并入 `CARD_KEYS` 并更新条数）
- Modify: `docs/怪兽出图提示词.md`（补一节卡片出图提示词）

**Interfaces:**
- Consumes: `cardSheetUrl` 约定的路径（Task 4）
- Produces: 无代码接口

**这个任务依赖用户产出美术资源**，代码侧不阻塞——图没到时 `CardImage` 回落到「渐变 + 编号」，功能完整可用。

- [ ] **Step 1: 出图**

按 `docs/怪兽出图提示词.md` 的既有流程，每套出两张 4×4 网格图：
- 尺寸 1536×1536（单卡 384×384），透明背景，风格统一，**不画分隔线**
- `pony` 套：原创彩虹小马风格（独角、翅膀、彩虹鬃毛、糖果配色）。**不得使用小马宝莉的官方角色形象或角色名**——仓库是公开的
- `deep` 套：深海精灵（海豚、水母、独角鲸、美人鱼一类）
- **每套 sheet2 的最后一行（第 29-32 张）是闪卡**，画得最华丽

- [ ] **Step 2: 转 webp 并放进 public**

按项目既有做法转 webp q85，放到：
```
public/cards/pony/sheet1.webp
public/cards/pony/sheet2.webp
public/cards/deep/sheet1.webp
public/cards/deep/sheet2.webp
```

- [ ] **Step 3: 核对体积**

Run: `node -e "for (const p of ['pony/sheet1','pony/sheet2','deep/sheet1','deep/sheet2']) console.log(p, (require('fs').statSync('public/cards/'+p+'.webp').size/1024).toFixed(0)+'KB')"`
Expected: 每张约 400-600KB，四张合计约 2MB（现有勋章两张共 442KB 作参照）。明显超过时降 q 或降到 1024²。

- [ ] **Step 4: 浏览器里核对切格没错位**

Run: `npm run dev`，在卡册页给自己加够分连开若干包，确认：
- 每张卡显示的是**一格**而不是整张网格被压缩
- 第 17 张开始取自 sheet2（不是 sheet1 重复）
- 闪卡（第 29-32 张）确实是最华丽的那 4 张，且金边

- [ ] **Step 5: 补卡名（可选）**

若要给 64 张卡起名，zh 与 en **必须成对**加，否则 `src/i18n.test.ts` 会红。加完把这些键并入 `CARD_KEYS` 并把条数断言从 15 改成 79。不加也可以——UI 会显示「魔法小马 #07」。

- [ ] **Step 6: 跑测试与构建**

Run: `npm test`
Expected: PASS。

Run: `npm run build`
Expected: 成功，且构建输出里能看到四张 webp 进了 PWA precache manifest。

- [ ] **Step 7: 提交**

```bash
git add public/cards docs/怪兽出图提示词.md
git commit -m "feat(卡册): 接入两套正式卡图（各 32 张，1536² webp）"
```

---

## 收尾检查

- [ ] `npm test` 全绿，总数 = 起始基线 + **40**（分别是 3 / 2 / 4 / 9 / 6 / 1 / 4 / 9 / 0 / 2 / 0）
- [ ] `npm run typecheck` 与 `npm run typecheck:functions` 都无输出
- [ ] `npm run build` 成功
- [ ] 本地整栈验一遍同步（服务端 `KINDS` 改过，必须重新 build 才能让 wrangler 拿到新 Functions）：
  ```bash
  npm run build && npm run db:migrate:local
  ```
  另开一个终端 `npx wrangler pages dev dist --port 8788`，然后 `npm run test:api`
  Expected: 31/31。撞 429 就先停服务再 `npm run db:reset:limits`（wrangler 运行期间持 D1 文件锁）
- [ ] 浏览器里注册/登录一个账号，开一包卡，点「立即同步」，确认 outbox 归零、`card` 记录上云
- [ ] 按 [CLAUDE.md](CLAUDE.md) 的惯例把本迭代写进「当前状态」条目，含定价常量位置与「pack 恒 fulfilled」等关键口径
