# 邀请码配额与管理员换码 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把邀请码从「一个永久不变的字符串 + 看不见的 5 人配额」改成「一张有 5 次使用次数、管理员可随时换新的券」，并把已用/额度显示给所有家长。

**Architecture:** `users` 加一列 `invite_reset_at`，注册时的配额子查询只数「这一刻之后注册的人」；新增 `functions/api/account/invite.ts`，`GET` 读状态、`POST` 换码（仅管理员，同一条 UPDATE 写新码与新 reset 时刻）。客户端设置页拉这个 GET 拿权威值，顺带修掉「邀请码与 isAdmin 只是登录快照」的既有缺口。管理后台的邀请排行拆成「累计」与「当前码 n/quota」两个口径。

**Tech Stack:** Cloudflare Pages Functions + D1（SQLite）、TypeScript、React 18、vitest、`scripts/test-api.mjs`（集成断言）。

**设计文档：** [docs/superpowers/specs/2026-07-29-邀请码配额与换码-design.md](../specs/2026-07-29-邀请码配额与换码-design.md)

## Global Constraints

- **语言**：与用户交流一律简体中文；代码注释中文，技术标识符英文。
- **国际化**：所有新增用户可见文案必须同时进 `src/i18n.tsx` 的 zh 与 en 两份字典，键名一致。
- **CI 质量门只跑 `npm test` + 两份 typecheck + build，不跑 `npm run test:api`。** 凡是只有集成断言覆盖的逻辑，必须另有纯函数单测或 `?raw` 源文本契约测试进 `npm test`。
- **两份 typecheck 都要过**：`npm run typecheck`（app，`tsconfig.json` 排除 `functions/`）与 `npm run typecheck:functions`（Workers，`functions/tsconfig.json`）。
- **不解析 `records.payload`**：本迭代不碰同步与统计的 payload 口径。
- **改 schema 一律新增 migration 文件，不改历史文件**（`migrations/*.sql` 顺序执行）。
- **不要用 PowerShell here-string（`@'…'@`）写 Bash 工具里的多行字符串**；本仓源码是 LF，改文件优先用 Edit 工具。
- 提交信息末尾附 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。

## 开工前先记基线

跑一遍 `npm test`，记下真实的「N 个测试 / M 个文件」。CLAUDE.md 里写的数字是历史快照，不要当现值。

## 文件结构

| 文件 | 职责 |
|---|---|
| `migrations/0002_invite_rotate.sql` | 新增 `users.invite_reset_at`（Task 1） |
| `functions/api/auth/register.ts` | 配额子查询加世代边界（Task 1，全仓唯一一处判据） |
| `functions/raw-modules.d.ts` | 给 Workers 侧的 tsconfig 声明 `*?raw`（Task 1） |
| `functions/lib/invite-quota.test.ts` | 源文本契约：那条 SQL 里必须有 `invite_reset_at`（Task 1） |
| `functions/lib/auth.ts` | 收下从 `admin-stats.ts` 搬来的 `adminGate`（Task 2） |
| `functions/lib/auth.test.ts` | `adminGate` 的三条鉴权锚点（Task 2，从 `admin-stats.test.ts` 搬） |
| `functions/api/account/invite.ts` | `GET` 读状态 / `POST` 换码，共用 `readState()`（Task 3） |
| `src/sync/invite-api.ts` | 客户端薄编排层：调上面两个方法、把权威值写回 syncMeta（Task 4） |
| `src/sync/CloudSyncCard.tsx` | 显示「已邀 n/5」、管理员的「换一个码」按钮（Task 4） |
| `functions/lib/admin-stats.ts` / `functions/api/admin/stats.ts` | 邀请排行拆两个口径（Task 5） |
| `src/admin/{admin-api,format,AdminPage}.tsx` | 排行多一列 + 两个新 metric 的文案（Task 5） |
| `src/i18n.tsx` / `src/i18n.test.ts` | 中英双语；**测试用的是显式键名清单，加删键都要同步改**（Task 4、5） |

---

### Task 1: 数据模型与配额判据

给 `users` 加世代边界列，把注册端的配额子查询改成只数当前世代，并用源文本契约把这条 SQL 钉住。

本任务**不改变任何可观测行为**——没有换码入口，所有账号的 `invite_reset_at` 恒为 0，`created_at >= 0` 永真。这正是验收标准：现有的 `npm run test:api` 必须一条不差地继续全绿。

**Files:**
- Create: `migrations/0002_invite_rotate.sql`
- Create: `functions/raw-modules.d.ts`
- Create: `functions/lib/invite-quota.test.ts`
- Modify: `functions/api/auth/register.ts:64-68`

**Interfaces:**
- Consumes: 无
- Produces: `users.invite_reset_at`（INTEGER NOT NULL DEFAULT 0，毫秒时间戳）——Task 3 的 `readState()` 与 `POST` 的 UPDATE、Task 5 的排行 SQL 都依赖这一列。

- [ ] **Step 1: 写失败的源文本契约测试**

先建 `functions/raw-modules.d.ts`（否则 `typecheck:functions` 会在下一步报 TS2307）：

```ts
/**
 * `?raw` 是 Vite 的导入后缀（把源文件当纯文本拿）。
 *
 * `functions/tsconfig.json` 只装 @cloudflare/workers-types、**刻意不装 vite/client**
 * ——后者会把整套 DOM 全局塞进 Workers 的类型环境，与 workers-types 打架。
 * 所以这里单独声明这一个通配模块。
 *
 * 只有 `*.test.ts` 会用到；部署到 Pages 的代码里没有任何 ?raw 导入。
 */
declare module '*?raw' {
  const src: string
  export default src
}
```

再建 `functions/lib/invite-quota.test.ts`：

```ts
import { describe, it, expect } from 'vitest'

// 用 Vite 的 ?raw 拿源文件文本（本仓没装 @types/node，node:fs 会让 tsc 报 TS2307）。
// 与 src/nav.test.ts、src/admin/admin-entry.test.ts 同一套做法。
import register from '../api/auth/register.ts?raw'

/**
 * 邀请配额的「世代边界」契约。
 *
 * 本迭代的核心口径——换码即重置已用名额——落在 register.ts 那条 used 子查询的
 * `AND c.created_at >= u.invite_reset_at` 上。它写在 SQL 里，**抽不成纯函数**，
 * 而 .github/workflows/deploy-cf.yml 的质量门只跑 npm test / typecheck / build、
 * **从不跑 npm run test:api**，且它是 push master 即构建即部署。
 *
 * 也就是说：谁哪天顺手把这个条件删掉，CI 会一路绿灯把「换码不再重置名额」发到线上，
 * 而且不报错、没有任何症状——管理员换了码，名额却还是用完的状态，没人会想到是这里。
 *
 * 这条契约拦不住语义错误，但能拦住「被删掉」，而后者才是这条口径最可能的死法。
 */
describe('邀请配额的计数口径', () => {
  it('register 的 used 子查询必须带 invite_reset_at 这道世代边界', () => {
    const sub = register.match(/SELECT COUNT\(\*\) FROM users c[\s\S]*?AS used/)
    expect(sub, '没找到 used 子查询——正则要跟着 register.ts 的写法更新').not.toBeNull()
    expect(sub![0]).toContain('invite_reset_at')
  })
})
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
npx vitest run functions/lib/invite-quota.test.ts
```

预期：FAIL，`expected '…' to contain 'invite_reset_at'`（子查询能匹配上，但还没有那个条件）。

- [ ] **Step 3: 写 migration**

`migrations/0002_invite_rotate.sql`：

```sql
-- 邀请码「换码即重置已用名额」（spec 2026-07-29-邀请码配额与换码 §3）。
--
-- 语义：邀请码 = 一张有 invite_quota 次使用次数的券。管理员换码时把 invite_reset_at
-- 写成当下，此后注册端只数「这一刻之后注册的人」，于是名额重新算起。
--
-- 选这个语义而不是「管理员不限量」，理由是安全：即使管理员的码泄露，最多被薅
-- invite_quota 个注册就自动锁死，不是无限。
--
-- 零回填：存量行取默认值 0，而 users.created_at 恒 > 0，所以
-- `c.created_at >= u.invite_reset_at` 对存量账号永真——计数行为与加这列之前完全一致。
ALTER TABLE users ADD COLUMN invite_reset_at INTEGER NOT NULL DEFAULT 0;
```

- [ ] **Step 4: 改注册端的配额子查询**

`functions/api/auth/register.ts`，把「解析邀请码」那段的 `SELECT` 换成：

```ts
  // 解析邀请码：先查归属码，再看引导码。
  // used 只数**当前世代**（invite_reset_at 之后注册的人）——管理员换码时会把
  // invite_reset_at 写成当下，于是新码的名额重新算起。这一条件被
  // functions/lib/invite-quota.test.ts 的源文本契约钉住，别删。
  let invitedBy: string | null = null
  const inviter = await env.DB.prepare(
    `SELECT id, invite_quota,
            (SELECT COUNT(*) FROM users c
              WHERE c.invited_by = u.id AND c.created_at >= u.invite_reset_at) AS used
       FROM users u WHERE invite_code = ?`,
  ).bind(code).first<{ id: string; invite_quota: number; used: number }>()
```

- [ ] **Step 5: 跑测试确认通过**

```bash
npx vitest run functions/lib/invite-quota.test.ts
npm test
npm run typecheck && npm run typecheck:functions
```

预期：契约测试 PASS；全量测试比基线多 1 个（多 1 个文件）；两份 typecheck 无输出。

- [ ] **Step 6: 应用迁移并确认行为没变**

```bash
npm run build && npm run db:migrate:local
```

另开终端起服务（`npx wrangler pages dev dist --port 8788`），然后：

```bash
npm run db:seed:admin
npm run test:api
```

⚠️ `db:seed:admin` 必须在 wrangler **没在跑**的时候执行（它持着本地 D1 文件锁）。
预期：47 项全绿——本任务不改变任何可观测行为，一条都不该变。

- [ ] **Step 7: 提交**

```bash
git add migrations/0002_invite_rotate.sql functions/raw-modules.d.ts functions/lib/invite-quota.test.ts functions/api/auth/register.ts
git commit -F - <<'EOF'
feat: 邀请配额加世代边界 invite_reset_at

为「换码即重置已用名额」铺路：注册端的 used 子查询只数 invite_reset_at
之后注册的人。存量行该列为 0、created_at 恒 > 0，故行为与此前完全一致。

这条口径写在 SQL 里抽不成纯函数，而 CI 质量门不跑 test:api，所以配一条
?raw 源文本契约把它钉住——被删掉时至少 npm test 会红。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 2: `adminGate` 搬到 `lib/auth.ts`

Task 3 的换码端点需要同一套「401 vs 403」判定。它与现有 `adminGate` 逐字相同，**不新写一个** —— 本仓反复吃过「两处各写一份必然漂移」的亏。出现第二个消费者之后，它待在统计整形模块里就是位置错了。

纯搬运，零行为变化。

**Files:**
- Modify: `functions/lib/auth.ts`（追加 `adminGate`）
- Modify: `functions/lib/admin-stats.ts`（删除 `adminGate`）
- Modify: `functions/api/admin/stats.ts`（改 import）
- Create: `functions/lib/auth.test.ts`（收下三条测试）
- Modify: `functions/lib/admin-stats.test.ts`（删掉 `adminGate` 的 describe 与 import）

**Interfaces:**
- Consumes: `functions/lib/auth.ts` 现有的 `AuthedUser`、`requireUser`
- Produces: `adminGate(u: { isAdmin: boolean } | null): 'unauthorized' | 'forbidden' | 'ok'`，从 `functions/lib/auth.ts` 导出（Task 3 会 import）

- [ ] **Step 1: 把测试搬到新文件**

建 `functions/lib/auth.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { adminGate } from './auth'

describe('adminGate', () => {
  // 这三条是全仓唯一进 CI 质量门的鉴权锚点：deploy-cf.yml 只跑 npm test / typecheck / build，
  // 从不跑 npm run test:api，所以集成断言拦不住"谁删掉 isAdmin 判断"这种回归。
  it('没登录 → unauthorized（401）', () => {
    expect(adminGate(null)).toBe('unauthorized')
  })

  it('登录了但不是管理员 → forbidden（403，与 401 刻意分开）', () => {
    expect(adminGate({ isAdmin: false })).toBe('forbidden')
  })

  it('管理员 → ok', () => {
    expect(adminGate({ isAdmin: true })).toBe('ok')
  })
})
```

从 `functions/lib/admin-stats.test.ts` 删掉整个 `describe('adminGate', …)` 块，并把顶部 import 里的 `adminGate` 去掉。

- [ ] **Step 2: 跑测试确认它失败**

```bash
npx vitest run functions/lib/auth.test.ts
```

预期：FAIL，`auth.ts` 没有导出 `adminGate`。

- [ ] **Step 3: 搬函数**

`functions/lib/auth.ts` 末尾追加（注释按新位置改写，保留原有的两条理由）：

```ts
/**
 * 需要管理员权限的端点的鉴权判定。消费者：`/api/admin/stats`、`/api/account/invite` 的 POST（换码）。
 *
 * 抽成纯函数不是为了好看：能锚定"非管理员 403"的集成断言在 scripts/test-api.mjs，
 * 而 .github/workflows/deploy-cf.yml 的质量门**从不跑 npm run test:api**，且它是 push master
 * 即构建即部署。将来谁删掉 isAdmin 判断、或改了 requireUser 的返回结构，CI 会一路绿灯把
 * "登录即可读全站统计"发到线上。单测进 npm test = 进 CI。
 *
 * 401 与 403 刻意分开：合成一个码，排障时分不清"该重新登录"还是"该去 D1 里设 is_admin"。
 */
export function adminGate(u: { isAdmin: boolean } | null): 'unauthorized' | 'forbidden' | 'ok' {
  if (u === null) return 'unauthorized'
  return u.isAdmin ? 'ok' : 'forbidden'
}
```

从 `functions/lib/admin-stats.ts` 删掉 `adminGate` 函数与它上面那段注释。

- [ ] **Step 4: 改 stats.ts 的 import**

`functions/api/admin/stats.ts` 顶部：`adminGate` 从 `../../lib/admin-stats` 的导入列表里移出，改到 `requireUser` 那一行：

```ts
import { requireUser, adminGate } from '../../lib/auth'
import {
  ADMIN_DAYS, ADMIN_TZ_SQL, RECENT_DAYS, dateList, shapeAdminStats, windowStartMs,
  type InviterRow, type RawAdminStats, type RecentUser,
} from '../../lib/admin-stats'
```

- [ ] **Step 5: 跑测试确认通过**

```bash
npm test
npm run typecheck:functions
```

预期：全绿，总数与 Task 1 结束时相同（测试只是换了文件），typecheck 无输出。

- [ ] **Step 6: 提交**

```bash
git add functions/lib/auth.ts functions/lib/auth.test.ts functions/lib/admin-stats.ts functions/lib/admin-stats.test.ts functions/api/admin/stats.ts
git commit -F - <<'EOF'
refactor: adminGate 从 admin-stats 搬到 lib/auth

换码端点要用同一套 401/403 判定。它与 adminGate 逐字相同，另写一个就是
本仓反复吃过亏的「两处各写一份必然漂移」。出现第二个消费者之后，这个纯
鉴权谓词待在统计整形模块里就是位置错了。

纯搬运，零行为变化；三条鉴权锚点随文件迁到 auth.test.ts。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 3: 服务端 —— 读状态与换码

一个文件、两个方法。`GET` 任何已登录用户可调，`POST` 仅管理员。

**Files:**
- Create: `functions/api/account/invite.ts`
- Modify: `scripts/test-api.mjs`（两处插入 + 更新头部注释里的额度估算）

**Interfaces:**
- Consumes: `adminGate`、`requireUser`（`functions/lib/auth.ts`）、`genInviteCode`（`functions/lib/invite.ts`）、`bumpCounter`/`hitRateLimit`/`readRateLimit`（`functions/lib/db.ts`）、`DAY_MS`/`overLimit`（`functions/lib/ratelimit.ts`）、`users.invite_reset_at`（Task 1）
- Produces: `GET|POST /api/account/invite` → `{ inviteCode: string; used: number; quota: number; isAdmin: boolean }`（Task 4 的客户端按这个形状解析；Task 5 不依赖它）

- [ ] **Step 1: 写端点**

`functions/api/account/invite.ts`：

```ts
import type { Env } from '../../lib/db'
import { bumpCounter, hitRateLimit, readRateLimit } from '../../lib/db'
import { json, errorJson } from '../../lib/http'
import { requireUser, adminGate } from '../../lib/auth'
import { genInviteCode } from '../../lib/invite'
import { DAY_MS, overLimit } from '../../lib/ratelimit'

/**
 * 换码日限。无限换码等于无限名额，而这个端点的唯一凭证是一个 **365 天不过期**的 token
 * （见 lib/auth.ts 的 TOKEN_TTL_MS）。按用户计而不是按 IP：换码是账号级操作，
 * 一家人共用出口 IP 时不该互相挤额度。
 */
const ROTATE_PER_DAY_MAX = 5

/** 新码撞 invite_code 的 UNIQUE 约束时的重试次数 */
const ROTATE_TRIES = 3

interface InviteState {
  inviteCode: string
  used: number
  quota: number
  isAdmin: boolean
}

/**
 * 读当前邀请码状态。
 *
 * ⚠️ `used` 的口径必须与 functions/api/auth/register.ts 的配额判据**逐字一致**：
 * 一处显示"还剩 1 个"、另一处判满，是最难排查的一类不一致——家长看着还有名额，
 * 亲友那边却一直报"名额用完了"，两边都没有错误日志。
 */
async function readState(env: Env, userId: string, isAdmin: boolean): Promise<InviteState | null> {
  const row = await env.DB.prepare(
    `SELECT u.invite_code AS code, u.invite_quota AS quota,
            (SELECT COUNT(*) FROM users c
              WHERE c.invited_by = u.id AND c.created_at >= u.invite_reset_at) AS used
       FROM users u WHERE u.id = ?`,
  ).bind(userId).first<{ code: string; quota: number; used: number }>()
  if (!row) return null
  return { inviteCode: row.code, used: row.used, quota: row.quota, isAdmin }
}

/**
 * GET /api/account/invite —— 读自己的邀请码、已用数与额度。
 *
 * 任何已登录用户可调（家长得看得见"已邀 2/5"才知道还能给谁）。
 * 顺带回 isAdmin：客户端那份是登录响应的快照，改完 D1 里的 is_admin 本来要重新登录
 * 才生效，有了这个字段打开设置页就生效。
 */
export const onRequestGet: PagesFunction<Env> = async ({ request, env }) => {
  const user = await requireUser(request, env)
  if (user === null) return errorJson('unauthorized', 401)
  const state = await readState(env, user.id, user.isAdmin)
  // requireUser 已 JOIN users，理论上取不到行只可能是并发删号；按未授权处理
  if (state === null) return errorJson('unauthorized', 401)
  return json(state)
}

/**
 * POST /api/account/invite —— 换一个新邀请码（**仅管理员**）。
 *
 * 同一条 UPDATE 里写 invite_code 与 invite_reset_at：旧码立刻失效，名额从这一刻重新算起
 * （spec §2.1）。**不做宽限期**——立即失效正是"作废泄露的码"的全部意义。
 *
 * 与 GET 同路径、用方法区分，是为了让两者共用 readState()：响应形状必须一致，
 * 分成两个文件就会有两份 used 口径。
 */
export const onRequestPost: PagesFunction<Env> = async ({ request, env }) => {
  const now = Date.now()
  const user = await requireUser(request, env, now)
  const gate = adminGate(user)
  if (gate !== 'ok') return errorJson(gate, gate === 'unauthorized' ? 401 : 403)
  // gate === 'ok' 蕴含 user !== null，但 TS 收窄不到这一步，显式再判一次
  if (user === null) return errorJson('unauthorized', 401)

  const scope = `invite.rotate.${user.id}`
  // 先读后判、不自增：成功换码才吃额度（与 register 的成功额度同一套做法）
  if (overLimit(await readRateLimit(env, scope, DAY_MS, now), ROTATE_PER_DAY_MAX)) {
    await bumpCounter(env, 'invite.rotate.ratelimit', now)
    return errorJson('too_many_requests', 429)
  }

  for (let i = 0; i < ROTATE_TRIES; i++) {
    try {
      await env.DB.prepare(
        `UPDATE users SET invite_code = ?, invite_reset_at = ? WHERE id = ?`,
      ).bind(genInviteCode(), now, user.id).run()
    } catch (e) {
      // 撞上别人已有的码就重摇一个再试；其余错误照常抛
      if (String(e).includes('UNIQUE')) continue
      throw e
    }
    const state = await readState(env, user.id, user.isAdmin)
    if (state === null) return errorJson('unauthorized', 401)
    await hitRateLimit(env, scope, DAY_MS, now)
    await bumpCounter(env, 'invite.rotate.ok', now)
    return json(state)
  }
  // 连撞 ROTATE_TRIES 次：客户端重试即可（沿用 register 里同名的 retry 码）
  return errorJson('retry', 503)
}
```

- [ ] **Step 2: typecheck**

```bash
npm run typecheck:functions
```

预期：无输出。

- [ ] **Step 3: 加集成断言 —— 读状态与拒绝路径**

`scripts/test-api.mjs`，在「---- 登录 ----」那一节**之前**（`myInvite` 与 `token1` 此时都已就绪）插入：

```js
// ---- 邀请码状态 ----
r = await api('/api/account/invite')
check('无 token 读邀请码状态得 401', r.status === 401, `实际 ${r.status}`)

r = await api('/api/account/invite', { token: token1 })
check('登录用户可读自己的邀请码状态',
  r.status === 200 && r.json?.inviteCode === myInvite && r.json?.quota === 5,
  JSON.stringify(r.json))
check('已用数与实际邀请人数一致（email2 正是用这个码注册的）',
  r.json?.used === 1, JSON.stringify(r.json))

r = await api('/api/account/invite', { method: 'POST' })
check('无 token 换码得 401', r.status === 401, `实际 ${r.status}`)

// token1 是用引导码注册的普通账号（register 一律写 is_admin=0）
r = await api('/api/account/invite', { method: 'POST', token: token1 })
check('非管理员换码得 403（而非 401，区分"没登录"与"没权限"）',
  r.status === 403 && r.json?.error === 'forbidden', `实际 ${r.status} ${JSON.stringify(r.json)}`)
```

- [ ] **Step 4: 加集成断言 —— 管理员换码（本迭代核心口径）**

在文件末尾、`console.log(\`\n通过 ${passed}，失败 ${failed}\`)` **之前**插入（`adminToken` 已在管理后台那一节拿到）：

```js
// ---- 管理员换码（本迭代核心：换码即重置已用名额）----
r = await api('/api/account/invite', { token: adminToken })
const beforeCode = r.json?.inviteCode
check('管理员可读自己的邀请码状态', r.status === 200 && !!beforeCode, JSON.stringify(r.json))

// 先用管理员的码注册一个人，把"已用数"做上去——否则换码后的 used === 0 什么也证明不了
const email3 = `s-${rnd()}@example.com`
r = await api('/api/auth/register', {
  method: 'POST', body: { email: email3, authKey: fakeAuthKey('feed'), inviteCode: beforeCode },
})
check('管理员的码可用于注册', r.status === 201, `实际 ${r.status} ${JSON.stringify(r.json)}`)

r = await api('/api/account/invite', { token: adminToken })
check('刚才那次注册计入当前码的已用数', r.json?.used >= 1, JSON.stringify(r.json))

r = await api('/api/account/invite', { method: 'POST', token: adminToken })
const afterCode = r.json?.inviteCode
check('管理员换码成功且拿到新码',
  r.status === 200 && !!afterCode && afterCode !== beforeCode,
  `实际 ${r.status} ${JSON.stringify(r.json)}`)
check('换码后已用名额归零（本迭代的核心口径）', r.json?.used === 0, JSON.stringify(r.json))

r = await api('/api/auth/register', {
  method: 'POST', body: { email: `t-${rnd()}@example.com`, authKey: fakeAuthKey('beef'), inviteCode: beforeCode },
})
check('旧码换掉后立刻失效', r.status === 400 && r.json?.error === 'bad_invite_code',
  `实际 ${r.status} ${JSON.stringify(r.json)}`)

r = await api('/api/auth/register', {
  method: 'POST', body: { email: `u-${rnd()}@example.com`, authKey: fakeAuthKey('beef'), inviteCode: afterCode },
})
check('新码可用于注册', r.status === 201, `实际 ${r.status}`)
```

- [ ] **Step 5: 更新脚本头部的额度估算**

本任务把每遍的注册次数从「成功 2 / 失败 2」抬到「成功 4 / 失败 3」，日额度 20 意味着**一天最多跑 5 遍**。把 `scripts/test-api.mjs` 头部注释里那段改成：

```
 * 限速计数会跨运行累积（存在 counters 表、按时间窗口分桶）。本脚本每遍成功建 4 个号、
 * 失败 3 次，而额度是成功 20/日、失败 10/时（见 functions/lib/ratelimit.ts）——
 * 也就是**同一天最多跑 5 遍**就会撞上成功额度。撞了不是 bug，见下面的清理办法。
```

- [ ] **Step 6: 跑集成测试**

```bash
npm run build && npm run db:migrate:local
```

（wrangler 没在跑时）`npm run db:seed:admin`，起 `npx wrangler pages dev dist --port 8788`，另开终端：

```bash
npm run test:api
```

预期：56 项全绿（47 + 本任务新增 9）。撞 429 就先停 wrangler 再 `npm run db:reset:limits`。

- [ ] **Step 7: 提交**

```bash
git add functions/api/account/invite.ts scripts/test-api.mjs
git commit -F - <<'EOF'
feat: 邀请码状态端点与管理员换码

GET /api/account/invite 任何登录用户可读自己的码、已用数与额度；
POST 仅管理员，同一条 UPDATE 写新码与 invite_reset_at——旧码立刻失效、
名额从这一刻重新算起。

同路径用方法区分是为了共用 readState()：两者响应形状必须一致，分成两个
文件就会有两份 used 口径，而那与注册端判据分叉正是最难排查的一类不一致。

换码限速 5 次/日/用户：无限换码等于无限名额，而这个端点的唯一凭证是一个
365 天不过期的 token。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 4: 客户端 —— 名额可见 + 管理员换码入口

**Files:**
- Create: `src/sync/invite-api.ts`
- Modify: `src/sync/account.ts`（`authErrorKey` 加 `forbidden`）
- Modify: `src/sync/CloudSyncCard.tsx`
- Modify: `src/i18n.tsx`（zh + en 各 6 条新增、1 条改写）

**Interfaces:**
- Consumes: `GET|POST /api/account/invite`（Task 3）；`getAccount`/`setMeta`/`META`/`authErrorKey`（`src/sync/account.ts`）
- Produces: `fetchInviteState(): Promise<InviteResult>`、`rotateInviteCode(): Promise<InviteResult>`，其中
  `type InviteResult = { ok: true; state: InviteState } | { ok: false; errorKey: string }`，
  `interface InviteState { inviteCode: string; used: number; quota: number; isAdmin: boolean }`

- [ ] **Step 1: `authErrorKey` 加 `forbidden`**

`src/sync/account.ts` 的 `MAP` 里加一行：

```ts
    forbidden: 'sync.err.forbidden',
```

理由写进注释（放在 MAP 上方那段注释末尾）：

```ts
/**
 * 服务端错误码（3b-1 已固定）→ i18n key；未知码兜底成网络错误，不显示空白。
 *
 * forbidden 来自换码端点：客户端的 isAdmin 是快照，D1 里被撤掉管理员之后按钮还在，
 * 点下去就是 403。落到默认的"连不上服务器"会把权限问题说成网络问题。
 */
```

- [ ] **Step 2: 写客户端编排层**

`src/sync/invite-api.ts`：

```ts
import { getAccount, setMeta, META, authErrorKey } from './account'

export interface InviteState {
  inviteCode: string
  used: number
  quota: number
  isAdmin: boolean
}

export type InviteResult =
  | { ok: true; state: InviteState }
  | { ok: false; errorKey: string }

/**
 * 把服务端的权威值写回 syncMeta。
 *
 * ⚠️ **只在成功响应时调用。** 离线时若把 isAdmin 清成 '0'，管理后台入口会凭空消失，
 * 而家长完全看不出是断网导致的——这台设备从此"不是管理员"，直到重新登录。
 */
async function persist(state: InviteState): Promise<void> {
  await setMeta(META.inviteCode, state.inviteCode)
  await setMeta(META.isAdmin, state.isAdmin ? '1' : '0')
}

/** GET 读状态 / POST 换码，两者响应形状相同，故共用这一条路径 */
async function call(method: 'GET' | 'POST'): Promise<InviteResult> {
  const acc = await getAccount()
  if (acc === null) return { ok: false, errorKey: 'sync.err.unauthorized' }
  try {
    const res = await fetch('/api/account/invite', {
      method,
      headers: { Authorization: `Bearer ${acc.token}` },
    })
    if (!res.ok) {
      const body = (await res.json().catch(() => null)) as { error?: string } | null
      return { ok: false, errorKey: authErrorKey(String(body?.error ?? '')) }
    }
    const state = (await res.json()) as InviteState
    await persist(state)
    return { ok: true, state }
  } catch {
    return { ok: false, errorKey: 'sync.err.network' }
  }
}

/** 读自己的邀请码、已用数与额度。失败时调用方应保留 syncMeta 里的快照码，不要清空。 */
export const fetchInviteState = (): Promise<InviteResult> => call('GET')

/** 换一个新码（仅管理员；非管理员会拿到 sync.err.forbidden） */
export const rotateInviteCode = (): Promise<InviteResult> => call('POST')
```

- [ ] **Step 3: 改设置页的云同步卡**

`src/sync/CloudSyncCard.tsx`：

顶部 import 加一行：

```ts
import { fetchInviteState, rotateInviteCode } from './invite-api'
```

state 加两个（注意**不要**叫 `invite`——那个名字已被注册表单的输入框占着）：

```ts
  const [inviteUsage, setInviteUsage] = useState<{ used: number; quota: number } | null>(null)
  const [rotating, setRotating] = useState(false)
```

在 `refresh()` **上方**加一个函数：

```ts
  /**
   * 拉一次服务端的权威邀请码状态。
   * 失败（离线）时**什么都不改**——保留 syncMeta 里的快照码，只是不显示名额；
   * 显示 "0/5" 是撒谎，清空成转圈或空白是退步。
   */
  async function loadInvite(): Promise<void> {
    const r = await fetchInviteState()
    if (!r.ok) return
    setInviteUsage({ used: r.state.used, quota: r.state.quota })
    setAcc((prev) => (prev === null ? prev : { ...prev, inviteCode: r.state.inviteCode, isAdmin: r.state.isAdmin }))
    onAccountChange?.()
  }
```

`refresh()` 末尾追加一行（放在 `onAccountChange?.()` 之后）：

```ts
    if (a !== null) void loadInvite()
```

再加换码处理函数（放在 `onLogout` 之后）：

```ts
  async function onRotate(): Promise<void> {
    if (!window.confirm(t('sync.inviteRotateConfirm'))) return
    setRotating(true)
    const r = await rotateInviteCode()
    setRotating(false)
    if (!r.ok) {
      setErrorKey(r.errorKey)
      return
    }
    setErrorKey(null)
    setInviteUsage({ used: r.state.used, quota: r.state.quota })
    setAcc((prev) => (prev === null ? prev : { ...prev, inviteCode: r.state.inviteCode, isAdmin: r.state.isAdmin }))
    // 码换掉了，"已复制 ✓" 必须收回——否则家长以为剪贴板里是新码，实际是刚作废的那个
    setCopied(false)
    onAccountChange?.()
  }
```

已登录分支的邀请码那一行改成：

```tsx
        <div style={{ fontSize: 13, marginTop: 8, display: 'flex', alignItems: 'center', gap: 8, flexWrap: 'wrap' }}>
          <b>{t('sync.myInvite')}</b>
          <code style={{ letterSpacing: 1, fontSize: 14 }}>{acc.inviteCode}</code>
          <button
            className="fq-btn"
            onClick={() => { void navigator.clipboard?.writeText(acc.inviteCode); setCopied(true) }}
          >
            {copied ? t('sync.copied') : t('sync.copy')}
          </button>
          {inviteUsage !== null && (
            <span style={{ color: 'var(--muted)' }}>
              {t('sync.inviteUsage', { used: inviteUsage.used, quota: inviteUsage.quota })}
            </span>
          )}
          {acc.isAdmin && (
            <button className="fq-btn" disabled={rotating} onClick={() => void onRotate()}>
              {rotating ? t('sync.working') : t('sync.inviteRotate')}
            </button>
          )}
        </div>
```

已登录分支还要能显示错误（换码失败的唯一出口）。在 `{status === 'failed' && …}` 那一行**之后**加：

```tsx
        {errorKey !== null && <p style={{ fontSize: 13, color: '#e8590c', marginTop: 8 }}>{t(errorKey)}</p>}
```

- [ ] **Step 4: 加中英文案**

`src/i18n.tsx`，zh 字典里 `'sync.inviteHint'` 改写、并在其后加 5 条：

```ts
  'sync.inviteHint': '把它给想用的亲友；注册来源可追溯。名额用完后请联系管理员。',
  'sync.inviteUsage': '已邀 {used}/{quota}',
  'sync.inviteRotate': '换一个码',
  'sync.inviteRotateConfirm': '换一个新邀请码？旧码会立刻失效——已经发出去但还没注册的人需要重新发一次。新码的名额重新算起。',
```

zh 的 `'sync.err.*'` 一组里加：

```ts
  'sync.err.forbidden': '这个操作需要管理员权限',
```

en 字典对应位置：

```ts
  'sync.inviteHint': 'Share it with family or friends; sign-ups are traceable. Ask the admin once the slots run out.',
  'sync.inviteUsage': 'Invited {used}/{quota}',
  'sync.inviteRotate': 'New code',
  'sync.inviteRotateConfirm': 'Issue a new invite code? The old one stops working immediately — anyone you already gave it to will need the new one. The slot count starts over.',
```

```ts
  'sync.err.forbidden': 'That action requires an admin account',
```

- [ ] **Step 5: 把新键加进 i18n 的键名清单**

`src/i18n.test.ts` 用的是**显式键名清单**（不是自动比对两份字典），新键不加进去就没有覆盖。
在 `SYNC_KEYS` 里补 4 个：

```ts
  'sync.inviteUsage', 'sync.inviteRotate', 'sync.inviteRotateConfirm', 'sync.err.forbidden',
```

- [ ] **Step 6: 跑测试与 typecheck**

```bash
npm test
npm run typecheck
npm run build
```

预期：全绿。`src/i18n.test.ts` 会校验 zh/en 键集一致——若报缺键，是某一侧漏加了。

- [ ] **Step 7: 浏览器实测**

起本地整栈（`npm run build && npx wrangler pages dev dist --port 8788`），用种子管理员登录（`admin-seed@example.com` / 该账号的密码在 `scripts/seed-admin.mjs` 里是 authKey 直填，浏览器走不通——**改用你自己注册的账号，并先在停掉 wrangler 时用 `wrangler d1 execute focal-quest-db --local --command "UPDATE users SET is_admin=1 WHERE email='<你的邮箱>'"` 设成管理员，再重启服务并重新登录一次**）。

逐条确认：

1. 设置页云同步卡显示「已邀 0/5」；
2. 管理员账号能看到「换一个码」按钮；普通账号看不到；
3. 点换码 → 弹确认 → 确认后码变了、名额显示 0/5、「已复制 ✓」变回「复制」；
4. 断网（DevTools Offline）后刷新设置页：码仍显示（快照），名额那一段消失，页面不报错、管理后台入口不消失。

- [ ] **Step 8: 提交**

```bash
git add src/sync/invite-api.ts src/sync/account.ts src/sync/CloudSyncCard.tsx src/i18n.tsx src/i18n.test.ts
git commit -F - <<'EOF'
feat: 设置页显示邀请名额，管理员可换码

家长此前只看到一串码和"额度有限"四个字，一个数字都没有——名额用完之前
没人知道快用完了，用完之后是亲友那边收到报错。现在拉 GET /api/account/invite
显示"已邀 n/5"，管理员多一个「换一个码」按钮。

顺带修掉两个既有缺口：客户端的邀请码与 isAdmin 原本都只是登录快照，
在 iPad 上换了码笔记本会一直显示旧码，改完 D1 的 is_admin 也要重新登录。

离线时刻意什么都不改：保留快照码、只是不显示名额。把 isAdmin 清成 0 会让
管理后台入口凭空消失，而家长看不出是断网导致的。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 5: 管理后台的邀请排行拆两个口径

换码之后，`COUNT(c.id)` 数的是历史累计，而配额判据只数当前世代。不改就会出现「已邀 7 / 配额 5」这种自相矛盾的数字。

**Files:**
- Modify: `functions/lib/admin-stats.ts`（`InviterRow` 加字段）
- Modify: `functions/lib/admin-stats.test.ts`（加一条透传断言）
- Modify: `functions/api/admin/stats.ts`（排行 SQL + 映射）
- Modify: `src/admin/admin-api.ts`（`InviterRow` 加字段）
- Modify: `src/admin/format.ts`（两个新 metric 进 KNOWN_METRICS）
- Modify: `src/admin/format.test.ts`（断言新 metric 有专属文案）
- Modify: `src/admin/AdminPage.tsx`（表头与单元格）
- Modify: `src/i18n.tsx`（改 1 条、加 3 条、删 1 条，zh + en）

**Interfaces:**
- Consumes: `users.invite_reset_at`（Task 1）、`invite.rotate.*` 两个 counter（Task 3）
- Produces: `InviterRow` 增加 `currentUsed: number`（服务端与客户端两份声明必须同步改）

- [ ] **Step 1: 写失败的测试**

`functions/lib/admin-stats.test.ts`，在 `shapeAdminStats` 的 describe 里加：

```ts
  it('邀请排行透传 currentUsed（累计与当前码是两个口径，不能在整形层丢掉）', () => {
    const s = shapeAdminStats(
      { ...raw, inviters: [{ email: 'a@b.com', invited: 7, quota: 5, currentUsed: 2 }] },
      NOW,
    )
    expect(s.inviters[0]).toEqual({ email: 'a@b.com', invited: 7, quota: 5, currentUsed: 2 })
  })
```

> `raw` 是该 `describe('shapeAdminStats', …)` 块里已有的 `RawAdminStats` 夹具（约 186 行）。
> ⚠️ 它自己那行 `inviters: [{ email: 'a@b.com', invited: 2, quota: 5 }]` **也要补上 `currentUsed`**
> （比如 `currentUsed: 2`），否则 Step 3 加了必填字段后 `typecheck` 会在这里报错。

`src/admin/format.test.ts` 加：

```ts
  it('换码计数有专属文案（否则后台会原样显示 invite.rotate.ok）', () => {
    expect(metricLabel('invite.rotate.ok')).toEqual({ key: 'admin.metric.invite.rotate.ok' })
    expect(metricLabel('invite.rotate.ratelimit')).toEqual({ key: 'admin.metric.invite.rotate.ratelimit' })
  })
```

- [ ] **Step 2: 跑测试确认失败**

```bash
npx vitest run functions/lib/admin-stats.test.ts src/admin/format.test.ts
```

预期：两条都 FAIL。

- [ ] **Step 3: 改类型与整形层**

`functions/lib/admin-stats.ts`：

```ts
export interface InviterRow {
  email: string
  /** 历史累计邀请人数（跨所有世代的码） */
  invited: number
  quota: number
  /** **当前这个码**已用掉的名额（invite_reset_at 之后注册的人）——与注册端的配额判据同口径 */
  currentUsed: number
}
```

`src/admin/admin-api.ts` 里那份同名接口做同样的改动（两份声明必须同步，否则前端拿不到字段）。

`src/admin/format.ts` 的 `KNOWN_METRICS` 加两项：

```ts
const KNOWN_METRICS = new Set([
  'register.ok', 'register.badcode', 'register.ratelimit', 'register.quotaexhausted',
  'login.ok', 'login.fail', 'login.ratelimit',
  'push.ok', 'active.user',
  'invite.rotate.ok', 'invite.rotate.ratelimit',
])
```

- [ ] **Step 4: 改排行 SQL 与映射**

`functions/api/admin/stats.ts` 的邀请排行那条查询：

```ts
      // 邀请排行：只列真的邀请过人的账号，故用 INNER JOIN。
      // invited 是**跨世代的历史累计**，currentUsed 只数当前这个码——管理员换过码之后
      // 两者会不等，并排显示成 "累计 7 · 当前码 2/5"。合成一个数就会出现
      // "已邀 7 / 配额 5" 这种自相矛盾的界面。
      // 同邀请数时按邀请人注册时间倒序（"最近谁在拉人"），不按字母序——字母序会让
      // 本地 D1 里累积的历史测试账号把新账号挤出 LIMIT 20。
      env.DB.prepare(
        `SELECT u.email AS email, u.invite_quota AS quota, COUNT(c.id) AS invited,
                SUM(CASE WHEN c.created_at >= u.invite_reset_at THEN 1 ELSE 0 END) AS currentUsed
           FROM users u JOIN users c ON c.invited_by = u.id
          GROUP BY u.id, u.email, u.invite_quota, u.invite_reset_at
          ORDER BY invited DESC, u.created_at DESC LIMIT ?1`,
      ).bind(LIST_LIMIT),
```

映射处：

```ts
    inviters: many<{ email: string; quota: number; invited: number; currentUsed: number }>(inviterRes)
      .map((r): InviterRow => ({
        email: r.email, invited: r.invited, quota: r.quota, currentUsed: r.currentUsed ?? 0,
      })),
```

- [ ] **Step 5: 改表格与文案**

`src/admin/AdminPage.tsx` 的邀请排行表：表头把 `admin.col.quota` 换成 `admin.col.currentUsed`，单元格改成 `{x.currentUsed}/{x.quota}`：

```tsx
                  <th style={TH}>{t('admin.col.email')}</th>
                  <th style={TH}>{t('admin.col.invited')}</th>
                  <th style={TH}>{t('admin.col.currentUsed')}</th>
```

```tsx
                    <td style={TD}>{x.email}</td>
                    <td style={TD}>{x.invited}</td>
                    <td style={TD}>{x.currentUsed}/{x.quota}</td>
```

`src/i18n.tsx`（zh 与 en 各改）：

- `'admin.col.invited'`：zh 改成 `'累计邀请'`，en 改成 `'Total invited'`（与「当前码」区分开，否则两列都叫 Invited）
- **删掉** `'admin.col.quota'`（两份字典都删，保持键集一致）
- 新增：
  - `'admin.col.currentUsed'`：zh `'当前码'` / en `'Current code'`
  - `'admin.metric.invite.rotate.ok'`：zh `'换发邀请码'` / en `'Invite code rotated'`
  - `'admin.metric.invite.rotate.ratelimit'`：zh `'换码被限速'` / en `'Rotation rate-limited'`

同步改 `src/i18n.test.ts` 的 `ADMIN_KEYS` 清单（第 45 行那一带）：**删掉 `'admin.col.quota'`**
（不删会因为字典里已经没有这个键而直接红），补上三个新键：

```ts
  'admin.col.email', 'admin.col.at', 'admin.col.invited', 'admin.col.currentUsed',
  'admin.col.metric', 'admin.col.value',
```

```ts
  'admin.metric.invite.rotate.ok', 'admin.metric.invite.rotate.ratelimit',
```

- [ ] **Step 6: 跑全部检查**

```bash
npm test
npm run typecheck && npm run typecheck:functions
npm run build
```

预期：全绿。若 `src/i18n.test.ts` 报键集不一致，是 `admin.col.quota` 只删了一侧。

- [ ] **Step 7: 集成验证**

重新 `npm run build`、起 wrangler，跑 `npm run test:api`，预期 56 项全绿（本任务不新增断言，只确认排行 SQL 改动没把 admin 那批打挂）。

- [ ] **Step 8: 提交**

```bash
git add functions/lib/admin-stats.ts functions/lib/admin-stats.test.ts functions/api/admin/stats.ts src/admin/admin-api.ts src/admin/format.ts src/admin/format.test.ts src/admin/AdminPage.tsx src/i18n.tsx src/i18n.test.ts
git commit -F - <<'EOF'
feat: 邀请排行拆成累计与当前码两个口径

换码之后 COUNT(invited_by) 数的是跨世代的历史累计，而配额判据只数当前
世代。不拆就会在同一行显示「已邀 7 / 配额 5」这种自相矛盾的数字。

两个口径都留着：累计反映真实拉新贡献，当前码反映还能邀几个。
顺带给 invite.rotate.* 两个计数配上文案，不然后台原样显示 metric 串。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

### Task 6: 文档收尾

**Files:**
- Modify: `docs/部署到-Cloudflare-Pages.md`（「邀请码」一节）
- Modify: `CLAUDE.md`（当前状态加一条、「下一步」补一句）

- [ ] **Step 1: 更新部署文档**

`docs/部署到-Cloudflare-Pages.md` 的「### 邀请码」整节替换成：

```markdown
### 邀请码

注册需邀请码，每个账号有专属码（`users.invite_code`），新用户落库 `invited_by`，注册来源可追溯。

**码 = 一张有 `invite_quota`（默认 5）次使用次数的券。** 已用数由
`COUNT(invited_by) WHERE created_at >= invite_reset_at` 派生——只数**当前世代**。

- **换码**：管理员在设置页「☁️ 云同步」卡点「换一个码」（`POST /api/account/invite`，
  限速 5 次/日/用户）。同一条 UPDATE 写新码与 `invite_reset_at`，**旧码立刻失效、名额重新算起**。
  普通用户没有这个按钮，也没有对应的服务端路径。
- **看名额**：所有登录用户在同一张卡上看到「已邀 n/5」（`GET /api/account/invite`）。
  管理后台的邀请排行另外显示「累计邀请」与「当前码 n/quota」两个口径。
- **封停某人的码**：把 `invite_quota` 改 0（无 UI，去 D1 Console 跑 SQL）。
- **提额**：同样是手写 SQL 改 `invite_quota`，本版本刻意不做提额 UI。

⚠️ `BOOTSTRAP_INVITE_CODE` secret **无限量且不可追溯**（`invited_by` 记 NULL），
是系统里唯一绕过配额的路径。管理员能自助换码之后它已无存在理由——**站长注册完就该删掉**。
```

- [ ] **Step 2: 更新 CLAUDE.md**

在「当前状态」列表末尾（「视标自适应·三个已知缺口收尾」那条之后）加一条，并把「下一步」里与邀请码相关的内容补上。新条目内容：

```markdown
- 邀请码配额与管理员换码 ✅ 完成并上线：**「每码限 5 人」其实一直就有**（`invite_quota` 默认 5 + register 的 COUNT 校验），缺的只是可见性——设置页只显示一串码和「额度有限」四个字，一个数字都没有，于是名额用完之前没人知道、用完之后是亲友那边收到报错。本轮补上「已邀 n/5」，并新增**管理员换码**。**名额语义选的是「换码即重置已用名额」**（码 = 一张有 5 次使用次数的券），而不是「管理员不限量」——理由是安全：即使管理员的码泄露，最多被薅 5 个注册就自动锁死。实现是 `users` 加一列 `invite_reset_at`（存量行为 0、`created_at >= 0` 永真，**零回填**），注册端的配额子查询加 `AND c.created_at >= u.invite_reset_at`。**四个反直觉但必须坚持的口径**：①这条 SQL 条件**抽不成纯函数**而 CI 质量门不跑 `test:api`，删掉它 CI 全绿、线上静默恢复旧行为且毫无症状，故配了 `functions/lib/invite-quota.test.ts` 的 `?raw` 源文本契约钉住；②`GET`/`POST` **同路径用方法区分**（`functions/api/account/invite.ts` 一个文件两个导出），为的是共用 `readState()`——两处 `used` 口径分叉是最难排查的一类不一致（一边显示还剩 1 个、另一边判满，两边都没日志）；③客户端**离线时什么都不改**，保留 syncMeta 的快照码、只是不显示名额——把 `isAdmin` 清成 0 会让管理后台入口凭空消失且看不出是断网；④后台邀请排行必须**拆成「累计」与「当前码 n/quota」两个口径**，否则换过码就会显示「已邀 7 / 配额 5」。顺带修掉两个既有缺口（客户端的邀请码与 `isAdmin` 原本都只是登录快照，iPad 上换了码笔记本会一直显示旧码、改完 D1 的 `is_admin` 要重新登录）与一处代码重复（`adminGate` 从 `admin-stats.ts` 搬到 `lib/auth.ts`，出现第二个消费者后原位置就是错的）。换码限速 5 次/日/用户——无限换码等于无限名额，而这个端点的唯一凭证是 365 天不过期的 token。**明示接受**：配额检查 `COUNT` 后 `INSERT` 非事务，并发注册理论上能超一个额（家庭规模不管）；旧码不做宽限期（立即失效正是作废泄露码的意义）；普通用户无自助换码/提额路径，只能联系管理员（设计意图）。⚠️ **`BOOTSTRAP_INVITE_CODE` 该删了**——它无限量且 `invited_by` 记 NULL 不可追溯，是唯一绕过配额的路径，管理员能自助换码之后已无存在理由。设计见 [spec](docs/superpowers/specs/2026-07-29-邀请码配额与换码-design.md)、[计划](docs/superpowers/plans/2026-07-29-邀请码配额与换码.md)。
```

同时把「开发命令」那段的单测基线数字更新成本次实测值（在 Task 1 开工前记的基线 + 本轮新增），并在「下一步」里加一句：**线上部署后需跑 `npm run db:migrate:remote`，并在 Cloudflare 控制台删掉 `BOOTSTRAP_INVITE_CODE` secret**。

- [ ] **Step 3: 提交**

```bash
git add docs/部署到-Cloudflare-Pages.md CLAUDE.md
git commit -F - <<'EOF'
docs: 记邀请码配额与管理员换码

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## 上线清单（代码之外，必须人工做）

1. **线上迁移**：`npm run db:migrate:remote`（加 `invite_reset_at` 列）。**必须先于代码部署**，否则新版 `register.ts` 的 SQL 会引用不存在的列、注册整个挂掉。
2. **删 `BOOTSTRAP_INVITE_CODE` secret**（Cloudflare 控制台 → Pages 项目 → Settings → Environment variables）。它无限量且不可追溯，管理员能自助换码之后已无存在理由。
3. **确认自己是管理员**：`UPDATE users SET is_admin=1 WHERE email='<你的邮箱>'`。改完打开设置页即可生效（本迭代的 GET 会刷新这个标记，不必再重新登录）。
