# 限时挑战模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给「变焦大冒险」加一个当天练完训练才解锁的 30 秒冲分小游戏——双眼不遮眼、答题窗口按孩子实测反应时间自适应收紧、翻拍照旧要真翻，数据不进统计不落库、最高分只存 localStorage。

**Architecture:** 新增 `src/challenge/` 目录，四个纯函数模块（节奏推导 `challenge-pace.ts`、状态机 `challenge-session.ts`、最高分 `challenge-storage.ts`、解锁判定 `challenge-unlock.ts`）+ 两个组件（`ChallengePage.tsx` 挑战页、`ChallengeCard.tsx` 首页入口卡）。**不复用**训练的 `src/training/session.ts` 状态机（规则不同：有每题时限、不分眼、不落库），**只复用**皮肤 Stage（`getSkin`）、`pickDirection`、`playSfx`、`.fzp-train/.fzp-stage/.fzp-answer` 三段外壳与翻拍引导视觉、`lsGet/lsSet`、`getHomeStats`。挑战页是 `App.tsx` 的一个新 View 分支（**不进 NAV**，入口只在首页当天已打卡时出现），与 `privacy` 同构（带 `onBack`）。

**Tech Stack:** TypeScript + React 18 + Vite 6；DOM + CSS（无游戏引擎、无 canvas）；vitest（`environment: 'node'`，无 jsdom）；localStorage（`lsGet/lsSet`）；Dexie 仅**只读**取 `sessions.avgReactionMs` 做节奏基准。

## Global Constraints

- 交流、代码注释、提交信息一律**简体中文**；技术术语与代码标识符保留英文。
- **医学边界 ①（不可动）：下限系数不得低于 1.3。** 落地为 `FASTEST_FACTOR_FLOOR = 1.3`，`derivePace()` 里用 `Math.max(FASTEST_FACTOR, FASTEST_FACTOR_FLOOR)`，Task 1 有单测锚定。压到 ×1.05 会让约一半的题超时，等于逼孩子瞎猜。
- **医学边界 ②（不可动）：翻拍过渡强制不低于 600ms。** 落地为 `MIN_FLIP_MS = 600` + 唯一出口 `challengeFlipMs(settingMs)`，Task 1 有单测锚定，Task 6 有源文本契约测试禁止挑战页直接用原始 `fzp.flipMs`。若节拍快到孩子来不及真翻拍，她会干脆不翻——挑战退化成纯反应游戏，还会腐蚀主流程。
- 视标物理尺寸恒为 `毫米设定 × 屏幕标定`（`sizeMm * pxPerMm`），**不因挑战而变**；视标及其祖先链上**禁止** `transform: scale`（神庙皮肤那次 0.8 缩放就是先例，历史数据从此不可比）。
- **总时长 30 秒**；**反应时间基准 = 最近 20 次训练 `avgReactionMs` 的中位数**（不是平均）；**初始答题窗口 = 基准 × 2.0**；**最快答题窗口 = 基准 × 1.3**；**每 3 题降一档**；**每档减少 `(初始 − 最快) / 4`，四档到底**。
- **数据不足**（有效 `avgReactionMs` 样本 < 3）→ 固定默认：初始 **2000ms**、最快 **1300ms**。
- **钳制只钳基准，不钳两个窗口**（评审 blocker 修正，见文末「评审意见处置」①）：基准 `b ∈ [INITIAL_MIN_MS / INITIAL_FACTOR = 750, BASELINE_MAX_MS = 3000]` ms，两个窗口一律由 `b` 按固定系数派生（`initialMs = b × 2.0`、`fastestMs = max(FASTEST_MIN_MS, b × 1.3)`）。**禁止**再给 `initialMs` / `fastestMs` 加上限——那种上限会在 `b ≥ 1924ms` 时把最快窗口压回 `b × 1.0` 甚至更低，等于架空医学边界①（b=2500 时窗口正好等于孩子自己的中位反应时间，约一半的题必超时）。低端把窗口抬高是安全方向，可以留（`FASTEST_MIN_MS = 1000`）。
- **时限只覆盖答题窗口**：翻拍过渡是它之后的独立阶段，**不计入答题窗口、也不被压缩**（30 秒总时长是墙钟，包含翻拍过渡）。
- **超时 = 未答**：不算错、不得分、直接下一题；答错与超时**都清连击**，但**分开计数**（`wrong` / `timedOut`）并在结算页分开显示。
- **双眼进行、不遮眼**（binocular accommodative facility 是标准训练项目），不分左右眼。
- **不给积分 / 勋章 / 怪兽，不进统计，不落 Dexie，不同步**。最高分只写 localStorage 键 **`fzp.challengeBest`**。
- **解锁条件**：当天已完成正经训练（`getHomeStats(today).checkedInToday === true`）；未解锁则首页不渲染入口卡，挑战页不可达。IndexedDB 不可用时 `getHomeStats` 降级为 `checkedInToday: false`，即入口不出现（安全方向）。
- **不做**：排行榜、手动难度档位、左右眼分别挑战、游戏引擎。
- **⚠️ 语音作答是待用户拍板的开放项，不由本计划擅自决定**：CLAUDE.md「关键决策」#2「语音报答案为主 + 触控/键盘兜底」被标注为「反复讨论后确定的边界，勿擅自推翻」，而 spec §9「明确不做」里**没有**「不接语音」这一条。必须先走 **Step 0.4** 拿到用户结论、并把结论补进 spec §9，才能按对应分支写 Task 6。
- 测试文件**必须以 `.test.ts` 结尾**——`vitest.config.ts` 的 `include` 只收 `src/**/*.test.ts` 与 `functions/**/*.test.ts`，`.test.tsx` 不会被跑。
- `vitest.config.ts` 是 `globals: false` + `environment: 'node'`：每个测试文件顶部必须 `import { describe, it, expect } from 'vitest'`；**没有 jsdom，组件不可渲染测试**，UI 用手测清单 + `?raw` 源文本契约测试兜底。
- 本仓**没装 `@types/node`**：测试里要读源文件文本请用 Vite 的 `?raw`（如 `import css from '../index.css?raw'`），用 `node:fs` 会让 `tsc` 报 TS2307。
- i18n 新增文案必须 **ZH/EN 双份**（缺 en 会静默回退成中文），并同步 `src/i18n.test.ts` 的键齐全性断言。
- 挑战页**复用** `src/layout/layout-budget.ts` 的既有常量与 `src/index.css` 的既有断点（`@media (max-width: 560px)` / `@media (max-height: 560px)`），**不新造断点、不新增 CSS 类**；顶栏必须保持**一行**，否则 `TOP_BAR_PX = 45` 的布局预算不再准。
- **提交时只 `git add` 本任务列出的确切文件路径**，禁止 `git add -A` / `git add <目录>`（本仓发生过并行 agent 用宽泛 add 卷走并丢失他人改动的事故）。
- 每个任务收尾都要跑 `npm test` **和** `npm run typecheck`，两者都干净才提交。
- 已知副作用（不用额外改代码，但要知道）：`src/backup/backup-service.ts` 的 `collectSettings()` 按 `fzp.` 前缀收录全部 localStorage 键，所以 `fzp.challengeBest` **会自动进 JSON 备份/恢复**；`src/reset/reset.ts` 只清 7 张 Dexie 表、明确保留 localStorage，所以「清空数据」后最高分**仍在**——这是有意的（那不是训练记录）。

## 开工前必做

- [ ] **Step 0.1: 记下测试基线。** 跑 `npm test`，把末尾两行形如 `Test Files  N passed (N)` / `Tests  M passed (M)` 的数字抄到便签上，记作 **基线 = N 文件 / M 测试**。本计划所有测试数字都写成「基线 + 本任务新增」，**不要**去信 CLAUDE.md 里的历史快照（那是过时的）。
- [ ] **Step 0.2: 确认 typecheck 干净起步。** 跑 `npm run typecheck`，必须零输出（有报错先停手，不是本计划引入的）。
- [ ] **Step 0.3: 读 spec 一遍。** `docs/superpowers/specs/2026-07-26-限时挑战-design.md`。注意 spec §6 把按 id 取皮肤的函数写成了 `skinById`，**实际叫 `getSkin(id)`**（`src/skins/registry.ts:24`），本计划已按 `getSkin` 写。

- [ ] **Step 0.4: 🚦 阻塞项——找用户拍板「挑战页要不要接语音作答」。** 这是**孩子主输入方式的产品行为变更**，不是实现细节：正经训练里她张嘴报答案，若挑战不接语音，她就得在 40cm 外一边扶着拍子一边点屏幕。CLAUDE.md 关键决策 #2 写着「语音报答案为主 + 触控/键盘兜底」且标注「勿擅自推翻」，spec §9 也没写这一条，所以**实现计划无权顺手定掉**。把下面三个选项原样念给用户，拿到明确选择后再动 Task 6：

  | 选项 | 做法 | 代价 |
  |---|---|---|
  | **A. 挑战不接语音**（触控/键盘） | Task 6 按现文照写；`.fzp-voice-hint` 那格放免责文案 | 与关键决策 #2 不一致——**必须**先把「挑战页不接语音」补进 spec §9「明确不做」再实施 |
  | **B. 准备页给「语音 / 触控」二选一开关**（默认触控） | 选语音时给答题窗口整体加固定补偿常量 `VOICE_COMPENSATION_MS = 400`（同样收进 `challenge-pace.ts`，即 `windowMsForIndex` 的结果 + 补偿），语音链路复用训练页那套 vosk | 多一个设置项与一层交互；补偿值需实测校准 |
  | **C. 语音只作并行输入** | 语音与触控/键盘同时挂着，窗口计时终点按**最早到达的那一路**算 | 首次进挑战会拉 42MB 模型（已装机的 iPad 有缓存）；识别延迟仍可能让"说了但没被判到"，需实测 |

  技术依据（讲给用户听，但不替他做决定）：vosk 从出声到出词有几百 ms 延迟，而挑战的答题窗口最短只有 ~1.3 秒，延迟会让"明明及时说出来了却判超时"——这是**支持 A/B 的理由，不是替代用户决定的理由**。
  - 选 **A** → 先编辑 `docs/superpowers/specs/2026-07-26-限时挑战-design.md` §9「明确不做」加一条「**不接语音作答**（挑战窗口最短 ~1.3 秒，vosk 出词延迟会让判超时不公平；正经训练那边语音照旧）」，再开工。
  - 选 **B/C** → 先把方案写进 spec §3/§9，再回来改本计划的 Task 1（加 `VOICE_COMPENSATION_MS`）与 Task 6（接语音链路 + 相应 i18n 键），**不要**边写边定。
  - 用户没给结论前**不要**开工 Task 6（Task 1-5 与本项无关，可以先做）。

- [ ] **Step 0.5: 修正 spec §4.4 的钳制口径（开工 Task 1 之前做，顺带知会用户一句）。** spec 自己打架了：§4.3 定「最快窗口 = 基准 × 1.3，**下限系数不得低于 1.3**……这条是本设计的医学边界」，§4.4 又定「最快窗口钳在 [1000, **2500**]ms」。手工推演：基准 b ≥ 1924ms 时 `round(1.3b) > 2500` 被钳到 2500，实际系数掉到 1.3 以下——**b=2000 → ×1.25；b=2500 → ×1.0（窗口正好等于孩子自己的中位反应时间）；b=4000 → ×0.63**；`INITIAL_FACTOR=2.0` 同样在 b ≥ 2000 时被 4000 的上限架空。而 `avgReactionMs` 记的是「视标出现 → 答对」，**包含透过 ±2.00D 拍子把模糊看清的时间**，2000-2500ms 对刚开始练或累了的孩子完全现实，正好落在破口上——这恰恰是边界①要防的「约一半的题超时 = 逼孩子瞎猜」。**上限赢了下限，医学边界被静默架空。**
  - 编辑 `docs/superpowers/specs/2026-07-26-限时挑战-design.md` §4.4，把「钳制区间」那一行改成：

    ```markdown
    - **钳制区间**（防异常数据算出荒谬值）：**只钳基准，不钳窗口**——基准钳在 `[750, 3000]`ms
      （下限 = 初始窗口地板 1500 ÷ 2.0；上限 3000 = 真孩子最慢也就 2-3 秒，再大即脏数据），
      两个窗口一律由钳后的基准按 §4.3 的系数派生。**不得给初始/最快窗口本身加上限**：那种
      上限会在基准 ≥1924ms 时把最快窗口的实际系数压到 1.3 以下，等于静默架空 §4.3 的医学边界。
      最快窗口保留一个下限 1000ms（把窗口抬高＝多给时间，是安全方向）。
    ```
  - 知会用户一句（不是征求同意，是告知一处**只会让挑战更宽松**的口径修正）：「原 spec 的窗口上限会在你闺女反应变慢时把节拍压到比她自己的反应时间还快，已改成只钳基准；影响只有一个方向——反应慢的日子窗口更长，绝不会更短。」
  - 若用户**坚持**保留 2500 上限，则必须在 spec §4.3/§4.4 里显式写明「基准 >1900ms 时放弃医学边界①」，并让 `derivePace` 多返回一个 `capped: true` 供 UI 提示家长「今天的节拍已达上限，可能偏快」——**不能像原稿那样静默降级**，且本计划的 Task 1 需按该分支重写。

---

## File Structure

**新建**

| 路径 | 职责 |
|---|---|
| `src/challenge/challenge-pace.ts` | 全部节奏常量（含两条医学边界）+ `medianMs` / `derivePace` / `windowMsForIndex` / `challengeFlipMs` / `DEFAULT_PACE`。**所有可调数字的唯一出处** |
| `src/challenge/challenge-pace.test.ts` | 上述纯函数与两条医学边界的单测 |
| `src/challenge/challenge-session.ts` | 挑战状态机纯函数：`createChallenge` / `startChallenge` / `answerChallenge` / `advanceChallenge` / `tickChallenge` / `scoreForAnswer` + 计分常量 |
| `src/challenge/challenge-session.test.ts` | 状态机单测（含超时语义、时限只覆盖答题窗口、总时长优先） |
| `src/challenge/challenge-storage.ts` | 最高分：纯函数 `parseBest` / `bestAfter` + 薄包装 `readBest` / `writeBestIfHigher` + 键名常量 |
| `src/challenge/challenge-storage.test.ts` | 最高分单测（含注入内存 localStorage 覆盖薄包装） |
| `src/challenge/challenge-unlock.ts` | `challengeUnlocked(stats)` 解锁判定纯函数 |
| `src/challenge/challenge-unlock.test.ts` | 解锁判定单测（有/无今日打卡 + 加载中 + 缺字段） |
| `src/challenge/ChallengePage.tsx` | 挑战页：准备 → 答题/翻拍 → 结算三态，复用三段外壳与皮肤 Stage |
| `src/challenge/ChallengeCard.tsx` | 首页入口卡（只在解锁时被 HomePage 渲染） |
| `src/challenge/challenge-shell.test.ts` | `?raw` 源文本契约：外壳复用 / 无自造断点 / 无 scale / 不落库 / 翻拍必过 `challengeFlipMs` |
| `src/challenge/challenge-entry.test.ts` | `?raw` 源文本契约：首页入口只在解锁时渲染、挑战页不进 NAV、回调已接线 |
| `src/training/key-map.ts` | 从 `TrainingPage.tsx` 抽出的键盘映射 `KEY_MAP` + `dirForKey(key)`，训练页与挑战页共用 |
| `src/training/key-map.test.ts` | 键盘映射单测 |

**修改**

| 路径 | 改动 |
|---|---|
| `src/training/TrainingPage.tsx` | 删掉 effect 内的局部 `KEY_MAP`，改用 `dirForKey`（行为不变） |
| `src/training/sfx.ts` | `SfxKind` 加 `'timeout'`（模块私有类型，只改这一个文件）+ 一个短促下滑音；**超时不许再用 `'wrong'` 音效**——那与 spec §4.5「超时=未答、不算错」矛盾 |
| `src/i18n.tsx` | ZH/EN 各加 22 条 `challenge.*` 文案 |
| `src/i18n.test.ts` | 新增 `CHALLENGE_KEYS` 常量 + 一个齐全性 `it` |
| `src/App.tsx` | `View` 加 `'challenge'`；新增 `{view === 'challenge' && <ChallengePage onBack={…} />}` 分支；给 `HomePage` 传 `onOpenChallenge`（**NAV 不动**） |
| `src/HomePage.tsx` | props 加 `onOpenChallenge`；`challengeUnlocked(stats)` 为真时渲染 `<ChallengeCard>` |
| `CLAUDE.md` | 状态区加「迭代 2·限时挑战 ✅」条目；「下一步」去掉「限时挑战需确认节奏」 |

---

### Task 1: 自适应节奏纯函数（含两条医学边界）

**Files**
- Create: `E:\developer\fzp\src\challenge\challenge-pace.ts`
- Test: `E:\developer\fzp\src\challenge\challenge-pace.test.ts`

**Interfaces**
- Consumes: 无（零依赖纯函数模块）
- Produces:
  ```ts
  export const CHALLENGE_DURATION_MS = 30_000
  export const PACE_SAMPLE_N = 20
  export const MIN_SAMPLES = 3
  export const DEFAULT_BASELINE_MS = 1000
  export const INITIAL_FACTOR = 2.0
  export const FASTEST_FACTOR = 1.3
  export const FASTEST_FACTOR_FLOOR = 1.3
  export const STEP_EVERY = 3
  export const TIERS = 4
  export const INITIAL_MIN_MS = 1500
  export const FASTEST_MIN_MS = 1000
  export const BASELINE_MAX_MS = 3000
  export const MIN_FLIP_MS = 600
  export interface Pace { baselineMs: number; initialMs: number; fastestMs: number; stepMs: number; adaptive: boolean }
  export function medianMs(values: number[]): number | null
  export function derivePace(reactionMsList: number[]): Pace
  export function windowMsForIndex(pace: Pace, index: number): number
  export function challengeFlipMs(settingMs: number | null | undefined): number
  export const DEFAULT_PACE: Pace
  ```

- [ ] **Step 1.1: 写失败测试。** 新建 `src/challenge/challenge-pace.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import {
  medianMs, derivePace, windowMsForIndex, challengeFlipMs, DEFAULT_PACE,
  CHALLENGE_DURATION_MS, PACE_SAMPLE_N, MIN_SAMPLES, DEFAULT_BASELINE_MS,
  INITIAL_FACTOR, FASTEST_FACTOR, FASTEST_FACTOR_FLOOR, STEP_EVERY, TIERS,
  INITIAL_MIN_MS, FASTEST_MIN_MS, BASELINE_MAX_MS, MIN_FLIP_MS,
} from './challenge-pace'

describe('medianMs', () => {
  it('奇数个取中间那个', () => {
    expect(medianMs([900, 1000, 5000])).toBe(1000)
  })

  it('偶数个取中间两个的平均', () => {
    expect(medianMs([800, 900, 1100, 1200])).toBe(1000)
  })

  it('过滤 0 与非法值（写库时没有反应数据会落 0，不是 undefined）', () => {
    expect(medianMs([0, 0, 1000, Number.NaN, 1000])).toBe(1000)
  })

  it('没有有效值返回 null', () => {
    expect(medianMs([])).toBeNull()
    expect(medianMs([0, 0, 0])).toBeNull()
  })
})

describe('derivePace', () => {
  it('有效样本少于 3 个 → 固定默认 2000/1300（spec §4.4）', () => {
    const p = derivePace([1000, 0, 1000])
    expect(p).toMatchObject({ adaptive: false, baselineMs: DEFAULT_BASELINE_MS, initialMs: 2000, fastestMs: 1300 })
    expect(p.stepMs).toBe(175)
    expect(MIN_SAMPLES).toBe(3)
  })

  it('基准 1000ms → 初始 2000 / 最快 1300 / 每档 175（spec §4.3 的算例）', () => {
    const p = derivePace([1000, 1000, 1000, 1000])
    expect(p).toMatchObject({ adaptive: true, baselineMs: 1000, initialMs: 2000, fastestMs: 1300, stepMs: 175 })
    expect(INITIAL_FACTOR).toBe(2.0)
    expect(STEP_EVERY).toBe(3)
    expect(TIERS).toBe(4)
  })

  it('只看最近 PACE_SAMPLE_N 个（调用方传新→旧），第 21 个极值不影响基准', () => {
    const twenty = Array.from({ length: PACE_SAMPLE_N }, () => 1000)
    expect(derivePace([...twenty, 9000]).baselineMs).toBe(1000)
  })

  it('基准过小：钳的是基准（下限 750 = INITIAL_MIN_MS / INITIAL_FACTOR），窗口仍按系数派生 → 1500 / 1000', () => {
    const p = derivePace([200, 200, 200, 200])
    expect(p.baselineMs).toBe(INITIAL_MIN_MS / INITIAL_FACTOR)
    expect(p.initialMs).toBe(INITIAL_MIN_MS)
    expect(p.fastestMs).toBe(FASTEST_MIN_MS) // 750×1.3=975 被 FASTEST_MIN_MS 抬到 1000（抬高=安全方向）
  })

  it('基准过大：只钳基准到 BASELINE_MAX_MS=3000（真孩子最慢也就 2-3 秒，再大即脏数据），两窗仍按系数派生', () => {
    const p = derivePace([5000, 5000, 5000, 5000])
    expect(p.baselineMs).toBe(BASELINE_MAX_MS)
    expect(p.initialMs).toBe(6000) // 3000 × 2.0
    expect(p.fastestMs).toBe(3900) // 3000 × 1.3 —— 系数没有被任何窗口上限压过
  })

  it('医学边界①：任何基准下最快窗口都不低于 基准×1.3（低端只会被抬高，高端只钳基准本身）', () => {
    for (const b of [150, 400, 700, 1000, 1234, 1800, 2400, 4000, 8000]) {
      const p = derivePace([b, b, b, b])
      expect(p.fastestMs, `基准 ${b}`).toBeGreaterThanOrEqual(
        Math.round(Math.min(b, BASELINE_MAX_MS) * FASTEST_FACTOR_FLOOR),
      )
      expect(p.fastestMs).toBeLessThanOrEqual(p.initialMs)
    }
  })

  it('回归锚：基准 2500ms 的最快窗口必须 ≥3250ms（旧版 FASTEST_MAX_MS=2500 会把它压成 2500=×1.0，约一半的题必超时）', () => {
    expect(derivePace([2500, 2500, 2500, 2500]).fastestMs).toBeGreaterThanOrEqual(3250)
    // 顺带锁住"钳制不得再爬回窗口上"：两窗恒等于 基准×系数（低端抬高除外）
    const p = derivePace([2000, 2000, 2000, 2000])
    expect(p.initialMs).toBe(Math.round(p.baselineMs * INITIAL_FACTOR))
    expect(p.fastestMs).toBe(Math.round(p.baselineMs * FASTEST_FACTOR))
  })

  it('DEFAULT_PACE 就是数据不足时那一档，且总时长是 30 秒', () => {
    expect(DEFAULT_PACE).toMatchObject({ adaptive: false, initialMs: 2000, fastestMs: 1300 })
    expect(CHALLENGE_DURATION_MS).toBe(30_000)
  })
})

describe('windowMsForIndex', () => {
  it('每 3 题降一档、四档到底（基准 1000ms 的完整序列）', () => {
    const p = derivePace([1000, 1000, 1000, 1000])
    const seq = Array.from({ length: 15 }, (_, i) => windowMsForIndex(p, i))
    expect(seq).toEqual([
      2000, 2000, 2000,
      1825, 1825, 1825,
      1650, 1650, 1650,
      1475, 1475, 1475,
      1300, 1300, 1300,
    ])
  })

  it('永远落在 [fastestMs, initialMs] 区间内（第 40 题也不会破底）', () => {
    for (const b of [300, 1000, 1700, 6000]) {
      const p = derivePace([b, b, b, b])
      for (let i = 0; i <= 40; i++) {
        const w = windowMsForIndex(p, i)
        expect(w, `基准 ${b} 第 ${i} 题`).toBeGreaterThanOrEqual(p.fastestMs)
        expect(w).toBeLessThanOrEqual(p.initialMs)
      }
    }
  })
})

describe('challengeFlipMs', () => {
  it('医学边界②：设置页三档（600/900/1500）恒等通过', () => {
    expect(challengeFlipMs(600)).toBe(600)
    expect(challengeFlipMs(900)).toBe(900)
    expect(challengeFlipMs(1500)).toBe(1500)
  })

  it('医学边界②：任何低于 600 的值（含手改 localStorage / 将来加更快档）都被抬到 600', () => {
    expect(MIN_FLIP_MS).toBe(600)
    for (const v of [0, 1, 120, 599]) expect(challengeFlipMs(v), `输入 ${v}`).toBe(MIN_FLIP_MS)
  })

  it('非法值兜底 600，不是 NaN', () => {
    expect(challengeFlipMs(Number.NaN)).toBe(MIN_FLIP_MS)
    expect(challengeFlipMs(null)).toBe(MIN_FLIP_MS)
    expect(challengeFlipMs(undefined)).toBe(MIN_FLIP_MS)
  })

  it('医学边界的两个系数常量本身也被锚定（有人调参时必须撞到红）', () => {
    expect(FASTEST_FACTOR).toBeGreaterThanOrEqual(1.3)
    expect(FASTEST_FACTOR_FLOOR).toBe(1.3)
    // 最快窗口必须真的比初始窗口短，否则"加速"会变成"减速"。
    // 这条替代了旧实现里 Math.min(..., initialMs) 那条安全带——安全带会把误配静默吞掉，
    // 而误配正该在测试里炸出来。
    expect(FASTEST_FACTOR).toBeLessThan(INITIAL_FACTOR)
  })
})
```

- [ ] **Step 1.2: 跑到失败。** `npm test -- challenge-pace`。预期失败信息：`Failed to resolve import "./challenge-pace" from "src/challenge/challenge-pace.test.ts"`（模块还不存在）。

- [ ] **Step 1.3: 最小实现。** 新建 `src/challenge/challenge-pace.ts`：

```ts
/**
 * 限时挑战的节奏推导：把孩子实测的反应时间（sessions.avgReactionMs）折成"每题给多少时间"。
 *
 * ⚠️ 本文件是挑战全部可调数字的唯一出处，实测后只改这里。其中两条是**医学边界，不可动**：
 *   ① FASTEST_FACTOR_FLOOR = 1.3 —— 下限系数不得低于 1.3。avgReactionMs 是平均值、
 *      实际有波动，压到 ×1.05 会让约一半的题超时，那就是逼孩子瞎猜。
 *   ② MIN_FLIP_MS = 600 —— 翻拍过渡不得低于 600ms（设置页最快档，用户已认可的下限）。
 *      节拍快到来不及真翻拍，孩子就会干脆不翻，挑战退化成纯反应游戏并腐蚀主流程。
 *
 * ⚠️⚠️ 钳制只钳**基准**，绝不钳两个窗口。曾经的写法是给 initialMs / fastestMs 各加一个
 * 上限（4000 / 2500），那会在基准 ≥1924ms 时把最快窗口压回 基准×1.0 甚至更低——
 * 基准 2500ms 时窗口正好等于孩子自己的中位反应时间，约一半的题必超时，医学边界①被
 * 静默架空。而 avgReactionMs 记的是"视标出现→答对"，含透过 ±2.00D 拍子把模糊看清的
 * 时间，2000-2500ms 对刚开始练或累了的孩子完全现实，正好落在那个破口上。
 * 现在的口径：b = clamp(中位数, INITIAL_MIN_MS/INITIAL_FACTOR, BASELINE_MAX_MS)，
 * 两窗一律 b × 固定系数；低端把窗口抬高（FASTEST_MIN_MS）是安全方向，予以保留。
 *
 * 语义要点（spec §4.2）：这里算的是**答题窗口**（视标出现 → 必须答出），翻拍过渡是它之后
 * 的独立阶段，不计入窗口、也不被压缩。
 */

/** 总时长：30 秒（可控、不伤眼、有紧张感） */
export const CHALLENGE_DURATION_MS = 30_000
/** 基准取最近多少次训练 */
export const PACE_SAMPLE_N = 20
/** 少于这么多个有效样本就不自适应，走固定默认 */
export const MIN_SAMPLES = 3
/** 数据不足时的假定基准（= 1 秒，用户实测孩子约 1 秒或更快） */
export const DEFAULT_BASELINE_MS = 1000
/** 初始答题窗口 = 基准 × 此系数 */
export const INITIAL_FACTOR = 2.0
/** 最快答题窗口 = 基准 × 此系数 */
export const FASTEST_FACTOR = 1.3
/** 医学边界①：下限系数的硬地板，FASTEST_FACTOR 再怎么调都不得低于它 */
export const FASTEST_FACTOR_FLOOR = 1.3
/** 每多少题降一档 */
export const STEP_EVERY = 3
/** 一共几档到底 */
export const TIERS = 4
/** 初始窗口的地板（基准由它反推下限 = INITIAL_MIN_MS / INITIAL_FACTOR = 750ms） */
export const INITIAL_MIN_MS = 1500
/** 最快窗口的地板：把窗口**抬高**是安全方向，故保留 */
export const FASTEST_MIN_MS = 1000
/**
 * 基准的天花板：真孩子最慢也就 2-3 秒，超过即脏数据（走神/中途放下拍子/统计口径出错）。
 * 上限只该拦垃圾数据，**不许**改成给两个窗口封顶——那会压过下限系数、架空医学边界①。
 */
export const BASELINE_MAX_MS = 3000
/** 医学边界②：翻拍过渡时长的硬地板 */
export const MIN_FLIP_MS = 600

export interface Pace {
  /** 反应时间基准（中位数或数据不足时的默认值，**已钳到 [750, BASELINE_MAX_MS]**——即实际用于派生的那个值） */
  baselineMs: number
  /** 第一档答题窗口 */
  initialMs: number
  /** 最后一档答题窗口 */
  fastestMs: number
  /** 每降一档减少多少 */
  stepMs: number
  /** true=按实测数据推导，false=数据不足走固定默认 */
  adaptive: boolean
}

function clamp(v: number, lo: number, hi: number): number {
  return Math.min(hi, Math.max(lo, v))
}

/** 中位数（自动过滤 0 与非法值；无有效值返回 null）。用中位数而非平均，避免个别走神的题拉偏 */
export function medianMs(values: number[]): number | null {
  const v = values.filter((x) => Number.isFinite(x) && x > 0).sort((a, b) => a - b)
  if (v.length === 0) return null
  const mid = v.length >> 1
  return v.length % 2 === 1 ? v[mid] : (v[mid - 1] + v[mid]) / 2
}

/**
 * 从反应时间列表推导节奏。
 * @param reactionMsList 调用方按**新→旧**传入（本函数只取前 PACE_SAMPLE_N 个）；
 *   写库时无反应数据会落 0（不是 undefined），故内部按 >0 过滤。
 */
export function derivePace(reactionMsList: number[]): Pace {
  const recent = reactionMsList.slice(0, PACE_SAMPLE_N)
  const valid = recent.filter((v) => Number.isFinite(v) && v > 0)
  const adaptive = valid.length >= MIN_SAMPLES
  const raw = adaptive ? (medianMs(valid) ?? DEFAULT_BASELINE_MS) : DEFAULT_BASELINE_MS

  // 钳制**只作用于基准**：低端保证初始窗口不低于 INITIAL_MIN_MS，高端只拦脏数据。
  // 两个窗口一律由基准按固定系数派生，故系数（含医学边界①）永远不会被上限压过。
  const baselineMs = clamp(raw, INITIAL_MIN_MS / INITIAL_FACTOR, BASELINE_MAX_MS)

  const initialMs = Math.round(baselineMs * INITIAL_FACTOR)
  const factor = Math.max(FASTEST_FACTOR, FASTEST_FACTOR_FLOOR) // 医学边界①
  // 只有下限（把窗口抬高=给孩子更多时间=安全方向）；**不许**再加上限。
  const fastestMs = Math.max(FASTEST_MIN_MS, Math.round(baselineMs * factor))
  const stepMs = Math.round((initialMs - fastestMs) / TIERS)

  return { baselineMs, initialMs, fastestMs, stepMs, adaptive }
}

/** 第 index 题（0 起）的答题窗口：每 STEP_EVERY 题降一档，TIERS 档到底，永不低于 fastestMs */
export function windowMsForIndex(pace: Pace, index: number): number {
  const tier = Math.min(TIERS, Math.floor(Math.max(0, index) / STEP_EVERY))
  return Math.max(pace.fastestMs, pace.initialMs - tier * pace.stepMs)
}

/**
 * 医学边界②的唯一出口：挑战用的翻拍过渡时长。
 * 对设置页的正常路径（600/900/1500）是恒等；它防的是手改 localStorage 与将来新增更快档。
 */
export function challengeFlipMs(settingMs: number | null | undefined): number {
  const v = Number(settingMs)
  if (!Number.isFinite(v)) return MIN_FLIP_MS
  return Math.max(MIN_FLIP_MS, Math.round(v))
}

/** 数据不足时的兜底节奏（也给 UI 在读库失败时直接用） */
export const DEFAULT_PACE: Pace = derivePace([])
```

- [ ] **Step 1.4: 跑到通过。** `npm test -- challenge-pace` → 预期 `Test Files 1 passed (1)` / `Tests 18 passed (18)`（medianMs 4 + derivePace 8 + windowMsForIndex 2 + challengeFlipMs 4）。再跑 `npm test` 全量 → 预期 **基线 + 18 个测试、基线 + 1 个文件**。跑 `npm run typecheck` → 零输出。
  > 自查（手算一遍，别只看绿）：基准 1000 → 2000/1300/175；基准 2500 → 5000/3250/438；基准 5000 →（钳到 3000）6000/3900/525。**任何一档的 `fastestMs / min(基准,3000)` 都必须 ≥ 1.3**，若不是，说明有人把窗口上限加回来了。

- [ ] **Step 1.5: 提交。**
```
git add src/challenge/challenge-pace.ts src/challenge/challenge-pace.test.ts
git commit -m "feat(挑战): 自适应节奏纯函数——中位数基准+四档收紧，钳制只钳基准（不架空下限系数），两条医学边界落成常量与单测"
```

---

### Task 2: 挑战状态机纯函数

**Files**
- Create: `E:\developer\fzp\src\challenge\challenge-session.ts`
- Test: `E:\developer\fzp\src\challenge\challenge-session.test.ts`

**Interfaces**
- Consumes: `Pace` / `windowMsForIndex` / `CHALLENGE_DURATION_MS`（`./challenge-pace`）、`Direction`（`../speech/answer-mapping`）
- Produces:
  ```ts
  export type ChallengePhase = 'ready' | 'showing' | 'transitioning' | 'finished'
  export type OutcomeKind = 'correct' | 'wrong' | 'timeout'
  export interface ChallengeState {
    phase: ChallengePhase
    pace: Pace
    target: Direction | null
    index: number
    answered: number
    correct: number
    wrong: number
    timedOut: number
    score: number
    streak: number
    bestStreak: number
    windowMs: number
    windowLeftMs: number
    elapsedMs: number
    durationMs: number
    seq: number
    lastOutcome: { kind: OutcomeKind; dir: Direction; seq: number } | null
  }
  export const BASE_SCORE = 10
  export const SPEED_BONUS_MAX = 10
  export const COMBO_BONUS_STEP = 2
  export const COMBO_BONUS_CAP = 5
  export function scoreForAnswer(windowMs: number, windowLeftMs: number, streakAfter: number): number
  export function createChallenge(pace: Pace, durationMs?: number): ChallengeState
  export function startChallenge(state: ChallengeState, firstTarget: Direction): ChallengeState
  export function answerChallenge(state: ChallengeState, dir: Direction): ChallengeState
  export function advanceChallenge(state: ChallengeState, nextTarget: Direction): ChallengeState
  export function tickChallenge(state: ChallengeState, deltaMs: number): ChallengeState
  ```

- [ ] **Step 2.1: 写失败测试。** 新建 `src/challenge/challenge-session.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { derivePace, windowMsForIndex, CHALLENGE_DURATION_MS } from './challenge-pace'
import {
  createChallenge, startChallenge, answerChallenge, advanceChallenge, tickChallenge,
  scoreForAnswer, BASE_SCORE, SPEED_BONUS_MAX, COMBO_BONUS_STEP, COMBO_BONUS_CAP,
  type ChallengeState,
} from './challenge-session'

/** 基准 1000ms：初始 2000 / 最快 1300 / 每档 175（见 challenge-pace 的算例） */
const PACE = derivePace([1000, 1000, 1000, 1000])

function playing(over: Partial<ChallengeState> = {}): ChallengeState {
  return { ...startChallenge(createChallenge(PACE), 'up'), ...over }
}

describe('createChallenge', () => {
  it('初值：ready、零分、窗口已按第 1 档预置、总时长 30 秒', () => {
    const s = createChallenge(PACE)
    expect(s).toMatchObject({
      phase: 'ready', target: null, index: 0, answered: 0, correct: 0, wrong: 0,
      timedOut: 0, score: 0, streak: 0, bestStreak: 0, elapsedMs: 0,
      durationMs: CHALLENGE_DURATION_MS, seq: 0, lastOutcome: null,
    })
    expect(s.windowMs).toBe(windowMsForIndex(PACE, 0))
    expect(s.windowLeftMs).toBe(s.windowMs)
  })
})

describe('startChallenge', () => {
  it('ready → showing，并给出首题与满格窗口', () => {
    const s = startChallenge(createChallenge(PACE), 'left')
    expect(s).toMatchObject({ phase: 'showing', target: 'left', windowMs: 2000, windowLeftMs: 2000 })
  })

  it('非 ready 时返回同一个对象引用（不合法转移不产生新状态）', () => {
    const s = playing()
    expect(startChallenge(s, 'down')).toBe(s)
  })
})

describe('scoreForAnswer', () => {
  it('满窗答对 = 基础 + 满速度奖励', () => {
    expect(scoreForAnswer(2000, 2000, 1)).toBe(BASE_SCORE + SPEED_BONUS_MAX)
  })

  it('半窗答对 = 基础 + 半速度奖励 + 连击奖励', () => {
    expect(scoreForAnswer(2000, 1000, 3)).toBe(BASE_SCORE + 5 + 2 * COMBO_BONUS_STEP)
  })

  it('窗尽才答上也有基础分；连击奖励封顶', () => {
    expect(scoreForAnswer(2000, 0, 20)).toBe(BASE_SCORE + COMBO_BONUS_CAP * COMBO_BONUS_STEP)
  })
})

describe('answerChallenge', () => {
  it('答对：加分、连击 +1、bestStreak 跟进、转入翻拍过渡', () => {
    const s = playing({ windowLeftMs: 2000 })
    const n = answerChallenge(s, 'up')
    expect(n).toMatchObject({
      phase: 'transitioning', answered: 1, correct: 1, wrong: 0, timedOut: 0,
      streak: 1, bestStreak: 1, seq: 1,
    })
    expect(n.score).toBe(scoreForAnswer(2000, 2000, 1))
    expect(n.lastOutcome).toEqual({ kind: 'correct', dir: 'up', seq: 1 })
  })

  it('答错：不加分、连击清零、只进 wrong 计数（bestStreak 保留历史最高）', () => {
    const s = playing({ windowLeftMs: 1500, streak: 4, bestStreak: 4, score: 99 })
    const n = answerChallenge(s, 'down')
    expect(n).toMatchObject({
      phase: 'transitioning', answered: 1, correct: 0, wrong: 1, timedOut: 0,
      score: 99, streak: 0, bestStreak: 4,
    })
    expect(n.lastOutcome).toMatchObject({ kind: 'wrong', dir: 'down' })
  })

  it('非 showing（翻拍过渡中重复点）返回同一引用', () => {
    const s = playing({ phase: 'transitioning' })
    expect(answerChallenge(s, 'up')).toBe(s)
  })
})

describe('advanceChallenge', () => {
  it('题号 +1、换靶、窗口按新档位重置', () => {
    const s = playing({ phase: 'transitioning', index: 2 })
    const n = advanceChallenge(s, 'right')
    expect(n).toMatchObject({ phase: 'showing', target: 'right', index: 3, windowMs: 1825, windowLeftMs: 1825 })
  })

  it('非 transitioning 返回同一引用', () => {
    const s = playing()
    expect(advanceChallenge(s, 'left')).toBe(s)
  })
})

describe('tickChallenge', () => {
  it('showing 里同时推进总时长与本题窗口', () => {
    const n = tickChallenge(playing(), 300)
    expect(n).toMatchObject({ phase: 'showing', elapsedMs: 300, windowLeftMs: 1700 })
  })

  it('时限只覆盖答题窗口：翻拍过渡期间只走总时长，窗口不被压缩（spec §4.2）', () => {
    const s = playing({ phase: 'transitioning', windowLeftMs: 640 })
    const n = tickChallenge(s, 900)
    expect(n).toMatchObject({ phase: 'transitioning', elapsedMs: 900, windowLeftMs: 640 })
  })

  it('窗口耗尽 = 超时：不算错、不得分、清连击、单独计数、answered 不变（spec §4.5）', () => {
    const s = playing({ windowLeftMs: 100, streak: 3, bestStreak: 3, score: 50, answered: 4, correct: 3, wrong: 1 })
    const n = tickChallenge(s, 150)
    expect(n).toMatchObject({
      phase: 'transitioning', windowLeftMs: 0, timedOut: 1,
      answered: 4, correct: 3, wrong: 1, score: 50, streak: 0, bestStreak: 3, seq: 1,
    })
    expect(n.lastOutcome).toEqual({ kind: 'timeout', dir: 'up', seq: 1 })
  })

  it('总时长到 → finished，elapsed 钳到 durationMs', () => {
    const s = playing({ elapsedMs: CHALLENGE_DURATION_MS - 50 })
    const n = tickChallenge(s, 500)
    expect(n).toMatchObject({ phase: 'finished', elapsedMs: CHALLENGE_DURATION_MS })
  })

  it('总时长与超时同一跳命中时以"结束"优先（不给最后一题记一次超时）', () => {
    const s = playing({ elapsedMs: CHALLENGE_DURATION_MS - 50, windowLeftMs: 10 })
    const n = tickChallenge(s, 500)
    expect(n.phase).toBe('finished')
    expect(n.timedOut).toBe(0)
  })

  it('ready / finished / 非正 delta 都返回同一引用', () => {
    const r = createChallenge(PACE)
    expect(tickChallenge(r, 100)).toBe(r)
    const f = playing({ phase: 'finished' })
    expect(tickChallenge(f, 100)).toBe(f)
    const p = playing()
    expect(tickChallenge(p, 0)).toBe(p)
  })

  it('30 秒整局：答对与超时混跑，最终必然 finished 且 elapsed 恰好等于总时长', () => {
    let s = startChallenge(createChallenge(PACE), 'up')
    let guard = 0
    // 每题：答/超时 → 翻拍 600ms → 下一题；每跳 100ms
    while (s.phase !== 'finished' && guard++ < 2000) {
      if (s.phase === 'showing') {
        // 偶数题快答，奇数题故意拖到超时
        if (s.index % 2 === 0) s = answerChallenge(s, s.target!)
        else s = tickChallenge(s, s.windowLeftMs + 1)
      } else {
        const before = s.seq
        s = tickChallenge(s, 600)
        if (s.phase === 'transitioning' && s.seq === before) s = advanceChallenge(s, 'left')
      }
    }
    expect(s.phase).toBe('finished')
    expect(s.elapsedMs).toBe(CHALLENGE_DURATION_MS)
    expect(s.correct + s.timedOut).toBeGreaterThan(0)
    expect(s.answered).toBe(s.correct + s.wrong)
  })

  it('seq 每次离开 showing 都单调 +1（皮肤怪兽轮换与答题演出都依赖它）', () => {
    let s = startChallenge(createChallenge(PACE), 'up')
    const seen: number[] = []
    for (let i = 0; i < 4; i++) {
      s = i % 2 === 0 ? answerChallenge(s, s.target!) : tickChallenge(s, s.windowLeftMs + 1)
      seen.push(s.seq)
      s = advanceChallenge(s, 'right')
    }
    expect(seen).toEqual([1, 2, 3, 4])
  })
})
```

- [ ] **Step 2.2: 跑到失败。** `npm test -- challenge-session`。预期失败信息：`Failed to resolve import "./challenge-session" from "src/challenge/challenge-session.test.ts"`。

- [ ] **Step 2.3: 最小实现。** 新建 `src/challenge/challenge-session.ts`：

```ts
import type { Direction } from '../speech/answer-mapping'
import { windowMsForIndex, CHALLENGE_DURATION_MS, type Pace } from './challenge-pace'

/**
 * 限时挑战状态机（纯函数）。刻意**不复用** src/training/session.ts：
 * 挑战有每题时限、不分眼、不落库，硬合并会把那个被大量测试压住的训练状态机搞复杂，
 * 风险不对等。形状沿用它的两个范式：全部纯函数；不合法转移返回同一个对象引用。
 *
 * 时间单位是**毫秒**（训练那套是秒）——挑战要在 ms 级判超时。
 */

export type ChallengePhase = 'ready' | 'showing' | 'transitioning' | 'finished'
export type OutcomeKind = 'correct' | 'wrong' | 'timeout'

export interface ChallengeState {
  phase: ChallengePhase
  pace: Pace
  target: Direction | null
  /** 第几题，0 起；决定当前档位 */
  index: number
  /** 真答了几题（**不含超时**） */
  answered: number
  correct: number
  wrong: number
  /** 超时数：不算错也不得分，单独计数好让孩子知道是"看错"还是"没跟上" */
  timedOut: number
  score: number
  streak: number
  bestStreak: number
  /** 本题答题窗口 */
  windowMs: number
  /** 本题剩余窗口（只在 showing 里递减；翻拍过渡不压缩它） */
  windowLeftMs: number
  /** 已用总时长（墙钟，含翻拍过渡） */
  elapsedMs: number
  durationMs: number
  /** 每次离开 showing（答题或超时）都 +1；皮肤怪兽轮换与一次性演出靠它 */
  seq: number
  lastOutcome: { kind: OutcomeKind; dir: Direction; seq: number } | null
}

/** 答对基础分 */
export const BASE_SCORE = 10
/** 速度奖励上限（按剩余窗口比例给） */
export const SPEED_BONUS_MAX = 10
/** 每级连击奖励 */
export const COMBO_BONUS_STEP = 2
/** 连击奖励级数上限 */
export const COMBO_BONUS_CAP = 5

/** 答对得分 = 基础 + 速度奖励（剩余窗口比例）+ 连击奖励（封顶） */
export function scoreForAnswer(windowMs: number, windowLeftMs: number, streakAfter: number): number {
  const ratio = windowMs > 0 ? Math.max(0, Math.min(1, windowLeftMs / windowMs)) : 0
  const speed = Math.round(SPEED_BONUS_MAX * ratio)
  const combo = Math.min(Math.max(0, streakAfter - 1), COMBO_BONUS_CAP) * COMBO_BONUS_STEP
  return BASE_SCORE + speed + combo
}

export function createChallenge(pace: Pace, durationMs: number = CHALLENGE_DURATION_MS): ChallengeState {
  const windowMs = windowMsForIndex(pace, 0)
  return {
    phase: 'ready', pace, target: null, index: 0,
    answered: 0, correct: 0, wrong: 0, timedOut: 0,
    score: 0, streak: 0, bestStreak: 0,
    windowMs, windowLeftMs: windowMs,
    elapsedMs: 0, durationMs, seq: 0, lastOutcome: null,
  }
}

export function startChallenge(state: ChallengeState, firstTarget: Direction): ChallengeState {
  if (state.phase !== 'ready') return state
  const windowMs = windowMsForIndex(state.pace, 0)
  return { ...state, phase: 'showing', target: firstTarget, windowMs, windowLeftMs: windowMs }
}

export function answerChallenge(state: ChallengeState, dir: Direction): ChallengeState {
  if (state.phase !== 'showing' || state.target === null) return state
  const right = dir === state.target
  const streak = right ? state.streak + 1 : 0
  const gain = right ? scoreForAnswer(state.windowMs, state.windowLeftMs, streak) : 0
  const seq = state.seq + 1
  return {
    ...state,
    phase: 'transitioning',
    answered: state.answered + 1,
    correct: state.correct + (right ? 1 : 0),
    wrong: state.wrong + (right ? 0 : 1),
    score: state.score + gain,
    streak,
    bestStreak: Math.max(state.bestStreak, streak),
    seq,
    lastOutcome: { kind: right ? 'correct' : 'wrong', dir, seq },
  }
}

export function advanceChallenge(state: ChallengeState, nextTarget: Direction): ChallengeState {
  if (state.phase !== 'transitioning') return state
  const index = state.index + 1
  const windowMs = windowMsForIndex(state.pace, index)
  return { ...state, phase: 'showing', target: nextTarget, index, windowMs, windowLeftMs: windowMs }
}

export function tickChallenge(state: ChallengeState, deltaMs: number): ChallengeState {
  if (state.phase !== 'showing' && state.phase !== 'transitioning') return state
  if (!(deltaMs > 0)) return state

  const elapsedMs = state.elapsedMs + deltaMs
  // 总时长优先：最后一跳同时撞上"整局结束"和"本题超时"时，不给那一题记超时
  if (elapsedMs >= state.durationMs) {
    return { ...state, elapsedMs: state.durationMs, phase: 'finished', windowLeftMs: 0 }
  }
  // 翻拍过渡：只走总时长，答题窗口不递减、也不被压缩（spec §4.2）
  if (state.phase !== 'showing') return { ...state, elapsedMs }

  const windowLeftMs = state.windowLeftMs - deltaMs
  if (windowLeftMs > 0) return { ...state, elapsedMs, windowLeftMs }

  // 超时 = 未答：不算错、不得分，但清连击并单独计数（spec §4.5）
  const seq = state.seq + 1
  return {
    ...state,
    elapsedMs,
    windowLeftMs: 0,
    phase: 'transitioning',
    timedOut: state.timedOut + 1,
    streak: 0,
    seq,
    // showing 阶段 target 必非空（start/advance 都会设），断言只为消掉可选类型
    lastOutcome: { kind: 'timeout', dir: state.target!, seq },
  }
}
```

- [ ] **Step 2.4: 跑到通过。** `npm test -- challenge-session` → 预期 `Tests 19 passed (19)`（createChallenge 1 + startChallenge 2 + scoreForAnswer 3 + answerChallenge 3 + advanceChallenge 2 + tickChallenge 8）。全量 `npm test` → **基线 + 37 个测试 / 基线 + 2 个文件**。`npm run typecheck` 零输出。

- [ ] **Step 2.5: 提交。**
```
git add src/challenge/challenge-session.ts src/challenge/challenge-session.test.ts
git commit -m "feat(挑战): 状态机纯函数——超时不算错不得分、时限只覆盖答题窗口、总时长优先结束"
```

---

### Task 3: 最高分存储（localStorage）

**Files**
- Create: `E:\developer\fzp\src\challenge\challenge-storage.ts`
- Test: `E:\developer\fzp\src\challenge\challenge-storage.test.ts`

**Interfaces**
- Consumes: `lsGet` / `lsSet`（`../data/storage`）
- Produces:
  ```ts
  export const CHALLENGE_BEST_KEY = 'fzp.challengeBest'
  export function parseBest(raw: string | null): number
  export function bestAfter(prevRaw: string | null, score: number): { best: number; isNewRecord: boolean }
  export function readBest(): number
  export function writeBestIfHigher(score: number): { best: number; isNewRecord: boolean }
  ```

- [ ] **Step 3.1: 写失败测试。** 新建 `src/challenge/challenge-storage.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest'
import { CHALLENGE_BEST_KEY, parseBest, bestAfter, readBest, writeBestIfHigher } from './challenge-storage'

/**
 * 本仓 vitest 的 environment 是 'node'，没有 localStorage（lsGet/lsSet 会 try/catch 成
 * null / 静默丢弃）。为了也覆盖那两层薄包装，这里注入一个最小内存实现。
 */
function installMemoryStorage(): Map<string, string> {
  const mem = new Map<string, string>()
  const fake = {
    getItem: (k: string) => (mem.has(k) ? mem.get(k)! : null),
    setItem: (k: string, v: string) => { mem.set(k, String(v)) },
    removeItem: (k: string) => { mem.delete(k) },
    clear: () => mem.clear(),
    key: (i: number) => [...mem.keys()][i] ?? null,
    get length() { return mem.size },
  }
  ;(globalThis as unknown as { localStorage: unknown }).localStorage = fake
  return mem
}

let mem: Map<string, string>
beforeEach(() => { mem = installMemoryStorage() })
afterEach(() => { delete (globalThis as unknown as Record<string, unknown>).localStorage })

describe('parseBest', () => {
  it('没存过 → 0', () => {
    expect(parseBest(null)).toBe(0)
    expect(parseBest('')).toBe(0)
  })

  it('脏数据（手改过 / 别的版本写坏）→ 0，不是 NaN', () => {
    expect(parseBest('abc')).toBe(0)
    expect(parseBest('Infinity')).toBe(0)
  })

  it('负数 → 0', () => {
    expect(parseBest('-5')).toBe(0)
  })

  it('小数向下取整（分数是整数语义）', () => {
    expect(parseBest('12.9')).toBe(12)
  })
})

describe('bestAfter', () => {
  it('破纪录：返回新分并标记 isNewRecord', () => {
    expect(bestAfter('100', 137)).toEqual({ best: 137, isNewRecord: true })
  })

  it('没破：保留旧纪录', () => {
    expect(bestAfter('137', 100)).toEqual({ best: 137, isNewRecord: false })
  })

  it('平纪录不算新纪录（必须严格大于）', () => {
    expect(bestAfter('137', 137)).toEqual({ best: 137, isNewRecord: false })
  })

  it('0 分不算新纪录，即使从没玩过', () => {
    expect(bestAfter(null, 0)).toEqual({ best: 0, isNewRecord: false })
  })
})

describe('readBest / writeBestIfHigher（走真实 lsGet/lsSet）', () => {
  it('首次读为 0；破纪录后写入并能读回', () => {
    expect(readBest()).toBe(0)
    expect(writeBestIfHigher(88)).toEqual({ best: 88, isNewRecord: true })
    expect(mem.get(CHALLENGE_BEST_KEY)).toBe('88')
    expect(readBest()).toBe(88)
  })

  it('没破纪录时不写 storage（旧值原样留着）', () => {
    writeBestIfHigher(88)
    expect(writeBestIfHigher(50)).toEqual({ best: 88, isNewRecord: false })
    expect(mem.get(CHALLENGE_BEST_KEY)).toBe('88')
  })

  it('键名锚定 fzp.challengeBest —— 备份/恢复靠 fzp. 前缀自动收录，改名等于悄悄丢最高分', () => {
    expect(CHALLENGE_BEST_KEY).toBe('fzp.challengeBest')
  })
})
```

- [ ] **Step 3.2: 跑到失败。** `npm test -- challenge-storage`。预期失败信息：`Failed to resolve import "./challenge-storage" from "src/challenge/challenge-storage.test.ts"`。

- [ ] **Step 3.3: 最小实现。** 新建 `src/challenge/challenge-storage.ts`：

```ts
import { lsGet, lsSet } from '../data/storage'

/**
 * 挑战最高分只存 localStorage，**不进 Dexie、不同步**（spec §5）。
 *
 * 为什么不落库：若塞进 sessions 表加个 mode 字段，所有统计查询（aggregate / period /
 * weekly-report / 勋章判定 deriveStats / 首页 getHomeStats）都必须记得过滤挑战行——
 * 漏一处就污染趋势图。挑战数据的价值远不值这个风险。
 * 代价：换设备丢最高分（可接受；且 backup-service 按 fzp. 前缀会把它带进 JSON 备份）。
 */
export const CHALLENGE_BEST_KEY = 'fzp.challengeBest'

/** 原始值 → 分数；缺失/脏数据/负数都归 0，小数向下取整 */
export function parseBest(raw: string | null): number {
  const n = Number(raw)
  if (!Number.isFinite(n) || n < 0) return 0
  return Math.floor(n)
}

/** 纯判定：给定旧原始值与本局分数，算出新纪录与是否破纪录（平纪录与 0 分都不算破） */
export function bestAfter(prevRaw: string | null, score: number): { best: number; isNewRecord: boolean } {
  const prev = parseBest(prevRaw)
  const s = Number.isFinite(score) && score > 0 ? Math.floor(score) : 0
  return s > prev ? { best: s, isNewRecord: true } : { best: prev, isNewRecord: false }
}

export function readBest(): number {
  return parseBest(lsGet(CHALLENGE_BEST_KEY))
}

/** 破纪录才写；返回结果给结算页决定是否放「🎉 新纪录！」 */
export function writeBestIfHigher(score: number): { best: number; isNewRecord: boolean } {
  const r = bestAfter(lsGet(CHALLENGE_BEST_KEY), score)
  if (r.isNewRecord) lsSet(CHALLENGE_BEST_KEY, String(r.best))
  return r
}
```

- [ ] **Step 3.4: 跑到通过。** `npm test -- challenge-storage` → 预期 `Tests 11 passed (11)`（parseBest 4 + bestAfter 4 + 薄包装 3）。全量 `npm test` → **基线 + 48 个测试 / 基线 + 3 个文件**。`npm run typecheck` 零输出。

- [ ] **Step 3.5: 提交。**
```
git add src/challenge/challenge-storage.ts src/challenge/challenge-storage.test.ts
git commit -m "feat(挑战): 最高分只存 localStorage fzp.challengeBest（纯判定+薄包装，不落 Dexie）"
```

---

### Task 4: 解锁判定纯函数

**Files**
- Create: `E:\developer\fzp\src\challenge\challenge-unlock.ts`
- Test: `E:\developer\fzp\src\challenge\challenge-unlock.test.ts`

**Interfaces**
- Consumes: 结构类型 `{ checkedInToday: boolean }`（与 `HomeStats`（`src/data/checkin.ts:44`）结构兼容，**刻意不 import 那个类型**，避免纯函数被业务模块拖进依赖）
- Produces: `export function challengeUnlocked(stats: { checkedInToday: boolean } | null | undefined): boolean`

- [ ] **Step 4.1: 写失败测试。** 新建 `src/challenge/challenge-unlock.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { challengeUnlocked } from './challenge-unlock'

describe('challengeUnlocked', () => {
  it('今天已完成训练打卡 → 解锁', () => {
    expect(challengeUnlocked({ checkedInToday: true })).toBe(true)
  })

  it('今天还没练 → 不解锁（挑战是训练的奖赏，不是替代品）', () => {
    expect(challengeUnlocked({ checkedInToday: false })).toBe(false)
  })

  it('stats 还在加载（null）→ 不解锁，入口不闪现', () => {
    expect(challengeUnlocked(null)).toBe(false)
  })

  it('undefined / 缺字段 → 不解锁；getHomeStats 在 IndexedDB 不可用时也降级为 false（安全方向）', () => {
    expect(challengeUnlocked(undefined)).toBe(false)
    expect(challengeUnlocked({} as { checkedInToday: boolean })).toBe(false)
  })
})
```

- [ ] **Step 4.2: 跑到失败。** `npm test -- challenge-unlock`。预期失败信息：`Failed to resolve import "./challenge-unlock" from "src/challenge/challenge-unlock.test.ts"`。

- [ ] **Step 4.3: 最小实现。** 新建 `src/challenge/challenge-unlock.ts`：

```ts
/**
 * 挑战解锁判定：当天已完成正经训练（checkins 里有今天的行）才给玩（spec §7）。
 *
 * 参数用结构类型而不 import HomeStats：这一层是纯函数，不该被 data/checkin 拖进依赖。
 * 调用方传的就是 getHomeStats(today) 的结果——它在 IndexedDB 不可用时会降级为
 * checkedInToday:false，也即"读不出来就不给玩"，方向是安全的。
 * 加载中（null）同样返回 false，避免入口卡先闪一下再消失。
 */
export function challengeUnlocked(stats: { checkedInToday: boolean } | null | undefined): boolean {
  return stats?.checkedInToday === true
}
```

- [ ] **Step 4.4: 跑到通过。** `npm test -- challenge-unlock` → 预期 `Tests 4 passed (4)`。全量 `npm test` → **基线 + 52 个测试 / 基线 + 4 个文件**。`npm run typecheck` 零输出。

- [ ] **Step 4.5: 提交。**
```
git add src/challenge/challenge-unlock.ts src/challenge/challenge-unlock.test.ts
git commit -m "feat(挑战): 解锁判定纯函数——当天练完才给玩，加载中与读库失败都不解锁"
```

---

### Task 5: i18n 文案（ZH/EN 各 22 条）

**Files**
- Modify: `E:\developer\fzp\src\i18n.tsx`
- Modify(Test): `E:\developer\fzp\src\i18n.test.ts`

**Interfaces**
- Consumes: `hasKey(lang, key)`（`src/i18n.tsx:50`）
- Produces: 22 个 `challenge.*` 键在 `ZH` 与 `EN` 两份字典里都存在。挑战页另外**复用**既有键：`train.calibFirst` / `train.calibFirstBody` / `train.screenTooSmall` / `train.rotateHint` / `train.flip` / `direction.up|down|left|right`（不新造）
- **22 条每一条都有落点**（Task 6/7 的代码里逐个用到，不留死键；漏用的键会在下一次改文案时被误当成"还在用"而白白维护）

- [ ] **Step 5.1: 先写失败断言。** 编辑 `src/i18n.test.ts`——在 `ADMIN_KEYS` 常量之后插入：

```ts
/** 限时挑战文案（迭代 2·限时挑战） */
const CHALLENGE_KEYS = [
  'challenge.title', 'challenge.sub', 'challenge.notTraining',
  'challenge.homeCard', 'challenge.homeCardHint',
  'challenge.best', 'challenge.noBest', 'challenge.start',
  'challenge.ready', 'challenge.readyHint', 'challenge.paceHint',
  'challenge.score', 'challenge.time', 'challenge.timeout',
  'challenge.done', 'challenge.newRecord',
  'challenge.correctCount', 'challenge.wrongCount', 'challenge.timeoutCount',
  'challenge.bestStreak', 'challenge.again', 'challenge.back',
]
```

> 刻意**没有** `challenge.locked`：未解锁时首页整张卡不渲染（spec §7），根本没有地方显示"锁着"的文案——加了就是死键。

并在 `describe('云同步与隐私政策文案', …)` 的最后一个 `it` 之后插入：

```ts
  it('限时挑战 22 个文案键在 zh 与 en 都存在（漏一份就会在另一种语言下回退成中文）', () => {
    expect(CHALLENGE_KEYS.length).toBe(22)
    for (const k of CHALLENGE_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```

- [ ] **Step 5.2: 跑到失败。** `npm test -- i18n`。预期失败信息：`AssertionError: zh 缺 challenge.title: expected false to be true`。

- [ ] **Step 5.3: 补 ZH 字典。** 编辑 `src/i18n.tsx`，在 `'train.trySkinHint': '去「⚙️ 设置」换上试试～',`（`ZH` 里那一行，紧接着是 `  // 统计`）**之后**插入：

```ts
  // 限时挑战（当天练完才解锁的 30 秒小游戏；不进统计、不给奖励）
  'challenge.title': '⚡ 30 秒挑战',
  'challenge.sub': '双眼一起看，不用遮眼',
  'challenge.notTraining': '这是小游戏，不算训练成绩',
  'challenge.homeCard': '30 秒挑战',
  'challenge.homeCardHint': '今天练完啦，来冲个分！',
  'challenge.best': '最高 {n} 分',
  'challenge.noBest': '还没有记录',
  'challenge.start': '⚡ 开始挑战',
  'challenge.ready': '准备好了吗？',
  'challenge.readyHint': '**双眼都睁开、不用遮眼**，但拍子照旧要真翻。看到 E 立刻答，太慢会算超时。',
  'challenge.paceHint': '每 3 题加速一档，一共加速 4 次；速度按你平时的反应快慢自动定',
  'challenge.score': '分数',
  'challenge.time': '剩余',
  'challenge.timeout': '⏰ 超时！',
  'challenge.done': '🏁 挑战结束',
  'challenge.newRecord': '🎉 新纪录！',
  'challenge.correctCount': '答对',
  'challenge.wrongCount': '答错',
  'challenge.timeoutCount': '超时',
  'challenge.bestStreak': '最长连击',
  'challenge.again': '🔁 再来一次',
  'challenge.back': '← 回首页',
```

- [ ] **Step 5.4: 补 EN 字典。** 同一文件，在 `'train.trySkinHint': 'Switch to it in ⚙️ Settings ~',`（`EN` 里那一行）**之后**插入：

```ts
  // Timed challenge
  'challenge.title': '⚡ 30-Second Challenge',
  'challenge.sub': 'Both eyes open — no patching',
  'challenge.notTraining': 'Just a mini game — it does not count as training',
  'challenge.homeCard': '30s Challenge',
  'challenge.homeCardHint': "Today's training is done — go for a high score!",
  'challenge.best': 'Best {n}',
  'challenge.noBest': 'No record yet',
  'challenge.start': '⚡ Start challenge',
  'challenge.ready': 'Ready?',
  'challenge.readyHint': '**Both eyes open, no patching** — but you still flip the lenses for real. Answer as soon as the E shows up; too slow counts as a timeout.',
  'challenge.paceHint': 'Speeds up every 3 questions, 4 steps in total; the pace follows your usual reaction time',
  'challenge.score': 'Score',
  'challenge.time': 'Left',
  'challenge.timeout': '⏰ Too slow!',
  'challenge.done': '🏁 Challenge over',
  'challenge.newRecord': '🎉 New record!',
  'challenge.correctCount': 'Correct',
  'challenge.wrongCount': 'Wrong',
  'challenge.timeoutCount': 'Timeouts',
  'challenge.bestStreak': 'Best combo',
  'challenge.again': '🔁 Play again',
  'challenge.back': '← Back home',
```

- [ ] **Step 5.5: 跑到通过。** `npm test -- i18n` → 预期全绿。全量 `npm test` → **基线 + 53 个测试 / 基线 + 4 个文件**（本任务只加 1 个 `it`，不加文件）。`npm run typecheck` 零输出。

- [ ] **Step 5.6: 提交。**
```
git add src/i18n.tsx src/i18n.test.ts
git commit -m "feat(挑战): 中英双份 22 条挑战文案 + i18n 键齐全性断言"
```

---

### Task 6: 挑战页 UI + 键盘映射抽取 + App 路由分支

> **🚦 前置闸门：Step 0.4（语音作答归属）必须已拿到用户结论。** 下面的代码按**选项 A（不接语音）**写；若用户选了 B/C，先按 Step 0.4 的说明改 spec 与 Task 1，再回来重写本任务的输入层。没结论就先做 Task 7 之外的其他任务，别开写。

**Files**
- Create: `E:\developer\fzp\src\training\key-map.ts`
- Test: `E:\developer\fzp\src\training\key-map.test.ts`
- Create: `E:\developer\fzp\src\challenge\ChallengePage.tsx`
- Test: `E:\developer\fzp\src\challenge\challenge-shell.test.ts`
- Modify: `E:\developer\fzp\src\training\TrainingPage.tsx`
- Modify: `E:\developer\fzp\src\training\sfx.ts`
- Modify: `E:\developer\fzp\src\App.tsx`

**Interfaces**
- Consumes: `dirForKey`（新建）、`pickDirection`（`src/training/session.ts:33`）、`playSfx` / `setMuted`（`src/training/sfx.ts`）、`getSkin` / `getSkinId` / `isSkinUnlocked` / `pickRandomSkin` / `RANDOM_SKIN_ID`（`src/skins/registry.ts`）、`StageProps`（`src/skins/types.ts:5`）、`db`（`src/data/db.ts:199`，只读 `sessions`）、`getHomeStats`（`src/data/checkin.ts:50`）、`toDateStr`、`lsGet`、`useT` / `Rich`、Task 1-3 的全部导出
- Produces:
  ```ts
  // src/training/key-map.ts
  export const KEY_MAP: Record<string, Direction>
  export function dirForKey(key: string): Direction | null
  // src/challenge/ChallengePage.tsx
  export function ChallengePage({ onBack }: { onBack: () => void }): JSX.Element
  // src/App.tsx
  type View = … | 'challenge'   // 新增取值，NAV 不含它
  ```

**本任务的刻意决策（别"优化"掉）**
1. **输入方式按 Step 0.4 的用户结论落地。** 本文按**选项 A（触控/键盘，不接 vosk）**写：语音识别有几百 ms 的出词延迟且首次要加载 42MB 模型，会把"答题窗口"的计时变得不公平（孩子明明及时说出来了却判超时）。`.fzp-voice-hint` 那个格子改放「这是小游戏，不算训练成绩」的免责文案。**注意这是技术理由，不是拍板权**——CLAUDE.md 关键决策 #2 只有用户能改，Step 0.4 拿到结论并写进 spec §9 之后这条才生效。
2. **不用 `React.lazy`。** 挑战页依赖的都是已在主包里的模块（皮肤、sfx、Dexie），lazy 只会白白引入 `admin` 那套 `ErrorBoundary + Suspense` 仪式而省不下字节。
3. **超时用独立音效，绝不复用 `'wrong'`。** 界面上刚把「答错」与「超时」分成两格计数，耳朵里若还是同一个错误音，spec §4.5「超时=未答、不算错」这个教学区分就被抹平了——孩子分不清自己是"看错了"还是"没跟上"。故 Step 6.3b 给 `SfxKind` 加一个 `'timeout'`。

- [ ] **Step 6.1: 写键盘映射的失败测试。** 新建 `src/training/key-map.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { KEY_MAP, dirForKey } from './key-map'

describe('dirForKey', () => {
  it('方向键四向都认（最直观的那套）', () => {
    expect(dirForKey('ArrowUp')).toBe('up')
    expect(dirForKey('ArrowDown')).toBe('down')
    expect(dirForKey('ArrowLeft')).toBe('left')
    expect(dirForKey('ArrowRight')).toBe('right')
  })

  it('三套兜底键都按屏幕按钮顺序（上下左右）映射', () => {
    expect(['1', '2', '3', '4'].map(dirForKey)).toEqual(['up', 'down', 'left', 'right'])
    expect(['a', 's', 'd', 'f'].map(dirForKey)).toEqual(['up', 'down', 'left', 'right'])
    expect(['j', 'k', 'l', ';'].map(dirForKey)).toEqual(['up', 'down', 'left', 'right'])
  })

  it('大写也认（KeyboardEvent.key 受 Shift/CapsLock 影响）', () => {
    expect(dirForKey('A')).toBe('up')
    expect(dirForKey('L')).toBe('left')
  })

  it('不认识的键返回 null，而不是 undefined（调用方靠它决定是否 preventDefault）', () => {
    expect(dirForKey('Enter')).toBeNull()
    expect(dirForKey('z')).toBeNull()
  })

  it('一共 16 个键，四向各 4 个（改表时别把某一向漏掉）', () => {
    expect(Object.keys(KEY_MAP).length).toBe(16)
    for (const d of ['up', 'down', 'left', 'right']) {
      expect(Object.values(KEY_MAP).filter((v) => v === d).length, d).toBe(4)
    }
  })
})
```

- [ ] **Step 6.2: 跑到失败。** `npm test -- key-map`。预期失败信息：`Failed to resolve import "./key-map" from "src/training/key-map.test.ts"`。

- [ ] **Step 6.3: 实现并让训练页改用它。** 新建 `src/training/key-map.ts`：

```ts
import type { Direction } from '../speech/answer-mapping'

/**
 * 键盘作答映射：方向键最直观；1-4 / asdf / jkl; 按屏幕按钮顺序（上下左右）映射，
 * 方便电脑调试与无语音场景。训练页与挑战页共用一份——两处各写一份必然漂移。
 * 若将来出现数字视标，同样把 1-9 对到对应选项即可。
 */
export const KEY_MAP: Record<string, Direction> = {
  arrowup: 'up', arrowdown: 'down', arrowleft: 'left', arrowright: 'right',
  '1': 'up', '2': 'down', '3': 'left', '4': 'right',
  a: 'up', s: 'down', d: 'left', f: 'right',
  j: 'up', k: 'down', l: 'left', ';': 'right',
}

/** KeyboardEvent.key → 方向；不认识的键返回 null */
export function dirForKey(key: string): Direction | null {
  return KEY_MAP[key.toLowerCase()] ?? null
}
```

编辑 `src/training/TrainingPage.tsx`：①在 `import { playSfx, setMuted } from './sfx'` 之后加一行 `import { dirForKey } from './key-map'`；②把 `useEffect`（约 :200-217）里的这段

```ts
    const KEY_MAP: Record<string, Direction> = {
      arrowup: 'up', arrowdown: 'down', arrowleft: 'left', arrowright: 'right',
      '1': 'up', '2': 'down', '3': 'left', '4': 'right',
      a: 'up', s: 'down', d: 'left', f: 'right',
      j: 'up', k: 'down', l: 'left', ';': 'right',
    }
    const onKey = (e: KeyboardEvent) => {
      const t = e.target as HTMLElement | null
      if (t && /^(input|textarea|select)$/i.test(t.tagName)) return
      const dir = KEY_MAP[e.key.toLowerCase()]
```

替换为

```ts
    const onKey = (e: KeyboardEvent) => {
      const t = e.target as HTMLElement | null
      if (t && /^(input|textarea|select)$/i.test(t.tagName)) return
      const dir = dirForKey(e.key)
```

（`useEffect` 上方那段解释映射来由的注释保留，行为完全不变。）

- [ ] **Step 6.3b: 给超时加独立音效。** 编辑 `src/training/sfx.ts`（`SfxKind` 是模块私有类型，只改这一个文件，训练页零影响）：①第 1 行的联合类型末尾加 `| 'timeout'`，即

```ts
type SfxKind = 'correct' | 'wrong' | 'flip' | 'finish' | 'checkin' | 'badge' | 'egg' | 'timeout'
```

②在 `playSfx` 的 `case 'wrong':` 分支**之后**插入

```ts
    // 超时 = 没跟上，不是答错（spec §4.5）：用一个短促下滑音，和 wrong 那记刺耳低频区分开，
    // 好让孩子听得出自己是"看错了"还是"没跟上"——这正是结算页把两者分成两格的同一个理由。
    case 'timeout':
      tone(520, 0, 0.09, 0.13, 'triangle')
      tone(390, 0.08, 0.12, 0.11, 'triangle')
      break
```

（本仓没有 `sfx` 的单测——它整个是 WebAudio 副作用，node 环境里没有 `AudioContext`，硬测只能测到 mock。超时不许用 `'wrong'` 这条改由 Step 6.7 的源文本契约测试锚住。）

- [ ] **Step 6.4: 跑到通过。** `npm test -- key-map` → 预期 `Tests 5 passed (5)`。再 `npm run typecheck` → 零输出（若报 `'Direction' is declared but its value is never read`，说明 `TrainingPage.tsx` 里 `Direction` 只被删掉的那处用了——检查它在别处仍被用于 `ARROW` / `DPAD` / `handleAnswer` 签名，正常不会报）。

- [ ] **Step 6.5: 写挑战页。** 新建 `src/challenge/ChallengePage.tsx`：

```tsx
import { useEffect, useRef, useState } from 'react'
import { db } from '../data/db'
import { getHomeStats } from '../data/checkin'
import { toDateStr } from '../data/date-utils'
import { lsGet } from '../data/storage'
import { pickDirection } from '../training/session'
import { dirForKey } from '../training/key-map'
import { playSfx, setMuted } from '../training/sfx'
import type { Direction } from '../speech/answer-mapping'
import { getSkin, getSkinId, isSkinUnlocked, pickRandomSkin, RANDOM_SKIN_ID } from '../skins/registry'
import { useT, Rich } from '../i18n'
import { derivePace, challengeFlipMs, DEFAULT_PACE, PACE_SAMPLE_N, type Pace } from './challenge-pace'
import {
  createChallenge, startChallenge, answerChallenge, advanceChallenge, tickChallenge,
  type ChallengeState,
} from './challenge-session'
import { readBest, writeBestIfHigher } from './challenge-storage'

/** 计时步长：训练页那套「1 秒粒度」在这里不够——挑战要在 ms 级判超时 */
const TICK_MS = 100
/** 单跳最多推进多少：标签页被 Safari 节流后回来时，别一口气吃掉整局 */
const MAX_TICK_DELTA_MS = 400
const ARROW: Record<Direction, string> = { up: '↑', down: '↓', left: '←', right: '→' }
const DPAD: { dir: Direction; col: number; row: number }[] = [
  { dir: 'up', col: 2, row: 1 },
  { dir: 'left', col: 1, row: 2 },
  { dir: 'right', col: 3, row: 2 },
  { dir: 'down', col: 2, row: 3 },
]

function readPxPerMm(): number | null {
  const v = lsGet('fzp.cssPxPerMm')
  return v ? Number(v) : null
}

/**
 * 限时挑战页：30 秒冲分小游戏。与正经训练的关键区别——
 *   · 双眼不遮眼（binocular accommodative facility 本身就是标准训练项目）
 *   · 每题有答题窗口，超时不算错也不得分
 *   · 视标物理尺寸完全一致（毫米 × 标定），挑战调的是"给多少时间"不是"看多小的字"
 *   · 翻拍照旧要真翻，过渡时长强制不低于 600ms（challengeFlipMs）
 *   · 不给积分/勋章/怪兽、不进统计、不落 Dexie；最高分只写 localStorage
 * 外壳复用训练页的 .fzp-train / .fzp-stage / .fzp-answer 三段，手机竖屏自动适配。
 */
export function ChallengePage({ onBack }: { onBack: () => void }) {
  const t = useT()
  const [pace, setPace] = useState<Pace | null>(null)
  const [state, setState] = useState<ChallengeState | null>(null)
  const [best, setBest] = useState(() => readBest())
  const [record, setRecord] = useState<{ best: number; isNewRecord: boolean } | null>(null)
  const [muted, setMutedState] = useState(false)
  const [totalPoints, setTotalPoints] = useState<number | null>(null)
  const [randomSkinId, setRandomSkinId] = useState<string | null>(null)

  const stateRef = useRef<ChallengeState | null>(null)
  const lastTickRef = useRef(0)
  const savedRef = useRef(false)
  const handleAnswerRef = useRef<(d: Direction) => void>(() => {})

  const pxPerMm = readPxPerMm()
  // 医学边界②：翻拍过渡不得低于 600ms，统一从这一个出口拿
  const flipMs = challengeFlipMs(lsGet('fzp.flipMs') === null ? 900 : Number(lsGet('fzp.flipMs')))

  useEffect(() => { stateRef.current = state }, [state])
  useEffect(() => { setMuted(muted) }, [muted])

  // 节奏基准：读最近 PACE_SAMPLE_N 次训练的 avgReactionMs（**只读**，挑战不写任何表）。
  // sessions 没有 startedAtMs 索引，故沿用 StatsPage/badge-service 的全量 toArray 再排序
  // （家用数据量下无性能问题）；读失败就退回固定默认节奏，挑战照样能玩。
  useEffect(() => {
    let alive = true
    void (async () => {
      let p = DEFAULT_PACE
      try {
        const rows = await db.sessions.toArray()
        const recent = rows
          .slice()
          .sort((a, b) => b.startedAtMs - a.startedAtMs)
          .slice(0, PACE_SAMPLE_N)
        p = derivePace(recent.map((r) => r.avgReactionMs ?? 0))
      } catch {
        /* IndexedDB 不可用：用 DEFAULT_PACE（2000/1300） */
      }
      if (!alive) return
      setPace(p)
      setState(createChallenge(p))
    })()
    return () => { alive = false }
  }, [])

  // 皮肤解锁判定要累计分（与训练页同款只读逻辑，不打卡）
  useEffect(() => {
    void getHomeStats(toDateStr(new Date())).then((s) => setTotalPoints(s.totalPoints))
  }, [])

  useEffect(() => {
    if (getSkinId() === RANDOM_SKIN_ID && totalPoints !== null && randomSkinId === null) {
      setRandomSkinId(pickRandomSkin(totalPoints, Math.random()))
    }
  }, [totalPoints, randomSkinId])

  const running = state?.phase === 'showing' || state?.phase === 'transitioning'

  // 高频计时：100ms 一跳，且按**真实时间差**推进。
  // 训练页那套（1000ms 间隔 + tick(s,1) + 依赖 session.phase 重建 interval）在这里会走时
  // 不准：每次 showing↔transitioning 切换都丢掉未满一格的余量。这里 deps 只有 running
  // （整局只变两次），且 lastTickRef 在开局设一次、之后只被 interval 自己推进。
  useEffect(() => {
    if (!running) return
    const id = window.setInterval(() => {
      const now = Date.now()
      const delta = Math.min(now - lastTickRef.current, MAX_TICK_DELTA_MS)
      lastTickRef.current = now
      setState((s) => (s ? tickChallenge(s, delta) : s))
    }, TICK_MS)
    return () => window.clearInterval(id)
  }, [running])

  // 过渡调度：答题与超时两条路径都走这里（seq 每次离开 showing 都 +1，故每题只跑一次）。
  // 答题不再各自 setTimeout，超时也就不必另开一套——两路统一，翻拍时长只有一处。
  useEffect(() => {
    if (!state || state.phase !== 'transitioning' || !state.lastOutcome) return
    // 三种结局三种音效：超时**不能**用 'wrong'，那与 spec §4.5「超时=未答、不算错」矛盾，
    // 也会把结算页刚分开的"看错了 / 没跟上"在耳朵里重新糊成一团
    const k = state.lastOutcome.kind
    playSfx(k === 'correct' ? 'correct' : k === 'wrong' ? 'wrong' : 'timeout')
    const id = window.setTimeout(() => {
      playSfx('flip')
      setState((cur) =>
        cur && cur.phase === 'transitioning'
          ? advanceChallenge(cur, pickDirection(cur.target, Math.random()))
          : cur,
      )
    }, flipMs)
    return () => window.clearTimeout(id)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [state?.seq, state?.phase, flipMs])

  // 结束：只写 localStorage 最高分（不落 Dexie、不打卡、不给积分/勋章/怪兽）
  useEffect(() => {
    if (!state || state.phase !== 'finished' || savedRef.current) return
    savedRef.current = true
    const r = writeBestIfHigher(state.score)
    setRecord(r)
    setBest(r.best)
    playSfx(r.isNewRecord ? 'badge' : 'finish')
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [state?.phase, state?.score])

  function handleAnswer(dir: Direction) {
    const s = stateRef.current
    if (!s || s.phase !== 'showing' || s.target === null) return
    setState(answerChallenge(s, dir))
  }
  handleAnswerRef.current = handleAnswer

  // 键盘作答：与训练页共用 dirForKey，deps [] + ref 避免每次渲染重绑监听
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      const el = e.target as HTMLElement | null
      if (el && /^(input|textarea|select)$/i.test(el.tagName)) return
      const dir = dirForKey(e.key)
      if (!dir) return
      e.preventDefault()
      handleAnswerRef.current(dir)
    }
    window.addEventListener('keydown', onKey)
    return () => window.removeEventListener('keydown', onKey)
  }, [])

  function begin() {
    if (!pace) return
    savedRef.current = false
    setRecord(null)
    lastTickRef.current = Date.now()
    setState(startChallenge(createChallenge(pace), pickDirection(null, Math.random())))
  }

  // 没标定过就没有物理尺寸可言（与训练页同一道闸门，复用同两条文案）
  if (pxPerMm === null) {
    return (
      <div className="fq-page" style={{ textAlign: 'center' }}>
        <div style={{ fontSize: 46 }}>📐</div>
        <h2 style={{ fontSize: 20, fontWeight: 800, marginTop: 10 }}>{t('train.calibFirst')}</h2>
        <p style={{ color: 'var(--muted)', marginTop: 8, lineHeight: 1.6 }}>{t('train.calibFirstBody')}</p>
        <button className="fq-btn" style={{ marginTop: 18 }} onClick={onBack}>{t('challenge.back')}</button>
      </div>
    )
  }

  // 视标尺寸链与正经训练完全一致：毫米设定 × 屏幕标定（不因挑战而变）
  const sizeMm = Number(lsGet('fzp.optotypeSizeMm') ?? '1')
  const heightPx = sizeMm * pxPerMm
  const storedSkinId = getSkinId()
  const skinId = storedSkinId === RANDOM_SKIN_ID ? (randomSkinId ?? 'plain') : storedSkinId
  const effectiveSkinId = totalPoints === null || isSkinUnlocked(skinId, totalPoints) ? skinId : 'plain'
  const CurrentSkin = getSkin(effectiveSkinId)

  if (!state || state.phase === 'ready') {
    return (
      <div className="fq-page">
        <h2 className="fq-h2">{t('challenge.title')}</h2>
        <p className="fq-sub">{t('challenge.sub')}</p>
        <div className="fq-card fq-rise" style={{ marginTop: 14 }}>
          <p style={{ fontSize: 15, fontWeight: 800, marginBottom: 8 }}>{t('challenge.ready')}</p>
          <p style={{ fontSize: 13, lineHeight: 1.7 }}><Rich text={t('challenge.readyHint')} /></p>
          <p style={{ fontSize: 12, color: 'var(--muted)', marginTop: 8, lineHeight: 1.6 }}>{t('challenge.paceHint')}</p>
          <p style={{ fontSize: 12, color: 'var(--coral)', marginTop: 8, fontWeight: 700 }}>{t('challenge.notTraining')}</p>
        </div>
        <div
          className="fq-card fq-rise"
          style={{ marginTop: 12, textAlign: 'center', fontWeight: 800, color: 'var(--violet)', animationDelay: '0.05s' }}
        >
          {best > 0 ? t('challenge.best', { n: best }) : t('challenge.noBest')}
        </div>
        <button className="fq-cta coral" style={{ width: '100%', marginTop: 16 }} disabled={!pace} onClick={begin}>
          {t('challenge.start')}
        </button>
        <button className="fq-btn" style={{ marginTop: 12 }} onClick={onBack}>{t('challenge.back')}</button>
      </div>
    )
  }

  if (state.phase === 'finished') {
    return (
      <div className="fq-page" style={{ textAlign: 'center' }}>
        <div style={{ fontSize: 50 }}>🏁</div>
        <h2 style={{ fontSize: 22, fontWeight: 800, marginTop: 6 }}>{t('challenge.done')}</h2>
        <div
          className="fq-card"
          style={{ marginTop: 16, background: 'linear-gradient(135deg, #7c6cf0, #8b6cff)', border: 'none', color: '#fff', boxShadow: 'var(--shadow)' }}
        >
          <div style={{ fontSize: 42, fontWeight: 800, fontVariantNumeric: 'tabular-nums', lineHeight: 1.1 }}>{state.score}</div>
          <div style={{ fontSize: 13, opacity: 0.92, marginTop: 2 }}>{t('challenge.score')}</div>
          {record?.isNewRecord && <div style={{ fontSize: 16, fontWeight: 800, marginTop: 10 }}>{t('challenge.newRecord')}</div>}
          <div style={{ fontSize: 13, opacity: 0.92, marginTop: 8 }}>{t('challenge.best', { n: best })}</div>
        </div>
        <div className="fq-card" style={{ marginTop: 12, display: 'flex' }}>
          <Tally value={state.correct} label={t('challenge.correctCount')} tint="var(--violet)" />
          <Tally value={state.wrong} label={t('challenge.wrongCount')} tint="var(--coral)" />
          <Tally value={state.timedOut} label={t('challenge.timeoutCount')} tint="var(--muted)" />
          <Tally value={state.bestStreak} label={t('challenge.bestStreak')} tint="var(--mint)" />
        </div>
        <p style={{ fontSize: 12, color: 'var(--muted)', marginTop: 12 }}>{t('challenge.notTraining')}</p>
        <button className="fq-cta coral" style={{ width: '100%', marginTop: 14 }} onClick={begin}>{t('challenge.again')}</button>
        <button className="fq-btn" style={{ marginTop: 12 }} onClick={onBack}>{t('challenge.back')}</button>
      </div>
    )
  }

  const remainSec = Math.ceil(Math.max(0, state.durationMs - state.elapsedMs) / 1000)
  const windowRatio = state.windowMs > 0 ? Math.max(0, Math.min(1, state.windowLeftMs / state.windowMs)) : 0
  const justTimedOut = state.phase === 'transitioning' && state.lastOutcome?.kind === 'timeout'
  // 皮肤的怪兽轮换与一次性演出都吃 lastAnswer.seq；超时也当一次"答错"演出（抖一下）
  const lastAnswer = state.lastOutcome
    ? { dir: state.lastOutcome.dir, correct: state.lastOutcome.kind === 'correct', seq: state.lastOutcome.seq }
    : null

  return (
    <div className="fzp-train">
      {/* 手机横屏才显示（纯 CSS 媒体查询控制，见 index.css:228）——与训练页同一口径：
          横屏不是目标形态，只保证不破版并提示"竖屏更好用"（CLAUDE.md 关键决策 #1） */}
      <div className="fzp-rotate-hint">{t('train.rotateHint')}</div>
      <div className="fzp-tiny-warn">{t('train.screenTooSmall')}</div>
      {/* 顶栏刻意与训练页同高（一行）：layout-budget 的 TOP_BAR_PX=45 是按一行算的 */}
      <div className="fzp-train-top">
        <span className="fq-chip" style={{ fontWeight: 800, fontVariantNumeric: 'tabular-nums' }}>⚡ {state.score}</span>
        {/* 进度条 = 本题答题窗口剩余（不是总时长）；.fq-bar > i 自带 0.6s 过渡，这里必须关掉 */}
        <div className="fq-bar" style={{ flex: 1 }}>
          <i style={{ width: `${windowRatio * 100}%`, transition: 'none' }} />
        </div>
        {/* 顶栏放不下"剩余"二字（多一个词就可能折行、TOP_BAR_PX=45 的预算即失效），
            故文案只走 aria-label：读屏能念出"剩余"，视觉上仍是纯数字 */}
        <span
          aria-label={t('challenge.time')}
          style={{ fontVariantNumeric: 'tabular-nums', fontWeight: 800, color: 'var(--violet)', fontSize: 15, minWidth: 34, textAlign: 'right' }}
        >
          {remainSec}s
        </span>
        <button className="fq-btn" style={{ padding: '7px 10px' }} onClick={() => setMutedState((m) => !m)}>
          {muted ? '🔇' : '🔊'}
        </button>
      </div>

      <div className="fzp-stage">
        <CurrentSkin.Stage
          target={state.target}
          heightPx={heightPx}
          phase={state.phase === 'transitioning' ? 'transitioning' : 'showing'}
          lastAnswer={lastAnswer}
          isEgg={false}
          capturedReserveIds={[]}
        />
        {justTimedOut && (
          <div
            key={`timeout-${state.seq}`}
            style={{ position: 'absolute', top: '12%', left: '50%', fontSize: 26, fontWeight: 800, color: '#ff5c7a', textShadow: '0 2px 8px rgba(0,0,0,0.3)', animation: 'fzpCombo 0.9s ease-out forwards', pointerEvents: 'none', zIndex: 6, whiteSpace: 'nowrap' }}
          >
            {t('challenge.timeout')}
          </div>
        )}
        {/* 翻拍引导：与训练页同款柔和渐变光晕 + 优雅翻一次 + 紫→珊瑚进度条（皮肤无关） */}
        {state.phase === 'transitioning' && (
          <div
            style={{
              position: 'absolute',
              inset: 0,
              display: 'grid',
              placeItems: 'center',
              background: 'radial-gradient(circle at 50% 46%, rgba(108,75,240,0.13), rgba(255,138,138,0.05) 46%, transparent 72%)',
              backdropFilter: 'blur(2.5px)',
            }}
          >
            <div style={{ textAlign: 'center', animation: 'fzpGuideIn 0.35s cubic-bezier(0.2,0.8,0.2,1) both' }}>
              <div
                className="fzp-flip-icon"
                style={{ fontSize: 68, animation: `fzpFlip3d ${flipMs}ms ease-in-out infinite`, filter: 'drop-shadow(0 4px 10px rgba(108,75,240,0.28))' }}
              >
                🔄
              </div>
              <div style={{ fontSize: 24, fontWeight: 800, color: 'var(--violet)', marginTop: 8, letterSpacing: 1, textShadow: '0 1px 6px rgba(255,255,255,0.85)' }}>
                {t('train.flip')}
              </div>
              <div style={{ width: 168, height: 7, background: 'rgba(108,75,240,0.15)', borderRadius: 99, margin: '16px auto 0', overflow: 'hidden' }}>
                <div style={{ height: '100%', background: 'linear-gradient(90deg, var(--violet), var(--coral))', borderRadius: 99, animation: `fzpFlipBar ${flipMs}ms linear forwards` }} />
              </div>
            </div>
          </div>
        )}
      </div>

      <div className="fzp-answer">
        {/* 挑战不接语音（见文件头决策注），这一格放免责文案 */}
        <span className="fzp-voice-hint">{t('challenge.notTraining')}</span>
        <div className="fq-dpad">
          {DPAD.map(({ dir, col, row }) => (
            <button
              key={dir}
              className="fq-dpad-btn"
              aria-label={t(`direction.${dir}`)}
              onClick={() => handleAnswer(dir)}
              style={{ gridColumn: col, gridRow: row }}
            >
              {ARROW[dir]}
            </button>
          ))}
          <div className="fq-dpad-hub" style={{ gridColumn: 2, gridRow: 2 }}><i /></div>
        </div>
        <span className="fzp-answer-spacer" aria-hidden />
      </div>
    </div>
  )
}

/** 结算页的一格计数（答对 / 答错 / 超时 / 最长连击） */
function Tally({ value, label, tint }: { value: number; label: string; tint: string }) {
  return (
    <div style={{ flex: '1 1 0', minWidth: 0, textAlign: 'center' }}>
      <div style={{ fontSize: 24, fontWeight: 800, color: tint, lineHeight: 1.15, fontVariantNumeric: 'tabular-nums' }}>{value}</div>
      <div style={{ fontSize: 11, color: 'var(--muted)', marginTop: 4, whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>{label}</div>
    </div>
  )
}
```

- [ ] **Step 6.6: 接进 App 路由。** 编辑 `src/App.tsx`：①在 `import { PrivacyPage } from './PrivacyPage'` 之后加 `import { ChallengePage } from './challenge/ChallengePage'`；②`View` 联合类型末尾加 `| 'challenge'`，改成
```ts
type View = 'home' | 'train' | 'stats' | 'badges' | 'calib' | 'speech' | 'settings' | 'rewards' | 'privacy' | 'admin' | 'challenge'
```
③`NAV` **不动**（挑战入口只在首页当天练完时出现，见 spec §7）；在 `{view === 'privacy' && …}` 那一行**之后**插入
```tsx
        {/* 挑战页不进 NAV：入口只在首页"当天已练完"时出现（spec §7），与 privacy 同构 */}
        {view === 'challenge' && <ChallengePage onBack={() => setView('home')} />}
```
**本步只做 ①②③。** `<HomePage … />` 的 `onOpenChallenge` 属性**留到 Task 7 Step 7.3** 再加——`HomePage` 的 props 那时才扩，本步先加会让 `npm run typecheck` 报 `TS2322: Property 'onOpenChallenge' does not exist on type …`。所以 Task 6 结束时 `'challenge'` 这个 View 已经能渲染，但还没有任何 UI 入口指向它——**不要**为了手测把初始 View 临时改成 `'challenge'`（那条护栏要到 Task 7 的 `challenge-entry.test.ts` 才存在），浏览器手测统一放在 Task 7 Step 7.6 走真实入口。

- [ ] **Step 6.7: 写外壳与边界的契约测试。** 新建 `src/challenge/challenge-shell.test.ts`：

```ts
import { describe, it, expect } from 'vitest'

// 用 Vite 的 ?raw 拿源文件文本（本仓没装 @types/node，node:fs 会让 tsc 报 TS2307）。
// 参考 src/layout/css-contract.test.ts 与 src/admin/admin-entry.test.ts 的同类做法。
import page from './ChallengePage.tsx?raw'

/**
 * UI 不可渲染测试（本仓 vitest environment 是 node、没有 jsdom），
 * 但"挑战页有没有踩到医学边界与数据边界"可以用源文本锚住。
 */
describe('挑战页的外壳与边界契约', () => {
  it('复用训练页三段外壳与两条提示（手机竖屏靠既有断点自动适配，不新造断点）', () => {
    for (const cls of [
      'fzp-train', 'fzp-train-top', 'fzp-stage', 'fzp-answer', 'fq-dpad',
      // 训练页在 .fzp-train 里同时渲染这两条：矮屏警告 + 手机横屏"竖屏更好用"。
      // 挑战页少哪一条，迭代 3d 建立的口径就在新页面上缺一半（CLAUDE.md 关键决策 #1）。
      'fzp-rotate-hint', 'fzp-tiny-warn',
    ]) {
      expect(page, `缺 ${cls}`).toContain(cls)
    }
  })

  it('不写死导航高度、不自造媒体查询（断点只该在 index.css 里）', () => {
    expect(page).not.toContain('calc(100vh')
    expect(page).not.toContain('@media')
  })

  it('医学：视标链上没有 transform scale（视标恒为 毫米 × 标定）', () => {
    expect(page).toContain('sizeMm * pxPerMm')
    expect(page).not.toMatch(/scale\(/)
  })

  it('医学边界②：翻拍时长必须过 challengeFlipMs，不许直接用 fzp.flipMs 的原值', () => {
    expect(page).toContain('challengeFlipMs(')
    // 白名单而非黑名单：页面里**每一处** `const flipMs = …` 都必须经过 challengeFlipMs(。
    // 原先只禁了 `= Number(lsGet('fzp.flipMs'))` 这一种确切写法，换个写法（Number(x)||900、
    // 先取变量再赋值……）就能绕过去，等于闸门形同虚设。
    const decls = page.match(/const\s+flipMs\s*=\s*[^\n]*/g)
    expect(decls, '挑战页必须恰好有一处 flipMs 定义').toHaveLength(1)
    expect(decls).toEqual([expect.stringContaining('challengeFlipMs(')])
  })

  it('数据：不落 Dexie、不打卡、不给积分/勋章/怪兽、不写 outbox（挑战不入云同步）', () => {
    // 具体函数名保留作可读性……
    for (const forbidden of ['saveSession', 'doCheckIn', 'syncBadges', 'captureMonster', 'addRedemption']) {
      expect(page, `不该出现 ${forbidden}`).not.toContain(forbidden)
    }
    // ……但真正的闸门是这两条正则：黑名单挡不住 db.checkins.put / db.badges.bulkPut /
    // db.monsters.add / db.sessions.bulkPut，也挡不住 src/data/api.ts 那 7 个 pushXxx（写
    // outbox 就等于进云同步）。当前代码是干净的，这两条防的是将来顺手加落库的人。
    expect(page, '禁止任何 Dexie 写操作').not.toMatch(/db\.\w+\.(put|add|bulkPut|bulkAdd|update|delete|clear|modify)\b/)
    expect(page, '禁止调用 pushXxx（写 outbox = 进云同步）').not.toMatch(/\bpush[A-Z]\w*\(/)
  })

  it('超时不复用答错音效（spec §4.5：超时=未答、不算错）', () => {
    expect(page).toContain("'timeout'")
    // 若有人把三元收回成 `kind === 'correct' ? 'correct' : 'wrong'`，这条会红
    expect(page).not.toMatch(/kind\s*===\s*'correct'\s*\?\s*'correct'\s*:\s*'wrong'/)
  })

  it('数据：只读 sessions 取节奏基准，最高分只走 localStorage 层', () => {
    expect(page).toContain('db.sessions.toArray()')
    expect(page).toContain('writeBestIfHigher(')
  })
})
```

- [ ] **Step 6.8: 跑到通过。** `npm test -- challenge-shell` → 预期 `Tests 7 passed (7)`。全量 `npm test` → **基线 + 65 个测试 / 基线 + 6 个文件**（本任务新增 key-map 5 + shell 7 = 12）。`npm run typecheck` 零输出。

> **本任务没有浏览器手测**（原先那份挪到了 Task 7 Step 7.6）。原因：Task 6 阶段还没有 UI 入口，唯一进挑战页的办法是临时把 `App.tsx` 的 `useState<View>('home')` 改成 `'challenge'`，而防它被误提交的断言要到 Task 7 才存在——那一刻全仓没有任何自动化护栏拦住「初始视图=挑战页」上 master，只剩一句"记得跑 git diff"，而本仓有并行 agent 误提交的前科。Task 7 做完后走真实入口进挑战页，一行 `App.tsx` 都不用改。

- [ ] **Step 6.9: 提交。**（先 `git diff --stat` 确认只动了下面这 7 个文件，且 `App.tsx` 里 `useState<View>('home')` 原样未动——本任务不需要临时改它）
```
git add src/training/key-map.ts src/training/key-map.test.ts src/training/TrainingPage.tsx src/training/sfx.ts src/challenge/ChallengePage.tsx src/challenge/challenge-shell.test.ts src/App.tsx
git commit -m "feat(挑战): 挑战页 UI（ms 级计时/超时独立音效/翻拍引导复用）+ 键盘映射抽成共用模块 + App 新 View 分支"
```

---

### Task 7: 首页入口卡 + 接线

**Files**
- Create: `E:\developer\fzp\src\challenge\ChallengeCard.tsx`
- Test: `E:\developer\fzp\src\challenge\challenge-entry.test.ts`
- Modify: `E:\developer\fzp\src\HomePage.tsx`
- Modify: `E:\developer\fzp\src\App.tsx`

**Interfaces**
- Consumes: `challengeUnlocked`（`./challenge-unlock`）、`readBest`（`./challenge-storage`）、`useT`
- Produces:
  ```ts
  export function ChallengeCard({ onOpen }: { onOpen: () => void }): JSX.Element
  // HomePage 新签名
  export function HomePage({ onStart, onOpenDex, onOpenRewards, onOpenChallenge }: {
    onStart: () => void; onOpenDex: () => void; onOpenRewards: () => void; onOpenChallenge: () => void
  })
  ```

- [ ] **Step 7.1: 写入口卡。** 新建 `src/challenge/ChallengeCard.tsx`：

```tsx
import { useState } from 'react'
import { useT } from '../i18n'
import { readBest } from './challenge-storage'

/**
 * 首页「30 秒挑战」入口卡。**只在解锁时才被 HomePage 渲染**（判定在 HomePage，用
 * challengeUnlocked(stats)）——锁在当天训练之后，挑战才是训练的奖赏而非替代品。
 * 样式沿用首页既有入口卡（图鉴/奖励）那一套 .fq-card + 渐变边框。
 */
export function ChallengeCard({ onOpen }: { onOpen: () => void }) {
  const t = useT()
  const [best] = useState(() => readBest())
  return (
    <button
      onClick={onOpen}
      className="fq-card fq-rise"
      style={{
        width: '100%', textAlign: 'left', cursor: 'pointer',
        border: '1.5px solid var(--lemon)',
        background: 'linear-gradient(160deg, #fffaea, #fff)',
        padding: 14, animationDelay: '0.14s',
      }}
    >
      <div style={{ fontSize: 20 }}>⚡</div>
      <div style={{ fontSize: 14, fontWeight: 800, marginTop: 4 }}>{t('challenge.homeCard')}</div>
      <div style={{ fontSize: 12, color: 'var(--muted)', marginTop: 2, lineHeight: 1.4 }}>{t('challenge.homeCardHint')}</div>
      <div style={{ fontSize: 12, color: 'var(--violet)', fontWeight: 800, marginTop: 8, fontVariantNumeric: 'tabular-nums' }}>
        {best > 0 ? t('challenge.best', { n: best }) : t('challenge.noBest')}
      </div>
    </button>
  )
}
```

- [ ] **Step 7.2: 接进首页。** 编辑 `src/HomePage.tsx`：
  1. 在 `import { useT } from './i18n'` 之后加两行
     ```ts
     import { ChallengeCard } from './challenge/ChallengeCard'
     import { challengeUnlocked } from './challenge/challenge-unlock'
     ```
  2. 把签名改成
     ```ts
     export function HomePage({ onStart, onOpenDex, onOpenRewards, onOpenChallenge }: { onStart: () => void; onOpenDex: () => void; onOpenRewards: () => void; onOpenChallenge: () => void }) {
     ```
  3. 在「成绩小药丸」那个 `div`（`animationDelay: '0.12s'` 的那一块）**之后**、「入口：图鉴 | 奖励」那一块**之前**插入
     ```tsx
        {/* 限时挑战入口：只在当天已完成训练时出现（spec §7），未解锁则整卡不渲染 */}
        {challengeUnlocked(stats) && <ChallengeCard onOpen={onOpenChallenge} />}
     ```
     （入场动效延迟 0.14s，正好落在药丸 0.12s 与入口行 0.16s 之间，节奏不乱。）

- [ ] **Step 7.3: 接进 App。** 编辑 `src/App.tsx`，给 `<HomePage …/>` 补最后一个属性：
```tsx
          <HomePage
            onStart={() => setView('train')}
            onOpenDex={() => { setBadgeTab('dex'); setView('badges') }}
            onOpenRewards={() => setView('rewards')}
            onOpenChallenge={() => setView('challenge')}
          />
```

- [ ] **Step 7.4: 写接线契约测试。** 新建 `src/challenge/challenge-entry.test.ts`：

```ts
import { describe, it, expect } from 'vitest'

// ?raw 源文本核对（本仓没装 @types/node）；参考 src/admin/admin-entry.test.ts。
import home from '../HomePage.tsx?raw'
import app from '../App.tsx?raw'

/**
 * 锚定"挑战入口的可见性"这根链子：解锁判定写在首页、回调从 App 传下来、挑战页不进 NAV。
 * 这三处任何一处被顺手改坏，都会让挑战变成"随时可玩"——那就本末倒置（spec §2 的核心决策）。
 */
describe('挑战入口只在当天练完时出现', () => {
  it('首页把 ChallengeCard 挡在 challengeUnlocked(stats) 后面', () => {
    expect(home).toMatch(/challengeUnlocked\(stats\)\s*&&\s*<ChallengeCard/)
  })

  it('首页把 onOpenChallenge 透给入口卡', () => {
    expect(home).toMatch(/<ChallengeCard[\s\S]{0,80}?onOpen=\{onOpenChallenge\}/)
  })

  it('App 有 challenge 这个 View 并接了 ChallengePage', () => {
    expect(app).toMatch(/type View =[^\n]*'challenge'/)
    expect(app).toMatch(/view === 'challenge' && <ChallengePage/)
    expect(app).toContain('onOpenChallenge={() => setView(\'challenge\')}')
  })

  it('挑战页不进常驻导航（NAV 只有 5 项，且不含 challenge）', () => {
    const nav = app.slice(app.indexOf('const NAV'), app.indexOf('export function App'))
    expect(nav).not.toContain('challenge')
    expect(nav.match(/key:\s*'/g)?.length).toBe(5)
  })

  it('App 的初始 View 是 home（防手测时临时改成 challenge 忘改回来）', () => {
    expect(app).toContain("useState<View>('home')")
  })
})
```

- [ ] **Step 7.5: 跑到通过。** `npm test -- challenge-entry` → 预期 `Tests 5 passed (5)`。全量 `npm test` → **基线 + 70 个测试 / 基线 + 7 个文件**。`npm run typecheck` 零输出。
  > 全程账目（新增测试合计 70）：Task 1 的 18 + Task 2 的 19 + Task 3 的 11 + Task 4 的 4 + Task 5 的 1 + Task 6 的 12（key-map 5 + shell 7）+ Task 7 的 5。新增文件合计 7：challenge-pace / challenge-session / challenge-storage / challenge-unlock / key-map / challenge-shell / challenge-entry（Task 5 只往既有的 `i18n.test.ts` 加 `it`，不算新文件）。

- [ ] **Step 7.6: 浏览器手测（入口可见性 + 挑战页全流程，走真实入口，一行 `App.tsx` 都不改）。** 本项目已知 `preview_screenshot` 在训练/皮肤页必超时，全部用 `javascript_tool` / `read_page` 读回渲染结果。
  1. `preview_start` with `{ name: "focal-quest-dev" }` → 打开 `http://localhost:5173`；`javascript_tool` 播下前置条件（第一次一定要**先让页面加载一次**，Dexie 才会把库建到 v6）：
     ```js
     localStorage.setItem('fzp.onboarded', '1')
     localStorage.setItem('fzp.cssPxPerMm', '6.05')
     localStorage.setItem('fzp.optotypeSizeMm', '0.7')
     localStorage.setItem('fzp.flipMs', '600')
     localStorage.removeItem('fzp.challengeBest')
     'seeded'
     ```
  2. **未解锁路径**：清掉今天的打卡再重载——
     ```js
     await new Promise((res, rej) => { const r = indexedDB.open('focalquest'); r.onsuccess = () => { const d = r.result; const tx = d.transaction('checkins','readwrite'); tx.objectStore('checkins').clear(); tx.oncomplete = () => { d.close(); res('cleared') }; tx.onerror = () => rej(tx.error) } })
     ```
     `navigate` 重载 → `javascript_tool` 执行 `document.body.innerText.includes('30 秒挑战')` → 预期 **`false`**（入口卡不渲染，挑战页也就不可达）。
  3. **解锁路径**：写 IndexedDB 造出「今天已打卡」+ 4 条带反应时间的历史训练（入口卡与自适应节奏都要它）：
     ```js
     await new Promise((res, rej) => {
       const req = indexedDB.open('focalquest')
       req.onsuccess = () => {
         const d = req.result
         const tx = d.transaction(['checkins', 'sessions'], 'readwrite')
         const today = new Date().toLocaleDateString('sv')
         tx.objectStore('checkins').put({ date: today, streak: 1, dailyPoints: 10, totalPoints: 3000, profileId: 'default', updatedAt: Date.now() })
         const ss = tx.objectStore('sessions')
         for (let i = 0; i < 4; i++) ss.add({ date: today, startedAtMs: Date.now() - i * 1000, eye: i % 2 ? 'right' : 'left', answered: 10, correct: 9, flips: 10, elapsedSec: 180, acuity: 0.8, avgReactionMs: 1000, profileId: 'default', updatedAt: Date.now() })
         tx.oncomplete = () => { d.close(); res('seeded') }
         tx.onerror = () => rej(tx.error)
       }
       req.onerror = () => rej(req.error)
     })
     ```
     （`totalPoints: 3000` 是为了顺便解锁太空/神庙皮肤，好在第 12 步换肤复测。）`navigate` 重载 → `document.body.innerText.includes('30 秒挑战')` 预期 **`true`**；`read_page` 里能看到卡片文案「今天练完啦，来冲个分！」。
  4. **走真实入口进挑战页**：`computer` 用 `find` 定位「30 秒挑战」这张卡后 `left_click`，然后
     ```js
     JSON.stringify({ onChallenge: document.body.innerText.includes('30 秒挑战'), hasStart: [...document.querySelectorAll('button')].some(b => b.textContent.includes('开始挑战')) })
     ```
     预期两项都为 `true`。
  5. 挑战页准备态：`read_page` 应能读到 `⚡ 30 秒挑战` / `双眼一起看，不用遮眼` / `还没有记录` / `⚡ 开始挑战`。
  6. 点「⚡ 开始挑战」后立刻用 `javascript_tool` 连续读两次（间隔一次工具调用即可）：
     ```js
     const bar = document.querySelector('.fq-bar > i'); const chip = document.querySelector('.fq-chip');
     JSON.stringify({ score: chip && chip.textContent.trim(), barWidth: bar && bar.style.width, e: !!document.querySelector('svg, .fzp-skin-canvas') })
     ```
     预期：`barWidth` 从接近 `100%` 递减（说明答题窗口在走），`score` 形如 `⚡ 0`。
  7. 用键盘作答验计分与翻拍：`computer` 按 `ArrowUp`/`ArrowDown`/`ArrowLeft`/`ArrowRight` 各一次（其中总会撞对），然后
     ```js
     JSON.stringify({ score: document.querySelector('.fq-chip').textContent.trim(), flip: !!document.querySelector('.fzp-flip-icon'), flipText: document.body.innerText.includes('翻转拍子') })
     ```
     预期：答对后 `score` 大于 0；答完那 600ms 内 `flip` 为 `true`、`flipText` 为 `true`（翻拍引导在，且不被压缩）。
  8. 故意不答，等窗口跑完：
     ```js
     document.body.innerText.includes('超时')
     ```
     预期 `true`（超时提示飘出），且 `score` 不变。**顺便用耳朵确认超时那一下不是答错音**（`'timeout'` 是短促下滑音，`'wrong'` 是一记刺耳低频）——听不出差别就回 Step 6.3b 检查。
  9. 等到 30 秒结束，`read_page` 确认结算页有 `🏁 挑战结束`、四格计数（答对/答错/超时/最长连击）、`🎉 新纪录！`（首局分数 > 0 时），并确认
     ```js
     localStorage.getItem('fzp.challengeBest')
     ```
     是本局分数的字符串。再点「🔁 再来一次」故意打低分，确认 `fzp.challengeBest` **没有**被改小。
  10. **确认挑战不污染训练数据**（这是本功能最要紧的一条）：
      ```js
      await new Promise((res, rej) => { const r = indexedDB.open('focalquest'); r.onsuccess = () => { const d = r.result; const tx = d.transaction(['sessions','checkins','badges','monsters'],'readonly'); const out = {}; let left = 4; ['sessions','checkins','badges','monsters'].forEach(n => { const q = tx.objectStore(n).count(); q.onsuccess = () => { out[n] = q.result; if (--left === 0) { d.close(); res(JSON.stringify(out)) } } }); tx.onerror = () => rej(tx.error) } })
      ```
      玩挑战前后各跑一次，**四个计数必须完全一样**（`sessions` 仍是 4、`checkins` 仍是 1）。
  11. 手机竖屏：`resize_window` with `{ preset: "mobile" }`（375×812），在答题态执行
      ```js
      const down = [...document.querySelectorAll('.fq-dpad-btn')].find(b => b.textContent.trim() === '↓');
      const r = down.getBoundingClientRect();
      JSON.stringify({ noHScroll: document.documentElement.scrollWidth <= window.innerWidth, downBottom: Math.round(r.bottom), vh: window.innerHeight, inView: r.bottom <= window.innerHeight })
      ```
      预期 `noHScroll: true` 且 `inView: true`（↓ 键完整在首屏内——这正是迭代 3d 修过的那个 bug 的形态）。再 `resize_window` with `{ preset: "tablet" }`（768×1024）复测一次同一断言。首页也测一遍不横向溢出：
      ```js
      JSON.stringify({ noHScroll: document.documentElement.scrollWidth <= window.innerWidth, vw: window.innerWidth })
      ```
  12. 手机横屏提示（迭代 3d 的口径，挑战页不能少这一半）：`resize_window` with `{ width: 812, height: 375 }`，在挑战答题态执行
      ```js
      const h = document.querySelector('.fzp-rotate-hint');
      JSON.stringify({ text: h && h.textContent, visible: h && getComputedStyle(h).display !== 'none' })
      ```
      预期 `visible: true` 且文案是「📱 竖屏更好用哦」。
  13. 换肤复测：`javascript_tool` 执行 `localStorage.setItem('fzp.skinId','space')` → `navigate` 重载 → 从第 4 步重跑到第 7 步与第 11 步（太空皮肤画布用 `100cqh`，窄屏最容易露馅）；再用 `'shrine'` 重复一次。
  14. 切英文复测文案不回退中文：`javascript_tool` 执行 `localStorage.setItem('fzp.lang','en')` → `navigate` 重载 → `document.body.innerText.includes('30s Challenge')` 预期 `true`；再切回 `zh`。
  15. 最后 `git status` / `git diff` 确认手测**没有**留下任何源码改动（本轮全程只动 localStorage 与 IndexedDB）。

- [ ] **Step 7.7: 提交。**
```
git add src/challenge/ChallengeCard.tsx src/challenge/challenge-entry.test.ts src/HomePage.tsx src/App.tsx
git commit -m "feat(挑战): 首页入口卡（仅当天练完可见）+ App/HomePage 接线 + 入口可见性契约测试"
```

---

### Task 8: 收尾——文档与全量验证

**Files**
- Modify: `E:\developer\fzp\CLAUDE.md`
- Modify: `E:\developer\fzp\docs\superpowers\specs\2026-07-26-限时挑战-design.md`（把 Step 0.4 的用户结论落进 §9，若开工时已改过就跳过）

**Interfaces**
- Consumes: 无
- Produces: 项目宪法里的状态条目（下一个 agent 读它来判断"这功能已经在 master 了"）+ spec 与实现一致

- [ ] **Step 8.1: 全量验证。** 依次跑：
  - `npm test` → 预期 **基线 + 70 个测试 / 基线 + 7 个文件**，全绿。
  - `npm run typecheck` → 零输出。
  - `npm run typecheck:functions` → 零输出（本计划没动 `functions/`，这一步只是确认没顺手弄坏）。
  - `npm run build` → 成功（挑战页要进 SW 预缓存才能离线打开；构建失败就说明有 import 环或类型问题）。
  - `git status` → 工作区干净（前 7 个任务都已提交）。

- [ ] **Step 8.2: 更新 CLAUDE.md 状态区。** 在「- 迭代 3e·管理后台 ✅ 完成并合并：…」那一整条**之后**、「- 下一步：…」那一条**之前**，插入：

```markdown
- 迭代 2·限时挑战 ✅ 完成并合并：当天练完训练才解锁的 **30 秒冲分小游戏**（`src/challenge/`），**双眼不遮眼**（binocular accommodative facility 是标准训练项目）、**不给积分/勋章/怪兽、不进统计、不落 Dexie**，最高分只写 localStorage `fzp.challengeBest`。**自适应节奏**：读最近 20 次训练 `avgReactionMs` 的**中位数**为基准，初始答题窗口 = 基准×2.0、最快 = 基准×1.3，每 3 题降一档共四档；样本 <3 个回落固定 2000/1300ms（全部常量集中在 `challenge-pace.ts`，调参只改这一处）。**钳制只钳基准 `b ∈ [750, BASELINE_MAX_MS=3000]`，两个窗口一律由 `b × 固定系数` 派生——绝不给窗口加上限**：计划评审阶段的原稿给两窗封了顶（4000/2500），实测推演发现基准 ≥1924ms 时最快窗口的实际系数会掉到 1.3 以下（b=2500 时正好 ×1.0＝窗口等于孩子自己的中位反应时间，约一半的题必超时），把医学边界①静默架空了；`challenge-pace.test.ts` 有一条 `derivePace([2500×4]).fastestMs ≥ 3250` 的回归锚死死钉住这个破口，**别把窗口上限加回来**。**两条医学边界不可动**：①下限系数不得低于 1.3（`FASTEST_FACTOR_FLOOR`，压到 ×1.05 会让约一半的题超时=逼孩子瞎猜）②翻拍过渡强制不低于 600ms（`challengeFlipMs` 是唯一出口；孩子来不及真翻就会干脆不翻，挑战退化成纯反应游戏还会腐蚀主流程）。**时限只覆盖答题窗口**，翻拍过渡是独立阶段、不计入也不被压缩；**超时不算错也不得分**，与答错都清连击但分开计数、结算页分开显示，**音效也分开**（`sfx.ts` 新增 `'timeout'` 短促下滑音——超时若沿用 `'wrong'`，界面刚分清的"看错了/没跟上"会在耳朵里被重新糊成一团）。视标恒为 毫米×标定，不因挑战而变。刻意**不复用** `training/session.ts`（规则不同，硬合并会把被大量测试压住的训练状态机搞复杂）；复用皮肤 Stage（`getSkin`，**注意 spec 里写的 `skinById` 不存在**）、`pickDirection`、`playSfx`、`.fzp-train/.fzp-stage/.fzp-answer` 三段外壳（含 `.fzp-rotate-hint` 与 `.fzp-tiny-warn` 两条提示）与翻拍引导视觉。计时**不能照抄训练页**（那是 1 秒粒度 + 依赖 phase 重建 interval，会丢余量），改成 100ms 高频 + 真实时间差推进。**输入方式以 spec §9 为准**（挑战页要不要接语音是产品决策，已单独找用户拍板并写进 spec；实现按那条结论走，别看代码猜）。键盘映射从 `TrainingPage` 抽成共用的 `src/training/key-map.ts`（两处各写一份必然漂移）。入口只在首页「当天已打卡」时渲染，**不进 NAV**；`challenge-shell.test.ts` / `challenge-entry.test.ts` 用 `?raw` 源文本锚住"不落库（`db.*.put|add|bulkPut…` 与 `pushXxx(` 两条正则，不是函数名黑名单）/无 scale/翻拍必过闸门/超时不复用答错音/入口不许随时可玩"。
```

- [ ] **Step 8.3: 更新「下一步」条目。** 把
```
- 下一步：真机持续使用收集反馈；迭代 3 按上述子迭代推进；迭代 2 其余（限时挑战需确认节奏 / 深海动物需先验 vosk）；皮肤储备怪兽池扩充（已有 22 只素材待挑用）。
```
改成
```
- 下一步：真机持续使用收集反馈（限时挑战的节奏松紧待闺女实测，觉得太难就调 `src/challenge/challenge-pace.ts` 的 `INITIAL_FACTOR`，**不要**动 `FASTEST_FACTOR_FLOOR` 与 `MIN_FLIP_MS` 这两条医学边界，也**不要**给两个窗口加上限——上限会压过下限系数、把边界①静默架空，`derivePace([2500×4]) ≥ 3250` 那条回归锚就是防它的）；迭代 3 按上述子迭代推进；迭代 2 其余（深海动物需先验 vosk）；皮肤储备怪兽池扩充（已有 22 只素材待挑用）。
```

- [ ] **Step 8.4: 复核 spec 与实现一致（开工前那两处若已改，这步只是确认）。** 打开 `docs/superpowers/specs/2026-07-26-限时挑战-design.md`：
  - §4.4 的钳制口径是不是 Step 0.5 改过的「只钳基准 [750, 3000]，不钳窗口」——若还写着「最快窗口钳在 [1000, 2500]」，说明 Step 0.5 漏了，现在补。
  - §9「明确不做」里有没有 Step 0.4 的用户结论（选 A 就该有「不接语音作答」那条；选 B/C 则该在 §3/§9 写清方案）。**spec 与代码不一致比两边都没写更危险**——下一个 agent 会照 spec 改代码。

- [ ] **Step 8.5: 提交。**
```
git add CLAUDE.md docs/superpowers/specs/2026-07-26-限时挑战-design.md
git commit -m "docs: CLAUDE.md 记入迭代 2·限时挑战；spec 同步钳制口径与语音归属结论"
```

---

## 自查对照表（spec 每节 → 落点）

| spec 节 | 落点 |
|---|---|
| §1 定位（练完解锁的小游戏） | Task 4 `challengeUnlocked` + Task 7 首页入口卡 + `challenge-entry.test.ts` |
| §2 数据去向 / 30 秒 / 不给奖励 | Task 3（只 localStorage）+ Task 1 `CHALLENGE_DURATION_MS` + `challenge-shell.test.ts` 的"不落库"断言 |
| §3.1 双眼不遮眼 | 状态机无 `eye` 字段（Task 2）+ 文案 `challenge.sub` / `challenge.readyHint`（Task 5） |
| §3.2 翻拍 ≥600ms（**医学边界②**） | Task 1 `MIN_FLIP_MS` + `challengeFlipMs` + 3 个单测 + Task 6 源文本闸门 |
| §3.3 视标尺寸不变 | Task 6 `sizeMm * pxPerMm` + `challenge-shell.test.ts` 的"无 scale"断言 |
| §4.1-4.3 自适应参数 | Task 1 `derivePace` / `windowMsForIndex` + 完整序列断言 |
| §4.2 时限只覆盖答题窗口 | Task 2 `tickChallenge` 的 transitioning 分支 + 专门一个 `it` |
| §4.3 下限系数 ≥1.3（**医学边界①**） | Task 1 `FASTEST_FACTOR_FLOOR` + 遍历基准的**无条件**边界单测 + `derivePace([2500×4]) ≥ 3250` 回归锚 |
| §4.4 兜底与钳制 | **spec 原文与 §4.3 打架，开工前经 Step 0.5 修正为「只钳基准」**；Task 1 `MIN_SAMPLES` / `BASELINE_MAX_MS` / 两个窗口地板 + 四个单测 |
| §4.5 超时语义 | Task 2 超时单测（不算错/不得分/清连击/answered 不变）+ Task 6 结算页四格分开显示 + 独立 `'timeout'` 音效（Step 6.3b）+ 源文本断言禁止复用 `'wrong'` |
| §5 最高分只 localStorage | Task 3 全部 |
| §6 架构与复用 | File Structure；spec 写错的 `skinById` 已在开工前 Step 0.3 与本表纠正为 `getSkin` |
| §7 解锁条件 | Task 4 + Task 7 |
| §8 测试策略 | Task 1-4 纯函数全覆盖；UI 用 Task 7 Step 7.6 的手测清单（走真实入口，不改 `App.tsx`）+ `?raw` 契约测试 |
| §9 明确不做 | 无排行榜/无难度档位/无分眼/无引擎；**语音作答归属由 Step 0.4 交用户拍板后写进 spec §9，不由本计划自定** |
| §10 风险 | 文案 `challenge.notTraining` 出现在准备页、答题带、结算页三处；调参入口收在 `challenge-pace.ts` 一处 |

---

## 评审意见处置

计划评审（verdict: needs-fixes）共 6 条，**全部采纳并已改进本计划**；其中 1 条在采纳之外另加了一处收紧（见 ③）。逐条记录如下。

| # | 严重度 | 评审发现 | 处置 |
|---|---|---|---|
| ① | blocker | `FASTEST_MAX_MS=2500` 的窗口上限架空医学边界①（基准 ≥1924ms 起系数掉到 1.3 以下，b=2500 时窗口＝孩子自己的中位反应时间），而那条名叫「医学边界①」的断言用 `Math.min(FASTEST_MAX_MS, …)` 把违规写进了期望值 | **已修（真改代码，不是改注释）**。Global Constraints 与 Task 1 的常量、`derivePace` 实现、单测三处同步改成「钳制只钳基准」：新增 `BASELINE_MAX_MS = 3000`，`b = clamp(中位数, INITIAL_MIN_MS/INITIAL_FACTOR, BASELINE_MAX_MS)`，`initialMs = round(b × 2.0)`、`fastestMs = max(FASTEST_MIN_MS, round(b × 1.3))`；**删掉 `INITIAL_MAX_MS` / `FASTEST_MAX_MS`**。边界①断言改成无条件的 `≥ round(min(b, BASELINE_MAX_MS) × FASTEST_FACTOR_FLOOR)`，并加一条 `derivePace([2500×4]).fastestMs ≥ 3250` 的回归锚。矛盾源头在 spec §4.4，故另加**开工前 Step 0.5** 去修 spec 并知会用户（这处修正只有一个方向：反应慢的日子窗口更长）。另：原实现里 `Math.min(…, initialMs)` 那条安全带删掉了，改为在单测里断言 `FASTEST_FACTOR < INITIAL_FACTOR`——安全带会把误配静默吞掉，而误配正该炸红 |
| ② | major | 计划单方面决定挑战页不接 vosk，推翻了 CLAUDE.md 标注「勿擅自推翻」的关键决策 #2，且 spec §9 里没有这一条 | **已修**。新增**阻塞式 Step 0.4**：把 A（不接语音）/ B（准备页开关 + `VOICE_COMPENSATION_MS` 补偿）/ C（并行输入、按最早到达的一路计时）三个选项原样交用户拍板，结论必须先写进 spec §9 才能开工 Task 6；Global Constraints 的「不做」清单里删掉「语音作答」，Task 6 顶部加前置闸门横幅，原「刻意决策」第 1 条改写为「技术理由成立，但没有拍板权」；CLAUDE.md 状态条目改成「输入方式以 spec §9 为准」 |
| ③ | minor | 超时走 `'wrong'` 音效，与 spec §4.5「超时=未答、不算错」冲突——界面刚把两者分成两格，耳朵里又糊回一起 | **已修，并按评审的首选方案（扩 `SfxKind`）而非退路（复用 `'flip'`）**。新增 **Step 6.3b**：`src/training/sfx.ts` 的私有 `SfxKind` 加 `'timeout'` + 一个短促下滑音（triangle 520→390Hz，与 `'wrong'` 的低频刺耳音区分）；挑战页改成三分支；File Structure 修改表补 `sfx.ts`，Task 6 的 `git add` 一并补上。**额外收紧**：`challenge-shell.test.ts` 加一条断言，禁止把三元收回成 `kind === 'correct' ? 'correct' : 'wrong'`（本仓没有 sfx 单测——它整个是 WebAudio 副作用，node 环境无 `AudioContext`，只能靠源文本锚） |
| ④ | minor | 「不落 Dexie」契约测试只黑名单了 7 个标识符，`db.checkins.put` / `db.badges.*` / `db.sessions.bulkPut` / `api.ts` 的 7 个 `pushXxx` 全能溜过；「翻拍必过闸门」也只禁了一种确切写法 | **已修**。落库断言换成两条正则 `/db\.\w+\.(put\|add\|bulkPut\|bulkAdd\|update\|delete\|clear\|modify)\b/` 与 `/\bpush[A-Z]\w*\(/`（函数名黑名单保留作可读性）；翻拍闸门从黑名单改成**白名单**：`page.match(/const\s+flipMs\s*=\s*[^\n]*/g)` 必须恰好一处且 `stringContaining('challengeFlipMs(')` |
| ⑤ | minor | Task 6 的手测要临时把 `App.tsx` 初始 View 改成 `'challenge'`，而防误提交的断言要到 Task 7 才存在 | **已修，取评审的首选方案（整体后移）而非次选（提前建 app-view.test.ts）**。Step 6.9 那份手测清单整体并入 **Task 7 Step 7.6**，走真实入口卡进挑战页，全程一行 `App.tsx` 都不改；Task 6 只剩自动化测试（`npm test` / `typecheck` / shell 契约），原 Step 6.10 顺延为 Step 6.9，并在 Step 6.6 与 Step 6.8 各留一句说明为什么不在这里手测。合并后的清单另加了两步：横屏 `.fzp-rotate-hint` 可见性（配合 ⑥）、手测收尾 `git status` 确认零源码残留 |
| ⑥ | minor | 挑战页只抄了 `.fzp-tiny-warn`，漏了训练页同层的 `.fzp-rotate-hint`，手机横屏缺「竖屏更好用」提示（CLAUDE.md 关键决策 #1 的口径） | **已修**。`ChallengePage.tsx` 在 `.fzp-train` 之后、`.fzp-tiny-warn` 之前补 `<div className="fzp-rotate-hint">{t('train.rotateHint')}</div>`（复用既有键，ZH/EN 都已存在于 `src/i18n.tsx:93/563`；`.fzp-rotate-hint` 的显隐纯由 `src/index.css:228` 的媒体查询控制，不新增 CSS）；`challenge-shell.test.ts` 的外壳类名清单同时加入 `fzp-rotate-hint` 与 `fzp-tiny-warn`；Task 5 的「复用既有键」清单补 `train.rotateHint` |

**无驳回项。** 逐条核对后认为六条都成立，其中 ① 的推演本人手算复现（b=2500 → 旧式 `clamp(3250, 1000, 2500) = 2500`，实际系数 ×1.0）。

**测试数字随之变动**（都写成「实测基线 + 新增」，基线以 Step 0.1 现跑的为准）：Task 1 由 17 → **18**（新增回归锚 1 条）；Task 6 由 11 → **12**（shell 契约 6 → 7）；全程新增合计由 68 → **70**，新增测试文件仍是 **7** 个。**任务数仍是 8 个**（Task 1-8），只是开工前多了两个必做步骤（Step 0.4 语音归属 🚦 阻塞、Step 0.5 修 spec §4.4 钳制口径）。
