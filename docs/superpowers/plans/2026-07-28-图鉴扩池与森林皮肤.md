# 图鉴扩池 + 魔法森林皮肤 实施计划（计划 A）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把怪兽图鉴从 34 只扩到 82 只，并新增第四套皮肤「魔法森林」，让闺女的收集内容重新跟上每天最多 3 只的获取速率。

**Architecture:** 先把散落在 6 处的世界字面量改成从单一的 `WORLDS` 数组派生（这样加世界只改一个地方），同时给 `MonsterImage` 加缺图回落——后者让代码可以先于美术落地。然后加 48 条 monster def、写 `ForestStage`（结构照抄 `ShrineStage`：精灵每题轮换、视标 E 印在身上）、注册进 registry。

**Tech Stack:** TypeScript + React + Vite；vitest + fake-indexeddb；纯 DOM/CSS 演出，不引游戏引擎。

**配套计划：** 闪光变体（spec §5）是独立可交付的子系统，单独写在计划 B，不依赖本计划。

## Global Constraints

- **怪兽 id 不得含冒号。** monster 是 `KEYED_KIND`，同步 uuid 形如 `monster:default:space-ufo`，`naturalKeyFromUuid` 靠「自然键侧不含冒号」反解。
- **给太空/神庙新增的怪一律不是 `common`。** `reserveMonstersOfWorld` 只收 `rarity !== 'common'`，而 6 只 BASE 写死在 Stage 里——新增 common 会造出「只在图鉴里、训练中永不出现」的怪。
- **视标祖先链上不许出现 `transform: scale`。** 缩放要折进容器的 `width/height`。祖先上挂 scale 会等比缩掉视标，等于换皮肤就偷偷改训练强度（神庙曾因此让 E 只有标定值的 80%）。
- **视标深色圆底的 padding 走 `heightPx * 0.4`，不许换成 `cqmin`。** 圆底必须与视标同比例，而不是与画布同比例。
- **中英文案必须成对。** `src/i18n.test.ts` 用 `hasKey('zh'|'en', k)` 逐键校验。
- **开工第一步先跑 `npm test` 记真实基线**（写这份计划时是 665 个 / 70 文件，但可能已变）。

---

## File Structure

**新建**
- `src/dex/dex-service.test.ts` — 锚住三个 Record 从 `WORLDS` 派生
- `src/skins/rotation.ts` — 三个皮肤共用的轮换池逻辑
- `src/skins/rotation.test.ts`
- `src/skins/forest/ForestStage.tsx` — 森林皮肤舞台
- `src/skins/forest/ForestStage.test.ts` — 池轮换与储备门控

**修改**
- `src/dex/monster-defs.ts` — `World` 加 `'forest'`；导出 `WORLDS` / `emptyByWorld` / `isWorld`；`forest()` 工厂；48 条新 def
- `src/dex/monster-defs.test.ts` — 总数、分世界、稀有度、id 约束
- `src/dex/dex-service.ts` — 三处 Record 改派生
- `src/dex/DexWall.tsx` — 用导出的 `WORLDS`，跳过空世界
- `src/dex/MonsterImage.tsx` — 缺图回落
- `src/training/TrainingPage.tsx` — state 初值改派生；三元链改 `isWorld` 判定
- `src/skins/space/SpaceStage.tsx` / `src/skins/shrine/ShrineStage.tsx` — 轮换池改为委托共用函数
- `src/skins/registry.ts` — 注册 forest + `SKIN_UNLOCK_COST.forest`
- `src/skins/registry.test.ts` — 跟改断言
- `src/i18n.tsx` / `src/i18n.test.ts` — 52 个新键 ×2 语言

**用户交付物（不在代码任务里）**
- `public/skins/forest/bg.webp`
- `public/skins/forest/<slug>.webp` ×6、`public/skins/forest/reserve/<slug>.webp` ×10
- `public/skins/space/reserve/<slug>.webp` ×16
- `public/skins/shrine/reserve/<slug>.webp` ×16

---

## Task 1: 世界派生化 + 缺图回落

不依赖美术，可独立验收。

**Files:**
- Modify: `src/dex/monster-defs.ts`（`World` 类型、新增三个导出）
- Modify: `src/dex/dex-service.ts:43-44`、`:56`
- Modify: `src/dex/DexWall.tsx:21`、渲染循环
- Modify: `src/dex/MonsterImage.tsx`（整个重写）
- Modify: `src/training/TrainingPage.tsx:63`、`:561`
- Modify: `src/dex/monster-defs.test.ts`
- Create: `src/dex/dex-service.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `type World = 'space' | 'shrine' | 'forest'`
  - `WORLDS: readonly World[]`
  - `emptyByWorld<T>(make: () => T): Record<World, T>`
  - `isWorld(id: string): id is World`

- [ ] **Step 1: 记基线**

Run: `npm test`
把「N passed / M files」记下来，后面每一步都对着它看有没有意外变红。

- [ ] **Step 2: 写失败的测试**

`src/dex/monster-defs.test.ts` 追加：

```ts
import { WORLDS, emptyByWorld, isWorld } from './monster-defs'

describe('WORLDS —— 加世界只改这一个数组', () => {
  it('三个世界，顺序固定（图鉴分组按它渲染）', () => {
    expect([...WORLDS]).toEqual(['space', 'shrine', 'forest'])
  })

  it('emptyByWorld 为每个世界建同样的初值，且键恰为 WORLDS', () => {
    const zero = emptyByWorld(() => 0)
    expect(Object.keys(zero).sort()).toEqual([...WORLDS].sort())
    expect(Object.values(zero)).toEqual(WORLDS.map(() => 0))
  })

  it('emptyByWorld 每个世界拿到独立实例，不是共享同一个引用', () => {
    // 若写成 Object.fromEntries(WORLDS.map(w => [w, []]))，三个世界会共享同一个数组，
    // 往一个世界 push 会串到另外两个
    const lists = emptyByWorld<string[]>(() => [])
    lists.space.push('x')
    expect(lists.shrine).toEqual([])
    expect(lists.forest).toEqual([])
  })

  it('isWorld 只认这三个（皮肤 id 里 plain / random 不是世界）', () => {
    expect(isWorld('space')).toBe(true)
    expect(isWorld('forest')).toBe(true)
    expect(isWorld('plain')).toBe(false)
    expect(isWorld('random')).toBe(false)
  })
})
```

新建 `src/dex/dex-service.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { db } from '../data/db'
import { WORLDS } from './monster-defs'
import { getDexProgress, getOwnedReserveIdsByWorld } from './dex-service'

beforeEach(async () => { await db.monsters.clear() })

describe('dex-service 的分世界统计从 WORLDS 派生', () => {
  it('getDexProgress 的 byWorld / byWorldTotal 键恰为 WORLDS（不是写死的两个世界）', async () => {
    const p = await getDexProgress()
    expect(Object.keys(p.byWorld).sort()).toEqual([...WORLDS].sort())
    expect(Object.keys(p.byWorldTotal).sort()).toEqual([...WORLDS].sort())
  })

  it('getOwnedReserveIdsByWorld 的键也恰为 WORLDS', async () => {
    const r = await getOwnedReserveIdsByWorld()
    expect(Object.keys(r).sort()).toEqual([...WORLDS].sort())
  })

  it('还没有 forest 怪时，forest 的计数是 0 而不是 undefined（空世界不能炸）', async () => {
    const p = await getDexProgress()
    expect(p.byWorld.forest).toBe(0)
    expect(p.byWorldTotal.forest).toBe(0)
  })
})
```

- [ ] **Step 3: 跑测试确认它们红**

Run: `npx vitest run src/dex`
Expected: FAIL —— `WORLDS` / `emptyByWorld` / `isWorld` 未导出；`byWorld.forest` 是 `undefined`。

- [ ] **Step 4: 改 `monster-defs.ts`**

把 `World` 类型改掉并在其后加三个导出：

```ts
export type World = 'space' | 'shrine' | 'forest'

/**
 * 世界清单。**加世界只改这一个数组**——下游的分世界 Record 与图鉴分组渲染全部由它派生。
 *
 * 曾经散落 6 处 `{ space: …, shrine: … }` 字面量，其中 TrainingPage 那条
 * `=== 'space' ? … : === 'shrine' ? … : []` 三元链带 `: []` 兜底，漏改不报错，
 * 只会让新皮肤永远吃不到储备怪、静默失效。
 */
export const WORLDS: readonly World[] = ['space', 'shrine', 'forest']

/**
 * 按世界建一个 Record，每个世界一个**独立**初值。
 * `make` 是工厂而不是直接传值：传值的话 `emptyByWorld([])` 会让三个世界共享同一个数组。
 */
export function emptyByWorld<T>(make: () => T): Record<World, T> {
  return Object.fromEntries(WORLDS.map((w) => [w, make()])) as Record<World, T>
}

/** 皮肤 id 是否同时是一个世界名（plain / random 不是），供按 id 取分世界数据用 */
export function isWorld(id: string): id is World {
  return (WORLDS as readonly string[]).includes(id)
}
```

- [ ] **Step 5: 改 `dex-service.ts` 的三处 Record**

import 里加 `WORLDS`（若测试需要）与 `emptyByWorld`，然后：

```ts
  const byWorld = emptyByWorld(() => 0)
  const byWorldTotal = emptyByWorld(() => 0)
```

```ts
  const byWorld = emptyByWorld<string[]>(() => [])
```

三处原本的 `Record<World, …> = { space: …, shrine: … }` 类型标注可以去掉——`emptyByWorld` 的返回类型已经是 `Record<World, T>`。

- [ ] **Step 6: 改 `DexWall.tsx`**

删掉本地的 `const WORLDS: { key: World }[] = [{ key: 'space' }, { key: 'shrine' }]`，改从 `./monster-defs` import `WORLDS`。渲染循环从 `WORLDS.map(({ key }) => …)` 改成：

```tsx
      {/* 跳过还没有怪兽的世界：先加世界类型、后补数据时不会多出一个空分组 */}
      {WORLDS.filter((key) => monstersOfWorld(key).length > 0).map((key) => {
```

（循环体内原来用的 `key` 变量名不变，其余代码不动。）

- [ ] **Step 7: 改 `TrainingPage.tsx` 两处**

import 加 `WORLDS` 不需要，加 `emptyByWorld, isWorld`（`World` 已 import）：

```ts
import type { MonsterDef, World, Rarity } from '../dex/monster-defs'
import { emptyByWorld, isWorld } from '../dex/monster-defs'
```

第 63 行：

```ts
  const [capturedByWorld, setCapturedByWorld] = useState<Record<World, string[]>>(() => emptyByWorld<string[]>(() => []))
```

第 561 行：

```tsx
          capturedReserveIds={isWorld(effectiveSkinId) ? capturedByWorld[effectiveSkinId] : []}
```

⚠️ 这一处是本任务的**真正理由**。原来的三元链要为每个新世界加一段，而它有 `: []` 兜底——漏加不报错，只会让新皮肤永远吃不到储备怪。改成按 id 索引后这条路不存在了，前提是「皮肤 id 与世界 id 同名」，那条由 Task 6 的一条测试锚住。

- [ ] **Step 8: 重写 `MonsterImage.tsx` 加缺图回落**

```tsx
import { useState } from 'react'
import type { MonsterDef } from './monster-defs'

/**
 * 图鉴/开箱里的怪兽视觉：填满父容器。
 * - 普通静态图：<img objectFit:contain>
 * - 精灵条素材（sprite）：用 background 只取第 0 帧，避免整条 8 帧胶片被拉伸成连环画。
 * 父容器负责尺寸/圆角/裁剪；filter 用于未捕获剪影（brightness(0)）。
 *
 * 缺图时回落到「渐变 + 🐾」，两个理由：
 *  ① 让 monster def 可以先于美术落地（否则新加的 def 在图鉴里全是破图图标）；
 *  ② 线上万一某张图没进 SW 预缓存，也不该给孩子看破图。
 * 回落刻意不用 '?'——那是"未捕获神秘格"的视觉，两者混淆会让人以为怪没捕到。
 * sprite 分支用 background-image，onError 不会触发，所以两条分支都挂一个隐藏的 <img> 探针。
 */
export function MonsterImage({ def, filter }: { def: MonsterDef; filter?: string }) {
  const [failed, setFailed] = useState(false)

  if (failed) {
    return (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'grid',
          placeItems: 'center',
          background: 'linear-gradient(135deg, #f1ecfb, #e7f3ff)',
          fontSize: '2em',
          filter,
        }}
      >
        🐾
      </div>
    )
  }

  const probe = <img src={def.img} alt="" onError={() => setFailed(true)} style={{ display: 'none' }} />

  if (def.sprite) {
    return (
      <>
        {probe}
        <div
          style={{
            width: '100%',
            height: '100%',
            backgroundImage: `url(${def.img})`,
            backgroundSize: `${def.sprite.frames * 100}% 100%`,
            backgroundPosition: '0 0',
            backgroundRepeat: 'no-repeat',
            imageRendering: 'pixelated',
            filter,
          }}
        />
      </>
    )
  }

  return (
    <img
      src={def.img}
      alt=""
      onError={() => setFailed(true)}
      style={{ width: '100%', height: '100%', objectFit: 'contain', filter }}
    />
  )
}
```

- [ ] **Step 9: 跑全量测试与 typecheck**

Run: `npm test`
Expected: PASS，比基线多 7 条（monster-defs 4 + dex-service 3）。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 10: 提交**

```bash
git add src/dex/monster-defs.ts src/dex/monster-defs.test.ts src/dex/dex-service.ts src/dex/dex-service.test.ts src/dex/DexWall.tsx src/dex/MonsterImage.tsx src/training/TrainingPage.tsx
git commit -m "refactor(图鉴): 世界从 WORLDS 派生，不再散落硬编码；MonsterImage 加缺图回落"
```

---

## Task 2: 轮换池逻辑抽成共用函数

三个 Stage 各有一份 `buildXPool` / `xForSeq`，逻辑逐字相同。两份还算照抄，第三份就是评审会直接判缺陷的重复——而且里面 `((seq % n) + n) % n` 那个负数兜底是有意为之的，抄第三遍迟早抄漏。先抽出来，Task 5 的森林直接调它。

**Files:**
- Create: `src/skins/rotation.ts`
- Create: `src/skins/rotation.test.ts`
- Modify: `src/skins/space/SpaceStage.tsx`（`buildEnemyPool` / `enemyForSeq` 改为委托）
- Modify: `src/skins/shrine/ShrineStage.tsx`（`buildGuardianPool` / `guardianForSeq` 改为委托）

**Interfaces:**
- Consumes: 无
- Produces:
  - `buildRotationPool<T extends { name: string }>(base: readonly T[], reserve: readonly T[], world: string, capturedReserveIds?: readonly string[]): T[]`
  - `pickForSeq<T>(pool: readonly T[], seq: number): T`

⚠️ **既有的 `src/skins/space/SpaceStage.test.ts` 与 `src/skins/shrine/ShrineStage.test.ts` 必须一条不红**——它们是这次重构没改变行为的唯一证据。若有红，是重构错了，**不要改测试去迁就**。

- [ ] **Step 1: 写失败的测试**

新建 `src/skins/rotation.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { buildRotationPool, pickForSeq } from './rotation'

type Item = { name: string }
const BASE: Item[] = [{ name: 'a' }, { name: 'b' }]
const RESERVE: Item[] = [{ name: 'x' }, { name: 'y' }]

describe('buildRotationPool', () => {
  it('无捕获时只有基础池', () => {
    expect(buildRotationPool(BASE, RESERVE, 'w').map((i) => i.name)).toEqual(['a', 'b'])
    expect(buildRotationPool(BASE, RESERVE, 'w', []).map((i) => i.name)).toEqual(['a', 'b'])
  })

  it('只有 world-name 完全匹配的 id 才把储备项拉进来', () => {
    expect(buildRotationPool(BASE, RESERVE, 'w', ['w-x']).map((i) => i.name)).toEqual(['a', 'b', 'x'])
    // 别的世界的同名 id 不算数——否则捕获太空怪会点亮神庙的轮换池
    expect(buildRotationPool(BASE, RESERVE, 'w', ['other-x']).map((i) => i.name)).toEqual(['a', 'b'])
    // 不在储备里的 id 也不算数
    expect(buildRotationPool(BASE, RESERVE, 'w', ['w-zzz']).map((i) => i.name)).toEqual(['a', 'b'])
  })

  it('储备项按 reserve 传入的顺序追加在基础池之后', () => {
    expect(buildRotationPool(BASE, RESERVE, 'w', ['w-y', 'w-x']).map((i) => i.name)).toEqual(['a', 'b', 'x', 'y'])
  })
})

describe('pickForSeq', () => {
  it('按 seq 循环取模', () => {
    const pool = [{ name: 'a' }, { name: 'b' }, { name: 'c' }]
    expect(pickForSeq(pool, 0).name).toBe('a')
    expect(pickForSeq(pool, 2).name).toBe('c')
    expect(pickForSeq(pool, 3).name).toBe('a')
  })

  it('负数 seq 不越界 —— JS 的 % 对负数返回负值，必须再加一次 n', () => {
    const pool = [{ name: 'a' }, { name: 'b' }, { name: 'c' }]
    expect(pickForSeq(pool, -1).name).toBe('c')
    expect(pickForSeq(pool, -3).name).toBe('a')
    expect(pickForSeq(pool, -4).name).toBe('c')
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/skins/rotation.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `src/skins/rotation.ts`**

```ts
/**
 * 皮肤的怪兽轮换池：三个游戏皮肤（太空 / 神庙 / 森林）共用同一套规则——
 * 基础池恒在轮换里，储备怪只有孩子在图鉴中捕获后才加入。
 *
 * 抽出来是因为这段逻辑本来在每个 Stage 里各有一份，第三份出现时就该合并了。
 * 尤其是 pickForSeq 里的负数兜底：JS 的 % 对负数返回负值，
 * 直接 pool[seq % n] 会拿到 undefined，抄第三遍迟早抄漏。
 */

/** 实际轮换池 = 基础池 + 已捕获的本世界储备怪。id 形状是 `世界-名字`。 */
export function buildRotationPool<T extends { name: string }>(
  base: readonly T[],
  reserve: readonly T[],
  world: string,
  capturedReserveIds: readonly string[] = [],
): T[] {
  const extra = reserve.filter((item) => capturedReserveIds.includes(`${world}-${item.name}`))
  return [...base, ...extra]
}

/** 第 seq 项（0-based），循环轮换；seq 为负也不越界 */
export function pickForSeq<T>(pool: readonly T[], seq: number): T {
  const n = pool.length
  return pool[((seq % n) + n) % n]
}
```

- [ ] **Step 4: 让 `SpaceStage` 委托过去**

`src/skins/space/SpaceStage.tsx` 加 import：

```ts
import { buildRotationPool, pickForSeq } from '../rotation'
```

把 `buildEnemyPool` 与 `enemyForSeq` 两个函数体换成：

```ts
/** 实际轮换池 = 基础池 + 已捕获的本世界储备怪（逻辑与神庙/森林共用，见 ../rotation） */
export function buildEnemyPool(capturedReserveIds: string[] = []): Enemy[] {
  return buildRotationPool(BASE_ENEMIES, RESERVE_ENEMIES, 'space', capturedReserveIds)
}

/** 第 seq 道视标（=已答题数）对应的敌人，循环轮换整个池。
 *  不传 capturedReserveIds 时回退基础池（向后兼容现有单测）。 */
export function enemyForSeq(seq: number, capturedReserveIds?: string[]): Enemy {
  return pickForSeq(buildEnemyPool(capturedReserveIds), seq)
}
```

- [ ] **Step 5: 让 `ShrineStage` 委托过去**

`src/skins/shrine/ShrineStage.tsx` 加同样的 import，把两个函数体换成：

```ts
/** 实际轮换池 = 基础池 + 已捕获的本世界储备怪（逻辑与太空/森林共用，见 ../rotation） */
export function buildGuardianPool(capturedReserveIds: string[] = []): Guardian[] {
  return buildRotationPool(BASE_GUARDIANS, RESERVE_GUARDIANS, 'shrine', capturedReserveIds)
}

/** 第 seq 道视标（0-based，= 已答题数）对应的守护者，循环轮换整个池。
 *  不传 capturedReserveIds 时回退基础池（向后兼容现有单测）。 */
export function guardianForSeq(seq: number, capturedReserveIds?: string[]): Guardian {
  return pickForSeq(buildGuardianPool(capturedReserveIds), seq)
}
```

- [ ] **Step 6: 跑两个既有 Stage 的测试 —— 这是重构正确性的证据**

Run: `npx vitest run src/skins`
Expected: PASS，且 `SpaceStage.test.ts` 与 `ShrineStage.test.ts` **一条不红**。若有红，是重构写错了，回去改实现，不要改测试。

- [ ] **Step 7: 跑全量与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 1 结束时多 5 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 8: 提交**

```bash
git add src/skins/rotation.ts src/skins/rotation.test.ts src/skins/space/SpaceStage.tsx src/skins/shrine/ShrineStage.tsx
git commit -m "refactor(皮肤): 轮换池逻辑抽成共用的 buildRotationPool / pickForSeq"
```

---

## Task 3: 出图（用户交付物，代码侧不阻塞）

**Files:**
- Create: `public/skins/forest/bg.webp`
- Create: `public/skins/forest/<slug>.webp` ×6、`public/skins/forest/reserve/<slug>.webp` ×10
- Create: `public/skins/space/reserve/<slug>.webp` ×16
- Create: `public/skins/shrine/reserve/<slug>.webp` ×16
- Modify: `docs/怪兽出图提示词.md`（补三段网格提示词 + 一段森林背景）

**Interfaces:**
- Consumes: Task 4 定下的 48 个 slug（见该任务的三张表，文件名必须逐字对应）
- Produces: 无代码接口

**这个任务依赖用户产出美术，代码侧不阻塞**——Task 1 的回落让缺图只显示 🐾。

- [ ] **Step 1: 出三张 4×4 怪兽网格**

沿用 `docs/怪兽出图提示词.md` 既有的怪兽提示词结构，**每格一只、透明背景、不画分隔线**，各自匹配所在世界的既有画风：

| 图 | 世界 | 画风 | 内容 |
|---|---|---|---|
| 1 | forest | 柔和插画（同卡册） | Task 4 表一的 16 个 slug |
| 2 | space | 半写实科幻游戏美术 | Task 4 表二的 16 个 slug |
| 3 | shrine | 16-bit 像素风 | Task 4 表三的 16 个 slug |

⚠️ **怪兽必须透明背景**（与卡片正好相反——卡片要满幅插画）。这些图会被抠出来叠在皮肤的动态场景上，烘焙进去的背景会带一块难看的方块。

- [ ] **Step 2: 出森林背景**

一张古树林光束的横向背景（不透明，会被 `background: cover`），柔和插画风，与 16 只森林精灵同源。

- [ ] **Step 3: 切片并转 webp**

Run（把 `<sheet>` 换成实际文件名，`<slug>` 按 Task 4 的表逐格对应）：

```bash
magick "<sheet>.png" -crop 25%x25% +repage +adjoin "cell-%d.png"
```

每格转 webp 后按 slug 命名，放进对应目录（见 File Structure）。

- [ ] **Step 4: 核对**

Run: `node -e "const fs=require('fs');for(const d of ['forest','forest/reserve','space/reserve','shrine/reserve'])console.log(d, fs.readdirSync('public/skins/'+d).filter(f=>f.endsWith('.webp')).length)"`
Expected: `forest 6` / `forest/reserve 10` / `space/reserve 27`（既有 11 + 新 16）/ `shrine/reserve 27`。

- [ ] **Step 5: 提交**

```bash
git add public/skins docs/怪兽出图提示词.md
git commit -m "feat(图鉴): 48 只新怪与森林背景的正式图"
```

---

## Task 4: 48 条 monster def

**Files:**
- Modify: `src/dex/monster-defs.ts`
- Modify: `src/dex/monster-defs.test.ts`

**Interfaces:**
- Consumes: `WORLDS` / `World`（Task 1）
- Produces: `MONSTER_DEFS` 变为 82 条；`TOTAL_MONSTERS === 82`；`monstersOfWorld('forest')` 返回 16 条

**三张 slug 表**（文件名必须与之逐字一致）：

**表一 · forest 16**

| 稀有度 | slug |
|---|---|
| common ×6（进 BASE 轮换） | `sprout` `mushroom` `firefly` `acorn` `fawn` `bluebird` |
| rare ×7 | `moss_golem` `vine_serpent` `owl_sage` `crystal_stag` `fox_spirit` `petal_fairy` `bark_treant` |
| epic ×3 | `elder_tree` `moon_deer` `forest_king` |

**表二 · space +16（全部非 common）**

| 稀有度 | slug |
|---|---|
| rare ×10 | `star_whale` `ion_moth` `drone_swarm` `meteor_hound` `plasma_ray` `void_crab` `satellite_owl` `comet_fox` `nebula_slug` `radar_bat` |
| epic ×6 | `black_hole` `star_forge` `cosmic_titan` `solar_dragon` `quantum_king` `galaxy_serpent` |

**表三 · shrine +16（全部非 common）**

| 稀有度 | slug |
|---|---|
| rare ×10 | `bone_archer` `cursed_knight` `swamp_hag` `shadow_wolf` `ember_moth` `tomb_spider` `rune_sentinel` `frost_shade` `grave_bat` `lantern_ghost` |
| epic ×6 | `bone_dragon` `shadow_king` `stone_titan` `flame_phoenix` `spirit_warden` `ancient_guardian` |

- [ ] **Step 1: 写失败的测试**

`src/dex/monster-defs.test.ts` 追加：

```ts
import { MONSTER_DEFS, TOTAL_MONSTERS, monstersOfWorld, reserveMonstersOfWorld } from './monster-defs'

describe('扩池后的图鉴规模', () => {
  it('总数 82；分世界 33 / 33 / 16', () => {
    expect(TOTAL_MONSTERS).toBe(82)
    expect(monstersOfWorld('space').length).toBe(33)
    expect(monstersOfWorld('shrine').length).toBe(33)
    expect(monstersOfWorld('forest').length).toBe(16)
  })

  it('稀有度分布：普通 18 / 稀有 43 / 史诗 21', () => {
    const n = { common: 0, rare: 0, epic: 0 }
    for (const m of MONSTER_DEFS) n[m.rarity]++
    expect(n).toEqual({ common: 18, rare: 43, epic: 21 })
  })

  it('太空与神庙的新增怪全部非普通 —— 否则会造出训练里永不出现的怪', () => {
    // 储备池筛的是 rarity !== 'common'，而 6 只 BASE 写死在 Stage 里：
    // 给这两个世界加 common，它就既不在 BASE 也不在储备，只存在于图鉴。
    expect(monstersOfWorld('space').filter((m) => m.rarity === 'common').length).toBe(6)
    expect(monstersOfWorld('shrine').filter((m) => m.rarity === 'common').length).toBe(6)
    expect(reserveMonstersOfWorld('space').length).toBe(27)
    expect(reserveMonstersOfWorld('shrine').length).toBe(27)
  })

  it('森林 6 只普通进 BASE、10 只非普通进储备', () => {
    expect(monstersOfWorld('forest').filter((m) => m.rarity === 'common').length).toBe(6)
    expect(reserveMonstersOfWorld('forest').length).toBe(10)
  })

  it('id 全局唯一', () => {
    const ids = MONSTER_DEFS.map((m) => m.id)
    expect(new Set(ids).size).toBe(ids.length)
  })

  it('id 不含冒号 —— 同步 uuid 反解靠"自然键侧不含冒号"', () => {
    for (const m of MONSTER_DEFS) expect(m.id.includes(':'), m.id).toBe(false)
  })

  it('每条 def 的 world 都在 WORLDS 里，且 id 以世界名开头', () => {
    for (const m of MONSTER_DEFS) {
      expect(WORLDS).toContain(m.world)
      expect(m.id.startsWith(`${m.world}-`), m.id).toBe(true)
    }
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/dex/monster-defs.test.ts`
Expected: FAIL —— `TOTAL_MONSTERS` 是 34，`monstersOfWorld('forest')` 是空数组。

- [ ] **Step 3: 加 `forest()` 工厂**

在既有的 `space()` / `shrine()` 工厂之后加：

```ts
function forest(slug: string, rarity: Rarity, img: string): MonsterDef {
  return { id: `forest-${slug}`, world: 'forest', rarity, img: asset(img), nameKey: `forest.spirit.${slug}` }
}
```

- [ ] **Step 4: 加森林的三组常量**

在 `SHRINE_EPIC` 之后加：

```ts
/** 森林 6 只基础精灵（common），与 ForestStage 的 BASE_SPIRITS 对齐 */
const FOREST_COMMON: MonsterDef[] = [
  forest('sprout', 'common', '/skins/forest/sprout.webp'),
  forest('mushroom', 'common', '/skins/forest/mushroom.webp'),
  forest('firefly', 'common', '/skins/forest/firefly.webp'),
  forest('acorn', 'common', '/skins/forest/acorn.webp'),
  forest('fawn', 'common', '/skins/forest/fawn.webp'),
  forest('bluebird', 'common', '/skins/forest/bluebird.webp'),
]
const FOREST_RARE: MonsterDef[] = [
  forest('moss_golem', 'rare', '/skins/forest/reserve/moss_golem.webp'),
  forest('vine_serpent', 'rare', '/skins/forest/reserve/vine_serpent.webp'),
  forest('owl_sage', 'rare', '/skins/forest/reserve/owl_sage.webp'),
  forest('crystal_stag', 'rare', '/skins/forest/reserve/crystal_stag.webp'),
  forest('fox_spirit', 'rare', '/skins/forest/reserve/fox_spirit.webp'),
  forest('petal_fairy', 'rare', '/skins/forest/reserve/petal_fairy.webp'),
  forest('bark_treant', 'rare', '/skins/forest/reserve/bark_treant.webp'),
]
const FOREST_EPIC: MonsterDef[] = [
  forest('elder_tree', 'epic', '/skins/forest/reserve/elder_tree.webp'),
  forest('moon_deer', 'epic', '/skins/forest/reserve/moon_deer.webp'),
  forest('forest_king', 'epic', '/skins/forest/reserve/forest_king.webp'),
]
```

- [ ] **Step 5: 给太空与神庙各追加 16 只**

在既有的 `SPACE_RARE` 数组**末尾**追加 10 条、`SPACE_EPIC` 末尾追加 6 条：

```ts
  // ↓ 2026-07 扩池新增 10 只（全部非 common，才能进储备池 → 捕获后进训练轮换）
  space('star_whale', 'rare', '/skins/space/reserve/star_whale.webp'),
  space('ion_moth', 'rare', '/skins/space/reserve/ion_moth.webp'),
  space('drone_swarm', 'rare', '/skins/space/reserve/drone_swarm.webp'),
  space('meteor_hound', 'rare', '/skins/space/reserve/meteor_hound.webp'),
  space('plasma_ray', 'rare', '/skins/space/reserve/plasma_ray.webp'),
  space('void_crab', 'rare', '/skins/space/reserve/void_crab.webp'),
  space('satellite_owl', 'rare', '/skins/space/reserve/satellite_owl.webp'),
  space('comet_fox', 'rare', '/skins/space/reserve/comet_fox.webp'),
  space('nebula_slug', 'rare', '/skins/space/reserve/nebula_slug.webp'),
  space('radar_bat', 'rare', '/skins/space/reserve/radar_bat.webp'),
```

```ts
  // ↓ 2026-07 扩池新增 6 只
  space('black_hole', 'epic', '/skins/space/reserve/black_hole.webp'),
  space('star_forge', 'epic', '/skins/space/reserve/star_forge.webp'),
  space('cosmic_titan', 'epic', '/skins/space/reserve/cosmic_titan.webp'),
  space('solar_dragon', 'epic', '/skins/space/reserve/solar_dragon.webp'),
  space('quantum_king', 'epic', '/skins/space/reserve/quantum_king.webp'),
  space('galaxy_serpent', 'epic', '/skins/space/reserve/galaxy_serpent.webp'),
```

`SHRINE_RARE` 末尾追加 10 条：

```ts
  // ↓ 2026-07 扩池新增 10 只
  shrine('bone_archer', 'rare', '/skins/shrine/reserve/bone_archer.webp'),
  shrine('cursed_knight', 'rare', '/skins/shrine/reserve/cursed_knight.webp'),
  shrine('swamp_hag', 'rare', '/skins/shrine/reserve/swamp_hag.webp'),
  shrine('shadow_wolf', 'rare', '/skins/shrine/reserve/shadow_wolf.webp'),
  shrine('ember_moth', 'rare', '/skins/shrine/reserve/ember_moth.webp'),
  shrine('tomb_spider', 'rare', '/skins/shrine/reserve/tomb_spider.webp'),
  shrine('rune_sentinel', 'rare', '/skins/shrine/reserve/rune_sentinel.webp'),
  shrine('frost_shade', 'rare', '/skins/shrine/reserve/frost_shade.webp'),
  shrine('grave_bat', 'rare', '/skins/shrine/reserve/grave_bat.webp'),
  shrine('lantern_ghost', 'rare', '/skins/shrine/reserve/lantern_ghost.webp'),
```

`SHRINE_EPIC` 末尾追加 6 条：

```ts
  // ↓ 2026-07 扩池新增 6 只
  shrine('bone_dragon', 'epic', '/skins/shrine/reserve/bone_dragon.webp'),
  shrine('shadow_king', 'epic', '/skins/shrine/reserve/shadow_king.webp'),
  shrine('stone_titan', 'epic', '/skins/shrine/reserve/stone_titan.webp'),
  shrine('flame_phoenix', 'epic', '/skins/shrine/reserve/flame_phoenix.webp'),
  shrine('spirit_warden', 'epic', '/skins/shrine/reserve/spirit_warden.webp'),
  shrine('ancient_guardian', 'epic', '/skins/shrine/reserve/ancient_guardian.webp'),
```

- [ ] **Step 6: 把森林并进 `MONSTER_DEFS`**

```ts
export const MONSTER_DEFS: MonsterDef[] = [
  ...SPACE_COMMON, ...SPACE_RARE, ...SPACE_EPIC,
  ...SHRINE_COMMON, ...SHRINE_RARE, ...SHRINE_EPIC,
  ...FOREST_COMMON, ...FOREST_RARE, ...FOREST_EPIC,
]
```

- [ ] **Step 7: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 2 结束时多 7 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 8: 提交**

```bash
git add src/dex/monster-defs.ts src/dex/monster-defs.test.ts
git commit -m "feat(图鉴): 扩池 34 → 82 只（森林 16 + 太空 16 + 神庙 16）"
```

---

## Task 5: `ForestStage`

**Files:**
- Create: `src/skins/forest/ForestStage.tsx`
- Create: `src/skins/forest/ForestStage.test.ts`

**Interfaces:**
- Consumes: `reserveMonstersOfWorld('forest')`（Task 4）、`buildRotationPool` / `pickForSeq`（Task 2）、`StageProps`（`src/skins/types.ts`）、`TumblingE`、`asset`、`useT`
- Produces:
  - `ForestStage: FC<StageProps>`
  - `buildSpiritPool(capturedReserveIds?: string[]): Spirit[]`
  - `spiritForSeq(seq: number, capturedReserveIds?: string[]): Spirit`
  - `type Spirit = { kind: 'img'; src: string; name: string }`

- [ ] **Step 1: 写失败的测试**

新建 `src/skins/forest/ForestStage.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { buildSpiritPool, spiritForSeq } from './ForestStage'

describe('spiritForSeq — 精灵每题轮换', () => {
  it('第 0 题是 sprout（基础池首位）', () => {
    expect(spiritForSeq(0).name).toBe('sprout')
  })

  it('逐题轮换到不同精灵', () => {
    expect(spiritForSeq(1).name).not.toBe(spiritForSeq(0).name)
  })

  it('转满一圈（基础池长 6）回到起点', () => {
    expect(spiritForSeq(6).name).toBe(spiritForSeq(0).name)
  })

  it('负数 / 异常 seq 兜底不越界', () => {
    expect(spiritForSeq(-1).name).toBeTruthy()
    expect(spiritForSeq(-7).name).toBeTruthy()
  })
})

describe('皮肤池联动 — 已捕获的储备精灵加入轮换', () => {
  it('无捕获时只有基础 6 只', () => {
    expect(buildSpiritPool().length).toBe(6)
    expect(buildSpiritPool([]).length).toBe(6)
  })

  it('未捕获对应 id 时，传入的无关 id 不生效', () => {
    expect(buildSpiritPool(['space-ufo', 'shrine-golem']).length).toBe(6)
  })

  it('捕获 forest-elder_tree 后池长 7，第 6 题变成它', () => {
    const pool = buildSpiritPool(['forest-elder_tree'])
    expect(pool.length).toBe(7)
    expect(spiritForSeq(6, ['forest-elder_tree']).name).toBe('elder_tree')
  })

  it('捕获多只后顺序 = 基础 + 已捕获储备（储备按 id 排序）', () => {
    const pool = buildSpiritPool(['forest-moon_deer', 'forest-elder_tree'])
    expect(pool.length).toBe(8)
    // reserveMonstersOfWorld 按 id.localeCompare 排序：elder_tree < moon_deer
    expect(pool.slice(6).map((s) => s.name)).toEqual(['elder_tree', 'moon_deer'])
  })

  it('储备精灵都是 img 类型（森林没有 sprite 素材）', () => {
    for (const s of buildSpiritPool(['forest-elder_tree'])) expect(s.kind).toBe('img')
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/skins/forest`
Expected: FAIL —— 模块不存在。

- [ ] **Step 3: 实现 `ForestStage.tsx`**

```tsx
import { useEffect, useState } from 'react'
import { TumblingE } from '../../training/TumblingE'
import type { StageProps } from '../types'
import { asset } from '../../data/asset'
import { useT } from '../../i18n'
import { reserveMonstersOfWorld } from '../../dex/monster-defs'
import { buildRotationPool, pickForSeq } from '../rotation'

/**
 * 魔法森林皮肤（柔和插画风，与积分卡册同源）：古树林里的森林精灵承载视标 E。
 * 念对 → 光点爆散 + 花瓣飞舞，叶片 +1（10 片点亮一棵古树）；答错 → 精灵抖动 + 暗闪。
 *
 * 结构照 ShrineStage：精灵每题轮换、E 印在深色圆底上。刻意**不做**下方的动画角色——
 * 逐帧精灵图 AI 生成不了、要找 CC0 素材，而它对训练体验贡献很小。
 */
/** 精灵池：每题轮换一只承载视标 E，全部为静态图。
 *  name 是翻译 key 的 slug（对应 i18n 的 forest.spirit.<name>），非展示文本。 */
type Spirit = { kind: 'img'; src: string; name: string }

/** 基础池（6 只 common，与图鉴 monster-defs 的 FOREST_COMMON 对齐） */
const BASE_SPIRITS: Spirit[] = [
  { kind: 'img', src: asset('/skins/forest/sprout.webp'), name: 'sprout' },
  { kind: 'img', src: asset('/skins/forest/mushroom.webp'), name: 'mushroom' },
  { kind: 'img', src: asset('/skins/forest/firefly.webp'), name: 'firefly' },
  { kind: 'img', src: asset('/skins/forest/acorn.webp'), name: 'acorn' },
  { kind: 'img', src: asset('/skins/forest/fawn.webp'), name: 'fawn' },
  { kind: 'img', src: asset('/skins/forest/bluebird.webp'), name: 'bluebird' },
]

/** 储备精灵池：由图鉴定义派生（rare+epic 共 10 只），按 id 排序保持稳定；
 *  仅当孩子已捕获对应怪兽时，对应项才进入实际轮换池。 */
const RESERVE_SPIRITS: Spirit[] = reserveMonstersOfWorld('forest').map((m) => ({
  kind: 'img' as const,
  src: m.img,
  name: m.id.replace('forest-', ''),
}))

/** 实际轮换池 = 基础池 + 已捕获的本世界储备精灵（逻辑与太空/神庙共用，见 ../rotation） */
export function buildSpiritPool(capturedReserveIds: string[] = []): Spirit[] {
  return buildRotationPool(BASE_SPIRITS, RESERVE_SPIRITS, 'forest', capturedReserveIds)
}

/** 第 seq 道视标（0-based，= 已答题数）对应的精灵，循环轮换整个池。 */
export function spiritForSeq(seq: number, capturedReserveIds?: string[]): Spirit {
  return pickForSeq(buildSpiritPool(capturedReserveIds), seq)
}

export function ForestStage({ target, heightPx, phase, lastAnswer, isEgg, capturedReserveIds }: StageProps) {
  const t = useT()
  const [fx, setFx] = useState<{ correct: boolean; key: number } | null>(null)
  const [leaves, setLeaves] = useState(0)
  const [trees, setTrees] = useState(1)

  useEffect(() => {
    if (!lastAnswer) return
    setFx({ correct: lastAnswer.correct, key: lastAnswer.seq })
    if (lastAnswer.correct) {
      setLeaves((n) => {
        if (n + 1 >= 10) { setTrees((s) => s + 1); return 0 }
        return n + 1
      })
    }
    const timer = window.setTimeout(() => setFx(null), 800)
    return () => window.clearTimeout(timer)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [lastAnswer?.seq])

  const spirit = spiritForSeq(lastAnswer?.seq ?? 0, capturedReserveIds)
  const transitioning = phase === 'transitioning'
  const hit = fx?.correct === true
  const miss = fx?.correct === false

  return (
    <div
      className="fzp-skin-canvas"
      style={{ background: `url(${asset('/skins/forest/bg.webp')}) center / cover, #16281c` }}
    >
      {/* 林间柔光 */}
      <div style={{ position: 'absolute', inset: 0, background: 'radial-gradient(circle at 50% 30%, rgba(214,255,180,0.18), rgba(10,26,16,0.45) 78%)' }} />

      {/* 计数 */}
      <div style={{ position: 'absolute', top: '2.4cqmin', left: '2.9cqmin', fontSize: 'max(9px, 2.9cqmin)', background: 'rgba(0,0,0,0.42)', border: '1px solid #4e8a55', borderRadius: 99, padding: '3px 12px', color: '#dcffd0', zIndex: 5 }}>
        {t('forest.counter', { leaves, trees })}
      </div>

      {/* 答错暗闪 */}
      {miss && <div style={{ position: 'absolute', inset: 0, background: 'rgba(20,10,0,0.22)', animation: 'fzpFade 0.4s ease-out', zIndex: 4 }} />}

      {/* 精灵（每题轮换）+ 视标 E。
          ⚠️ 祖先链上不许出现 transform: scale —— 那会等比缩掉视标，等于换皮肤就偷偷
          改训练强度（神庙曾因 scale(0.8) 让 E 只有标定值的 80%）。要缩就折进 width/height。 */}
      {phase === 'showing' && target && !hit && (
        <div
          style={{
            position: 'absolute',
            top: '36%',
            left: '50%',
            width: '37cqmin',
            height: '43cqmin',
            transform: 'translate(-50%,-50%)',
            animation: miss ? 'fzpShakeG 0.35s' : 'fzpFloat 2.6s ease-in-out infinite',
            filter: isEgg ? 'drop-shadow(0 0 14px gold)' : 'drop-shadow(0 0 10px rgba(150,255,150,0.45))',
          }}
        >
          <img src={spirit.src} alt="" style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', objectFit: 'contain' }} />
          {/* 精灵名牌 */}
          <div style={{ position: 'absolute', top: '-6.7cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(9px, 2.6cqmin)', letterSpacing: 1, color: '#eaffd6', whiteSpace: 'nowrap', textShadow: '0 0 4px #000' }}>{t(`forest.spirit.${spirit.name}`)}</div>
          {/* 视标 E：深色圆底保证对比（铁律：看清优先）。
              ⚠️ padding 跟着 heightPx 走，绝不能换成 cqmin —— 圆底必须与视标同比例。 */}
          <div style={{ position: 'absolute', top: '52%', left: '50%', transform: 'translate(-50%,-50%)', background: 'rgba(0,0,0,0.6)', borderRadius: '50%', padding: Math.max(6, heightPx * 0.4), display: 'flex' }}>
            <span style={{ color: '#ffffff' }}>
              <TumblingE direction={target} heightPx={heightPx} />
            </span>
          </div>
          {isEgg && <div style={{ position: 'absolute', top: '-2.9cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(14px, 4.8cqmin)' }}>✨</div>}
        </div>
      )}

      {/* 翻拍：藤蔓重织 */}
      {transitioning && !hit && (
        <div style={{ position: 'absolute', top: '36%', left: '50%', transform: 'translate(-50%,-50%)', color: '#bff5a8', fontSize: 'max(12px, 3.8cqmin)', textShadow: '0 0 8px #052', zIndex: 3 }}>
          {t('forest.reweaving')}
        </div>
      )}

      {/* 答对：光点爆散 + 花瓣 + 萤火虫上升 */}
      {hit && (
        <div key={`b${fx!.key}`} style={{ position: 'absolute', top: '36%', left: '50%', width: '45cqmin', height: '45cqmin', transform: 'translate(-50%,-50%)', borderRadius: '50%', background: 'radial-gradient(circle, rgba(226,255,196,0.95), transparent 62%)', animation: 'fzpFade 0.45s 0.1s ease-out forwards', opacity: 0, zIndex: 2 }} />
      )}
      {hit && (
        <div key={`p${fx!.key}`} style={{ position: 'absolute', top: '34%', left: '44%', fontSize: 'max(14px, 5.2cqmin)', animation: 'fzpPetal 0.9s 0.1s ease-out forwards', opacity: 0, zIndex: 4 }}>🌸</div>
      )}
      {hit && (
        <div key={`f${fx!.key}`} style={{ position: 'absolute', top: '36%', left: '50%', transform: 'translate(-50%,-50%)', fontSize: 'max(16px, 6.7cqmin)', animation: 'fzpSpirit 0.9s 0.2s ease-out forwards', opacity: 0, zIndex: 4 }}>✨</div>
      )}

      <style>{`
        @keyframes fzpPetal { 0% { opacity: 0 } 25% { opacity: 1 } 100% { opacity: 0; transform: translate(-160%,-120%) rotate(-70deg) } }
      `}</style>
    </div>
  )
}
```

⚠️ `fzpFloat` / `fzpShakeG` / `fzpFade` / `fzpSpirit` 已由 `ShrineStage` 注入到全局（同名 `@keyframes` 定义在它的 `<style>` 里）。**但森林可能在神庙没挂载时单独使用**，所以下一步要确认它们在森林页也生效。

- [ ] **Step 4: 确认共用 keyframes 在森林单独使用时也在**

Run: `grep -n "fzpFloat\|fzpShakeG\|fzpFade\|fzpSpirit" src/index.css`

若 `src/index.css` 里没有这四个 keyframes（即它们只定义在 `ShrineStage` 的内联 `<style>` 里），把它们**复制一份**进 `ForestStage` 的 `<style>` 块（与 `fzpPetal` 并列）。同名 keyframes 重复定义在 CSS 里是合法的，后定义的覆盖前一个、内容相同则无副作用。

Expected：要么 grep 命中 index.css（无需处理），要么把这四段补进 ForestStage：

```
        @keyframes fzpFloat { 0%,100% { transform: translate(-50%,-50%) } 50% { transform: translate(-50%,-56%) } }
        @keyframes fzpShakeG { 25% { transform: translate(-56%,-50%) } 75% { transform: translate(-44%,-50%) } }
        @keyframes fzpFade { from { opacity: 1 } to { opacity: 0 } }
        @keyframes fzpSpirit { 0% { opacity: 0 } 25% { opacity: 1 } 100% { opacity: 0; transform: translate(-50%,-190%) scale(1.3) } }
```

- [ ] **Step 5: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 4 结束时多 10 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 6: 提交**

```bash
git add src/skins/forest
git commit -m "feat(皮肤): 魔法森林 Stage（精灵轮换 + 叶片计数，无动画角色）"
```

---

## Task 6: 注册、文案与接线

**Files:**
- Modify: `src/skins/registry.ts`
- Modify: `src/skins/registry.test.ts`
- Modify: `src/i18n.tsx`、`src/i18n.test.ts`

**Interfaces:**
- Consumes: `ForestStage`（Task 5）、`WORLDS` / `isWorld`（Task 1）
- Produces: `SKINS` 变为 4 条；`SKIN_UNLOCK_COST.forest === 4000`

- [ ] **Step 1: 写失败的测试**

改 `src/skins/registry.test.ts` 的第一条与解锁那组，并加一条新不变量：

```ts
import { SKINS, getSkin, skinUnlockCost, isSkinUnlocked, newlyUnlockedSkins, pickRandomSkin } from './registry'
import { WORLDS } from '../dex/monster-defs'
```

```ts
  it('has plain, space, shrine and forest', () => {
    expect(SKINS.map((s) => s.id)).toEqual(['plain', 'space', 'shrine', 'forest'])
  })
```

```ts
  it('森林 4000 分档，边界严格 >=', () => {
    expect(skinUnlockCost('forest')).toBe(4000)
    expect(isSkinUnlocked('forest', 3999)).toBe(false)
    expect(isSkinUnlocked('forest', 4000)).toBe(true)
    // 分档：到 2500 只解锁到神庙，森林仍锁
    expect(isSkinUnlocked('forest', 2500)).toBe(false)
  })
```

新增一个 describe：

```ts
describe('皮肤 id 与世界 id 必须同名', () => {
  it('每个非 plain 的皮肤都对应一个世界', () => {
    // TrainingPage 用 capturedByWorld[skinId] 取该皮肤的储备怪，这条是它成立的前提。
    // 从前那里是 === 'space' ? … : === 'shrine' ? … : [] 的三元链，带 : [] 兜底，
    // 加了皮肤却忘了加分支不会报错，只会让新皮肤永远吃不到储备怪、静默失效。
    for (const s of SKINS) {
      if (s.id === 'plain') continue
      expect(WORLDS, `皮肤 ${s.id} 没有同名世界`).toContain(s.id)
    }
  })
})
```

`src/i18n.test.ts` 加常量与断言：

```ts
/** 魔法森林皮肤 + 三个世界的新怪名 */
const FOREST_UI_KEYS = ['skin.forest', 'dex.world.forest', 'forest.counter', 'forest.reweaving']

const NEW_MONSTER_KEYS = [
  ...['sprout', 'mushroom', 'firefly', 'acorn', 'fawn', 'bluebird',
      'moss_golem', 'vine_serpent', 'owl_sage', 'crystal_stag', 'fox_spirit', 'petal_fairy', 'bark_treant',
      'elder_tree', 'moon_deer', 'forest_king'].map((s) => `forest.spirit.${s}`),
  ...['star_whale', 'ion_moth', 'drone_swarm', 'meteor_hound', 'plasma_ray', 'void_crab',
      'satellite_owl', 'comet_fox', 'nebula_slug', 'radar_bat',
      'black_hole', 'star_forge', 'cosmic_titan', 'solar_dragon', 'quantum_king', 'galaxy_serpent']
      .map((s) => `space.enemy.${s}`),
  ...['bone_archer', 'cursed_knight', 'swamp_hag', 'shadow_wolf', 'ember_moth', 'tomb_spider',
      'rune_sentinel', 'frost_shade', 'grave_bat', 'lantern_ghost',
      'bone_dragon', 'shadow_king', 'stone_titan', 'flame_phoenix', 'spirit_warden', 'ancient_guardian']
      .map((s) => `shrine.guardian.${s}`),
]
```

```ts
  it('森林皮肤 4 个界面键在 zh 与 en 都存在', () => {
    expect(FOREST_UI_KEYS.length).toBe(4)
    for (const k of FOREST_UI_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })

  it('48 个新怪名在 zh 与 en 都齐全（缺了只会显示成 key 本身）', () => {
    expect(NEW_MONSTER_KEYS.length).toBe(48)
    for (const k of NEW_MONSTER_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```

- [ ] **Step 2: 跑测试确认它们红**

Run: `npx vitest run src/skins/registry.test.ts src/i18n.test.ts`
Expected: FAIL —— `SKINS` 只有 3 条；`skinUnlockCost('forest')` 是 0；52 个键都不存在。

- [ ] **Step 3: 注册森林皮肤**

`src/skins/registry.ts`：

```ts
import { ForestStage } from './forest/ForestStage'
```

```ts
export const SKINS: Skin[] = [
  { id: 'plain', name: '朴素', Stage: PlainStage },
  { id: 'space', name: '太空射击', Stage: SpaceStage },
  { id: 'shrine', name: '神庙勇者', Stage: ShrineStage },
  { id: 'forest', name: '魔法森林', Stage: ForestStage },
]
```

```ts
  forest: 4000,  // ≈ 练三周解锁（最新的一套）
```

⚠️ `Skin.name` **不是界面显示来源**（UI 一律走 `t('skin.' + s.id)`，见 `SettingsPage.tsx:190`、`HomePage.tsx:113`、`TrainingPage.tsx:432`），但它**被 `registry.test.ts:9` 的 `getSkin('space').name` 断言用着**，所以照既有格式填中文即可，不要留空。

- [ ] **Step 4: 加 i18n（zh）**

`src/i18n.tsx` 的 zh 字典，在 `'skin.shrine'` 之后加：

```ts
  'skin.forest': '魔法森林',
```

在 `'dex.world.shrine'` 之后加：

```ts
  'dex.world.forest': '🌲 森林',
```

在 shrine 那批怪名之后加森林的界面文案与 16 个精灵名：

```ts
  'forest.counter': '🍃 {leaves}/10 · 古树 {trees}',
  'forest.reweaving': '藤蔓重织…',

  'forest.spirit.sprout': '小芽苗', 'forest.spirit.mushroom': '蘑菇精',
  'forest.spirit.firefly': '萤火虫', 'forest.spirit.acorn': '橡果宝宝',
  'forest.spirit.fawn': '小鹿', 'forest.spirit.bluebird': '青羽鸟',
  'forest.spirit.moss_golem': '苔藓石人', 'forest.spirit.vine_serpent': '藤蔓蛇',
  'forest.spirit.owl_sage': '智慧猫头鹰', 'forest.spirit.crystal_stag': '水晶鹿',
  'forest.spirit.fox_spirit': '狐灵', 'forest.spirit.petal_fairy': '花瓣仙子',
  'forest.spirit.bark_treant': '树皮巨人',
  'forest.spirit.elder_tree': '远古树灵', 'forest.spirit.moon_deer': '月光神鹿',
  'forest.spirit.forest_king': '森林之王',
```

太空 16 个新怪名（加在既有 `space.enemy.*` 之后）：

```ts
  'space.enemy.star_whale': '星鲸', 'space.enemy.ion_moth': '离子蛾',
  'space.enemy.drone_swarm': '无人机群', 'space.enemy.meteor_hound': '陨石猎犬',
  'space.enemy.plasma_ray': '等离子鳐', 'space.enemy.void_crab': '虚空蟹',
  'space.enemy.satellite_owl': '卫星枭', 'space.enemy.comet_fox': '彗星狐',
  'space.enemy.nebula_slug': '星云蛞蝓', 'space.enemy.radar_bat': '雷达蝠',
  'space.enemy.black_hole': '黑洞兽', 'space.enemy.star_forge': '恒星熔炉',
  'space.enemy.cosmic_titan': '宇宙泰坦', 'space.enemy.solar_dragon': '烈日龙',
  'space.enemy.quantum_king': '量子之王', 'space.enemy.galaxy_serpent': '银河巨蛇',
```

神庙 16 个新怪名（加在既有 `shrine.guardian.*` 之后）：

```ts
  'shrine.guardian.bone_archer': '白骨弓手', 'shrine.guardian.cursed_knight': '诅咒骑士',
  'shrine.guardian.swamp_hag': '沼泽巫婆', 'shrine.guardian.shadow_wolf': '暗影狼',
  'shrine.guardian.ember_moth': '余烬蛾', 'shrine.guardian.tomb_spider': '古墓蛛',
  'shrine.guardian.rune_sentinel': '符文哨兵', 'shrine.guardian.frost_shade': '霜影',
  'shrine.guardian.grave_bat': '墓地蝠', 'shrine.guardian.lantern_ghost': '提灯幽灵',
  'shrine.guardian.bone_dragon': '骨龙', 'shrine.guardian.shadow_king': '暗影之王',
  'shrine.guardian.stone_titan': '巨石泰坦', 'shrine.guardian.flame_phoenix': '烈焰凤凰',
  'shrine.guardian.spirit_warden': '守魂者', 'shrine.guardian.ancient_guardian': '远古守卫',
```

- [ ] **Step 5: 加 i18n（en）**

en 字典对应位置加：

```ts
  'skin.forest': 'Magic Forest',
```
```ts
  'dex.world.forest': '🌲 Forest',
```
```ts
  'forest.counter': '🍃 {leaves}/10 · Trees {trees}',
  'forest.reweaving': 'Vines reweaving…',

  'forest.spirit.sprout': 'Sprout', 'forest.spirit.mushroom': 'Mushroom Imp',
  'forest.spirit.firefly': 'Firefly', 'forest.spirit.acorn': 'Acorn Baby',
  'forest.spirit.fawn': 'Fawn', 'forest.spirit.bluebird': 'Bluebird',
  'forest.spirit.moss_golem': 'Moss Golem', 'forest.spirit.vine_serpent': 'Vine Serpent',
  'forest.spirit.owl_sage': 'Owl Sage', 'forest.spirit.crystal_stag': 'Crystal Stag',
  'forest.spirit.fox_spirit': 'Fox Spirit', 'forest.spirit.petal_fairy': 'Petal Fairy',
  'forest.spirit.bark_treant': 'Bark Treant',
  'forest.spirit.elder_tree': 'Elder Treant', 'forest.spirit.moon_deer': 'Moonlight Deer',
  'forest.spirit.forest_king': 'Forest King',

  'space.enemy.star_whale': 'Star Whale', 'space.enemy.ion_moth': 'Ion Moth',
  'space.enemy.drone_swarm': 'Drone Swarm', 'space.enemy.meteor_hound': 'Meteor Hound',
  'space.enemy.plasma_ray': 'Plasma Ray', 'space.enemy.void_crab': 'Void Crab',
  'space.enemy.satellite_owl': 'Satellite Owl', 'space.enemy.comet_fox': 'Comet Fox',
  'space.enemy.nebula_slug': 'Nebula Slug', 'space.enemy.radar_bat': 'Radar Bat',
  'space.enemy.black_hole': 'Black Hole', 'space.enemy.star_forge': 'Star Forge',
  'space.enemy.cosmic_titan': 'Cosmic Titan', 'space.enemy.solar_dragon': 'Solar Dragon',
  'space.enemy.quantum_king': 'Quantum King', 'space.enemy.galaxy_serpent': 'Galaxy Serpent',

  'shrine.guardian.bone_archer': 'Bone Archer', 'shrine.guardian.cursed_knight': 'Cursed Knight',
  'shrine.guardian.swamp_hag': 'Swamp Hag', 'shrine.guardian.shadow_wolf': 'Shadow Wolf',
  'shrine.guardian.ember_moth': 'Ember Moth', 'shrine.guardian.tomb_spider': 'Tomb Spider',
  'shrine.guardian.rune_sentinel': 'Rune Sentinel', 'shrine.guardian.frost_shade': 'Frost Shade',
  'shrine.guardian.grave_bat': 'Grave Bat', 'shrine.guardian.lantern_ghost': 'Lantern Ghost',
  'shrine.guardian.bone_dragon': 'Bone Dragon', 'shrine.guardian.shadow_king': 'Shadow King',
  'shrine.guardian.stone_titan': 'Stone Titan', 'shrine.guardian.flame_phoenix': 'Flame Phoenix',
  'shrine.guardian.spirit_warden': 'Spirit Warden', 'shrine.guardian.ancient_guardian': 'Ancient Guardian',
```

- [ ] **Step 6: 跑全量测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 5 结束时多 **4** 条：registry 新增「森林 4000 分档」与「皮肤 id 必须有同名世界」两条、i18n 新增两条。（`has plain, space, shrine and forest` 那条是**改**既有断言，不计入新增。）

Run: `npm run typecheck`
Expected: 无输出。

Run: `npm run build`
Expected: 成功。

- [ ] **Step 7: 浏览器里真验一遍**

Run: `npm run dev`，然后：

1. 设置页能看到第四个皮肤「魔法森林」；累计分不足 4000 时显示 🔒 与「再练 N 分」
2. 浏览器控制台给自己加够累计分：
   ```js
   const db = (await import('/src/data/db.ts')).db
   await db.checkins.put({ date: '2026-07-28', streak: 5, dailyPoints: 300, totalPoints: 9999 })
   ```
   刷新后森林可选，准备页预览框能渲染森林 Stage
3. 选森林开始训练：精灵每题轮换、名牌显示中文名、视标 E 清晰印在深色圆底上、答对有光点与花瓣、答错有抖动、翻拍显示「藤蔓重织…」
4. **量一下视标没有被缩放**（这是医学铁律）：
   ```js
   const e = document.querySelector('.fzp-stage svg, .fzp-stage [data-tumbling-e]')
   console.log(e && e.getBoundingClientRect())
   ```
   高度应等于设置里的毫米数 × 标定 px/mm，与朴素皮肤下的同一读数一致
5. 图鉴页出现第三个分组「🌲 森林」，共 16 格；总进度显示 `N/82`
6. DevTools Console 无 error

- [ ] **Step 8: 提交**

```bash
git add src/skins/registry.ts src/skins/registry.test.ts src/i18n.tsx src/i18n.test.ts
git commit -m "feat(皮肤): 注册魔法森林（4000 分解锁）+ 52 条中英文案"
```

---

## 收尾检查

- [ ] `npm test` 全绿，总数 = 基线 + **33**（Task 1 加 7、Task 2 加 5、Task 4 加 7、Task 5 加 10、Task 6 加 4）
- [ ] `npm run typecheck` 与 `npm run typecheck:functions` 都无输出
- [ ] `npm run build` 成功
- [ ] 图鉴三个世界分组齐全、总数 82；森林皮肤可选可玩；视标尺寸与朴素皮肤一致
- [ ] 按 `CLAUDE.md` 的惯例把本迭代写进「当前状态」，含「加世界只改 `WORLDS`」与「太空神庙不许加 common」两条口径
- [ ] 计划 B（闪光变体）另行执行
