# 迭代 1a 核心训练循环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在迭代 0 之上做出能完整玩一次的单眼训练闭环（左眼 3 分钟 + 右眼 3 分钟）：Tumbling E 视标按物理尺寸显示 → vosk 语音/触控答题 → 音效 → 翻拍过渡 → 计时换眼 → 单节结算。

**Architecture:** 纯逻辑（视标尺寸换算、CPM 推算、训练节状态机、方向→旋转）抽成 `src/training/` 下的独立可测模块，用 Vitest TDD；视标组件、音效、训练界面把这些逻辑与迭代 0 的 vosk/标定/答案映射组装成 `TrainingPage`，手动验证。1a 不落数据库，单节结果内存态。

**Tech Stack:** TypeScript · React 18 · Vite 6 · Vitest 2 · Web Audio API（音效合成）· 复用迭代 0 的 vosk-browser / calibration / answer-mapping

## Global Constraints

- 平台：iPad Web 单平台。本机开发 `npm run dev`（http localhost）；iPad `npm run dev:lan`（https 局域网）。
- 视标：**Tumbling E**，语音答"上/下/左/右"；触控四方向按钮**始终显示**（兜底）。
- 训练距离 **400mm**；单眼默认 **180 秒**（3 分钟）。
- 视力级别默认 **0.8**（1a 提供最简选择）。
- 视标高度换算（verbatim）：`h_mm = 2 × D × tan( ((5/acuity) 弧分转弧度) / 2 )`，`D=400`；`px = h_mm × cssPxPerMm`；笔画 = `h/5`。
- 标定值来自迭代 0：localStorage key `fzp.cssPxPerMm`。
- CPM 推算（verbatim）：`cpm = (flips / 2) / (elapsedSec / 60)`；**训练中不显示，仅单节结算给平均值**。
- 顶部精简：只显眼别 + 安静时间进度条（**不显秒数、不显 CPM/正确率**）。
- 答错**照常继续**，记入正确率；视标**随机方向、不连续重复**。
- **1a 不落数据库**，单节结果内存态。
- 纯逻辑 TDD；界面/语音/音效手动验证。
- 频繁提交；commit message 末尾附 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

---

## 文件结构

```
src/training/
  optotype-size.ts / .test.ts   # Task 1  视力级别+距离+标定 → 视标 mm/px（纯函数）
  cpm.ts / .test.ts             # Task 2  推算式 CPM（纯函数）
  session.ts / .test.ts         # Task 3  训练节状态机纯逻辑
  TumblingE.tsx / TumblingE.test.ts  # Task 4  方向→旋转纯函数 + 视标 SVG 组件
  sfx.ts                        # Task 5  Web Audio 音效
  TrainingPage.tsx              # Task 6  训练界面（组装一切）
src/App.tsx  (modify)           # Task 7  加训练入口
```
复用（勿改）：`src/calibration/calibration-math.ts`、`src/speech/answer-mapping.ts`、`src/speech/vosk.ts`。

---

### Task 1: 视标尺寸换算（optotype-size，纯函数 TDD）

**Files:**
- Create: `src/training/optotype-size.ts`
- Test: `src/training/optotype-size.test.ts`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `TRAIN_DISTANCE_MM = 400`
  - `optotypeHeightMm(acuity: number, distanceMm?: number): number` — 视标整字高度(mm)，`acuity<=0` 抛错。
  - `optotypeHeightPx(acuity: number, pxPerMm: number, distanceMm?: number): number`
  - `strokeWidthPx(heightPx: number): number` — = heightPx/5。

- [ ] **Step 1: 写失败测试 `src/training/optotype-size.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import {
  TRAIN_DISTANCE_MM,
  optotypeHeightMm,
  optotypeHeightPx,
  strokeWidthPx,
} from './optotype-size'

describe('optotypeHeightMm', () => {
  it('uses 400mm default distance', () => {
    expect(TRAIN_DISTANCE_MM).toBe(400)
  })
  it('acuity 1.0 → ~0.582mm at 40cm (5 arcmin)', () => {
    expect(optotypeHeightMm(1.0)).toBeCloseTo(0.582, 2)
  })
  it('acuity 0.8 → ~0.727mm (larger optotype)', () => {
    expect(optotypeHeightMm(0.8)).toBeCloseTo(0.727, 2)
  })
  it('lower acuity value → bigger optotype', () => {
    expect(optotypeHeightMm(0.5)).toBeGreaterThan(optotypeHeightMm(1.0))
  })
  it('scales ~linearly with distance (small angle)', () => {
    expect(optotypeHeightMm(1.0, 800) / optotypeHeightMm(1.0, 400)).toBeCloseTo(2, 3)
  })
  it('throws on non-positive acuity', () => {
    expect(() => optotypeHeightMm(0)).toThrow()
    expect(() => optotypeHeightMm(-1)).toThrow()
  })
})

describe('optotypeHeightPx', () => {
  it('multiplies mm by pxPerMm', () => {
    expect(optotypeHeightPx(1.0, 10)).toBeCloseTo(optotypeHeightMm(1.0) * 10, 6)
  })
})

describe('strokeWidthPx', () => {
  it('is one fifth of the optotype height', () => {
    expect(strokeWidthPx(50)).toBe(10)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- optotype-size`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/training/optotype-size.ts`**

```ts
/** 训练标准距离（mm） */
export const TRAIN_DISTANCE_MM = 400

/**
 * 视标整字高度（mm）。视力级别 acuity 下整字张角 = 5/acuity 弧分。
 * h = 2·D·tan( (角/2) )，角由弧分转弧度。
 */
export function optotypeHeightMm(acuity: number, distanceMm = TRAIN_DISTANCE_MM): number {
  if (!(acuity > 0)) {
    throw new Error(`acuity must be > 0, got ${acuity}`)
  }
  const arcMinutes = 5 / acuity
  const radians = (arcMinutes * Math.PI) / (180 * 60)
  return 2 * distanceMm * Math.tan(radians / 2)
}

/** 视标整字高度（CSS px），pxPerMm 来自屏幕标定 */
export function optotypeHeightPx(
  acuity: number,
  pxPerMm: number,
  distanceMm = TRAIN_DISTANCE_MM,
): number {
  return optotypeHeightMm(acuity, distanceMm) * pxPerMm
}

/** Tumbling E 笔画宽 = 缺口宽 = 整字高 / 5（标准 5×5 网格） */
export function strokeWidthPx(heightPx: number): number {
  return heightPx / 5
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- optotype-size`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/training/optotype-size.ts src/training/optotype-size.test.ts
git commit -m "feat(1a): 视标尺寸换算（视力级别+距离+标定→mm/px）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 推算式 CPM（cpm，纯函数 TDD）

**Files:**
- Create: `src/training/cpm.ts`
- Test: `src/training/cpm.test.ts`

**Interfaces:**
- Consumes: 无。
- Produces: `cpm(flips: number, elapsedSec: number): number` — `elapsedSec<=0` 返回 0。

- [ ] **Step 1: 写失败测试 `src/training/cpm.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { cpm } from './cpm'

describe('cpm', () => {
  it('24 flips in 60s = 12 cpm (1 cycle = 2 flips)', () => {
    expect(cpm(24, 60)).toBeCloseTo(12, 6)
  })
  it('12 flips in 30s = 12 cpm', () => {
    expect(cpm(12, 30)).toBeCloseTo(12, 6)
  })
  it('0 flips = 0', () => {
    expect(cpm(0, 60)).toBe(0)
  })
  it('guards divide-by-zero', () => {
    expect(cpm(10, 0)).toBe(0)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- cpm`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/training/cpm.ts`**

```ts
/**
 * 推算式 CPM（cycles per minute）。
 * 每完成一次「答题 + 翻拍过渡」= 一次翻转（半 cycle）；1 cycle = 2 flips。
 * 依赖孩子诚实翻拍（迭代0结论），非临床精确值。
 */
export function cpm(flips: number, elapsedSec: number): number {
  if (elapsedSec <= 0) return 0
  return flips / 2 / (elapsedSec / 60)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- cpm`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/training/cpm.ts src/training/cpm.test.ts
git commit -m "feat(1a): 推算式 CPM 计算

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 训练节状态机（session，纯逻辑 TDD）

**Files:**
- Create: `src/training/session.ts`
- Test: `src/training/session.test.ts`

**Interfaces:**
- Consumes: `Direction`（`src/speech/answer-mapping.ts`，值为 'up'|'down'|'left'|'right'）。
- Produces:
  - `type Phase = 'preparing' | 'showing' | 'transitioning' | 'finished'`
  - `type Eye = 'left' | 'right'`
  - `interface SessionState { phase; eye; target: Direction|null; answered; correct; flips; elapsedSec; durationSec }`
  - `createSession(eye: Eye, durationSec: number): SessionState`
  - `pickDirection(prev: Direction|null, rand: number): Direction` — 随机方向，rand∈[0,1)，避免与 prev 相同
  - `start(state, firstTarget: Direction): SessionState`
  - `answer(state, dir: Direction): SessionState`
  - `advance(state, nextTarget: Direction): SessionState`
  - `tick(state, deltaSec: number): SessionState`
  - `accuracy(state): number`

- [ ] **Step 1: 写失败测试 `src/training/session.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import {
  createSession, pickDirection, start, answer, advance, tick, accuracy,
} from './session'

describe('createSession', () => {
  it('starts in preparing with zeroed counters', () => {
    const s = createSession('left', 180)
    expect(s).toMatchObject({
      phase: 'preparing', eye: 'left', target: null,
      answered: 0, correct: 0, flips: 0, elapsedSec: 0, durationSec: 180,
    })
  })
})

describe('pickDirection', () => {
  it('never repeats the previous direction', () => {
    for (const r of [0, 0.25, 0.5, 0.75, 0.999]) {
      expect(pickDirection('up', r)).not.toBe('up')
    }
  })
  it('picks from all four when prev is null', () => {
    expect(pickDirection(null, 0)).toBe('up')
  })
})

describe('start', () => {
  it('preparing → showing with a target', () => {
    const s = start(createSession('left', 180), 'right')
    expect(s.phase).toBe('showing')
    expect(s.target).toBe('right')
  })
})

describe('answer', () => {
  it('correct answer increments answered and correct, goes transitioning', () => {
    const s = start(createSession('left', 180), 'up')
    const a = answer(s, 'up')
    expect(a).toMatchObject({ phase: 'transitioning', answered: 1, correct: 1 })
  })
  it('wrong answer counts answered but not correct', () => {
    const s = start(createSession('left', 180), 'up')
    const a = answer(s, 'down')
    expect(a).toMatchObject({ phase: 'transitioning', answered: 1, correct: 0 })
  })
  it('ignores answer when not showing', () => {
    const s = createSession('left', 180) // preparing
    expect(answer(s, 'up')).toBe(s)
  })
})

describe('advance', () => {
  it('transitioning → showing, flips+1, new target', () => {
    let s = start(createSession('left', 180), 'up')
    s = answer(s, 'up')
    const n = advance(s, 'left')
    expect(n).toMatchObject({ phase: 'showing', target: 'left', flips: 1 })
  })
})

describe('tick', () => {
  it('accumulates elapsed time', () => {
    const s = start(createSession('left', 180), 'up')
    expect(tick(s, 1).elapsedSec).toBe(1)
  })
  it('finishes when duration reached', () => {
    const s = { ...start(createSession('left', 5), 'up'), elapsedSec: 4 }
    expect(tick(s, 1).phase).toBe('finished')
  })
  it('does not tick while preparing or finished', () => {
    const p = createSession('left', 180)
    expect(tick(p, 1)).toBe(p)
  })
})

describe('accuracy', () => {
  it('is 0 with no answers', () => {
    expect(accuracy(createSession('left', 180))).toBe(0)
  })
  it('is correct/answered', () => {
    const s = { ...createSession('left', 180), answered: 4, correct: 3 }
    expect(accuracy(s)).toBe(0.75)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- session`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/training/session.ts`**

```ts
import type { Direction } from '../speech/answer-mapping'

export type Phase = 'preparing' | 'showing' | 'transitioning' | 'finished'
export type Eye = 'left' | 'right'

export interface SessionState {
  phase: Phase
  eye: Eye
  target: Direction | null
  answered: number
  correct: number
  flips: number
  elapsedSec: number
  durationSec: number
}

const DIRECTIONS: Direction[] = ['up', 'down', 'left', 'right']

export function createSession(eye: Eye, durationSec: number): SessionState {
  return {
    phase: 'preparing', eye, target: null,
    answered: 0, correct: 0, flips: 0, elapsedSec: 0, durationSec,
  }
}

/** 随机方向，rand∈[0,1)，避免与 prev 相同 */
export function pickDirection(prev: Direction | null, rand: number): Direction {
  const pool = prev ? DIRECTIONS.filter((d) => d !== prev) : DIRECTIONS
  const i = Math.min(pool.length - 1, Math.floor(rand * pool.length))
  return pool[i]
}

export function start(state: SessionState, firstTarget: Direction): SessionState {
  if (state.phase !== 'preparing') return state
  return { ...state, phase: 'showing', target: firstTarget }
}

export function answer(state: SessionState, dir: Direction): SessionState {
  if (state.phase !== 'showing' || state.target === null) return state
  const isRight = dir === state.target
  return {
    ...state,
    phase: 'transitioning',
    answered: state.answered + 1,
    correct: state.correct + (isRight ? 1 : 0),
  }
}

export function advance(state: SessionState, nextTarget: Direction): SessionState {
  if (state.phase !== 'transitioning') return state
  return { ...state, phase: 'showing', target: nextTarget, flips: state.flips + 1 }
}

export function tick(state: SessionState, deltaSec: number): SessionState {
  if (state.phase === 'preparing' || state.phase === 'finished') return state
  const elapsedSec = state.elapsedSec + deltaSec
  if (elapsedSec >= state.durationSec) {
    return { ...state, elapsedSec: state.durationSec, phase: 'finished' }
  }
  return { ...state, elapsedSec }
}

export function accuracy(state: SessionState): number {
  return state.answered === 0 ? 0 : state.correct / state.answered
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- session`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/training/session.ts src/training/session.test.ts
git commit -m "feat(1a): 训练节状态机纯逻辑（出题/判定/计数/计时）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Tumbling E 视标（方向→旋转 TDD + SVG 组件）

**Files:**
- Create: `src/training/TumblingE.tsx`
- Test: `src/training/TumblingE.test.ts`

**Interfaces:**
- Consumes: `Direction`（answer-mapping）。
- Produces:
  - `directionToRotation(dir: Direction): number` — 开口朝向→旋转角（基准 E 开口朝右=0°）。
  - `TumblingE(props: { direction: Direction; heightPx: number }): JSX.Element`

- [ ] **Step 1: 写失败测试 `src/training/TumblingE.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { directionToRotation } from './TumblingE'

describe('directionToRotation', () => {
  it('right is the 0° baseline (E opening faces right)', () => {
    expect(directionToRotation('right')).toBe(0)
  })
  it('rotates clockwise for down/left/up', () => {
    expect(directionToRotation('down')).toBe(90)
    expect(directionToRotation('left')).toBe(180)
    expect(directionToRotation('up')).toBe(270)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- TumblingE`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/training/TumblingE.tsx`**

```tsx
import type { Direction } from '../speech/answer-mapping'

/** Tumbling E 开口朝向 → 旋转角度（基准 E 开口朝右 = 0°，顺时针） */
export function directionToRotation(dir: Direction): number {
  switch (dir) {
    case 'right': return 0
    case 'down': return 90
    case 'left': return 180
    case 'up': return 270
  }
}

/**
 * 标准 5×5 网格 Tumbling E（基准开口朝右），按 heightPx 显示、按方向旋转。
 * 颜色继承 currentColor。
 */
export function TumblingE({ direction, heightPx }: { direction: Direction; heightPx: number }) {
  return (
    <svg
      width={heightPx}
      height={heightPx}
      viewBox="0 0 5 5"
      role="img"
      aria-label={`视标 E 朝${direction}`}
      style={{ transform: `rotate(${directionToRotation(direction)}deg)`, color: 'inherit' }}
    >
      <g fill="currentColor">
        <rect x="0" y="0" width="5" height="1" />
        <rect x="0" y="2" width="4" height="1" />
        <rect x="0" y="4" width="5" height="1" />
        <rect x="0" y="0" width="1" height="5" />
      </g>
    </svg>
  )
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- TumblingE`
Expected: PASS。

- [ ] **Step 5: 手动验证视标渲染**（本机）

临时在 `App.tsx` 首页插 `<TumblingE direction="up" heightPx={120} />` 试渲染四个方向，确认 E 形状正确、旋转对应开口方向（up=开口朝上）。确认后移除临时代码。

- [ ] **Step 6: Commit**

```bash
git add src/training/TumblingE.tsx src/training/TumblingE.test.ts
git commit -m "feat(1a): Tumbling E 视标组件（方向→旋转）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 音效模块（sfx，Web Audio 合成，手动验证）

**Files:**
- Create: `src/training/sfx.ts`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `playSfx(kind: 'correct' | 'wrong' | 'flip' | 'finish'): void`
  - `setMuted(muted: boolean): void`

> 用 Web Audio 合成，无需音频资源文件。首次调用需在用户手势后（训练页点"开始"满足）。

- [ ] **Step 1: 写实现 `src/training/sfx.ts`**

```ts
type SfxKind = 'correct' | 'wrong' | 'flip' | 'finish'

let ctx: AudioContext | null = null
let muted = false

function audioCtx(): AudioContext {
  if (!ctx) ctx = new AudioContext()
  return ctx
}

function tone(freq: number, startOffset: number, dur: number, gain = 0.2): void {
  const c = audioCtx()
  const osc = c.createOscillator()
  const g = c.createGain()
  osc.type = 'sine'
  osc.frequency.value = freq
  osc.connect(g)
  g.connect(c.destination)
  const t0 = c.currentTime + startOffset
  g.gain.setValueAtTime(gain, t0)
  g.gain.exponentialRampToValueAtTime(0.0001, t0 + dur)
  osc.start(t0)
  osc.stop(t0 + dur)
}

export function setMuted(m: boolean): void {
  muted = m
}

export function playSfx(kind: SfxKind): void {
  if (muted) return
  switch (kind) {
    case 'correct':
      tone(660, 0, 0.12)
      tone(880, 0.1, 0.14)
      break
    case 'wrong':
      tone(300, 0, 0.22, 0.15)
      break
    case 'flip':
      tone(520, 0, 0.06, 0.12)
      break
    case 'finish':
      ;[523, 659, 784, 1047].forEach((f, i) => tone(f, i * 0.12, 0.16))
      break
  }
}
```

- [ ] **Step 2: 手动验证音效**（本机，Task 6 集成后一起听）

在 Task 6 训练页里触发对/错/翻拍/结算，确认四种音效可辨、不刺耳、静音开关生效。

- [ ] **Step 3: Commit**

```bash
git add src/training/sfx.ts
git commit -m "feat(1a): Web Audio 音效（对/错/翻拍/结算）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 训练界面（TrainingPage，组装 + 手动验证）

**Files:**
- Create: `src/training/TrainingPage.tsx`

**Interfaces:**
- Consumes: `optotypeHeightPx`（Task 1）、`cpm`（Task 2）、`session` 全部导出（Task 3）、`TumblingE`（Task 4）、`playSfx`/`setMuted`（Task 5）、`startVosk`（`src/speech/vosk.ts`）、`parseAnswer`（`src/speech/answer-mapping.ts`）。
- Produces: `TrainingPage(): JSX.Element`

**关键实现约束：**
- 用 `sessionRef` 同步最新 state，供 vosk 回调与定时器读取（避免闭包陈旧）。
- vosk 在点"开始"（用户手势）时启动一次，组件卸载时 stop。
- 翻拍过渡时长常量 `TRANSITION_MS = 1600`。
- 计时用 1 秒 interval 调 `tick`。
- 从 localStorage `fzp.cssPxPerMm` 读标定；未标定则提示先去标定页。

- [ ] **Step 1: 写 `src/training/TrainingPage.tsx`**

```tsx
import { useEffect, useRef, useState } from 'react'
import { optotypeHeightPx } from './optotype-size'
import { cpm } from './cpm'
import {
  createSession, pickDirection, start, answer, advance, tick, accuracy,
  type SessionState, type Eye,
} from './session'
import { TumblingE } from './TumblingE'
import { playSfx, setMuted } from './sfx'
import { startVosk, type VoskController } from '../speech/vosk'
import { parseAnswer, type Direction } from '../speech/answer-mapping'

const DURATION_SEC = 180
const TRANSITION_MS = 1600
const VOSK_MODEL_URL = '/models/vosk-model-small-cn-0.22.tar.gz'
const VOSK_GRAMMAR = ['上 下 左 右']
const EYE_LABEL: Record<Eye, string> = { left: '左眼 · 遮右眼', right: '右眼 · 遮左眼' }

function readPxPerMm(): number | null {
  const v = localStorage.getItem('fzp.cssPxPerMm')
  return v ? Number(v) : null
}

export function TrainingPage() {
  const [acuity, setAcuity] = useState(0.8)
  const [muted, setMutedState] = useState(false)
  const [session, setSession] = useState<SessionState>(() => createSession('left', DURATION_SEC))
  const [allDone, setAllDone] = useState(false)

  const pxPerMm = readPxPerMm()
  const sessionRef = useRef(session)
  const voskRef = useRef<VoskController | null>(null)

  useEffect(() => { sessionRef.current = session }, [session])
  useEffect(() => { setMuted(muted) }, [muted])

  // 计时：showing/transitioning 时每秒 tick
  useEffect(() => {
    if (session.phase !== 'showing' && session.phase !== 'transitioning') return
    const id = window.setInterval(() => setSession((s) => tick(s, 1)), 1000)
    return () => window.clearInterval(id)
  }, [session.phase])

  // 结束音效
  useEffect(() => {
    if (session.phase === 'finished') playSfx('finish')
  }, [session.phase])

  // 卸载时停 vosk
  useEffect(() => () => { voskRef.current?.stop() }, [])

  function handleAnswer(dir: Direction) {
    const s = sessionRef.current
    if (s.phase !== 'showing' || s.target === null) return
    playSfx(dir === s.target ? 'correct' : 'wrong')
    setSession(answer(s, dir))
    window.setTimeout(() => {
      playSfx('flip')
      setSession((cur) =>
        cur.phase === 'transitioning'
          ? advance(cur, pickDirection(cur.target, Math.random()))
          : cur,
      )
    }, TRANSITION_MS)
  }

  async function beginSession() {
    if (!voskRef.current) {
      try {
        voskRef.current = await startVosk({
          modelUrl: VOSK_MODEL_URL,
          grammar: VOSK_GRAMMAR,
          onResult: (text) => {
            const parsed = parseAnswer(text)
            if (parsed?.kind === 'direction') handleAnswer(parsed.value)
          },
        })
      } catch {
        // 语音起不来不阻塞，触控兜底仍可用
      }
    }
    setSession((s) => start(s, pickDirection(null, Math.random())))
  }

  function nextEyeOrFinish() {
    if (session.eye === 'left') {
      setSession(createSession('right', DURATION_SEC))
    } else {
      setAllDone(true)
    }
  }

  if (pxPerMm === null) {
    return (
      <div style={{ padding: 24 }}>
        <h2>训练</h2>
        <p>请先到「标定」页完成屏幕标定，视标才能按正确大小显示。</p>
      </div>
    )
  }

  if (allDone) {
    return (
      <div style={{ padding: 24, textAlign: 'center' }}>
        <h2>今天的训练完成啦 🎉</h2>
        <p>左右眼都练好了。</p>
      </div>
    )
  }

  const heightPx = optotypeHeightPx(acuity, pxPerMm)
  const progress = Math.min(1, session.elapsedSec / session.durationSec)

  // 准备态
  if (session.phase === 'preparing') {
    return (
      <div style={{ padding: 24, textAlign: 'center' }}>
        <h2>准备：{EYE_LABEL[session.eye]}</h2>
        <p>遮住另一只眼，拍子正镜片面朝眼，坐直、离屏幕约 40cm。</p>
        <p>
          视力级别：
          <select value={acuity} onChange={(e) => setAcuity(Number(e.target.value))}>
            <option value={0.4}>0.4（大）</option>
            <option value={0.6}>0.6</option>
            <option value={0.8}>0.8</option>
            <option value={1.0}>1.0（小）</option>
          </select>
        </p>
        <button onClick={beginSession} style={{ fontSize: 22, padding: '12px 28px' }}>开始</button>
      </div>
    )
  }

  // 结算态
  if (session.phase === 'finished') {
    return (
      <div style={{ padding: 24, textAlign: 'center' }}>
        <h2>{session.eye === 'left' ? '左眼' : '右眼'} · 本节完成</h2>
        <p style={{ fontSize: 18 }}>
          答对 {session.correct}/{session.answered} ·
          正确率 {Math.round(accuracy(session) * 100)}% ·
          平均 CPM {Math.round(cpm(session.flips, session.elapsedSec))}
        </p>
        <button onClick={nextEyeOrFinish} style={{ fontSize: 20, padding: '12px 24px' }}>
          {session.eye === 'left' ? '换右眼继续' : '完成'}
        </button>
      </div>
    )
  }

  // 进行态（showing / transitioning）
  const transitioning = session.phase === 'transitioning'
  return (
    <div style={{ display: 'flex', flexDirection: 'column', minHeight: '80vh' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 16, padding: '12px 20px' }}>
        <span style={{ padding: '4px 10px', background: '#e6f1fb', borderRadius: 8 }}>
          {EYE_LABEL[session.eye]}
        </span>
        <div style={{ flex: 1, height: 6, background: '#eee', borderRadius: 3 }}>
          <div style={{ width: `${progress * 100}%`, height: '100%', background: '#1d9e75', borderRadius: 3 }} />
        </div>
        <button onClick={() => setMutedState((m) => !m)}>{muted ? '🔇' : '🔊'}</button>
      </div>

      <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#111' }}>
        {transitioning ? (
          <div style={{ fontSize: 24, color: '#1d9e75' }}>翻！</div>
        ) : (
          session.target && (
            <div style={{ animation: 'fzpBlurIn 0.4s ease-out' }}>
              <TumblingE direction={session.target} heightPx={heightPx} />
            </div>
          )
        )}
      </div>

      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '12px 20px' }}>
        <span style={{ color: '#1d9e75' }}>在听…说出方向</span>
        <span style={{ display: 'inline-flex', gap: 8 }}>
          {(['up', 'down', 'left', 'right'] as Direction[]).map((d) => (
            <button key={d} onClick={() => handleAnswer(d)} style={{ width: 48, height: 48, fontSize: 20 }}>
              {{ up: '↑', down: '↓', left: '←', right: '→' }[d]}
            </button>
          ))}
        </span>
      </div>

      <style>{`@keyframes fzpBlurIn { from { filter: blur(8px); opacity: 0.2 } to { filter: blur(0); opacity: 1 } }`}</style>
    </div>
  )
}
```

- [ ] **Step 2: 跑测试确认纯逻辑仍绿**

Run: `npm run test`
Expected: PASS（全部纯逻辑测试）。

- [ ] **Step 3: Commit**

```bash
git add src/training/TrainingPage.tsx
git commit -m "feat(1a): 训练界面（组装状态机/视标/语音/触控/音效/计时/结算）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 接入训练入口 + 手动走查一轮

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `TrainingPage`（Task 6）。

- [ ] **Step 1: 修改 `src/App.tsx`，加"训练"视图**（替换整个文件）

```tsx
import { useState } from 'react'
import { CalibrationPage } from './calibration/CalibrationPage'
import { SpeechTestPage } from './speech/SpeechTestPage'
import { TrainingPage } from './training/TrainingPage'

type View = 'home' | 'train' | 'calib' | 'speech'

export function App() {
  const [view, setView] = useState<View>('home')
  return (
    <div>
      <nav style={{ display: 'flex', gap: 8, padding: 12, borderBottom: '1px solid #ccc' }}>
        <button onClick={() => setView('home')}>首页</button>
        <button onClick={() => setView('train')}>训练</button>
        <button onClick={() => setView('calib')}>标定</button>
        <button onClick={() => setView('speech')}>语音</button>
      </nav>
      {view === 'home' && (
        <div style={{ padding: 24 }}>
          <h1>变焦大冒险 · 迭代1a</h1>
          <p>先到「标定」完成一次屏幕标定，再进「训练」。</p>
        </div>
      )}
      {view === 'train' && <TrainingPage />}
      {view === 'calib' && <CalibrationPage />}
      {view === 'speech' && <SpeechTestPage />}
    </div>
  )
}
```

- [ ] **Step 2: 跑测试确认全绿**

Run: `npm run test`
Expected: PASS。

- [ ] **Step 3: 手动走查完整一轮**（本机 `npm run dev`，需先在标定页标定过一次）

1. 标定页保存过标定 → 训练页。
2. 准备态：选视力级别 0.8 → 点开始（允许麦克风）。
3. 进行态：说"上/下/左/右"或点方向按钮 → 听到对/错音效 → 看到"翻！"过渡 → 新 E 模糊→清晰淡入。
4. 顶部只有眼别 + 进度条（无 CPM/正确率/秒数）。
5. 让计时走完（或临时把 `DURATION_SEC` 调小如 20 测试）→ 单节结算显示答对数/正确率/CPM。
6. 点"换右眼继续" → 右眼准备态 → 再走一轮 → "完成"。
7. 确认视标大小合理、语音识别灵敏、触控兜底可用、音效不刺耳。
8. 记录 iPad 上需复验的点（视标物理尺寸、孩子发音识别率）到迭代0结论文档的遗留项。

- [ ] **Step 4: Commit**

```bash
git add src/App.tsx
git commit -m "feat(1a): 接入训练入口，完成核心训练循环

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review（计划作者自检）

**1. Spec 覆盖：**
- 视标 Tumbling E + 视力级别换算 → Task 1（换算）+ Task 4（渲染）✅
- 训练节流程/状态机/计时 → Task 3 ✅
- 语音 vosk 常驻 + 触控兜底始终显示 → Task 6 ✅
- 翻拍过渡 + 模糊→清晰 → Task 6（TRANSITION_MS + fzpBlurIn 动画）✅
- 音效对/错/翻拍/结算 → Task 5 ✅
- 精简顶部（眼别+进度条，无 CPM/秒数）→ Task 6 ✅
- CPM 仅结算 → Task 2 + Task 6 结算 ✅
- 换眼、结算 → Task 6 ✅
- 1a 不落库 → 全程内存态，无 IndexedDB ✅
- 默认决策 7 条 → 分布在 Task 3/6 ✅

**2. Placeholder 扫描：** 无 TBD/TODO；每个 code step 含完整代码；手动验证步骤含具体操作与判据。

**3. 类型一致性：** `Direction`（answer-mapping）在 session/TumblingE/TrainingPage 一致消费；`SessionState` 及 `createSession/start/answer/advance/tick/pickDirection/accuracy` 在 Task 3 定义、Task 6 消费签名一致；`optotypeHeightPx(acuity,pxPerMm)`、`cpm(flips,elapsedSec)`、`playSfx(kind)`、`startVosk({modelUrl,grammar,onResult})`（迭代0签名）一致。

**4. 方法论：** 纯逻辑（Task 1-4）严格 TDD；组件/音效/集成（Task 5-7）手动验证并给出明确判据。
