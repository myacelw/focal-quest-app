# 闪光变体 实施计划（计划 B）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给每只怪兽加一个极稀有的「闪光」版本，零新美术把收集量从 82 翻到 164，让图鉴的内容供给再撑一个多月。

**Architecture:** 闪光是**独立的 monster 行**（id 形如 `space-ufo~shiny`），不是在原行上加字段——后者会被同步的「整行取最早」语义静默吃掉。抽取改成两步掷骰：先掷是否闪光，再从对应的「未拥有」桶里用现成的 `pickWeighted` 抽。视觉是统一的金色微光（`MonsterImage` 加一个 `shiny` prop），不用 per-monster 色相旋转。图鉴仍是 82 格，拥有闪光的格子挂 ✨ 角标。

**Tech Stack:** TypeScript + React + Vite；vitest + fake-indexeddb；Dexie（IndexedDB）。

**来源：** [spec §5](../specs/2026-07-28-图鉴扩池与森林皮肤-design.md)。计划 A（扩池 + 森林皮肤）已完成并上线，本计划**不依赖它**——只是作用在 82 只而不是 34 只上。

## Global Constraints

- **怪兽 id 不得含冒号。** monster 是 `KEYED_KIND`，同步 uuid 形如 `monster:default:space-ufo~shiny`，`naturalKeyFromUuid` 靠「自然键侧不含冒号」反解。后缀用 `~` 正是为此。
- **不许在 `MonsterRow` 上加 `shinyAt` 字段。** `merge.ts` 对 monster 是「整行取最早 `capturedAt`」：设备 A `{capturedAt:100}`、设备 B `{capturedAt:100, shinyAt:200}`，两边 `capturedAt` 相等 → 判 `skip` → **B 的闪光被静默丢掉**。独立行让每个收集品一行一 uuid，同步层零改动。
- **闪光 id 不得进 `MONSTER_DEFS`。** 图鉴仍是 82 格；而且轮换池靠「id 在 `MONSTER_DEFS` 里」匹配，混进去会让闪光怪跑进训练画面。
- **闪光不进训练轮换。** 要进得给 `StageProps` 再加字段，还要回答「同时拥有普通与闪光时显示哪个」。本轮只在图鉴呈现。
- **视觉用统一的金色微光，不用 `hue-rotate`。** 82 只怪配色各异，同一个色相旋转角度必然在其中一部分身上翻车，而且没法逐只调。
- **中英文案必须成对。** `src/i18n.test.ts` 用 `hasKey('zh'|'en', k)` 逐键校验。
- **开工第一步先跑 `npm test` 记真实基线**（写这份计划时是 697 个 / 73 文件）。

---

## File Structure

**修改**
- `src/dex/monster-defs.ts` — `SHINY_SUFFIX` / `shinyIdOf` / `isShinyId` / `baseIdOf`
- `src/dex/monster-defs.test.ts` — id 纯函数互逆 + **闪光不污染轮换池**的隔离断言
- `src/dex/capture.ts` — `SHINY_CHANCE`、`CaptureResult`、`pickCapture` 改两步掷骰
- `src/dex/capture.test.ts` — 既有 16 条改 `.def.id`，新增两步掷骰的用例
- `src/dex/dex-service.ts` — `captureMonster` 按结果拼 id；`getDexProgress` 加 `shinyOwned` 并修正 `owned` 口径
- `src/dex/dex-service.test.ts` — 闪光行不串进普通计数
- `src/dex/MonsterImage.tsx` — `shiny?: boolean`
- `src/dex/shiny-visual.test.ts`（**新建**）— `?raw` 源文本契约：不许 hue-rotate、filter 必须叠加、闪光音效不复用普通音
- `src/dex/DexWall.tsx` — ✨ 角标、放大态双形态切换、进度双数字
- `src/HomePage.tsx` — 图鉴入口的「✨N」（`shinyOwned` 的消费者）
- `src/training/TrainingPage.tsx` — 捕获结果带 shiny，揭示卡显示闪光
- `src/i18n.tsx` / `src/i18n.test.ts` — 5 个新键 ×2 语言

**只新建一个测试文件。** 本计划是在既有图鉴模块上加一个维度，没有值得独立成文件的新实现职责。

---

## Task 1: 闪光 id 的纯函数与隔离断言

**Files:**
- Modify: `src/dex/monster-defs.ts`（文件末尾追加四个导出）
- Modify: `src/dex/monster-defs.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `SHINY_SUFFIX: string`（= `'~shiny'`）
  - `shinyIdOf(id: string): string`
  - `isShinyId(id: string): boolean`
  - `baseIdOf(id: string): string`

- [ ] **Step 1: 记基线**

Run: `npm test`
把「N passed / M files」记下来，后面每步对着它看有没有意外变红。

- [ ] **Step 2: 写失败的测试**

`src/dex/monster-defs.test.ts` 追加（import 里加上四个新符号，以及三个 Stage 的池构造函数）：

```ts
import { SHINY_SUFFIX, shinyIdOf, isShinyId, baseIdOf } from './monster-defs'
import { buildEnemyPool } from '../skins/space/SpaceStage'
import { buildGuardianPool } from '../skins/shrine/ShrineStage'
import { buildSpiritPool } from '../skins/forest/ForestStage'

describe('闪光 id', () => {
  it('后缀是 ~shiny，且不含冒号 —— 同步 uuid 反解靠"自然键侧不含冒号"', () => {
    expect(SHINY_SUFFIX).toBe('~shiny')
    expect(SHINY_SUFFIX.includes(':')).toBe(false)
    for (const m of MONSTER_DEFS) expect(shinyIdOf(m.id).includes(':'), m.id).toBe(false)
  })

  it('三个函数互逆：对全部 82 只都有 baseIdOf(shinyIdOf(id)) === id', () => {
    for (const m of MONSTER_DEFS) {
      const s = shinyIdOf(m.id)
      expect(isShinyId(s), s).toBe(true)
      expect(baseIdOf(s), s).toBe(m.id)
    }
  })

  it('baseIdOf 对本体 id 幂等，isShinyId 对本体 id 为 false', () => {
    expect(baseIdOf('space-ufo')).toBe('space-ufo')
    expect(baseIdOf(baseIdOf(shinyIdOf('space-ufo')))).toBe('space-ufo')
    expect(isShinyId('space-ufo')).toBe(false)
  })

  it('MONSTER_DEFS 里不含任何闪光 id —— 图鉴仍是 82 格', () => {
    for (const m of MONSTER_DEFS) expect(isShinyId(m.id), m.id).toBe(false)
  })
})

describe('闪光不污染训练轮换池', () => {
  // 这条锚住 spec §5.5 那个"天然被忽略"的性质。它现在是对的，但它是**巧合式正确**
  // ——靠闪光 id 匹配不上 MONSTER_DEFS。没有测试的话，将来谁把匹配改成前缀匹配
  // 或模糊匹配，闪光怪就会混进训练画面，而那是本轮明确不做的。
  const allShiny = MONSTER_DEFS.map((m) => shinyIdOf(m.id))

  it('把全部 82 个闪光 id 传进三个 Stage 的池构造，结果与不传时完全相同', () => {
    expect(buildEnemyPool(allShiny)).toEqual(buildEnemyPool())
    expect(buildGuardianPool(allShiny)).toEqual(buildGuardianPool())
    expect(buildSpiritPool(allShiny)).toEqual(buildSpiritPool())
  })

  it('真实储备 id + 闪光 id 混着传，只有真实的那些生效', () => {
    const real = 'shrine-golem'
    expect(buildGuardianPool([real, ...allShiny])).toEqual(buildGuardianPool([real]))
    expect(buildGuardianPool([real]).length).toBe(buildGuardianPool().length + 1)
  })
})
```

- [ ] **Step 3: 跑测试确认它红**

Run: `npx vitest run src/dex/monster-defs.test.ts`
Expected: FAIL —— `SHINY_SUFFIX` / `shinyIdOf` / `isShinyId` / `baseIdOf` 未导出。

- [ ] **Step 4: 实现四个导出**

`src/dex/monster-defs.ts` 文件末尾追加：

```ts
/**
 * 闪光变体的 id 后缀。**闪光是独立的 monster 行**（id 形如 'space-ufo~shiny'），
 * 不是在原行上加字段。
 *
 * ⚠️ 为什么不能加字段：`merge.ts` 对 monster 的规则是「整行取最早 capturedAt」。
 * 设备 A 有 `{capturedAt:100}`、设备 B 有 `{capturedAt:100, shinyAt:200}` 时两边
 * capturedAt 相等 → 判 skip → **B 的闪光被静默丢掉**。独立行让每个收集品一行一
 * uuid，capturedAt 取最早照常成立，同步层零改动。
 *
 * ⚠️ 用 '~' 而不是 '-'：slug 里可能出现连字符，用 '-' 无法可靠反解；而 '~' 不含
 * 冒号，满足 naturalKeyFromUuid 的「自然键侧不含冒号」前提。
 *
 * ⚠️ 闪光 id **不进 MONSTER_DEFS**：图鉴仍是 82 格，而且轮换池靠「id 在
 * MONSTER_DEFS 里」匹配，混进去会让闪光怪跑进训练画面。
 */
export const SHINY_SUFFIX = '~shiny'

/** 本体 id → 闪光 id */
export function shinyIdOf(id: string): string {
  return `${id}${SHINY_SUFFIX}`
}

export function isShinyId(id: string): boolean {
  return id.endsWith(SHINY_SUFFIX)
}

/** 闪光 id → 本体 id；传本体 id 原样返回（幂等） */
export function baseIdOf(id: string): string {
  return isShinyId(id) ? id.slice(0, -SHINY_SUFFIX.length) : id
}
```

- [ ] **Step 5: 跑测试确认绿**

Run: `npm test`
Expected: PASS，比基线多 6 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 6: 提交**

```bash
git add src/dex/monster-defs.ts src/dex/monster-defs.test.ts
git commit -m "feat(图鉴): 闪光变体的 id 纯函数，并锚住它不污染训练轮换池"
```

---

## Task 2: `pickCapture` 改两步掷骰

**Files:**
- Modify: `src/dex/capture.ts`
- Modify: `src/dex/capture.test.ts`

**Interfaces:**
- Consumes: `shinyIdOf`（Task 1）
- Produces:
  - `SHINY_CHANCE: number`（= `1 / 16`）
  - `interface CaptureResult { def: MonsterDef; shiny: boolean }`
  - `pickCapture(ownedIds: string[], source: CaptureSource, rand: number, shinyRand?: number): CaptureResult | null`

⚠️ **返回类型从 `MonsterDef | null` 变成 `CaptureResult | null`**，这是本计划唯一波及既有代码的地方。`capture.test.ts` 现有 16 条里读 `picked.id` 的都要改成 `picked.def.id`；`dex-service.ts` 的调用点在 Task 3 改。

- [ ] **Step 1: 写失败的测试**

先把 `src/dex/capture.test.ts` 里**所有** `pickCapture(...)` 的返回值访问从 `.id` / `.rarity` 改成 `.def.id` / `.def.rarity`（16 条里凡是解引用返回值的都要改；断言 `toBeNull()` 的那条不用动）。

⚠️ 既有那条「池内均匀：细扫 rand 能抽到每一只」会遍历 2000 个 rand，若不传第 4 个参数，闪光掷骰会用 `Math.random()` 而变得不确定。**给它补上第 4 个参数 `1`**（`1 < 1/16` 为假 → 恒不闪光），断言仍是 `seen.size === 82`。同理，其余既有用例凡是断言具体怪兽的，都补 `1`。

然后追加：

```ts
import { SHINY_CHANCE } from './capture'
import { shinyIdOf, MONSTER_DEFS } from './monster-defs'

describe('两步掷骰：闪光变体', () => {
  const ALL_NORMAL = MONSTER_DEFS.map((m) => m.id)
  const ALL_SHINY = MONSTER_DEFS.map((m) => shinyIdOf(m.id))

  it('闪光概率是 1/16', () => {
    expect(SHINY_CHANCE).toBe(1 / 16)
  })

  it('shinyRand 落在门槛内 → 出闪光；落在门槛外 → 出普通', () => {
    expect(pickCapture([], 'daily', 0, 0)!.shiny).toBe(true)
    expect(pickCapture([], 'daily', 0, SHINY_CHANCE - 0.001)!.shiny).toBe(true)
    expect(pickCapture([], 'daily', 0, SHINY_CHANCE)!.shiny).toBe(false)
    expect(pickCapture([], 'daily', 0, 1)!.shiny).toBe(false)
  })

  it('两个桶各自只出未拥有的：普通全拥有后，即使掷不中闪光也回落到闪光桶', () => {
    const r = pickCapture(ALL_NORMAL, 'daily', 0, 1)
    expect(r).not.toBeNull()
    expect(r!.shiny).toBe(true)
  })

  it('闪光全拥有后，即使掷中闪光也回落到普通桶', () => {
    const r = pickCapture(ALL_SHINY, 'daily', 0, 0)
    expect(r).not.toBeNull()
    expect(r!.shiny).toBe(false)
  })

  it('两个桶都空 → null（全图鉴 164 个收集品集齐）', () => {
    expect(pickCapture([...ALL_NORMAL, ...ALL_SHINY], 'daily', 0, 0)).toBeNull()
    expect(pickCapture([...ALL_NORMAL, ...ALL_SHINY], 'daily', 0, 1)).toBeNull()
  })

  it('闪光桶按"闪光是否已拥有"判定，与本体是否拥有无关', () => {
    // 已有本体、没有闪光 → 仍能抽到它的闪光
    const owned = [...ALL_NORMAL]
    const r = pickCapture(owned, 'daily', 0, 0)
    expect(r!.shiny).toBe(true)
    expect(ALL_NORMAL).toContain(r!.def.id) // def 仍是本体 def，闪光只是一个标记
  })

  it('桶内仍按稀有度权重抽（沿用 pickWeighted，不因闪光而改变手感）', () => {
    // daily 权重 common 70：rand=0 落在 common 段
    expect(pickCapture([], 'daily', 0, 0)!.def.rarity).toBe('common')
    // egg 权重 epic 25，覆盖 [75,100)：rand=0.99 落在 epic 段
    expect(pickCapture([], 'egg', 0.99, 0)!.def.rarity).toBe('epic')
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/dex/capture.test.ts`
Expected: FAIL —— `SHINY_CHANCE` 未导出；`pickCapture` 只接受 3 个参数、返回值没有 `.def` / `.shiny`。

- [ ] **Step 3: 改 `capture.ts`**

import 里加 `shinyIdOf`，然后把 `pickCapture` 整个替换掉，并在其上方加两个新导出：

```ts
/**
 * 闪光掷骰的门槛。**它只控制前期的惊喜密度，不控制总时长。**
 *
 * 因为普通桶抽空后所有掷骰都回落到闪光桶：前期普通桶还有货时闪光按 1/16 稀疏
 * 撒入（82 只普通抽完约需 87 次捕获、约 35 天，期间约出 5 只闪光），之后进入
 * 纯闪光收集期。合计约 68 天。想让闪光更稀有就调小它，不会拖长整体周期。
 */
export const SHINY_CHANCE = 1 / 16

/** 抽中的怪兽本体，以及这次是不是闪光变体 */
export interface CaptureResult {
  def: MonsterDef
  shiny: boolean
}

/**
 * 两步掷骰：先掷是否闪光，再从对应的「未拥有」桶里按稀有度权重抽 1 只。
 * 想要的桶空了就回落到另一个桶；两个都空返回 null（164 个收集品全集齐）。
 *
 * **「不出重复」的铁律照旧成立**——两个桶都只装未拥有的条目。
 *
 * `shinyRand` 与 `rand` 分开是为了可确定性测试：`rand` 决定抽哪只，
 * `shinyRand` 决定闪不闪。生产两个都传 Math.random()。
 */
export function pickCapture(
  ownedIds: string[],
  source: CaptureSource,
  rand: number,
  shinyRand: number = Math.random(),
): CaptureResult | null {
  const owned = new Set(ownedIds)
  const normalPool = MONSTER_DEFS.filter((m) => !owned.has(m.id))
  const shinyPool = MONSTER_DEFS.filter((m) => !owned.has(shinyIdOf(m.id)))

  const wantShiny = shinyRand < SHINY_CHANCE
  const primary = wantShiny ? shinyPool : normalPool
  const usePrimary = primary.length > 0
  const pool = usePrimary ? primary : (wantShiny ? normalPool : shinyPool)
  if (pool.length === 0) return null

  const def = pickWeighted(pool, (m) => m.rarity, WEIGHTS[source], RARITY_LIST, rand)
  if (!def) return null
  return { def, shiny: usePrimary ? wantShiny : !wantShiny }
}
```

- [ ] **Step 4: 跑测试确认绿**

Run: `npx vitest run src/dex/capture.test.ts`
Expected: PASS（既有 16 条 + 新增 7 条 = 23 条）。

⚠️ 此时 `npm run typecheck` **仍会红**——`dex-service.ts` 的调用点还没改，那是 Task 3。这一步只跑单文件测试。

- [ ] **Step 5: 提交**

```bash
git add src/dex/capture.ts src/dex/capture.test.ts
git commit -m "feat(图鉴): pickCapture 改两步掷骰，返回 { def, shiny }"
```

---

## Task 3: 落库闪光行与进度口径

**Files:**
- Modify: `src/dex/dex-service.ts`
- Modify: `src/dex/dex-service.test.ts`

**Interfaces:**
- Consumes: `CaptureResult` / `pickCapture`（Task 2）、`shinyIdOf`（Task 1）
- Produces:
  - `captureMonster(source, todayStr, now): Promise<CaptureResult | null>`
  - `captureDailyOnCheckin(alreadyCheckedIn, todayStr, now): Promise<CaptureResult | null>`
  - `DexProgress` 新增字段 `shinyOwned: number`

⚠️ **这个任务里藏着一个真实缺陷要修**：`getDexProgress` 现在算 `owned: owned.size`，而 `owned` 是**全部 monster 行的 id 集合**。闪光是独立行，所以不改的话首页会显示「95/82」这种数字。必须改成只数在 `MONSTER_DEFS` 里的那些。

- [ ] **Step 1: 写失败的测试**

`src/dex/dex-service.test.ts` 追加。文件顶部已有 `beforeEach(async () => { await db.monsters.clear() })`，所以用例里**不用**再自己清库；import 那行补上 `MONSTER_DEFS, shinyIdOf`，并从 `./dex-service` 补 `captureMonster`。

```ts
describe('闪光行与普通计数互不串', () => {
  const first = MONSTER_DEFS[0]

  it('owned 只数本体、shinyOwned 只数闪光 —— 闪光行不能把 owned 顶到 82 以上', async () => {
    // 这是真实缺陷的回归锚：owned 原本取 db 里全部行的 size，
    // 闪光是独立行，不修会让首页显示「95/82」。
    await db.monsters.put({ id: first.id, capturedAt: 1, source: 'daily' })
    await db.monsters.put({ id: shinyIdOf(first.id), capturedAt: 2, source: 'daily' })
    const p = await getDexProgress()
    expect(p.owned).toBe(1)
    expect(p.shinyOwned).toBe(1)
    expect(p.total).toBe(MONSTER_DEFS.length)
  })

  it('只有闪光、没有本体时：owned 是 0，shinyOwned 是 1，byWorld 也是 0', async () => {
    await db.monsters.put({ id: shinyIdOf(first.id), capturedAt: 1, source: 'daily' })
    const p = await getDexProgress()
    expect(p.owned).toBe(0)
    expect(p.shinyOwned).toBe(1)
    expect(p.byWorld[first.world]).toBe(0)
  })

  it('getOwnedReserveIdsByWorld 不返回闪光 id（否则闪光会混进训练轮换池）', async () => {
    const reserve = MONSTER_DEFS.find((m) => m.rarity !== 'common')!
    await db.monsters.put({ id: shinyIdOf(reserve.id), capturedAt: 1, source: 'daily' })
    const r = await getOwnedReserveIdsByWorld()
    expect(r[reserve.world]).toEqual([])
  })

  it('captureMonster 抽中闪光时落库的是闪光 id', async () => {
    // 先把全部本体占满，逼它只能出闪光
    await db.monsters.bulkPut(MONSTER_DEFS.map((m) => ({ id: m.id, capturedAt: 1, source: 'daily' as const })))
    const r = await captureMonster('daily', '2026-07-28', 1_700_000_000_000)
    expect(r).not.toBeNull()
    expect(r!.shiny).toBe(true)
    const rows = await db.monsters.toArray()
    expect(rows.some((x) => x.id === shinyIdOf(r!.def.id))).toBe(true)
  })
})
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/dex/dex-service.test.ts`
Expected: FAIL —— `DexProgress` 没有 `shinyOwned`；`p.owned` 是 2 而不是 1。

- [ ] **Step 3: 改 `getDexProgress`**

import 里加 `shinyIdOf`，然后：

```ts
export interface DexProgress {
  /** 已捕获的**本体**数（闪光是独立行，不计入这里） */
  owned: number
  total: number
  /** 已捕获的闪光数，与 owned 同分母（total） */
  shinyOwned: number
  byWorld: Record<World, number>
  /** 每世界总数（派生自 MONSTER_DEFS，扩池自动跟随，避免调用方硬编码） */
  byWorldTotal: Record<World, number>
}

export async function getDexProgress(): Promise<DexProgress> {
  const rows = await db.monsters.toArray()
  const ids = new Set(rows.map((r) => r.id))
  const byWorld = emptyByWorld(() => 0)
  const byWorldTotal = emptyByWorld(() => 0)
  let owned = 0
  let shinyOwned = 0
  // ⚠️ 必须遍历 MONSTER_DEFS 来数，不能用 ids.size —— 闪光是独立行，
  // 用 size 会把「95/82」这种数字显示到首页上。
  for (const def of MONSTER_DEFS) {
    byWorldTotal[def.world]++
    if (ids.has(def.id)) { owned++; byWorld[def.world]++ }
    if (ids.has(shinyIdOf(def.id))) shinyOwned++
  }
  return { owned, total: MONSTER_DEFS.length, shinyOwned, byWorld, byWorldTotal }
}
```

- [ ] **Step 4: 改 `captureMonster`**

```ts
export async function captureMonster(
  source: CaptureSource,
  todayStr: string,
  now: number,
): Promise<CaptureResult | null> {
  if (source === 'egg') {
    const todayEggCount = await getCaptureCountToday(todayStr, 'egg')
    if (!canEggCapture(todayEggCount)) return null
  }

  const owned = await getOwnedIds()
  const picked = pickCapture([...owned], source, Math.random(), Math.random())
  if (!picked) return null

  // 闪光落的是独立行（id 带 ~shiny 后缀），与本体互不覆盖
  const id = picked.shiny ? shinyIdOf(picked.def.id) : picked.def.id
  const row: MonsterRow = { id, capturedAt: now, source }
  await db.monsters.put(row)
  pushMonsters([row])
  return picked
}
```

`captureDailyOnCheckin` 的返回类型跟着从 `Promise<MonsterDef | null>` 改成 `Promise<CaptureResult | null>`，函数体不变。import 里把 `type MonsterDef` 换成 `type CaptureResult`（若 `MonsterDef` 在别处还用得到就都留着）。

- [ ] **Step 5: 跑测试确认绿**

Run: `npx vitest run src/dex`
Expected: PASS。

⚠️ 此时 `npm run typecheck` **仍会红**——`TrainingPage.tsx` 的调用点还没改，那是 Task 4。

- [ ] **Step 6: 提交**

```bash
git add src/dex/dex-service.ts src/dex/dex-service.test.ts
git commit -m "feat(图鉴): 闪光落独立行；修正 getDexProgress 的 owned 口径"
```

---

## Task 4: 闪光的视觉与捕获揭示

**Files:**
- Modify: `src/dex/MonsterImage.tsx`
- Modify: `src/training/TrainingPage.tsx`

**Interfaces:**
- Consumes: `CaptureResult`（Task 2/3）
- Produces: `MonsterImage({ def, filter, shiny })`；`CapturedMonsterReveal({ def, shiny, delayMs })`

- [ ] **Step 1: 给 `MonsterImage` 加 `shiny` prop**

在 `src/dex/MonsterImage.tsx` 顶部（import 之后）加常量，并改签名与 filter 组合：

```tsx
/**
 * 闪光变体的统一处理：加饱和 + 提亮 + 金色描边光。
 *
 * ⚠️ 刻意**不用 `hue-rotate`**：82 只怪配色各异，同一个色相旋转角度必然在其中
 * 一部分身上产出难看甚至看不清的结果，而且没法逐只调。统一的加亮 + 描边发光
 * 可预期得多，对像素风（神庙）和插画风（太空/森林）都成立。
 */
const SHINY_FILTER = 'saturate(1.35) brightness(1.08) drop-shadow(0 0 5px rgba(255,205,60,0.95))'

export function MonsterImage({ def, filter, shiny }: { def: MonsterDef; filter?: string; shiny?: boolean }) {
```

在组件体内，把原先直接用 `filter` 的地方换成组合后的值。在 `const [failed, setFailed] = useState(false)` 之后加：

```tsx
  // 与调用方传入的 filter（未捕获剪影用 brightness(0)）叠加，而不是互相覆盖
  const fx = [shiny ? SHINY_FILTER : '', filter ?? ''].filter(Boolean).join(' ') || undefined
```

然后把文件里三处 `filter,`（回落 div、sprite div、img）的样式值都改成 `filter: fx,`。

- [ ] **Step 2: 改 `TrainingPage` 的捕获状态**

`capturedThisSession` 的类型从 `MonsterDef[]` 改成 `CaptureResult[]`：

```ts
  const [capturedThisSession, setCapturedThisSession] = useState<CaptureResult[]>([])
```

import 里加 `import type { CaptureResult } from '../dex/capture'`。

彩蛋捕获那处（`void captureMonster('egg', ...)`）：

```ts
      void captureMonster('egg', todayStr, now).then((r) => {
        if (!r) return
        setCapturedThisSession((arr) => [...arr, r])
        setEggCaptureFx({ key: seqRef.current })
        // 储备怪（rarity !== common）加入对应世界的轮换池。
        // ⚠️ 闪光不进轮换池：这里用的是本体 def.id，闪光那一行只存在于图鉴。
        if (!r.shiny && r.def.rarity !== 'common') {
          setCapturedByWorld((prev) => ({ ...prev, [r.def.world]: [...prev[r.def.world], r.def.id] }))
        }
      })
```

保底捕获那处：

```ts
    let dailyCaptured: CaptureResult | null = null
    if (result.outcome === 'checked-in') {
      dailyCaptured = await captureDailyOnCheckin(false, roundDateRef.current, Date.now())
      if (dailyCaptured) {
        const d = dailyCaptured
        setCapturedThisSession((arr) => [...arr, d])
        if (!d.shiny && d.def.rarity !== 'common') {
          setCapturedByWorld((prev) => ({ ...prev, [d.def.world]: [...prev[d.def.world], d.def.id] }))
        }
      }
    }
```

- [ ] **Step 3: 改两处揭示卡的渲染**

文件里有**两处**结构相同的开箱区（不达标页与完成页各一处），都要改：

```tsx
              {capturedThisSession.map((r, i) => (
                <CapturedMonsterReveal key={`${r.def.id}-${r.shiny}-${i}`} def={r.def} shiny={r.shiny} delayMs={i * 500} />
              ))}
```

- [ ] **Step 4: 改 `CapturedMonsterReveal`**

```tsx
function CapturedMonsterReveal({ def, shiny, delayMs }: { def: MonsterDef; shiny: boolean; delayMs: number }) {
  const t = useT()
  const [revealed, setRevealed] = useState(false)
  useEffect(() => {
    const id = window.setTimeout(() => {
      setRevealed(true)
      // 闪光复用卡册那轮加的 'shiny' 音效——耳朵能立刻分出这次不一样；
      // 否则界面刚用金边分出的稀有度，在听觉上等于没分（与"超时音不能沿用答错音"同理）
      playSfx(shiny ? 'shiny' : def.rarity === 'common' ? 'egg' : 'badge')
    }, delayMs)
    return () => window.clearTimeout(id)
  }, [delayMs, def.rarity, shiny])
```

卡片外框在闪光时改成金边（把 `border` 那行替换）：

```tsx
          border: `2px solid ${shiny ? '#ffc83c' : RARITY_BORDER[def.rarity]}`,
          boxShadow: shiny ? '0 0 14px -2px rgba(255,200,60,0.9)' : undefined,
```

组件内渲染 `<MonsterImage def={def} />` 的地方补上 `shiny={shiny}`，并在名字下方（`{t(def.nameKey)}` 那一行的容器里）追加闪光标记：

```tsx
      {shiny && <span style={{ color: '#e0a400', fontWeight: 800 }}> ✨</span>}
```

- [ ] **Step 5: 用源文本契约锚住两条静默可回归的约束**

组件视觉在 node 环境下没有渲染基建可验，但本轮有两条约束**改错了不报错、只是悄悄变难看/变糊涂**，本项目一贯用 `?raw` 源文本契约锚这类东西（`challenge-shell.test.ts` / `settings-fold.test.ts` / `nav.test.ts` 都是这个套路）。新建 `src/dex/shiny-visual.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import monsterImageSrc from './MonsterImage.tsx?raw'
import trainingSrc from '../training/TrainingPage.tsx?raw'

describe('闪光视觉的源文本契约', () => {
  it('MonsterImage 不许用 hue-rotate 做闪光', () => {
    // 82 只怪配色各异，同一个色相旋转角度必然在其中一部分身上翻车，且没法逐只调。
    // 统一的加亮 + 金色描边光可预期得多，对像素风与插画风都成立。
    expect(monsterImageSrc).not.toMatch(/hue-rotate/)
  })

  it('MonsterImage 的 shiny 与调用方传入的 filter 是叠加，不是互相覆盖', () => {
    // 未捕获剪影传的是 brightness(0)；若 shiny 直接顶掉 filter，
    // 图鉴里没捕到的格子会在某些路径上露出彩色原图。
    expect(monsterImageSrc).toMatch(/shiny \? SHINY_FILTER : ''/)
    expect(monsterImageSrc).not.toMatch(/filter: filter\b/)
  })

  it('闪光揭示卡不复用普通捕获音（egg / badge）', () => {
    // 与"超时音不能沿用答错音"同理：界面刚用金边分出的稀有度，
    // 若耳朵里是同一个声音就等于没分。
    expect(trainingSrc).toMatch(/playSfx\(\s*shiny \? 'shiny'/)
  })
})
```

- [ ] **Step 6: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 3 结束时多 3 条。

Run: `npm run typecheck`
Expected: **无输出**——到这一步 Task 2/3 引入的类型波及才全部收口。

- [ ] **Step 7: 提交**

```bash
git add src/dex/MonsterImage.tsx src/dex/shiny-visual.test.ts src/training/TrainingPage.tsx
git commit -m "feat(图鉴): 闪光的金色微光与捕获揭示（复用 shiny 音效）"
```

---

## Task 5: 图鉴的 ✨ 角标、双形态与进度

**Files:**
- Modify: `src/dex/DexWall.tsx`
- Modify: `src/HomePage.tsx:183`
- Modify: `src/i18n.tsx`、`src/i18n.test.ts`

**Interfaces:**
- Consumes: `shinyIdOf` / `isShinyId`（Task 1）、`DexProgress.shinyOwned`（Task 3）、`MonsterImage` 的 `shiny` prop（Task 4）
- Produces: 无对外接口

- [ ] **Step 1: 写失败的测试**

`src/i18n.test.ts` 加常量与断言：

```ts
/** 闪光变体文案 */
const SHINY_KEYS = ['dex.shiny', 'dex.shinyProgress', 'dex.shinyToggle', 'dex.normalToggle', 'dex.newShiny']
```

```ts
  it('闪光变体 5 个文案键在 zh 与 en 都存在', () => {
    expect(SHINY_KEYS.length).toBe(5)
    for (const k of SHINY_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/i18n.test.ts`
Expected: FAIL —— 5 个键都不存在。

- [ ] **Step 3: 加 i18n（zh）**

`src/i18n.tsx` 的 zh 字典，在 `'dex.empty'` 之后加：

```ts
  'dex.shiny': '闪光',
  'dex.shinyProgress': '✨ 闪光 {n}/{total}',
  'dex.shinyToggle': '看闪光版 ✨',
  'dex.normalToggle': '看普通版',
  'dex.newShiny': '✨ 闪光怪兽！',
```

en 字典对应位置：

```ts
  'dex.shiny': 'Shiny',
  'dex.shinyProgress': '✨ Shiny {n}/{total}',
  'dex.shinyToggle': 'View shiny ✨',
  'dex.normalToggle': 'View normal',
  'dex.newShiny': '✨ Shiny monster!',
```

- [ ] **Step 4: `DexWall` 读闪光集合**

import 里加 `isShinyId, baseIdOf`（`DexWall` 不需要 `getDexProgress`——闪光数直接用下面这个集合的 size）。在 `capturedMap` 那个 state 旁加两个：

```tsx
  const [shinySet, setShinySet] = useState<Set<string>>(new Set())
  // 放大态里当前看的是哪一面；每次打开都从普通面开始
  const [zoomShiny, setZoomShiny] = useState(false)
```

在既有的 `getOwnedMonsters().then(...)` 里，除了填 `capturedMap`，同时填 `shinySet`：

```tsx
      const shiny = new Set<string>()
      for (const r of rows) if (isShinyId(r.id)) shiny.add(baseIdOf(r.id))
      setShinySet(shiny)
```

（import 里相应加 `isShinyId, baseIdOf`。）

打开放大态时复位到普通面——把 `onClick={() => owned && setZoom(def)}` 改成：

```tsx
                    onClick={() => { if (owned) { setZoomShiny(false); setZoom(def) } }}
```

- [ ] **Step 5: 格子加 ✨ 角标**

`MonsterCard` 的 props 加 `shinyOwned: boolean`，调用处传 `shinyOwned={shinySet.has(def.id)}`。

在格子最外层 div 里（它已经是定位上下文的话直接加；若不是，给它加 `position: 'relative'`）追加角标：

```tsx
      {shinyOwned && (
        <span
          title={t('dex.shiny')}
          style={{ position: 'absolute', top: 4, right: 6, fontSize: 13, filter: 'drop-shadow(0 0 3px rgba(255,200,60,0.9))' }}
        >
          ✨
        </span>
      )}
```

- [ ] **Step 6: 放大态支持双形态**

modal 里把 `<MonsterImage def={zoom} />` 改成 `<MonsterImage def={zoom} shiny={zoomShiny} />`，并在稀有度徽章下方、`dex.capturedAt` 上方插入切换按钮（**只有拥有该怪闪光时才渲染**）：

```tsx
            {shinySet.has(zoom.id) && (
              <button
                className="fq-btn"
                style={{ marginTop: 10 }}
                onClick={() => setZoomShiny((v) => !v)}
              >
                {zoomShiny ? t('dex.normalToggle') : t('dex.shinyToggle')}
              </button>
            )}
```

- [ ] **Step 7: 进度加第二个数字**

总进度那一行是个 `justifyContent: 'space-between'` 的 flex，现有两个 span（进度文案、百分比）。在两者之间插入第三个 span，三项自然分列左中右：

```tsx
          <span>{t('dex.progress', { n: ownedCount, total: TOTAL_MONSTERS })}</span>
          {shinySet.size > 0 && (
            <span style={{ color: '#e0a400' }}>
              {t('dex.shinyProgress', { n: shinySet.size, total: TOTAL_MONSTERS })}
            </span>
          )}
          <span style={{ color: 'var(--violet)' }}>{pct}%</span>
```

闪光数直接用 `shinySet.size`，不必再查一次库。

⚠️ **`ownedCount` 也要修**：它现在是 `Object.keys(capturedMap).length`，而 `capturedMap` 里混着闪光行，会把「95/82」显示出来。改成只数本体：

```tsx
  const ownedCount = Object.keys(capturedMap).filter((id) => !isShinyId(id)).length
```

- [ ] **Step 8: 首页图鉴入口显示闪光数**

`src/HomePage.tsx:183` 那行是 `getDexProgress().owned` 的**唯一消费者**——也就是 Task 3 修的那个口径，不修的话这里会显示「95/82」。同时 `shinyOwned` 必须在这里落地，否则它是个没人读的死字段。

把第 183 行那个 div 的内容改成：

```tsx
              {dex ? `${dex.owned}/${dex.total}` : '—'}
              {dex && dex.shinyOwned > 0 && (
                <span style={{ marginLeft: 6, color: '#e0a400', fontWeight: 800 }}>✨{dex.shinyOwned}</span>
              )}
```

（入口方块窄，所以只跟一个「✨N」，不写完整的 `dex.shinyProgress` 文案。）

- [ ] **Step 9: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 4 结束时多 1 条。

Run: `npm run typecheck`
Expected: 无输出。

Run: `npm run build`
Expected: 成功。

- [ ] **Step 10: 浏览器里真验一遍**

Run: `npm run dev`，然后在控制台造数据：

```js
const db = (await import('/src/data/db.ts')).db
const { MONSTER_DEFS, shinyIdOf } = await import('/src/dex/monster-defs.ts')
await db.monsters.clear()
// 前 5 只本体 + 其中 2 只的闪光
for (const m of MONSTER_DEFS.slice(0, 5)) await db.monsters.put({ id: m.id, capturedAt: Date.now(), source: 'daily' })
for (const m of MONSTER_DEFS.slice(0, 2)) await db.monsters.put({ id: shinyIdOf(m.id), capturedAt: Date.now(), source: 'daily' })
location.reload()
```

逐项确认：

1. 图鉴总进度显示 **5/82**（不是 7/82——闪光不计入本体计数）
2. 旁边出现 **✨ 闪光 2/82**
3. 前 2 只格子右上角有 ✨ 角标，第 3-5 只没有
4. 点第 1 只 → 放大态出现「看闪光版 ✨」按钮；点它，图变成**金色微光**版本，按钮文案变「看普通版」
5. 点第 3 只（无闪光）→ **没有**切换按钮
6. 关掉再打开第 1 只 → 回到普通面（不残留上次的闪光态）
7. 回首页，图鉴入口显示 **5/82** 与 **✨2**（不是 7/82）
8. DevTools Console 无 error

- [ ] **Step 11: 提交**

```bash
git add src/dex/DexWall.tsx src/HomePage.tsx src/i18n.tsx src/i18n.test.ts
git commit -m "feat(图鉴): 闪光的 ✨ 角标、放大态双形态与独立进度"
```

---

## 收尾检查

- [x] `npm test` 全绿，总数 = 基线 + **23**（Task 1 加 6、Task 2 加 7、Task 3 加 4、Task 4 加 3、Task 5 加 1，计 21；Task 2 另有 16 条既有用例被改写而非新增；合并前评审又追加 2 条源文本契约测试——✨ 角标 `owned` 门控 1 条、`isComplete` 本体×闪光双重门控 1 条）。基线 697 → 终点 **720 个 / 74 文件**
- [x] `npm run typecheck` 与 `npm run typecheck:functions` 都无输出
- [x] `npm run build` 成功
- [x] **同步层零改动**——确认 `src/sync/` 下一个文件都没被碰过（`git diff --stat` 里不该出现 `src/sync/`）。闪光靠独立行天然走通既有的 `monster` kind
- [x] 按 [CLAUDE.md](CLAUDE.md) 的惯例把本迭代写进「当前状态」，含「闪光是独立行不是行内字段」与「`SHINY_CHANCE` 只控惊喜密度不控总时长」两条口径
