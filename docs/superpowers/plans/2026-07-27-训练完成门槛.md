# 训练完成门槛 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 堵住「挂机 3 分钟照样打卡拿分续连续天数」这个漏洞——**一整轮训练（左眼一节 + 右眼一节）的总答对数**按时长比例要求一个最低值（每分钟 5 个 × 单眼分钟数 × 2 只眼，1/2/3/5 分钟档 → **10/20/30/50**），不达标就不打卡、不发积分、连续天数不续，结算页明确告知差多少并给「再练一轮」按钮。配套把设置页里「影响训练量」的两项（单眼时长 / 视标大小）折叠起来，让孩子不会随手把训练量调低。

**Architecture:** 新增 `src/training/goal.ts`（门槛的全部常量与纯函数 + 唯一的时长读取口 `readDurationSec`），闸门本身放进 `src/data/checkin.ts` 的 `doCheckIn()`——那是唯一写 `checkins` 表的正规路径，放 UI 层会被绕过。**判定口径是「两眼合计」**（用户 2026-07-27 拍板）：判据就是 `doCheckIn` 本来就在算的那个当天 `correctToday`，所以判定与发分天然同源、`doCheckIn` 的调用点一个都不用加——判定只发生在右眼节末点「完成这一轮 →」那一次。`CheckinResult` 从"两态布尔"改成三态 `outcome: 'checked-in' | 'already' | 'below-goal'`，`TrainingPage` 按 `outcome` 分流出一个新的不达标结算页并提供 `restartRound()` 就地复位（**刻意不用 `key` 重挂载**——那会 stop 掉 vosk 再重启，正是 commit `89743b6` 修过的换眼白屏内存尖峰路径）。配套堵住补签卡这条付费绕过通道（`canRepair` 新增 `'attempted'` 闸门），以及把「⏱️ 单眼时长」「👁️ 视标大小」挪进已有的 `<Collapsible>`（`src/settings/Collapsible.tsx`）。

**Tech Stack:** TypeScript + React 18 + Vite 6；Dexie（IndexedDB）；vitest（`environment: 'node'`，无 jsdom，`globals: false`）+ `fake-indexeddb`；i18n 走 `src/i18n.tsx` 的 ZH/EN 双字典。

## Global Constraints

- 交流、代码注释、提交信息一律**简体中文**；技术术语与代码标识符保留英文。
- **门槛的产品语义（不可动摇）：判定的是「有没有真练」，不是「练得好不好」。** 只挡挂机，不评判表现——所以不看正确率、不看 CPM、不看反应时间，数值刻意定得低。
- **不惩罚慢的孩子。** 调节灵敏度差的孩子本来就慢，而那正是最需要训练的。门槛按时长等比缩放、且只要求现实产出的 20-35%。**禁止**把门槛改成速率型（CPM / 每分钟答对率），那等于对最该练的孩子施加最严标准。
- **判定口径 = 两眼合计（用户 2026-07-27 拍板，不可改成单眼各判）。** 一整轮 = 左眼一节 + 右眼一节，**总答对数**达标即算完成；门槛因此是「每分钟 5 个 × 单眼分钟数 × **2 只眼**」= 1/2/3/5 分钟档 → **10/20/30/50**。理由：某只眼状态差不该让整次白练（弱眼天生慢，而那正是最该练的一只）。**唯一判定用的数是 `goalPerRound()`；`goalPerEye()` 只是算式的中间量，任何地方都不许拿它去比某一节的答对数。**
- **判定时机 = 只在右眼节结束、点「完成这一轮 →」那一次。** 左眼节结束照旧「本节完成 → 换右眼继续」，**不判门槛、不查累计、不提差额**。`doCheckIn` 的调用点一处都不加——本计划只是在它内部加闸门。
- 📌 **勘察风险 #1「门槛判本节、发分算全天 → 两把尺子」因两眼合计而自动消解**，不需要任何额外代码：`doCheckIn` 本来就在用 `db.sessions.where('date').equals(today)` 求和算 `correctToday` 来发分，判定直接复用同一个数。别再为它设计什么"对齐机制"。
- **门槛数值集中在 `src/training/goal.ts` 一处常量。** 调松紧只改 `GOAL_CORRECT_PER_MIN`；要完全回退本功能须把 `GOAL_CORRECT_PER_MIN` **和** `GOAL_MIN_PER_EYE` 同时设 0（只设前者会被后者的下限顶住）。
- **判定用「当天全部 sessions 的 correct 之和」**，与 `dailyPoints` 的取数完全同源（`checkin.ts` 本来就这么算分）。禁止改成"只看刚练完这两节"——那会变成两把尺子。
- **门槛的「时长」基准取「当天真实练过的最长一节 `elapsedSec`」，当天没练过才用当前设置**（spec §3.5，评审 blocker）。`fzp.durationSec` 就是设置页那四个按钮、而设置页在常驻导航里，孩子随手可点：只按当前设置算，就能"练完一轮不达标 → 把档位调到 1 分 → 门槛从 30 变 10 → 已练的答对数照样算 → 下次打卡直接过"。这条算法只有一个出处 `goalForDay()`，**`doCheckIn` / `getHomeStats` / 补签闸门三处必须都走它**。
- **补签闸门的判据是「那天确实没练够」，不是「那天有没有 session 行」**（spec §5.6，评审 major）。`saveSession` 只在计时走满时落库，所以"有行"其实等价于"完整走完过一节"——用它当判据会把"练到 40 个却没点完成键"的日子也堵掉（那是最该补的一天），同时放过"点开就退出"的日子（一行都不落）。
- **判定顺序硬定为 `already → below-goal → checked-in`。** 今天已打过卡必须先短路返回，否则"第一轮达标已打卡、第二轮又练但合计不够"会被门槛误判成打卡失败，把到手的连续天数吓没。
- **不达标的 session 照常落库、照常推云**，统计/周报/管理后台 DAU 一律不改口径（训练量是事实，完成度是判定，两个维度）。
- **不达标时勋章照常判定并落库**（`syncBadges` 必须在分流之前调用），但不放完成庆祝、只给一行低调提示。理由：勋章判据来自 `sessions` 的跨天累计，与"今天算不算完成"无关；延后调用会让 `unlockedAt` 失真，而 CLAUDE.md 明写"勋章/怪兽取最早——首次达成时刻才是正确语义"。
- **不达标时不发保底怪、不算皮肤解锁、不放 `'checkin'` 音效。**
- **积分不做任何"补发/重算"。** 撞 `src/sync/reconcile.ts` 写死的"dailyPoints 一律沿用行内原值"硬口径；积分是能换现实奖励与补签卡的货币。
- 测试文件**必须以 `.test.ts` 结尾**——`vitest.config.ts` 的 `include` 只收 `src/**/*.test.ts` 与 `functions/**/*.test.ts`，`.test.tsx` 不会被跑。
- `vitest.config.ts` 是 `globals: false` + `environment: 'node'`：每个测试文件顶部必须 `import { describe, it, expect } from 'vitest'`；**没有 jsdom，组件不可渲染测试**，UI 只能用 `?raw` 源文本契约测试 + 确切手测步骤兜底。
- 本仓**没装 `@types/node`**：测试里要读源文件文本请用 Vite 的 `?raw`（如 `import page from './TrainingPage.tsx?raw'`），用 `node:fs` 会让 `tsc` 报 TS2307。
- i18n 新增文案必须 **ZH/EN 双份**（缺 en 会静默回退成中文），并同步 `src/i18n.test.ts` 的键齐全性断言。`translate()` 的 `fill()` 用 `String.replace`，**同一个占位符只替换第一次出现**——本计划每条文案里每个占位符都只出现一次。
- **`src/challenge/ChallengePage.tsx` 一个字都不许改，也不许 import `goal.ts`。** `src/challenge/challenge-shell.test.ts:45` 是一条读 `ChallengePage.tsx?raw` 的黑名单契约测试（含 `'doCheckIn'`、`/\bpush[A-Z]\w*\(/`、`/db\.\w+\.(put|add|bulkPut|…)\b/`），被误伤时的报错信息看起来完全不相干、排查成本极高。
- **提交时只 `git add` 本任务列出的确切文件路径**，禁止 `git add -A` / `git add <目录>`（本仓发生过并行 agent 用宽泛 add 卷走并丢失他人改动的事故）。
- 每个任务收尾都要跑 `npm test` **和** `npm run typecheck`，两者都干净才提交。**唯一例外：Task 2**（它把 `CheckinResult` 改成三态，而调用点 `TrainingPage` 要到 Task 5 才跟进）。这条例外是显式授权的，配套两道护栏：
  1. **全程在特性分支上做**（Step 0.5 建分支），中间那次断裂关在分支里；
  2. **Step 5.6 的 typecheck 变干净之前，一律不许 `git push`**。`.github/workflows/deploy-cf.yml` 在 push 时依次跑 `npm test` / `typecheck` / `typecheck:functions` / `build`，Task 2 之后 push 会让整个 job 红、线上停在旧版本。
  除此之外**不许**在 Task 2 里顺手改 `TrainingPage`（那会让两个任务的 diff 纠缠）。
- 测试数量一律写成「基线 + 本任务新增」，**开工前先自己跑 `npm test` 记基线**，不要信 CLAUDE.md 里的历史快照（2026-07-27 当天的快照是 59 文件 / 562 测试，仅供对照）。

## 开工前必做

- [ ] **Step 0.1: 记基线。** 跑 `npm test`，把末尾两行 `Test Files  N passed (N)` / `Tests  M passed (M)` 抄下来，记作 **基线 = N 文件 / M 测试**。本计划所有测试数字都相对它。
- [ ] **Step 0.2: 确认 typecheck 干净起步。** 跑 `npm run typecheck`，必须零输出（有报错先停手，不是本计划引入的）。
- [ ] **Step 0.3: 读 spec 一遍。** `docs/superpowers/specs/2026-07-27-训练完成门槛-design.md`。特别是 **§3.5（门槛为什么必须按"真实练过的时长"算 —— 评审 blocker）**、**§4（两眼合计判的理由）与 §4.4（为什么只在右眼节末判一次）**、§4.3 末（发分不对称与那个封顶阀门）、§5.4（勋章为什么照常解锁）、**§5.6（补签闸门为什么不能用"那天有没有 session 行"当判据）**、§8（重来为什么不用 `key` 重挂载 / §8.5 跨零点）、**§7.7（设置页折叠的范围与定性）**。
- [ ] **Step 0.4: 确认工作区干净。** 跑 `git status`，应为 clean（本仓可能有并行 agent，脏工作区会让"只 add 本任务文件"的纪律失效）。

- [ ] **Step 0.5: 建特性分支（必做，别在 master 上逐任务提交）。**
  ```
  git switch -c feat/training-goal-gate
  ```
  理由：Task 2 会留下一次 typecheck 红的中间提交（`CheckinResult` 改三态、调用点 Task 5 才跟进）。本仓 CI（`.github/workflows/deploy-cf.yml`）在 push master 时跑 test/typecheck/typecheck:functions/build，那次断裂一旦 push 就会让 job 整体失败、线上停在旧版本。分支把它关住；Task 7 收尾时再合回 master。

- [ ] **Step 0.6: 一个产品决策点——先问用户，拿到明确答复再动 Task 2。**
  （原本有两条；**第 2 条已于 2026-07-27 由用户拍板**，见本步末尾。剩下这条是**货币语义**，不能埋在实现里默默定了，spec §4.3 已写清。）

  1. **一天两轮的发分不对称**（本次改动新引入）。`dailyPoints = floor((答对×5 + 30) × coef)`：
     - 第一轮 40（过门槛 30）→ 第二轮 40：只按第一轮结算 = **230 分**
     - 第一轮 29（差 1 个）→ 第二轮 40：按当天累计 69 结算 = **375 分（+63%）**

     即"多练**只有先失败才**发分"，而积分能换现实奖励与补签卡（50 分/次）。
     **默认方案 = 保留现状**（`POINTS_CORRECT_CAP_FACTOR = 0`，不封顶）：要吃到差价得先坐满 6 分钟故意不答、再认真练 6 分钟，成本 12 分钟；且"第二轮白练"本来就是既有行为。
     若用户不接受 → 把 `goal.ts` 的 `POINTS_CORRECT_CAP_FACTOR` 改成 **6**（3 分钟档 → 封在 180，高于一轮的现实上限 80-160，只削掉叠加那部分）。⚠️ **不要用评审建议的 2**：3 分钟档门槛只有 30，封在 60 会把诚实一轮的当日积分从 430-830 砍到 330，属于严重误伤。

  把用户的答复原样记在本节下方，再继续。

  ---

  **✅ 已拍板（2026-07-27，无需再问）——原第 2 条「长期把「单眼时长」钉在 1 分钟」：**
  用户决定**做**，方式是**降低可达性而非加口令**：把「⏱️ 单眼时长」与「👁️ 视标大小」挪进已有的 `<Collapsible>`（`src/settings/Collapsible.tsx`），孩子不会随手碰到、家长展开即可。**已纳入本计划的 Task 7**（spec §7.7）。
  另一半（**事后**改档不能追溯降低当天门槛）本来就在计划里，见 Task 1 的 `goalForDay`。

  **✅ 同批拍板——判定口径 = 两眼合计（已贯穿全计划）：**
  一整轮的**总答对数**达标即完成，门槛 10/20/30/50。**这一条已让勘察风险 #1「判定与发分两把尺子」自动消解**，实现时别再另外设计"对齐机制"。

---

## File Structure

**新建**

| 路径 | 职责 |
|---|---|
| `src/training/goal.ts` | 门槛的**全部**常量 + 纯函数 `sanitizeDurationSec` / `goalPerEye`（中间量） / **`goalPerRound`（唯一判定用）** / **`goalForDay`** / **`goalForPastDay`** / **`dayFellShort`** / `shortfall` / `meetsGoal` / **`pointsCorrect`** + 薄包装 `readDurationSec` |
| `src/training/goal.test.ts` | 上述纯函数单测（含两眼合计的 10/20/30/50、脏 `durationSec` 钳制、"事后改档不降门槛"、`dayFellShort` 三态、封顶开关） |
| `src/data/checkin.test.ts` | `doCheckIn` 三态的 IndexedDB 集成单测（本仓 `doCheckIn` 此前**零单测**），含**两眼合计的决定性用例**、"练完再调小档位不降门槛"与发分不对称的数值锚定 |
| `src/training/goal-gate.test.ts` | `?raw` 源文本契约：`doCheckIn` 带真实时长 / 一轮共用 `roundDateRef` / below-goal 分支先 return / 保底怪与皮肤只在 checked-in / `restartRound` 复位清单齐全 / App 不给 TrainingPage 加 key |
| `src/settings/settings-fold.test.ts` | `?raw` 源文本契约（Task 7）：「视标大小」「单眼时长」两张卡都在 `<Collapsible title={t('settings.trainingLoad')}>` 内、折叠默认收起 |

**修改**

| 路径 | 改动 |
|---|---|
| `src/data/checkin.ts` | `CheckinResult` 改三态（删 `alreadyCheckedIn`，加 `outcome` / `correctToday` / `goal`）；`doCheckIn(today, durationSec)` 加门槛闸门（门槛走 `goalForDay`，发分过 `pointsCorrect`）；`HomeStats` 加 `correctToday` / `goalToday`，`getHomeStats(today, durationSec?)` |
| `src/rewards/ledger.ts` | `RepairReason` 加 `'attempted'`；`canRepair` 加可选入参 `targetAttempted` 并排在第二位判定 |
| `src/rewards/ledger.test.ts` | 新增一个 `describe`（5 条） |
| `src/rewards/rewards-service.ts` | 新增 `fellShortOn()`（查缺口日 sessions → `dayFellShort`）；`getRepairStatus` / `doRepair` 把 `targetAttempted` 传给 `canRepair` |
| `src/i18n.tsx` | ZH/EN 各加 14 条（11 条 `goal.*` + `home.partial` + `repair.attempted` + `train.finishRound`），删 `train.finishCheckin` |
| `src/i18n.test.ts` | 新增 `GOAL_KEYS` 常量 + 一个齐全性 `it` |
| `src/training/TrainingPage.tsx` | 删私有 `DURATION_SEC` / `readDurationSec`，改用 `goal.ts`；加 `onHome` prop；加 `roundDateRef`（一轮锁一个日期，跨零点）；`nextEyeOrFinish` 按 `outcome` 分流；新增 `restartRound` 与不达标结算页；`checkin.alreadyCheckedIn` → `checkin.outcome === 'already'`；右眼末按钮改 `train.finishRound` |
| `src/App.tsx` | `<TrainingPage onHome={() => setView('home')} />` |
| `src/HomePage.tsx` | `getHomeStats` 传 `readDurationSec()`；今日状态行加第三态；补签横幅加 `'attempted'` 提示 |
| `src/SettingsPage.tsx` | 时长 state 初值走 `sanitizeDurationSec`；时长档位卡底部加门槛提示行 |
| `CLAUDE.md` | 状态区加「训练完成门槛 ✅」条目；「下一步」补一句实测校准 |

---

### Task 1: 门槛纯函数（数值 + 脏时长钳制）

**Files**
- Create: `E:\developer\fzp\src\training\goal.ts`
- Test: `E:\developer\fzp\src\training\goal.test.ts`

**Interfaces**
- Consumes: `lsGet`（`src/data/storage.ts`）
- Produces:
  ```ts
  export const DEFAULT_DURATION_SEC = 180
  export const GOAL_CORRECT_PER_MIN = 5
  export const GOAL_MIN_PER_EYE = 5
  export const DURATION_MIN_SEC = 60
  export const DURATION_MAX_SEC = 300
  export const EYES_PER_ROUND = 2
  export const POINTS_CORRECT_CAP_FACTOR = 0
  export function sanitizeDurationSec(sec: number): number
  export function goalPerEye(durationSec: number): number
  export function goalPerRound(durationSec: number): number
  export function goalForDay(durationSec: number, sessionElapsedSecs: number[]): number
  export function goalForPastDay(sessionElapsedSecs: number[]): number
  export function dayFellShort(correctSum: number, sessionElapsedSecs: number[]): boolean
  export function shortfall(correctToday: number, goal: number): number
  export function meetsGoal(correctToday: number, goal: number): boolean
  export function pointsCorrect(correctToday: number, goal: number, factor?: number): number
  export function readDurationSec(): number
  ```

- [ ] **Step 1.1: 写失败测试。** 新建 `E:\developer\fzp\src\training\goal.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import {
  DEFAULT_DURATION_SEC, GOAL_CORRECT_PER_MIN, GOAL_MIN_PER_EYE,
  DURATION_MIN_SEC, DURATION_MAX_SEC, EYES_PER_ROUND, POINTS_CORRECT_CAP_FACTOR,
  sanitizeDurationSec, goalPerEye, goalPerRound, goalForDay, goalForPastDay,
  dayFellShort, shortfall, meetsGoal, pointsCorrect, readDurationSec,
} from './goal'

describe('sanitizeDurationSec', () => {
  it('设置页的四个合法档位原样通过', () => {
    for (const s of [60, 120, 180, 300]) expect(sanitizeDurationSec(s)).toBe(s)
  })

  it('NaN / Infinity / 0 / 负数一律回落到默认 180', () => {
    // fzp.durationSec 是无校验的 localStorage：Number('abc')=NaN、Number('0')=0。
    // 若不钳，NaN 会让所有比较为 false（门槛静默全放行）、0 会让门槛变 0（同样失效）。
    for (const bad of [NaN, Infinity, -Infinity, 0, -5]) {
      expect(sanitizeDurationSec(bad), `脏值 ${bad} 没被兜住`).toBe(DEFAULT_DURATION_SEC)
    }
  })

  it('区间外的正数钳到 [60, 300]（= 设置页四档的上下界）', () => {
    expect(sanitizeDurationSec(30)).toBe(DURATION_MIN_SEC)
    expect(sanitizeDurationSec(9999)).toBe(DURATION_MAX_SEC)
  })
})

describe('goalPerEye / goalPerRound（用户拍板的数值）', () => {
  it('1/2/3/5 分钟 → 单节 5/10/15/25 个', () => {
    expect(goalPerEye(60)).toBe(5)
    expect(goalPerEye(120)).toBe(10)
    expect(goalPerEye(180)).toBe(15)
    expect(goalPerEye(300)).toBe(25)
  })

  it('整次门槛 = 单节 × 2（判定用的就是这个数：一轮 = 左眼一节 + 右眼一节）', () => {
    expect(EYES_PER_ROUND).toBe(2)
    expect(goalPerRound(60)).toBe(10)
    expect(goalPerRound(120)).toBe(20)
    expect(goalPerRound(180)).toBe(30)
    expect(goalPerRound(300)).toBe(50)
  })

  it('脏时长走钳制后的默认值，绝不会算出 0 或 NaN（0 门槛等于全放行）', () => {
    for (const bad of [NaN, 0, -1, Infinity]) {
      expect(goalPerRound(bad), `脏值 ${bad} 让门槛失效了`).toBe(30)
    }
  })

  it('下限兜底：极短时长也至少 GOAL_MIN_PER_EYE', () => {
    expect(goalPerEye(30)).toBe(GOAL_MIN_PER_EYE)
  })

  it('速率常量就是每分钟 5 个——调松紧只改这一处', () => {
    expect(GOAL_CORRECT_PER_MIN).toBe(5)
  })
})

describe('goalForDay：门槛按「当天真实练过的时长」算（堵住事后改档追溯降门槛）', () => {
  it('当天还没练过 → 按当前设置算', () => {
    expect(goalForDay(180, [])).toBe(30)
    expect(goalForDay(60, [])).toBe(10)
  })

  it('5 分钟档练完一轮后把设置改成 1 分钟，当天门槛仍是 50（本条是 blocker 的闸门）', () => {
    // 孩子随手就能点设置页的「1分」：只按当前设置算，就能"练完不达标 → 调小档位 →
    // 门槛从 50 掉到 10 → 已练的答对数照样算 → 下次打卡直接过"。
    expect(goalForDay(60, [300, 300])).toBe(50)
  })

  it('反向也不追溯：家长中途把 1 分改成 5 分，当天已练的 1 分钟节仍按 10 判', () => {
    // 若按当前设置算，门槛会从 10 跳到 50，当天已经练过的轮次全部作废。
    expect(goalForDay(300, [60, 60])).toBe(10)
  })

  it('一天里长短节混着练时按最长那节要求（避免"先长后短"稀释门槛）', () => {
    expect(goalForDay(60, [180, 60])).toBe(30)
  })

  it('脏 elapsedSec（0 / NaN）被忽略，不会把门槛拉低', () => {
    expect(goalForDay(180, [0, NaN, -5])).toBe(30)
    expect(goalForDay(60, [NaN, 300])).toBe(50)
  })

  it('goalForPastDay 只看那天练过的时长，绝不掺入"现在"的设置', () => {
    expect(goalForPastDay([180, 180])).toBe(30)
    expect(goalForPastDay([60])).toBe(10)
  })
})

describe('dayFellShort：补签闸门的判据（"那天确实没练够"，不是"那天有记录"）', () => {
  it('那天压根没练 → false（可补，这才是补签卡的设计意图）', () => {
    expect(dayFellShort(0, [])).toBe(false)
  })

  it('那天练了但没练够 → true（不可补，否则 50 分就能架空门槛）', () => {
    expect(dayFellShort(12, [180, 180])).toBe(true)
  })

  it('那天练够了却没点完成键（没打卡行）→ false，仍可补', () => {
    // saveSession 只在计时走满时落库，所以"有 session 行"其实等价于"完整走完过一节"，
    // 拿它当判据会把最该补的一天（练到 40 个却被收走 iPad）堵掉。
    expect(dayFellShort(40, [180, 180])).toBe(false)
    expect(dayFellShort(30, [180, 180])).toBe(false) // 边界正好在等号上
  })
})

describe('pointsCorrect（发分封顶开关，默认关）', () => {
  it('默认 factor 为 0 = 不封顶，原样返回（现状行为）', () => {
    expect(POINTS_CORRECT_CAP_FACTOR).toBe(0)
    expect(pointsCorrect(69, 30)).toBe(69)
  })

  it('factor > 0 时按 goal × factor 封顶——用于用户拍板要削「先失败再补够多发 63%」时', () => {
    expect(pointsCorrect(69, 30, 6)).toBe(69)   // 未到 180，不动
    expect(pointsCorrect(200, 30, 6)).toBe(180) // 封到 180
  })
})

describe('shortfall / meetsGoal', () => {
  it('达标边界正好在等号上', () => {
    expect(meetsGoal(29, 30)).toBe(false)
    expect(meetsGoal(30, 30)).toBe(true)
    expect(meetsGoal(31, 30)).toBe(true)
  })

  it('shortfall 不为负', () => {
    expect(shortfall(12, 30)).toBe(18)
    expect(shortfall(30, 30)).toBe(0)
    expect(shortfall(45, 30)).toBe(0)
  })

  it('答对数是 NaN 时按最不利处理（不放行、差额=整个门槛）', () => {
    expect(meetsGoal(NaN, 30)).toBe(false)
    expect(shortfall(NaN, 30)).toBe(30)
  })
})

describe('readDurationSec（薄包装）', () => {
  it('localStorage 读不到时回落到默认 180，而不是让门槛变成 NaN', () => {
    // 本仓 vitest 是 environment: 'node'，没有 localStorage → lsGet 内部 try/catch 返回 null。
    // 这条同时覆盖真实的隐私模式 / storage 受限场景：那时门槛绝不能跟着失效。
    expect(readDurationSec()).toBe(DEFAULT_DURATION_SEC)
  })
})
```

- [ ] **Step 1.2: 跑到失败。** `npm test -- src/training/goal.test.ts`
  预期失败信息：`Error: Failed to load url ./goal (resolved id: .../src/training/goal) ... Does the file exist?` —— 因为 `src/training/goal.ts` 还不存在。

- [ ] **Step 1.3: 实现。** 新建 `E:\developer\fzp\src\training\goal.ts`：

```ts
import { lsGet } from '../data/storage'

/**
 * 训练完成门槛——「这一天到底有没有真练」的唯一判据（spec §3）。
 *
 * 定位：**只挡挂机，不评判表现。** 所以这里没有正确率、没有 CPM、没有反应时间——
 * 那些是"练得好不好"，属于统计页与家长周报的职责，进了门槛就会变成惩罚慢的孩子，
 * 而调节灵敏度差的孩子本来就慢、恰恰是最该练的那一个。
 *
 * 数值推导：一个循环（看清 1-3 秒 + 报答案 + 翻拍过渡 0.6-1.5 秒）约 2-5 秒，
 * 3 分钟一节的现实产出是 40-80 个答对，门槛 15 只要求其中 20-35%
 * ——12 秒才要一个，只有"三分之二的时间在发呆都做不到"的人才会被拦。
 *
 * ⚠️ 调参只改 GOAL_CORRECT_PER_MIN 一处。要**完全回退**本功能，必须把
 * GOAL_CORRECT_PER_MIN 与 GOAL_MIN_PER_EYE **同时**设 0——只设前者会被后者的下限顶住。
 */

/** 单眼时长默认值（秒）。原先是 TrainingPage 私有的 DURATION_SEC，搬到这里做唯一出处 */
export const DEFAULT_DURATION_SEC = 180
/** 门槛速率：每分钟需答对几个 */
export const GOAL_CORRECT_PER_MIN = 5
/** 单节门槛下限（防极短时长算出 0） */
export const GOAL_MIN_PER_EYE = 5
/** 时长钳制区间 = 设置页四个档位 [60,120,180,300] 的上下界 */
export const DURATION_MIN_SEC = 60
export const DURATION_MAX_SEC = 300
/** 一轮训练的眼数（左 + 右） */
export const EYES_PER_ROUND = 2
/**
 * 当日结算的答对数封顶倍数（封在 goal × factor）。**0 = 不封顶，即现状行为。**
 *
 * 背景（spec §4.3）：加门槛后"第一轮不达标、第二轮补够"那天会按**当天累计**结算，
 * 而"第一轮就达标"那天只按第一轮结算 —— 同样练两轮，前者 375 分、后者 230 分（+63%）。
 * 方向是"多练只有先失败才发分"，而积分能换现实奖励与补签卡。
 *
 * ⚠️ 真要开这个开关，factor 取 **6** 左右（3 分钟档 → 封在 180，高于一轮的现实上限
 * 80-160）。**不要取 2**：3 分钟档门槛只有 30，封在 60 会把诚实一轮的当日积分从
 * 430-830 砍到 330，属于严重误伤诚实训练。
 */
export const POINTS_CORRECT_CAP_FACTOR = 0

/**
 * 把脏 localStorage 值钳成可用秒数。
 * fzp.durationSec 无任何校验：`Number('abc')=NaN`、`Number('0')=0`。
 * 不兜住的话 NaN 会让所有门槛比较为 false（**静默全放行**），0 会让门槛变 0（同样失效）。
 */
export function sanitizeDurationSec(sec: number): number {
  if (!Number.isFinite(sec) || sec <= 0) return DEFAULT_DURATION_SEC
  return Math.min(Math.max(sec, DURATION_MIN_SEC), DURATION_MAX_SEC)
}

/** 单眼一节的门槛：round(分钟 × 速率)，不低于下限 */
export function goalPerEye(durationSec: number): number {
  const sec = sanitizeDurationSec(durationSec)
  return Math.max(GOAL_MIN_PER_EYE, Math.round((sec / 60) * GOAL_CORRECT_PER_MIN))
}

/**
 * 整次（一轮双眼）门槛——**判定用的就是这个数**。
 * 刻意不做单眼各判：弱眼天生慢，各判等于对最该练的那只眼施加最严标准；
 * 而且某只眼状态差就整轮作废、要重练 6 分钟，会直接毁掉坚持引擎（spec §4.2）。
 */
export function goalPerRound(durationSec: number): number {
  return goalPerEye(durationSec) * EYES_PER_ROUND
}

/**
 * 某一天的门槛（spec §3.5）——**当天真实练过的最长一节说话，不是"现在设置的是几分钟"**。
 *
 * 为什么不能只看当前设置：`fzp.durationSec` 就是设置页「⏱️ 单眼时长」那四个按钮，
 * 而「⚙️ 设置」在常驻导航里、孩子随手可点。只按当前设置算就有两个方向的错：
 *  - 追溯降门槛（真漏洞）：3 分钟档练完只拿到 12 个（门槛 30）→ 点一下「1分」→ 门槛变 10
 *    → correctToday 里那 12 个照样算 → 下次 doCheckIn 直接打卡成功；
 *  - 追溯抬门槛（误伤家长）：中途把 1 分改成 5 分 → 门槛 10 跳到 50，当天已练的全作废。
 *
 * elapsedSec 靠得住的原因：saveSession 只在 phase==='finished' 落库，而 tick()
 * 到点时把 elapsedSec 钳成 durationSec（src/training/session.ts:73-74），
 * 所以它精确等于那一节当时的档位，事后改设置改不动它。
 * 取 max 而非 min/平均：长短节混练时按最长那节要求，避免"先长后短"稀释门槛。
 */
export function goalForDay(durationSec: number, sessionElapsedSecs: number[]): number {
  const practiced = sessionElapsedSecs.filter((s) => Number.isFinite(s) && s > 0)
  if (practiced.length === 0) return goalPerRound(durationSec)
  return goalPerRound(Math.max(...practiced))
}

/**
 * 过去某一天的门槛：只看那天真实练过的时长。
 * 第一个实参在 practiced 非空时用不上——但补签闸门永远在"那天有 session 行"之后才调，
 * 传 DEFAULT_DURATION_SEC 只是给空数组一个保守兜底，绝不会把"现在"的设置掺进历史判定。
 */
export function goalForPastDay(sessionElapsedSecs: number[]): number {
  return goalForDay(DEFAULT_DURATION_SEC, sessionElapsedSecs)
}

/**
 * 那天是不是「练了但没练够」——补签闸门的唯一判据（spec §5.6）。
 *
 * ⚠️ 刻意**不是**"那天有没有 session 行"。saveSession 只在计时走满时落库，所以"有行"
 * 真正等价的是"完整走完过一节"：
 *  - 练到 40 个（远超门槛 30）却没点完成键就被收走 iPad → 有行、无打卡行 →
 *    若按"有行就不可补"，最该补的一天反而被堵死；
 *  - 中途退出不满一节 → 一行都不落 → "点开就退出"的日子照样能花 50 分买回连续。
 * 净效果会把孩子教成"要么练够、要么干脆别开"，与坚持引擎的目标正相反。
 */
export function dayFellShort(correctSum: number, sessionElapsedSecs: number[]): boolean {
  const practiced = sessionElapsedSecs.filter((s) => Number.isFinite(s) && s > 0)
  if (practiced.length === 0) return false // 那天压根没练 → 可补，这才是补签卡的设计意图
  return !meetsGoal(correctSum, goalForPastDay(practiced))
}

/** 还差几个（不为负）；答对数是脏值时按最不利处理 */
export function shortfall(correctToday: number, goal: number): number {
  if (!Number.isFinite(correctToday)) return goal
  return Math.max(0, goal - correctToday)
}

/** 是否达标；答对数是脏值时不放行 */
export function meetsGoal(correctToday: number, goal: number): boolean {
  return Number.isFinite(correctToday) && correctToday >= goal
}

/**
 * 进当日结算的答对数。默认（factor = 0）原样返回，也就是**现状行为不变**。
 * 见 POINTS_CORRECT_CAP_FACTOR 的注释：这是"先失败再补够多发 63%"那条不对称的止损阀门，
 * 开不开由用户拍板（计划 Step 0.6）。
 */
export function pointsCorrect(
  correctToday: number,
  goal: number,
  factor: number = POINTS_CORRECT_CAP_FACTOR,
): number {
  if (!Number.isFinite(factor) || factor <= 0) return correctToday
  return Math.min(correctToday, goal * factor)
}

/**
 * 单眼时长的**唯一读取口**（薄包装，与 src/challenge/challenge-storage.ts 同构）。
 * TrainingPage 原先自己有一份不做校验的同名函数——那会让"训练实际跑多久"和
 * "门槛按多久算"两个数出现漂移。统一到这里之后，两者永远同源。
 */
export function readDurationSec(): number {
  const v = lsGet('fzp.durationSec')
  return sanitizeDurationSec(v === null ? NaN : Number(v))
}
```

- [ ] **Step 1.4: 跑到通过。** `npm test -- src/training/goal.test.ts`
  预期：`Test Files  1 passed (1)` / `Tests  23 passed (23)`。
- [ ] **Step 1.5: 全量回归 + typecheck。** `npm test`（预期 **基线文件数 + 1**、**基线测试数 + 23**）与 `npm run typecheck`（零输出）。
- [ ] **Step 1.6: 提交。**
  ```
  git add src/training/goal.ts src/training/goal.test.ts
  git commit -m "feat(门槛): 训练完成门槛的常量与纯函数（每分钟5个/整次双眼合计/按真实练过时长/脏时长钳制）"
  ```

---

### Task 2: `doCheckIn` 接门槛（三态 + IndexedDB 单测）

> 本仓 `doCheckIn` 此前**零单测**，它的三条口径（一天两轮短路、`correctToday` 全天求和、`totalPoints` 链式累积）只写在 `src/sync/reconcile.ts` 的注释里。**先补测再动实现。**
>
> ⚠️ **`src/sync/reconcile.ts` 与 `src/sync/reconcile.test.ts` 一个字都不要改。** 那里写着"一天练两轮时 `doCheckIn()` 第二轮直接短路返回，当天分只按第一轮结算"——加门槛后这句仍然成立，只是多了一个前提：**短路的条件是"今天已有打卡行"**。第一轮不达标时压根没写行，第二轮当然要重算，而且这次的 `correctToday` 会把两轮的答对数都算进去（发分因此比"一次练够"更高——那不是漏洞，每一个答对都是真答对的）。**绝不能**因此去"重算 dailyPoints"：`reconcile.ts` 里那条"dailyPoints 一律沿用行内原值"是写死的硬口径，动它会让"练两轮"和"补签过的日子"白涨可换现实奖励的货币。

**Files**
- Modify: `E:\developer\fzp\src\data\checkin.ts`
- Test: `E:\developer\fzp\src\data\checkin.test.ts`（新建）

**Interfaces**
- Consumes: `goalForDay` / `goalPerRound` / `meetsGoal` / `pointsCorrect` / `DEFAULT_DURATION_SEC`（Task 1）、既有 `nextStreak` / `currentStreak` / `dailyPoints` / `pushCheckin`
- Produces:
  ```ts
  export type CheckinOutcome = 'checked-in' | 'already' | 'below-goal'
  export interface CheckinResult {
    outcome: CheckinOutcome
    correctToday: number
    goal: number
    streak: number
    dailyPoints: number
    totalPoints: number
  }
  export async function doCheckIn(today: string, durationSec: number): Promise<CheckinResult>
  export interface HomeStats {
    checkedInToday: boolean; streak: number; totalPoints: number
    correctToday: number; goalToday: number
  }
  export async function getHomeStats(today: string, durationSec?: number): Promise<HomeStats>
  ```
  **`alreadyCheckedIn` 字段删除**（三态用布尔表达不了，留着两个字段迟早不同步）。调用点只有 `TrainingPage`，在 Task 5 一并改。

- [ ] **Step 2.1: 写失败测试。** 新建 `E:\developer\fzp\src\data\checkin.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { db, type SessionRow } from './db'
import { doCheckIn, getHomeStats } from './checkin'

/**
 * 造一条 session 行。参与判定的三个字段：date、correct、**elapsedSec**
 * （门槛的时长基准取"当天真实练过的最长一节"，见 goalForDay）。
 */
function ses(
  date: string,
  correct: number,
  eye: 'left' | 'right' = 'left',
  elapsedSec = 180,
): Omit<SessionRow, 'id'> {
  return {
    date, startedAtMs: 1, eye,
    answered: correct + 2, correct, flips: correct, elapsedSec, acuity: 0.8,
  }
}

/** pushCheckin 是 fire-and-forget（写 outbox），轮询等它落库 */
async function waitFor(fn: () => Promise<boolean>, ms = 2000): Promise<void> {
  const t0 = Date.now()
  for (;;) {
    if (await fn()) return
    if (Date.now() - t0 > ms) throw new Error('等待落库超时')
    await new Promise((r) => setTimeout(r, 5))
  }
}

beforeEach(async () => {
  // ⚠️ 先让出一拍再清表。pushCheckin 是 fire-and-forget：fire() → enqueuePut() 会在
  // **一个事务里同时** db.checkins.put(stamped) 和 db.outbox.add(...)。上一条用例的那个
  // 事务若在本次清表之后才提交，就会凭空复活一条今日打卡行——于是"挂机整轮 → below-goal
  // / checkins.count()===0"那条会读到 existing 而返回 'already'，偶发红且报错完全指不到根因。
  // （src/data/api.test.ts 之所以稳，是因为它每条用例都 waitFor 到入队落地才结束。）
  await new Promise((r) => setTimeout(r, 0))
  await Promise.all([db.sessions.clear(), db.checkins.clear(), db.outbox.clear()])
})

describe('doCheckIn：达标才打卡', () => {
  it('当天累计答对达到整次门槛 → checked-in，写 checkins 并发分', async () => {
    await db.sessions.add(ses('2026-07-27', 16))
    await db.sessions.add(ses('2026-07-27', 14, 'right'))
    const r = await doCheckIn('2026-07-27', 180)
    expect(r.outcome).toBe('checked-in')
    expect(r.goal).toBe(30)
    expect(r.correctToday).toBe(30)
    expect(r.streak).toBe(1)
    expect(r.dailyPoints).toBe(180) // floor((30*5 + 30) * 1.0)
    expect(await db.checkins.count()).toBe(1)
    // 入队落地后再结束本条，否则它的事务会漏到下一条用例清表之后（见 beforeEach 注释）
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)
  })

  it('挂机整轮（答对 0 个）→ below-goal，不写 checkins（这就是本功能要堵的漏洞）', async () => {
    await db.sessions.add(ses('2026-07-27', 0))
    await db.sessions.add(ses('2026-07-27', 0, 'right'))
    const r = await doCheckIn('2026-07-27', 180)
    expect(r.outcome).toBe('below-goal')
    expect(r.correctToday).toBe(0)
    expect(r.goal).toBe(30)
    expect(await db.checkins.count()).toBe(0)
  })

  it('差一个也不放行，正好够就放行（边界在等号上）', async () => {
    await db.sessions.add(ses('2026-07-27', 29))
    expect((await doCheckIn('2026-07-27', 180)).outcome).toBe('below-goal')
    await db.sessions.add(ses('2026-07-27', 1, 'right'))
    expect((await doCheckIn('2026-07-27', 180)).outcome).toBe('checked-in')
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)
  })

  it('门槛按时长比例：1 分钟档只要 10 个', async () => {
    // elapsedSec 也要给 60——门槛的时长基准取"当天真实练过的最长一节"，
    // 若这里仍留默认 180，门槛会按 3 分钟档算成 30。
    await db.sessions.add(ses('2026-07-27', 9, 'left', 60))
    expect((await doCheckIn('2026-07-27', 60)).goal).toBe(10)
    expect((await doCheckIn('2026-07-27', 60)).outcome).toBe('below-goal')
    await db.sessions.add(ses('2026-07-27', 1, 'right', 60))
    expect((await doCheckIn('2026-07-27', 60)).outcome).toBe('checked-in')
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)
  })

  it('【blocker 闸门】5 分钟档练完一轮后把设置改成 1 分钟，当天门槛仍是 50', async () => {
    // 不修的话：练完只拿到 12 个（门槛 50）→ 孩子点一下设置页「1分」→ 门槛变 10 →
    // 那 12 个照样算 → 下一次 doCheckIn 直接打卡成功。设置页就在常驻导航里，随手可点。
    await db.sessions.add(ses('2026-07-27', 6, 'left', 300))
    await db.sessions.add(ses('2026-07-27', 6, 'right', 300))
    const r = await doCheckIn('2026-07-27', 60) // ← 已经改成 1 分钟档了
    expect(r.goal, '门槛被事后改档追溯降低了').toBe(50)
    expect(r.outcome).toBe('below-goal')
    expect(await db.checkins.count()).toBe(0)
  })

  it('脏 durationSec 不会让门槛静默失效（NaN + 当天还没练过 → 按默认 180 的门槛 30 算）', async () => {
    // 当天没有 session 行时门槛只能按设置算，所以这条要在零 session 下测才隔离得干净。
    const r = await doCheckIn('2026-07-27', Number('abc'))
    expect(r.goal).toBe(30)
    expect(r.outcome).toBe('below-goal')
    expect(await db.checkins.count()).toBe(0)
  })
})

describe('doCheckIn：不达标时的安全不变式', () => {
  it('dailyPoints 恒为 0、totalPoints 恒等于既有累计（保证皮肤解锁不可能误弹）', async () => {
    await db.checkins.put({ date: '2026-07-20', streak: 3, dailyPoints: 90, totalPoints: 900 })
    await db.sessions.add(ses('2026-07-27', 5))
    const r = await doCheckIn('2026-07-27', 180)
    expect(r.outcome).toBe('below-goal')
    expect(r.dailyPoints).toBe(0)
    expect(r.totalPoints).toBe(900)
    // TrainingPage 的皮肤推导是 prevPoints = totalPoints - dailyPoints，
    // 这个恒等式保证 newlyUnlockedSkins(x, x) 必然返回空数组
    expect(r.totalPoints - r.dailyPoints).toBe(r.totalPoints)
  })

  it('streak 返回的是"还没保住"的连续天数（昨天打过卡 → 原值；更早 → 0）', async () => {
    await db.checkins.put({ date: '2026-07-26', streak: 4, dailyPoints: 90, totalPoints: 900 })
    await db.sessions.add(ses('2026-07-27', 3))
    expect((await doCheckIn('2026-07-27', 180)).streak).toBe(4)

    await db.checkins.clear()
    await db.checkins.put({ date: '2026-07-01', streak: 9, dailyPoints: 90, totalPoints: 900 })
    expect((await doCheckIn('2026-07-27', 180)).streak).toBe(0)
  })

  it('不达标不推云；补够后同一天只推出一条 checkin', async () => {
    await db.sessions.add(ses('2026-07-27', 12))
    expect((await doCheckIn('2026-07-27', 180)).outcome).toBe('below-goal')
    expect(await db.checkins.count()).toBe(0)

    await db.sessions.add(ses('2026-07-27', 20, 'right'))
    expect((await doCheckIn('2026-07-27', 180)).outcome).toBe('checked-in')
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) === 1)
    // 恰好 1 条 —— 不达标那次一条都没推（pushCheckin 只在 db.checkins.put 之后调用）
    expect(await db.outbox.where('kind').equals('checkin').count()).toBe(1)
  })
})

describe('doCheckIn：一天练两轮与已打卡', () => {
  it('第一轮不达标、再练一轮补够 → 打卡，且发分按当天累计算（与门槛同源）', async () => {
    await db.sessions.add(ses('2026-07-27', 8))
    await db.sessions.add(ses('2026-07-27', 6, 'right'))
    expect((await doCheckIn('2026-07-27', 180)).outcome).toBe('below-goal')

    await db.sessions.add(ses('2026-07-27', 10))
    await db.sessions.add(ses('2026-07-27', 12, 'right'))
    const r = await doCheckIn('2026-07-27', 180)
    expect(r.outcome).toBe('checked-in')
    expect(r.correctToday).toBe(36)
    expect(r.dailyPoints).toBe(210) // floor((36*5 + 30) * 1.0)
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)
  })

  it('【锚定发分不对称】"先失败再补够"比"第一轮就达标"多发 63%——这是本次改动新引入的，别当 bug 优化掉', async () => {
    // 现状（POINTS_CORRECT_CAP_FACTOR = 0，不封顶）刻意保留，理由与代价见 spec §4.3
    // 与计划 Step 0.6：要吃到这个差价必须先坐满 6 分钟故意不答、再认真练 6 分钟。
    // 若用户拍板要削，只改 goal.ts 的 POINTS_CORRECT_CAP_FACTOR（取 6，**不要取 2**），
    // 然后把下面第二个断言改成 floor(min(69,180)*5+30) —— 绝不许去改 reconcile.ts。
    await db.sessions.add(ses('2026-07-27', 40))
    expect((await doCheckIn('2026-07-27', 180)).dailyPoints).toBe(230) // 第一轮就达标：只按 40 结算
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)

    await db.checkins.clear()
    await db.sessions.clear()
    await db.sessions.add(ses('2026-07-28', 29))            // 第一轮压着停在 29（差 1 个）
    expect((await doCheckIn('2026-07-28', 180)).outcome).toBe('below-goal')
    await db.sessions.add(ses('2026-07-28', 40, 'right'))   // 第二轮认真练
    expect((await doCheckIn('2026-07-28', 180)).dailyPoints).toBe(375) // 按当天累计 69 结算
    await waitFor(async () => (await db.outbox.where('kind').equals('checkin').count()) >= 1)
  })

  it('今天已打过卡 → already，门槛不参与（否则会把到手的连续天数吓没）', async () => {
    await db.checkins.put({ date: '2026-07-27', streak: 5, dailyPoints: 120, totalPoints: 1200 })
    await db.sessions.add(ses('2026-07-27', 1)) // 远低于门槛
    const r = await doCheckIn('2026-07-27', 180)
    expect(r.outcome).toBe('already')
    expect(r.streak).toBe(5)
    expect(r.dailyPoints).toBe(120)
    expect(r.totalPoints).toBe(1200)
  })
})

describe('getHomeStats', () => {
  it('带回当天答对数与当天门槛（首页第三态"练了但没练够"靠它）', async () => {
    await db.sessions.add(ses('2026-07-27', 7))
    const s = await getHomeStats('2026-07-27', 180)
    expect(s.checkedInToday).toBe(false)
    expect(s.correctToday).toBe(7)
    expect(s.goalToday).toBe(30)
  })

  it('durationSec 缺省时按默认 180 算门槛（既有 3 个调用点不必改）', async () => {
    const s = await getHomeStats('2026-07-27')
    expect(s.goalToday).toBe(30)
    expect(s.correctToday).toBe(0)
  })

  it('goalToday 与 doCheckIn 一个算法：练过 5 分钟节后改成 1 分钟档，首页显示的门槛仍是 50', async () => {
    // 两处算法若不同源，首页会显示"练了 12/10 还没完成"这种自相矛盾的话。
    await db.sessions.add(ses('2026-07-27', 12, 'left', 300))
    const s = await getHomeStats('2026-07-27', 60)
    expect(s.goalToday).toBe(50)
    expect(s.correctToday).toBe(12)
  })
})
```

- [ ] **Step 2.2: 跑到失败。** `npm test -- src/data/checkin.test.ts`
  预期：**不是**报错崩溃，而是一片断言失败。vitest 不做类型检查，多传的第二个实参被旧实现直接忽略，所以函数照跑、只是返回的还是旧形状：
  - `AssertionError: expected undefined to be 'checked-in'`（`r.outcome` 还不存在）
  - `AssertionError: expected undefined to be 30`（`r.goal` 还不存在）
  - `AssertionError: expected 1 to be +0`（挂机那条：旧实现照样写了 checkins 行）

  预计 12-14 条 `it` 失败（只有「今天已打过卡」与 `getHomeStats` 的头两条里部分断言会侥幸通过）。

- [ ] **Step 2.3: 实现。** 用下面内容**整体替换** `E:\developer\fzp\src\data\checkin.ts`：

```ts
import { db, type SessionRow, type CheckinRow } from './db'
import { nextStreak, currentStreak, type LastCheckin } from './streak'
import { dailyPoints } from './points'
import { pushSession, pushCheckin } from './api'
import { goalForDay, goalPerRound, meetsGoal, pointsCorrect, DEFAULT_DURATION_SEC } from '../training/goal'

export async function saveSession(row: Omit<SessionRow, 'id'>): Promise<void> {
  const id = await db.sessions.add(row)
  pushSession({ ...row, id })
}

/**
 * 打卡三态。刻意不用布尔——"今天已打过"与"答对太少"是两种完全不同的结果，
 * 用两个布尔字段迟早会出现互相矛盾的组合。
 */
export type CheckinOutcome = 'checked-in' | 'already' | 'below-goal'

export interface CheckinResult {
  outcome: CheckinOutcome
  /** 当天累计答对（全部 sessions 求和），与 dailyPoints 的取数完全同源 */
  correctToday: number
  /** 当天整次门槛 */
  goal: number
  streak: number
  dailyPoints: number
  totalPoints: number
}

/**
 * 完成一轮训练后打卡。today = 本地 YYYY-MM-DD；durationSec = 刚练完那一节的实际时长。
 *
 * 判定顺序是硬口径（spec §4.3）：**already → below-goal → checked-in**。
 * "今天已打过卡"必须先短路——否则"第一轮达标已打卡、第二轮又练但这次合计不够"
 * 会被门槛误判成打卡失败，把已经到手的连续天数吓没。
 *
 * 门槛判的是"有没有真练"，判据是**当天全部 sessions 的答对数之和**，与
 * dailyPoints 同源。所以"再练一轮补够"是天然成立的（结算页那个重来按钮的基础）。
 *
 * ⚠️ durationSec 只是**兜底基准**：门槛的时长走 goalForDay(当天各节 elapsedSec)，
 * 当天练过就按最长那节算。否则孩子练完不达标后去设置页点一下「1分」，门槛会从 30
 * 掉到 10、已练的答对数照样算，下一次调用直接打卡成功（spec §3.5）。
 */
export async function doCheckIn(today: string, durationSec: number): Promise<CheckinResult> {
  const todaySessions = await db.sessions.where('date').equals(today).toArray()
  const correctToday = todaySessions.reduce((sum, r) => sum + r.correct, 0)
  const goal = goalForDay(durationSec, todaySessions.map((r) => r.elapsedSec))

  const existing = await db.checkins.get(today)
  if (existing) {
    return {
      outcome: 'already',
      correctToday,
      goal,
      streak: existing.streak,
      dailyPoints: existing.dailyPoints,
      totalPoints: existing.totalPoints,
    }
  }

  const last = await db.checkins.orderBy('date').last()
  const lastCk: LastCheckin | null = last ? { date: last.date, streak: last.streak } : null

  if (!meetsGoal(correctToday, goal)) {
    // 不写 checkins → 天然不推云（pushCheckin 只在 put 之后调用）。
    // dailyPoints 恒 0 且 totalPoints 恒等于既有累计：这个不变式让调用方即使
    // 误走皮肤解锁推导（prevPoints = totalPoints - dailyPoints）也拿到相等的两个数，
    // newlyUnlockedSkins 必然返回空数组，不可能误弹「解锁新皮肤」。
    return {
      outcome: 'below-goal',
      correctToday,
      goal,
      // 用 currentStreak 而非 nextStreak：它表达的是"**还没保住**的连续天数"，
      // 正好驱动结算页那句「连续 N 天还没保住，今天再练一轮就能接上」。
      streak: currentStreak(lastCk, today),
      dailyPoints: 0,
      totalPoints: last ? last.totalPoints : 0,
    }
  }

  const streak = nextStreak(lastCk, today)
  // pointsCorrect 默认（POINTS_CORRECT_CAP_FACTOR = 0）原样返回 correctToday，
  // 即发分行为与改动前完全一致。它是"先失败再补够多发 63%"那条不对称的止损阀门，
  // 开不开由用户拍板（spec §4.3）。⚠️ 只在这里首次结算时封顶，
  // src/sync/reconcile.ts 的"dailyPoints 一律沿用行内原值"一个字都不能动。
  const dp = dailyPoints(pointsCorrect(correctToday, goal), streak)
  const totalPoints = (last ? last.totalPoints : 0) + dp

  const row: CheckinRow = { date: today, streak, dailyPoints: dp, totalPoints }
  await db.checkins.put(row)
  pushCheckin(row)
  return { outcome: 'checked-in', correctToday, goal, streak, dailyPoints: dp, totalPoints }
}

export interface HomeStats {
  checkedInToday: boolean
  streak: number
  totalPoints: number
  /** 当天累计答对，驱动首页"练了但没练够"第三态 */
  correctToday: number
  /** 当天整次门槛 */
  goalToday: number
}

/**
 * durationSec 可选：只有首页需要显示准确门槛才传，SettingsPage / TrainingPage /
 * ChallengePage 三处既有调用只用 totalPoints / checkedInToday，一字不改。
 */
export async function getHomeStats(
  today: string,
  durationSec: number = DEFAULT_DURATION_SEC,
): Promise<HomeStats> {
  try {
    const last = await db.checkins.orderBy('date').last()
    const todaySessions = await db.sessions.where('date').equals(today).toArray()
    const lastCk: LastCheckin | null = last ? { date: last.date, streak: last.streak } : null
    return {
      checkedInToday: last?.date === today,
      streak: currentStreak(lastCk, today),
      totalPoints: last ? last.totalPoints : 0,
      correctToday: todaySessions.reduce((sum, r) => sum + r.correct, 0),
      // 与 doCheckIn 同一个算法（goalForDay）：两处若不同源，首页会显示
      // "练了 12/10 还没完成"这种自相矛盾的话
      goalToday: goalForDay(durationSec, todaySessions.map((r) => r.elapsedSec)),
    }
  } catch {
    // IndexedDB 不可用（隐私模式/storage 受限）时降级，不崩。
    // 拿不到 sessions，只能退回按设置算门槛。
    return {
      checkedInToday: false, streak: 0, totalPoints: 0,
      correctToday: 0, goalToday: goalPerRound(durationSec),
    }
  }
}
```

- [ ] **Step 2.4: 跑到通过。** `npm test -- src/data/checkin.test.ts`
  预期：`Test Files  1 passed (1)` / `Tests  15 passed (15)`。
- [ ] **Step 2.5: 全量回归。** `npm test` —— 预期 **基线 + 38 个测试 / +2 个文件**，且**全绿**（`CheckinResult` 只被 `TrainingPage` 消费，vitest 不做类型检查所以不会红）。
- [ ] **Step 2.6: typecheck 预期会红，这是本计划唯一被授权的一次。** 跑 `npm run typecheck`，预期看到 `src/training/TrainingPage.tsx` 报若干条：
  - `error TS2554: Expected 2 arguments, but got 1.`（`doCheckIn(todayStr)`）
  - `error TS2339: Property 'alreadyCheckedIn' does not exist on type 'CheckinResult'.`（4 处）

  **这些正是 Task 5 要修的**。不要在本任务里顺手改 `TrainingPage`——那会让两个任务的 diff 纠缠。
  **先确认自己在 `feat/training-goal-gate` 分支上**（`git branch --show-current`）；若不在，回 Step 0.5。
- [ ] **Step 2.7: 提交（带一条已授权的临时 typecheck 断裂）。**
  ```
  git add src/data/checkin.ts src/data/checkin.test.ts
  git commit -m "feat(门槛): doCheckIn 加完成门槛闸门，CheckinResult 改三态（TrainingPage 待 Task5 跟进）"
  ```
  ⛔ **本步之后到 Step 5.6 之前一律不许 `git push`**（CI 的 typecheck 关会红、部署 job 整体失败、线上停在旧版本）。

---

### Task 3: 堵死补签卡这条付费绕过通道

> 不堵的后果：不达标那天在 `checkins` 里就是个纯粹的日期缺口，与"压根没开 app"无法区分。第二天首页自动弹补签横幅，花 **50 分**（约 1-2 天打卡分）把连续天数原样接回 —— 等于「每天挂机 30 秒 + 50 分补签」彻底架空门槛。
>
> ⚠️ **判据是「那天确实没练够」，不是「那天有没有 session 行」**（评审 major #2）。`saveSession` 只在 `session.phase === 'finished'`（计时走满）时触发，所以"有行"真正等价的是"**完整走完过一节**"。用"有行"当判据会同时错两头：练到 40 个（远超门槛）却没点完成键就被收走 iPad 的那天**不可补**（最该补的一天被堵死），而"点开训练马上退出"的那天**一行都不落、照样能花 50 分买回连续**。净效果把孩子教成"要么练够、要么干脆别开"。

**Files**
- Modify: `E:\developer\fzp\src\rewards\ledger.ts`
- Modify: `E:\developer\fzp\src\rewards\rewards-service.ts`
- Test: `E:\developer\fzp\src\rewards\ledger.test.ts`（追加一个 `describe`）

**Interfaces**
- Consumes: `dayFellShort`（Task 1；`rewards-service.ts → training/goal` **无循环依赖**，`goal.ts` 只 import `data/storage`）
- Produces:
  ```ts
  export type RepairReason = 'not-broken' | 'attempted' | 'no-points' | 'month-limit'
  export function canRepair(p: {
    target: RepairTarget | null
    monthRepairCount: number
    available: number
    cost: number
    targetAttempted?: boolean
  }): RepairEligibility
  ```
  `findRepairTarget` 签名**不动**（保持"只找缺口"的单一职责）。

- [ ] **Step 3.1: 写失败测试。** 在 `E:\developer\fzp\src\rewards\ledger.test.ts` **文件末尾追加**：

```ts
describe('canRepair：训练完成门槛的配套闸门', () => {
  const target = { missedDate: '2026-07-26', phantomStreak: 4, phantomTotal: 800 }

  it('缺口日练了但没练够 → attempted，不可补', () => {
    // 不堵这里，门槛的成本就只是 50 分——比认真练一轮还便宜。
    expect(canRepair({ target, targetAttempted: true, monthRepairCount: 0, available: 999, cost: 50 }))
      .toEqual({ ok: false, reason: 'attempted' })
  })

  it('attempted 排在 month-limit / no-points 之前（先说清根本原因，别让家长以为是次数或余额问题）', () => {
    const r = canRepair({ target, targetAttempted: true, monthRepairCount: 2, available: 0, cost: 50 })
    expect(r.reason).toBe('attempted')
  })

  it('没缺口时仍是 not-broken（attempted 不能盖住它）', () => {
    expect(canRepair({ target: null, targetAttempted: true, monthRepairCount: 0, available: 999, cost: 50 }).reason)
      .toBe('not-broken')
  })

  it('缺省 targetAttempted 时行为与从前完全一致（完全没练的日子照样能补）', () => {
    expect(canRepair({ target, monthRepairCount: 0, available: 999, cost: 50 })).toEqual({ ok: true })
    expect(canRepair({ target, targetAttempted: false, monthRepairCount: 2, available: 999, cost: 50 }).reason)
      .toBe('month-limit')
  })

  it('targetAttempted 的语义是「练了但没练够」，不是「那天有记录」——练够却没点完成键的日子必须传 false（仍可补）', () => {
    // 判据由 dayFellShort 算（goal.test.ts 锚定），这里锚定的是 canRepair 的契约：
    // 服务层一旦把它退化成 "sessions.count() > 0"，最该补的那天（练到 40 个、
    // 门槛 30，却被收走 iPad 没点按钮）就会被永久堵死。
    expect(canRepair({ target, targetAttempted: false, monthRepairCount: 0, available: 999, cost: 50 }))
      .toEqual({ ok: true })
  })
})
```

- [ ] **Step 3.2: 跑到失败。** `npm test -- src/rewards/ledger.test.ts`
  预期失败信息：`AssertionError: expected { ok: true } to deeply equal { ok: false, reason: 'attempted' }` —— 因为 `canRepair` 还不认识 `targetAttempted`，直接返回 ok。

- [ ] **Step 3.3: 实现（ledger.ts）。** 在 `E:\developer\fzp\src\rewards\ledger.ts` 里：

  把
  ```ts
  export type RepairReason = 'not-broken' | 'no-points' | 'month-limit'
  ```
  改成
  ```ts
  export type RepairReason = 'not-broken' | 'attempted' | 'no-points' | 'month-limit'
  ```

  再把文件末尾的 `canRepair` **整体替换**为：

  ```ts
  /**
   * 补签资格：有可补缺口、那天不是"练了但没练够"、每月上限内、余额充足；不满足给出首个原因。
   *
   * `targetAttempted`（训练完成门槛的配套闸门，spec §5.6）：缺口日若「练了但没练够」，
   * 不可补。否则「每天挂机 30 秒 + 50 分补签」就能把门槛彻底架空，而 50 分只相当于
   * 1-2 天打卡分，比认真练一轮还便宜。
   *
   * ⚠️ 语义**不是**"那天有 session 行"。`saveSession` 只在 `phase === 'finished'`
   * （计时走满）时落库，所以"有行"真正等价的是"完整走完过一节"，拿它当判据会错两头：
   *  - 练到 40 个（远超门槛 30）却没点完成键就被收走 iPad → 有行、无打卡行 →
   *    **最该补的一天反而被永久堵死**；
   *  - 中途退出不满一节 → 一行都不落 → "点开就退出"的日子照样能花 50 分买回连续。
   * 所以判据必须**重算那天的门槛**（`dayFellShort`，用那天各节的 elapsedSec 算）。
   *
   * 为什么"练了没练够却不能补、压根没练反而能补"不反直觉：门槛失败是**当天可挽回**的
   * （结算页就有「再练一轮」按钮，判据是当天累计），过了当天才变成缺口；
   * 而补签卡的设计意图是"当天根本没机会练"（生病/外出），那才是不可挽回的。
   */
  export function canRepair(p: {
    target: RepairTarget | null
    monthRepairCount: number
    available: number
    cost: number
    targetAttempted?: boolean
  }): RepairEligibility {
    if (!p.target) return { ok: false, reason: 'not-broken' }
    if (p.targetAttempted) return { ok: false, reason: 'attempted' }
    if (p.monthRepairCount >= 2) return { ok: false, reason: 'month-limit' }
    if (p.available < p.cost) return { ok: false, reason: 'no-points' }
    return { ok: true }
  }
  ```

- [ ] **Step 3.4: 实现（rewards-service.ts）。** 在 `E:\developer\fzp\src\rewards\rewards-service.ts` 里：

  在顶部 import 区加一行：
  ```ts
  import { dayFellShort } from '../training/goal'
  ```
  （`goal.ts` 只 import `data/storage`，不会形成循环依赖。）

  在 `latestTotalPoints` 函数**下方**插入：
  ```ts
  /**
   * 缺口日是不是「练了但没练够」。是则不可补签——见 spec §5.6。
   *
   * ⚠️ 刻意**不是** `sessions.count() > 0`：saveSession 只在计时走满时落库，
   * 所以"有行"等价于"完整走完过一节"，用它当判据会把"练到 40 个（门槛 30）却没点
   * 完成键就被收走 iPad"那天永久堵死（那是最该补的一天），同时放过"点开就退出"
   * 的日子（一行都不落）。所以要**重算那天的门槛**，用那天各节真实的 elapsedSec。
   */
  async function fellShortOn(dateStr: string): Promise<boolean> {
    const rows = await db.sessions.where('date').equals(dateStr).toArray()
    return dayFellShort(
      rows.reduce((sum, r) => sum + r.correct, 0),
      rows.map((r) => r.elapsedSec),
    )
  }
  ```

  把 `getRepairStatus` 里的
  ```ts
    const target = findRepairTarget(checkins, today)
    const elig = canRepair({
      target,
      monthRepairCount: monthRepairCount(reds, monthOf(today)),
      available,
      cost: REPAIR_COST,
    })
  ```
  改成
  ```ts
    const target = findRepairTarget(checkins, today)
    const targetAttempted = target ? await fellShortOn(target.missedDate) : false
    const elig = canRepair({
      target,
      targetAttempted,
      monthRepairCount: monthRepairCount(reds, monthOf(today)),
      available,
      cost: REPAIR_COST,
    })
  ```

  把 `doRepair` 里的
  ```ts
    const target = findRepairTarget(checkins, today)
    const elig = canRepair({ target, monthRepairCount: monthRepairCount(reds, monthOf(today)), available, cost: REPAIR_COST })
  ```
  改成
  ```ts
    const target = findRepairTarget(checkins, today)
    const targetAttempted = target ? await fellShortOn(target.missedDate) : false
    const elig = canRepair({ target, targetAttempted, monthRepairCount: monthRepairCount(reds, monthOf(today)), available, cost: REPAIR_COST })
  ```

- [ ] **Step 3.5: 跑到通过。** `npm test -- src/rewards/ledger.test.ts`
  预期：该文件测试数 = 原有 + 5，全绿。
- [ ] **Step 3.6: 全量回归。** `npm test` —— 预期 **基线 + 43 个测试 / +2 个文件**，全绿。`npm run typecheck` 仍只剩 Task 2 遗留的 `TrainingPage` 那几条（不多不少）。
- [ ] **Step 3.7: 提交。**
  ```
  git add src/rewards/ledger.ts src/rewards/ledger.test.ts src/rewards/rewards-service.ts
  git commit -m "feat(门槛): 补签卡不再能买回「练了但没练够」的日子（新增 attempted 闸门）"
  ```

---

### Task 4: i18n 14 条文案（ZH/EN 双份）

**Files**
- Modify: `E:\developer\fzp\src\i18n.tsx`
- Modify: `E:\developer\fzp\src\i18n.test.ts`

**Interfaces**
- Consumes: 无
- Produces: 14 个文案键，供 Task 5 / Task 6 使用

> **两处措辞是评审改过的，别按直觉写回去：**
> - `goal.short` / `goal.kept` **不许**写成「再答对 N 个就完成」「这一轮的练习已经记下啦」（评审 major #4）。`correctToday` 只统计**已落库**的 session，而落库只发生在 `session.phase === 'finished'`（计时走满）。孩子按字面理解在新一轮左眼答对 18 个然后停手/切走，那一节根本不落库、18 个全丢，`correctToday` 纹丝不动；而且不走到右眼节末按下完成键就永远不会再调 `doCheckIn`。真正的要求是「**再完整走完左右两节并按完成键**」。
> - 新增 `train.finishRound` 顶替 `train.finishCheckin`（评审 minor #10）：右眼节末的按钮不能承诺「完成并打卡 🎊」却落到「😅 今天还差一点点」，而它是唯一能触发判定的入口（§10 刻意不做训练中进度 HUD）。**`train.finishCheckin` 在两份字典里都删掉**——全仓仅 `TrainingPage.tsx:409` 用它，Task 5 会同步改。

- [ ] **Step 4.1: 写失败测试。** 在 `E:\developer\fzp\src\i18n.test.ts` 里，在 `CHALLENGE_KEYS` 常量**下方**追加：

```ts
/** 训练完成门槛文案（2026-07-27） */
const GOAL_KEYS = [
  'goal.title', 'goal.progress', 'goal.short', 'goal.why', 'goal.noCheckin',
  'goal.streakWarn', 'goal.kept', 'goal.badgeStill', 'goal.retry', 'goal.later',
  'goal.settingsHint',
  'home.partial', 'repair.attempted',
  // 右眼末按钮改中性文案：不能承诺"完成并打卡"却落到"今天还差一点点"
  'train.finishRound',
]
```

并在 `describe('云同步与隐私政策文案', …)` 块的**最后一个 `it` 之后**追加：

```ts
  it('训练完成门槛 14 个文案键在 zh 与 en 都存在（漏一份就会在另一种语言下回退成中文）', () => {
    expect(GOAL_KEYS.length).toBe(14)
    for (const k of GOAL_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
    // 旧的「完成并打卡 🎊」已被 train.finishRound 顶替，两份字典都不该再留着它
    expect(hasKey('zh', 'train.finishCheckin'), 'train.finishCheckin 应已删除').toBe(false)
    expect(hasKey('en', 'train.finishCheckin'), 'train.finishCheckin 应已删除').toBe(false)
  })
```

- [ ] **Step 4.2: 跑到失败。** `npm test -- src/i18n.test.ts`
  预期失败信息：`AssertionError: zh 缺 goal.title: expected false to be true`。

- [ ] **Step 4.3: 实现（ZH 字典）。** 在 `E:\developer\fzp\src\i18n.tsx` 的 **ZH 块**里：

  在 `'home.notYetToday': '今天还没练，来一局吧！',` 这一行**下方**插入：
  ```ts
    'home.partial': '今天练了 {n}/{goal}，再练一轮就完成啦！',
  ```

  在 `'train.trySkinHint': '去「⚙️ 设置」换上试试～',` 这一行**下方**插入：
  ```ts
    // 训练完成门槛（不达标结算页）。语气纪律：全页不出现"失败/不合格/作弊"，
    // 孩子该看到的是"差一点、再来一轮就好"，不是"你被系统抓了"。
    'goal.title': '今天还差一点点',
    'goal.progress': '答对 {n} / 需要 {goal}',
    // ⚠️ 不许写成"再答对 {n} 个就完成"：没走满一整节根本不落库，答对数会全丢（评审 major #4）
    'goal.short': '再练一轮（左右各一节），答对 {n} 个就完成啦',
    'goal.why': '答对了才算真的透过拍子看清——挂着不动可不算练哦',
    'goal.noCheckin': '今天暂不打卡、不加分',
    'goal.streakWarn': '🔥 连续 {n} 天还没保住，今天再练一轮就能接上',
    'goal.kept': '练完整节才会记下，答对数会累加',
    'goal.badgeStill': '🏅 顺便解锁了新勋章，去勋章墙看看～',
    'goal.retry': '🔄 再练一轮',
    'goal.later': '回首页',
    'goal.settingsHint': '每天需答对 {n} 个才算完成（每分钟 {per} 个 × {min} 分钟 × 两只眼）',
  ```

  在 `'repair.monthLimit': '本月补签次数已用完（每月 2 次）',` 这一行**下方**插入：
  ```ts
    'repair.attempted': '昨天练了但没练够——补签卡只能补完全没练的日子',
  ```

  把 `'train.finishCheckin': '完成并打卡 🎊',`（第 108 行）**整行替换**为：
  ```ts
    // 中性文案：这个按钮是唯一能触发门槛判定的入口，点下去可能落到不达标页，
    // 所以不能承诺"打卡"——「打卡」二字留给真正打上卡的那一页
    'train.finishRound': '完成这一轮 →',
  ```

- [ ] **Step 4.4: 实现（EN 字典）。** 在同一文件的 **EN 块**里做对称插入：

  在 `'home.notYetToday': "Not trained today—let's go!",` 下方：
  ```ts
    'home.partial': 'Today: {n}/{goal} correct — one more round to finish!',
  ```

  在 `'train.trySkinHint': 'Switch to it in ⚙️ Settings ~',` 下方：
  ```ts
    'goal.title': 'Almost there!',
    'goal.progress': '{n} correct / {goal} needed',
    'goal.short': 'One more full round (both eyes) — {n} more correct answers to finish',
    'goal.why': 'Only correct answers count — they show you really focused through the paddle',
    'goal.noCheckin': 'No check-in or points for today yet',
    'goal.streakWarn': '🔥 Your {n}-day streak is not saved yet — one more round keeps it',
    'goal.kept': 'Each completed eye session is saved — correct answers keep adding up',
    'goal.badgeStill': '🏅 A new badge unlocked along the way — check the badge wall',
    'goal.retry': '🔄 One more round',
    'goal.later': 'Back home',
    'goal.settingsHint': '{n} correct answers needed per day ({per}/min × {min} min × 2 eyes)',
  ```

  在 `'repair.monthLimit': 'No repairs left this month (2/month)',` 下方：
  ```ts
    'repair.attempted': 'You trained yesterday but not enough — repair cards only cover days with no training at all',
  ```

  把 `'train.finishCheckin': 'Finish & check in 🎊',`（第 601 行）**整行替换**为：
  ```ts
    'train.finishRound': 'Finish this round →',
  ```

- [ ] **Step 4.5: 跑到通过。** `npm test -- src/i18n.test.ts`，预期该文件测试数 = 原有 + 1，全绿。
- [ ] **Step 4.6: 全量回归 + typecheck。** `npm test`（预期 **基线 + 44 个测试 / +2 个文件**）；`npm run typecheck` 仍只剩 Task 2 遗留的 `TrainingPage` 那几条（`train.finishCheckin` 是字典里的字符串键、不是类型，删掉它不会新增 typecheck 报错）。
  ⚠️ 但**运行时**会有一段短暂难看：`translate()` 的兜底是 `DICT[lang][key] ?? DICT.zh[key] ?? key`，所以在 Task 5 把第 409 行改掉之前，那个按钮会直接显示原文 `train.finishCheckin`。这只存在于分支中间状态，Task 5 收尾即消失——**不要为此提前去改 `TrainingPage`**（那会让 Task 4/5 的 diff 纠缠）。
- [ ] **Step 4.7: 提交。**
  ```
  git add src/i18n.tsx src/i18n.test.ts
  git commit -m "feat(门槛): 中英双语文案 14 条（不达标结算页/首页第三态/补签提示/设置页门槛说明/节末按钮改中性）"
  ```

---

### Task 5: 训练页不达标结算页 + 「再练一轮」

> 本任务同时修掉 Task 2 遗留的 typecheck 断裂。

**Files**
- Modify: `E:\developer\fzp\src\training\TrainingPage.tsx`
- Modify: `E:\developer\fzp\src\App.tsx`
- Test: `E:\developer\fzp\src\training\goal-gate.test.ts`（新建，`?raw` 源文本契约）

**Interfaces**
- Consumes: `readDurationSec` / `shortfall`（Task 1）、`doCheckIn` / `CheckinResult`（Task 2）、`goal.*` 文案（Task 4）
- Produces: `TrainingPage({ onHome }: { onHome: () => void })`、组件内 `restartRound()`

- [ ] **Step 5.1: 写失败测试。** 新建 `E:\developer\fzp\src\training\goal-gate.test.ts`：

```ts
import { describe, it, expect } from 'vitest'

// 用 Vite 的 ?raw 拿源文件文本（本仓没装 @types/node，node:fs 会让 tsc 报 TS2307）。
// 本仓 vitest 是 environment: 'node'、没有 jsdom，组件渲染测不了，
// 源文本是"训练页有没有踩到门槛边界"唯一能自动化的闸门。
// 参考 src/challenge/challenge-shell.test.ts 与 src/layout/css-contract.test.ts。
import page from './TrainingPage.tsx?raw'
import app from '../App.tsx?raw'

/** 不达标分支的起始锚点（渲染层用的是 `checkin.outcome`，不会撞上） */
const BELOW = "if (result.outcome === 'below-goal') {"

/**
 * 截出 below-goal 分支的**确切**函数体。
 * 刻意不用"从锚点起数 N 个字符"那种窗口——分支体只有 200 多字符，窗口稍微开大一点就会
 * 越过 `}` 把后面的 `captureDailyOnCheckin(...)` 一并吞进来，于是"不达标不许发保底怪"
 * 这条断言会在代码完全正确时报红，排查成本极高。按闭合花括号截才是稳的。
 */
function belowGoalBranch(src: string): string {
  const i = src.indexOf(BELOW)
  expect(i, '缺少 below-goal 分支').toBeGreaterThan(-1)
  const end = src.indexOf('\n    }', i) // 分支闭合花括号：4 空格缩进（组件内函数体里的 if）
  expect(end, 'below-goal 分支没有闭合（缩进变了？）').toBeGreaterThan(i)
  return src.slice(i, end)
}

describe('训练完成门槛在训练页的接线契约', () => {
  it('doCheckIn 必须带上真实时长（漏传门槛就按默认 180 算，与实际训练时长脱节）', () => {
    expect(page).toContain('doCheckIn(roundDateRef.current, session.durationSec)')
    expect(page, '不许再出现只传一个参数的旧调用').not.toMatch(/doCheckIn\([^,)]*\)/)
  })

  it('一轮训练用同一个日期：saveSession / doCheckIn / 保底怪都走 roundDateRef', () => {
    // 跨零点时左眼节落到昨天、右眼节落到今天，correctToday 只剩右眼那一节（3 分钟档
    // 大概率 20-40 个，而门槛是整次的 30）→ 可能整轮判 below-goal，而昨天同样没写打卡行
    // （doCheckIn 从未在零点前被调用）→ 两天都没打卡、连续天数直接断，孩子却真练满了 6 分钟。
    expect(page).toContain('const roundDateRef = useRef(')
    expect(page, 'roundDateRef 必须在左眼节开始时刷新').toMatch(/eye === 'left'[\s\S]{0,200}roundDateRef\.current = toDateStr\(new Date\(\)\)/)
    expect(page).toContain('date: roundDateRef.current,')           // saveSession
    expect(page).toContain('captureDailyOnCheckin(false, roundDateRef.current')
    expect(page, '落库日期不许再用落库时刻现算').not.toContain('date: toDateStr(new Date()),')
  })

  it('时长只能从 goal.ts 的 readDurationSec 读（本地再写一份必然与门槛口径漂移）', () => {
    expect(page).toContain("from './goal'")
    expect(page).toContain('readDurationSec')
    expect(page, '训练页不许自己再定义 readDurationSec').not.toMatch(/function\s+readDurationSec\s*\(/)
    expect(page, '不许绕过 goal.ts 直接读 fzp.durationSec').not.toContain("lsGet('fzp.durationSec')")
  })

  it('不达标分支存在，且在发放任何完成奖励之前 return', () => {
    const branch = belowGoalBranch(page)
    expect(branch).toContain('setCheckin(result)')
    expect(branch).toContain('return')
    expect(branch, '不达标不许发保底怪').not.toContain('captureDailyOnCheckin')
    expect(branch, '不达标不许算皮肤解锁').not.toContain('newlyUnlockedSkins')
    expect(branch, '不达标不许放打卡完成音').not.toContain("playSfx('checkin')")
  })

  it('保底怪与皮肤解锁各只有一处，且挂在 checked-in 上', () => {
    expect(page.match(/captureDailyOnCheckin\(/g)).toHaveLength(1)
    expect(page.match(/newlyUnlockedSkins\(/g)).toHaveLength(1)
    expect(page).toContain("result.outcome === 'checked-in'")
  })

  it('勋章照常判定：syncBadges 必须在 below-goal 分流之前调用', () => {
    // 勋章判据来自 sessions 的跨天累计，与"今天算不算完成"无关。
    // 挪到分流之后 = 不达标日的勋章被推迟到下次打卡，unlockedAt 从此失真，
    // 而 CLAUDE.md 明写"勋章/怪兽取最早——首次达成时刻才是正确语义"。
    // 注意找的是 `syncBadges(` 带括号的调用，顶部 import 里那个 `syncBadges,` 不会误命中
    const iBadge = page.indexOf('syncBadges(')
    const iBranch = page.indexOf(BELOW)
    expect(iBadge, '找不到 syncBadges 调用').toBeGreaterThan(-1)
    expect(iBranch, '找不到 below-goal 分支').toBeGreaterThan(-1)
    expect(iBadge).toBeLessThan(iBranch)
  })

  it('restartRound 的复位清单一条都不能少（漏一条的后果都很隐蔽，见 spec §8.2）', () => {
    const m = page.match(/function restartRound\(\)[\s\S]*?\n  \}/)
    expect(m, '缺少 restartRound').not.toBeNull()
    const body = m![0]
    for (const line of [
      'savedRef.current = false',
      'seqRef.current = 0',
      'targetShownAtRef.current = 0',
      'sumReactionRef.current = 0',
      'reactionCountRef.current = 0',
      'setCheckin(null)',
      'setNewBadges([])',
      'setNewSkins([])',
      'setCapturedThisSession([])',
      'setLastAnswer(null)',
      'setComboFx(null)',
      'setEggCaptureFx(null)',
      'setPaused(false)',
      'roundDateRef.current = toDateStr(new Date())',
      "createSession('left', readDurationSec())",
    ]) {
      expect(body, `restartRound 漏了 ${line}`).toContain(line)
    }
  })

  it('刻意不用 key 重挂载来实现「重来」', () => {
    // 卸载会跑 voskRef.current?.stop()，而 vosk-single 的 stop() 清空单例 →
    // 重挂载要重拉 3 个模型分片、拼 60MB Blob、worker 重新解压。
    // 那正是 commit 89743b6「iPad 换眼时白屏退出——vosk 模型并发加载把内存打爆」的路径。
    expect(app).toContain('<TrainingPage onHome=')
    // ⚠️ 不能用 /<TrainingPage[^>]*\bkey=/ ——实际写法是 `onHome={() => setView('home')}`，
    // 箭头函数里的 `>` 会让 [^>]* 在 `=>` 处停住，正则只扫到 `<TrainingPage onHome={() `，
    // 于是将来有人写 `<TrainingPage onHome={…} key={n} />` 这条闸门照样绿（评审 minor #8）。
    // 先按整个标签截出来再断言才是稳的。
    const i = app.indexOf('<TrainingPage')
    expect(i, '找不到 <TrainingPage').toBeGreaterThan(-1)
    const tag = app.slice(i, app.indexOf('/>', i))
    expect(tag, 'App 不许给 TrainingPage 加 key').not.toContain('key=')
  })
})
```

- [ ] **Step 5.2: 跑到失败。** `npm test -- src/training/goal-gate.test.ts`
  预期失败信息（多条）：`AssertionError: expected '…' to contain 'doCheckIn(roundDateRef.current, session.durationSec)'`、`expected '…' to contain 'const roundDateRef = useRef('`、`expected null not to be null`（缺 `restartRound`）、`expected '…' to contain '<TrainingPage onHome='`。

- [ ] **Step 5.3: 实现（TrainingPage 的 5 处改动）。**

  **① 顶部 import 与常量**（`src/training/TrainingPage.tsx` 第 1-51 行区域）

  - 删除第 24 行 `const DURATION_SEC = 180`
  - 删除第 42-45 行整个 `function readDurationSec() { … }`
  - 在 `import { dirForKey } from './key-map'` **下方**加一行：
    ```ts
    import { readDurationSec, shortfall } from './goal'
    ```

  **② 组件签名**（第 53 行）

  把
  ```tsx
  export function TrainingPage() {
  ```
  改成
  ```tsx
  export function TrainingPage({ onHome }: { onHome: () => void }) {
  ```

  **②b 一轮训练锁定一个日期**（跨零点，spec §8.5）

  在 `const savedRef = useRef(false)` **下方**加：
  ```ts
    /**
     * 本轮训练的日期，在**左眼节开始时**算一次，左右两节与打卡全都用它。
     *
     * 原实现 saveSession / doCheckIn 各自现算 toDateStr(new Date())，一轮跨零点时左眼节
     * 落到昨天、右眼节落到今天，correctToday 只剩右眼那一节（3 分钟档大概率 20-40 个，
     * 而门槛是整次的 30）。加门槛后这会让整轮判 below-goal，而昨天同样没写打卡行
     * （doCheckIn 从未在零点前被调用）→ **两天都没打卡、连续天数直接断**，孩子却真练满了 6 分钟。
     * 代价（可接受）：23:50 开始的一轮整体记到前一天。
     */
    const roundDateRef = useRef(toDateStr(new Date()))
  ```

  在 `beginSession()` 的 `savedRef.current = false` **下方**加：
  ```ts
      // 左眼节 = 一轮的开始：此刻定下本轮日期（换右眼时不刷新，否则又会跨零点裂成两天）
      if (sessionRef.current.eye === 'left') roundDateRef.current = toDateStr(new Date())
  ```

  把节结束落库 effect 里的
  ```ts
        date: toDateStr(new Date()),
  ```
  改成
  ```ts
        date: roundDateRef.current,
  ```

  `handleAnswer` 里彩蛋捕获那句 `const todayStr = toDateStr(new Date())` **保持不动**——彩蛋怪是"答对 5 连"的即时奖励、按墙上时钟记它发生在哪天是对的，与本轮的门槛/打卡结算无关（spec §5.5）。

  **③ `nextEyeOrFinish` 整体替换**（第 245-272 行）

  ```tsx
    async function nextEyeOrFinish() {
      if (session.eye === 'left') {
        savedRef.current = false
        setSession(createSession('right', readDurationSec()))
        return
      }
      const result = await doCheckIn(roundDateRef.current, session.durationSec)

      // 勋章必须在分流**之前**判定：它衡量的是 sessions 的跨天累计成果，
      // 与"今天算不算完成"无关。挪到分流之后 = 不达标日的勋章被推迟到下次打卡，
      // unlockedAt 从此失真（CLAUDE.md：勋章/怪兽取最早，首次达成时刻才是正确语义）。
      const unlocked = await syncBadges(Date.now())
      setNewBadges(unlocked)

      if (result.outcome === 'below-goal') {
        // 今天答对太少 = 今天不算完成：不打卡、不加分、不发保底怪、不算皮肤解锁、
        // 不放完成音（'finish' 在节结束时已经响过了）。只有勋章这个"累计事实"可以响。
        setNewSkins([])
        if (unlocked.length > 0) playSfx('badge')
        setCheckin(result)
        return
      }

      // 保底捕获：当天首次完成训练打卡时必得 1 只新怪兽
      let dailyCaptured: MonsterDef | null = null
      if (result.outcome === 'checked-in') {
        dailyCaptured = await captureDailyOnCheckin(false, roundDateRef.current, Date.now())
        if (dailyCaptured) {
          setCapturedThisSession((arr) => [...arr, dailyCaptured!])
          if (dailyCaptured.rarity !== 'common') {
            setCapturedByWorld((prev) => ({ ...prev, [dailyCaptured!.world]: [...prev[dailyCaptured!.world], dailyCaptured!.id] }))
          }
        }
      }
      const prevPoints = result.outcome === 'already' ? result.totalPoints : result.totalPoints - result.dailyPoints
      const skins = newlyUnlockedSkins(prevPoints, result.totalPoints)
      // 任何"额外收获"都用 badge 音效，纯打卡用 checkin
      playSfx(unlocked.length > 0 || skins.length > 0 || dailyCaptured ? 'badge' : 'checkin')
      setNewSkins(skins)
      setCheckin(result)
    }

    /**
     * 「再练一轮」：不达标后就地重来。
     *
     * 刻意**不用** `key` 让组件重挂载——卸载会跑 `voskRef.current?.stop()`，
     * 而 vosk-single 的 stop() 清空单例，重挂载就要重拉 3 个模型分片、拼 60MB Blob、
     * 让 worker 重新解压模型。那正是 commit 89743b6「iPad 换眼时白屏退出——vosk 模型
     * 并发加载把内存打爆」修过的路径。组件不卸载还顺带让语音一直在线，点完立刻能练。
     *
     * 代价是所有跨轮次状态都得手工复位，漏一个的后果都很隐蔽：
     *  - savedRef 不复位 → 新一轮的 session 根本不落库（finished effect 直接 return）
     *  - sumReactionRef/reactionCountRef 不复位 → 上一轮的反应样本混进 avgReactionMs，污染周报趋势
     *  - checkin 不复位 → 一进 finished 就被结算页抢先渲染成旧结果
     *  - capturedThisSession 不复位 → 上一轮的怪兽会在新一轮结算页再开箱一次
     * 这份清单由 src/training/goal-gate.test.ts 逐条锚定。
     *
     * 刻意**不**复位的：capturedByWorld（已拥有的储备怪，跨轮有效）、randomSkinId
     * （本次进页已挑定的皮肤，重掷会与 totalPoints 加载竞态）、totalPoints（只读缓存）、
     * muted / voskStatus / voskRef（语音与静音跨轮保持）。
     */
    function restartRound() {
      savedRef.current = false
      seqRef.current = 0
      targetShownAtRef.current = 0
      sumReactionRef.current = 0
      reactionCountRef.current = 0
      setCheckin(null)
      setNewBadges([])
      setNewSkins([])
      setCapturedThisSession([])
      setLastAnswer(null)
      setComboFx(null)
      setEggCaptureFx(null)
      setPaused(false)
      // 新一轮重新定日期（上一轮可能是零点前开始的）
      roundDateRef.current = toDateStr(new Date())
      setSession(createSession('left', readDurationSec()))
    }
  ```

  **④ 新增不达标结算页**：在 `if (checkin) {` 这一行（原第 286 行）**之前**插入：

  ```tsx
    // 不达标结算页。插在 if (checkin) 之前，靠 outcome 分流（同一个 state，天然互斥）。
    if (checkin && checkin.outcome === 'below-goal') {
      const need = shortfall(checkin.correctToday, checkin.goal)
      const pct = checkin.goal > 0 ? Math.min(1, checkin.correctToday / checkin.goal) : 1
      return (
        <div style={{ maxWidth: 420, margin: '0 auto', padding: '40px 20px', textAlign: 'center' }}>
          <div style={{ fontSize: 54 }}>😅</div>
          <h2 style={{ fontSize: 22, fontWeight: 800, marginTop: 6 }}>{t('goal.title')}</h2>
          <div className="fq-card" style={{ marginTop: 18 }}>
            <div style={{ fontSize: 22, fontWeight: 800, color: 'var(--violet)', fontVariantNumeric: 'tabular-nums' }}>
              {t('goal.progress', { n: checkin.correctToday, goal: checkin.goal })}
            </div>
            <div className="fq-bar" style={{ marginTop: 10 }}><i style={{ width: `${pct * 100}%` }} /></div>
            <p style={{ fontSize: 14, fontWeight: 700, marginTop: 12 }}>{t('goal.short', { n: need })}</p>
          </div>
          <p style={{ fontSize: 13, color: 'var(--muted)', marginTop: 12, lineHeight: 1.6 }}>{t('goal.why')}</p>
          <p style={{ fontSize: 13, color: 'var(--muted)', marginTop: 6 }}>{t('goal.noCheckin')}</p>
          {checkin.streak > 0 && (
            <p style={{ fontSize: 13, color: 'var(--coral)', fontWeight: 700, marginTop: 8 }}>
              {t('goal.streakWarn', { n: checkin.streak })}
            </p>
          )}
          <p style={{ fontSize: 12, color: 'var(--muted)', marginTop: 8 }}>{t('goal.kept')}</p>
          {newBadges.length > 0 && (
            <div className="fq-card" style={{ marginTop: 14 }}>
              <p style={{ fontSize: 13, fontWeight: 700 }}>{t('goal.badgeStill')}</p>
              <div style={{ display: 'flex', gap: 10, justifyContent: 'center', flexWrap: 'wrap', marginTop: 8 }}>
                {newBadges.map((b) => (
                  <span key={b.id} className="fq-chip">{b.emoji} {t(`badge.${b.id}`)}</span>
                ))}
              </div>
            </div>
          )}
          {/* 彩蛋怪在答对 5 连的瞬间就已落库（与打卡无关），是真实收获，藏起来反而奇怪 */}
          {capturedThisSession.length > 0 && (
            <div className="fq-card" style={{ marginTop: 14 }}>
              <p style={{ fontWeight: 700, marginBottom: 12 }}>{t('dex.openBox')}</p>
              <div style={{ display: 'flex', gap: 14, justifyContent: 'center', flexWrap: 'wrap' }}>
                {capturedThisSession.map((m, i) => (
                  <CapturedMonsterReveal key={`${m.id}-${i}`} def={m} delayMs={i * 500} />
                ))}
              </div>
            </div>
          )}
          <button className="fq-cta coral" style={{ width: '100%', marginTop: 16 }} onClick={restartRound}>
            {t('goal.retry')}
          </button>
          <button className="fq-btn" style={{ width: '100%', marginTop: 10 }} onClick={onHome}>
            {t('goal.later')}
          </button>
        </div>
      )
    }
  ```

  **⑤ 原打卡结果页的 3 处 `alreadyCheckedIn`**（原第 289 / 291 / 298 行）

  - `{checkin.alreadyCheckedIn ? '✓' : '🎉'}` → `{checkin.outcome === 'already' ? '✓' : '🎉'}`
  - `{checkin.alreadyCheckedIn ? t('train.checkedAlready') : t('train.checkedSuccess')}` → `{checkin.outcome === 'already' ? t('train.checkedAlready') : t('train.checkedSuccess')}`
  - `{!checkin.alreadyCheckedIn && <div …>{t('train.todayPoints', …)}</div>}` → `{checkin.outcome !== 'already' && <div …>{t('train.todayPoints', …)}</div>}`

  **⑥ 右眼节末按钮改中性文案**（原第 409 行）

  把
  ```tsx
          {session.eye === 'left' ? t('train.nextEye') : t('train.finishCheckin')}
  ```
  改成
  ```tsx
          {session.eye === 'left' ? t('train.nextEye') : t('train.finishRound')}
  ```
  理由：这个按钮是**唯一**能触发门槛判定的入口（§10 刻意不做训练中进度 HUD，孩子按下之前无从知道够不够），承诺「完成并打卡 🎊」却落到「😅 今天还差一点点」会制造新的挫败。「打卡」二字留给真正打上卡的那一页。

- [ ] **Step 5.4: 实现（App.tsx）。** 把
  ```tsx
  {view === 'train' && <TrainingPage />}
  ```
  改成
  ```tsx
  {/* onHome：不达标结算页的「回首页」次按钮。注意**不要**加 key——用重挂载来"重来"
      会 stop 掉 vosk 再重启，正是换眼白屏那条内存尖峰路径（见 TrainingPage.restartRound） */}
  {view === 'train' && <TrainingPage onHome={() => setView('home')} />}
  ```

- [ ] **Step 5.5: 跑到通过。** `npm test -- src/training/goal-gate.test.ts`
  预期：`Test Files  1 passed (1)` / `Tests  8 passed (8)`。
- [ ] **Step 5.6: 全量回归 + typecheck（解除 push 禁令的那一步）。** `npm test`（预期 **基线 + 52 个测试 / +3 个文件**）；`npm run typecheck` **必须零输出**（Task 2 遗留的断裂到此修完）。
  两者都干净之后，push 禁令才解除（真正的 push 在 Task 7 合并回 master 时做）。

- [ ] **Step 5.7: 手测（Browser 工具，不要截图）。**

  1. 起服务：`mcp__Claude_Browser__preview_start` 传 `{ "name": "focal-quest-dev" }`（`.claude/launch.json` 已有这个配置，端口 5173）。
  2. `mcp__Claude_Browser__navigate` 到 `http://localhost:5173`。
  3. **准备干净环境**（`javascript_tool`）：
     ```js
     localStorage.setItem('fzp.cssPxPerMm', '6')
     localStorage.setItem('fzp.durationSec', '60')   // 1 分钟档 → 整次门槛 10
     localStorage.setItem('fzp.flipMs', '600')
     localStorage.setItem('fzp.onboarded', '1')
     indexedDB.deleteDatabase('focalquest')
     'seeded'
     ```
     然后 `navigate` 到 `http://localhost:5173` 重新加载。
  4. **触发一轮不达标**：`javascript_tool` 执行
     ```js
     // 点「开始今日训练」
     ;[...document.querySelectorAll('button')].find(b => b.textContent.includes('开始今日训练')).click()
     // 1.5s 后自动进入答题；只按 5 次上（最多 5 个答对，必然 < 门槛 10）
     await new Promise(r => setTimeout(r, 2500))
     for (let i = 0; i < 5; i++) {
       window.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowUp' }))
       await new Promise(r => setTimeout(r, 800))
     }
     'left-eye answered 5'
     ```
  5. 等左眼节走完（`javascript_tool`：`await new Promise(r=>setTimeout(r,62000)); document.body.innerText.slice(0,200)`），预期含「本节完成」。点「换右眼继续 →」：
     ```js
     ;[...document.querySelectorAll('button')].find(b => b.textContent.includes('换右眼')).click(); 'switched'
     ```
  6. 右眼节同样只按 5 次、等走完，点「完成这一轮 →」（按钮文案已改中性，见 ⑥）。
  7. **验证不达标页**（`javascript_tool`）：
     ```js
     const txt = document.body.innerText
     JSON.stringify({
       hasTitle: txt.includes('今天还差一点点'),
       hasProgress: /答对 \d+ \/ 需要 10/.test(txt),
       hasRetry: txt.includes('再练一轮'),
       hasHome: txt.includes('回首页'),
       noCheckedSuccess: !txt.includes('打卡成功'),
     })
     ```
     **全部预期 `true`。**
  8. **验证没写打卡行**（`javascript_tool`）：
     ```js
     const d = await new Promise(res => { const r = indexedDB.open('focalquest'); r.onsuccess = () => res(r.result) })
     const n = await new Promise(res => { const q = d.transaction('checkins').objectStore('checkins').count(); q.onsuccess = () => res(q.result) })
     `checkins rows = ${n}`
     ```
     **预期 `checkins rows = 0`。**
  9. **验证重来复位**：点「🔄 再练一轮」，然后
     ```js
     ;[...document.querySelectorAll('button')].find(b => b.textContent.includes('再练一轮')).click()
     await new Promise(r => setTimeout(r, 2500))
     document.querySelector('.fzp-train-top')?.innerText
     ```
     **预期含「左眼 · 遮右眼」且计时接近 `0:5x`**（回到左眼、时间重新开始）。
  10. **验证练够能打卡**：在新一轮里每 700ms 轮流按四个方向直到两节走完（约 2 分钟）：
      ```js
      const keys = ['ArrowUp','ArrowDown','ArrowLeft','ArrowRight']
      let i = 0
      const id = setInterval(() => {
        window.dispatchEvent(new KeyboardEvent('keydown', { key: keys[i++ % 4] }))
        const b = [...document.querySelectorAll('button')].find(x => x.textContent.includes('换右眼') || x.textContent.includes('完成这一轮'))
        if (b) b.click()
      }, 700)
      await new Promise(r => setTimeout(r, 140000))
      clearInterval(id)
      document.body.innerText.slice(0, 200)
      ```
      **预期含「打卡成功！」**（随机四方向轮流的命中率约 25%，60 秒里约 85 次作答 → 约 21 个答对，两节合计远超门槛 10）。
  11. **验证已落打卡行**：重跑第 8 步的脚本，**预期 `checkins rows = 1`**。
  12. `mcp__Claude_Browser__read_console_messages` 传 `{ "onlyErrors": true }`，**预期无 error**。

- [ ] **Step 5.8: 提交。**
  ```
  git add src/training/TrainingPage.tsx src/App.tsx src/training/goal-gate.test.ts
  git commit -m "feat(门槛): 不达标结算页 + 「再练一轮」就地复位，训练页按 outcome 三态分流"
  ```

---

### Task 6: 首页第三态 + 补签提示 + 设置页门槛说明

**Files**
- Modify: `E:\developer\fzp\src\HomePage.tsx`
- Modify: `E:\developer\fzp\src\SettingsPage.tsx`

**Interfaces**
- Consumes: `readDurationSec` / `goalPerRound` / `GOAL_CORRECT_PER_MIN`（Task 1）、`HomeStats.correctToday` / `goalToday`（Task 2）、`RepairReason='attempted'`（Task 3）、`home.partial` / `repair.attempted` / `goal.settingsHint`（Task 4）
- Produces: 无新导出

- [ ] **Step 6.1: 实现（HomePage）。** 在 `E:\developer\fzp\src\HomePage.tsx` 里：

  在 `import { challengeUnlocked } from './challenge/challenge-unlock'` **下方**加：
  ```ts
  import { readDurationSec } from './training/goal'
  ```

  把 `useEffect` 里的
  ```ts
      getHomeStats(today).then(setStats)
  ```
  改成
  ```ts
      // 传时长：首页要显示准确的当天门槛（"练了 12/30"）
      getHomeStats(today, readDurationSec()).then(setStats)
  ```

  把补签成功回调里的
  ```ts
                  setStats(await getHomeStats(today))
  ```
  改成
  ```ts
                  setStats(await getHomeStats(today, readDurationSec()))
  ```

  把今日状态行整体替换为：
  ```tsx
          {/* 今日状态三态：已完成 / 练了但没练够 / 一点没练。
              第三态是训练完成门槛的配套——只说"今天还没练"会和统计页"今天 1 次"打架，
              而 correctToday > 0 这个条件保证答对 0 个时不会出现"今天练了 0/30"这种怪话。 */}
          <div
            className="fq-rise"
            style={{
              fontSize: 15,
              fontWeight: 800,
              color: stats?.checkedInToday ? 'var(--mint)' : stats && stats.correctToday > 0 ? 'var(--coral)' : 'var(--ink)',
              animationDelay: '0.05s',
            }}
          >
            {stats?.checkedInToday
              ? t('home.checkedToday')
              : stats && stats.correctToday > 0
                ? t('home.partial', { n: stats.correctToday, goal: stats.goalToday })
                : t('home.notYetToday')}
          </div>
  ```

  在 `repair.reason === 'month-limit'` 那个提示卡**下方**插入：
  ```tsx
          {repair && !repair.ok && repair.reason === 'attempted' && (
            <div className="fq-card" style={{ color: 'var(--muted)', fontSize: 13 }}>{t('repair.attempted')}</div>
          )}
  ```

- [ ] **Step 6.2: 实现（SettingsPage）。** 在 `E:\developer\fzp\src\SettingsPage.tsx` 里：

  在 `import { getAccount } from './sync/account'` **下方**加：
  ```ts
  import { goalPerRound, sanitizeDurationSec, GOAL_CORRECT_PER_MIN } from './training/goal'
  ```

  把时长 state 的初值（第 31-34 行）
  ```ts
    const [durationSec, setDurationSec] = useState(() => {
      const v = lsGet('fzp.durationSec')
      return v ? Number(v) : 180
    })
  ```
  改成
  ```ts
    // 时长口径只有一个出处：脏值（'abc'→NaN、'0'→0）在这里就被兜成默认 180，
    // 否则下面的门槛提示会显示成"× NaN 分钟"，四个档位按钮也会全都不高亮。
    // Number(null) 是 0，同样被 sanitize 兜成 180，与改动前行为一致。
    const [durationSec, setDurationSec] = useState(() => sanitizeDurationSec(Number(lsGet('fzp.durationSec'))))
  ```

  在时长档位卡片里，`</div>`（`.fq-seg` 的收尾）**之后**、该卡片 `</div>` **之前**插入：
  ```tsx
          {/* 家长唯一能看到门槛数值的地方——孩子说"我练了但没打上卡"时得有处可查 */}
          <p style={{ flexBasis: '100%', margin: 0, fontSize: 12, color: 'var(--muted)', lineHeight: 1.6 }}>
            {t('goal.settingsHint', { n: goalPerRound(durationSec), per: GOAL_CORRECT_PER_MIN, min: sanitizeDurationSec(durationSec) / 60 })}
          </p>
  ```

  改完那张卡应长这样（用于核对插入位置）：
  ```tsx
        <div className="fq-card" style={{ marginTop: 14, display: 'flex', alignItems: 'center', justifyContent: 'space-between', flexWrap: 'wrap', gap: 8 }}>
          <span style={{ fontSize: 14, fontWeight: 700 }}>{t('settings.duration')}</span>
          <div className="fq-seg">
            {[60, 120, 180, 300].map((sec) => (
              <button key={sec} className={durationSec === sec ? 'on' : ''} onClick={() => setDurationSec(sec)}>
                {sec / 60}{t('settings.minute')}
              </button>
            ))}
          </div>
          <p style={{ flexBasis: '100%', margin: 0, fontSize: 12, color: 'var(--muted)', lineHeight: 1.6 }}>
            {t('goal.settingsHint', { n: goalPerRound(durationSec), per: GOAL_CORRECT_PER_MIN, min: sanitizeDurationSec(durationSec) / 60 })}
          </p>
        </div>
  ```

  > 注意这行提示说的是**「按当前档位，一天要答对几个」**，不是"今天实际的门槛"。若孩子今天已经练过更长的一节，当天真实门槛按那节算（`goalForDay`，spec §3.5），会高于这里显示的数——那是有意的：首页第三态「今天练了 12/50」显示的才是当天真实门槛。家长看这行是为了知道"档位对应多少"，两处不矛盾。

- [ ] **Step 6.3: 回归 + typecheck。** `npm test`（预期与 Task 5 收尾**同一个数**：基线 + 52 / +3 文件，本任务不加测试）；`npm run typecheck` 零输出。

- [ ] **Step 6.4: 手测（Browser 工具，不要截图）。**

  1. 服务若已停，重新 `preview_start` `{ "name": "focal-quest-dev" }`；`navigate` 到 `http://localhost:5173`。
  2. **首页第三态**（`javascript_tool`）：
     ```js
     // 清库后只塞一条"练了但没练够"的 session（当天，答对 7 < 门槛 10）
     localStorage.setItem('fzp.durationSec', '60')
     await new Promise(res => { const r = indexedDB.deleteDatabase('focalquest'); r.onsuccess = res; r.onerror = res; r.onblocked = res })
     'cleared'
     ```
     `navigate` 到 `http://localhost:5173`（让 Dexie 重建 schema），再执行：
     ```js
     const today = (d => `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`)(new Date())
     const d = await new Promise(res => { const r = indexedDB.open('focalquest'); r.onsuccess = () => res(r.result) })
     await new Promise(res => {
       const tx = d.transaction('sessions', 'readwrite')
       tx.objectStore('sessions').add({ date: today, startedAtMs: 1, eye: 'left', answered: 10, correct: 7, flips: 7, elapsedSec: 60, acuity: 0.8 })
       tx.oncomplete = res
     })
     d.close(); 'seeded session'
     ```
     `navigate` 刷新后：
     ```js
     const txt = document.body.innerText
     JSON.stringify({ partial: /今天练了 7\/10/.test(txt), notNotYet: !txt.includes('今天还没练') })
     ```
     **两项均预期 `true`。**
  3. **补签 attempted 提示**（`javascript_tool`）：
     ```js
     const fmt = d => `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`
     const day = n => { const x = new Date(); x.setDate(x.getDate() + n); return fmt(x) }
     const d = await new Promise(res => { const r = indexedDB.open('focalquest'); r.onsuccess = () => res(r.result) })
     await new Promise(res => {
       const tx = d.transaction(['checkins', 'sessions'], 'readwrite')
       // 前天打过卡（积分足够补签），昨天练了但没练够（只有 session、没有 checkin 行）
       tx.objectStore('checkins').put({ date: day(-2), streak: 3, dailyPoints: 90, totalPoints: 900 })
       tx.objectStore('sessions').add({ date: day(-1), startedAtMs: 1, eye: 'left', answered: 5, correct: 3, flips: 3, elapsedSec: 60, acuity: 0.8 })
       tx.oncomplete = res
     })
     d.close(); 'seeded gap'
     ```
     `navigate` 刷新后：
     ```js
     const txt = document.body.innerText
     JSON.stringify({
       showsAttempted: txt.includes('补签卡只能补完全没练的日子'),
       noRepairButton: ![...document.querySelectorAll('button')].some(b => b.textContent.trim() === '补签'),
     })
     ```
     **两项均预期 `true`**（若 `noRepairButton` 为 false，说明 `attempted` 闸门没接上，回查 Task 3 的 `rewards-service.ts`）。

  3b. **反向验证（评审 major #2 的正例）：昨天练够了却没点完成键 → 仍可补。** 把昨天那条 session 的 `correct` 抬到门槛之上再刷新：
     ```js
     const fmt = d => `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`
     const y = (x => (x.setDate(x.getDate() - 1), fmt(x)))(new Date())
     const d = await new Promise(res => { const r = indexedDB.open('focalquest'); r.onsuccess = () => res(r.result) })
     await new Promise(res => {
       const st = d.transaction('sessions', 'readwrite').objectStore('sessions')
       st.openCursor().onsuccess = e => {
         const c = e.target.result
         if (!c) return res()
         // 昨天那条：elapsedSec 60 → 那天门槛 10，把 correct 抬到 12（练够了，只是没点完成键）
         if (c.value.date === y) { c.update({ ...c.value, correct: 12, answered: 14 }); return res() }
         c.continue()
       }
     })
     d.close(); 'bumped'
     ```
     `navigate` 刷新后：
     ```js
     const txt = document.body.innerText
     JSON.stringify({
       noAttemptedHint: !txt.includes('补签卡只能补完全没练的日子'),
       hasRepairButton: [...document.querySelectorAll('button')].some(b => b.textContent.trim() === '补签'),
     })
     ```
     **两项均预期 `true`。** 若 `hasRepairButton` 为 false，说明闸门被写成了"那天有 session 行就不可补"——那会把**最该补的一天**（练到远超门槛却被收走 iPad、没点完成键）永久堵死，回查 Task 3 的 `fellShortOn`。
  4. **设置页门槛提示**：点导航「⚙️」，然后
     ```js
     const txt = document.body.innerText
     JSON.stringify({
       hint60: txt.includes('每天需答对 10 个才算完成'),
       hasFormula: txt.includes('每分钟 5 个'),
     })
     ```
     **两项均预期 `true`**（当前 `fzp.durationSec = 60`）。再点「3分钟」档位后重读，**预期变成「每天需答对 30 个才算完成」**。
  5. `read_console_messages` `{ "onlyErrors": true }`，**预期无 error**。

- [ ] **Step 6.5: 提交。**
  ```
  git add src/HomePage.tsx src/SettingsPage.tsx
  git commit -m "feat(门槛): 首页「练了但没练够」第三态 + 补签 attempted 提示 + 设置页门槛说明"
  ```

---

### Task 7: 文档收尾

**Files**
- Modify: `E:\developer\fzp\CLAUDE.md`

**Interfaces**
- Consumes: 无
- Produces: 无

- [ ] **Step 7.1: CLAUDE.md 状态区加条目。** 在「迭代 2·限时挑战 ✅ 完成并合并：…」那条**下方**、「下一步」**上方**插入：

```markdown
- 训练完成门槛 ✅ 完成并合并：堵掉「挂机 3 分钟照样打卡拿分续连续天数」这个上线漏洞（`dailyPoints` 的 `+30` 是打卡奖励、与答对数无关，所以挂机能凭空印出可换现实奖励的货币）。门槛 = **每分钟 5 个答对 × 时长比例 × 双眼合计**（1/2/3/5 分钟档 → 单节 5/10/15/25、整次 10/20/30/50），全部常量在 `src/training/goal.ts` 一处，调松紧只改 `GOAL_CORRECT_PER_MIN`（**要全放行须把它与 `GOAL_MIN_PER_EYE` 同时设 0**，只设前者会被下限顶住）。**六个反直觉但必须坚持的口径**：①**整次合计判，不按单眼各判**——弱眼天生慢，各判等于对最该练的那只眼施加最严标准，且某只眼状态差就整轮作废要重练 6 分钟；②**判据是「当天全部 sessions 答对之和」，与 `dailyPoints` 完全同源**，于是"再练一轮补够"天然成立（结算页那个重来按钮的基础），也不会出现"门槛按单节、发分按全天"两把尺子；③判定顺序硬定 **already → below-goal → checked-in**，已打卡必须先短路，否则"第一轮达标已打卡、第二轮又练但合计不够"会被误判成打卡失败、把到手的连续天数吓没；④**不达标时 session 照常落库照常推云、勋章照常判定落库**（`syncBadges` 必须在分流之前调，挪到之后会让 `unlockedAt` 失真），只是不打卡/不加分/不发保底怪/不算皮肤解锁/不放完成音——**门槛只决定"今天算不算完成"，不决定"训练事实是否被记录"**；⑤**门槛的时长基准取「当天真实练过的最长一节 `elapsedSec`」**（`goalForDay`），当天没练过才用当前设置——`fzp.durationSec` 就是设置页那四个按钮、而设置页在常驻导航里，只按当前设置算就能"练完不达标 → 点一下「1分」→ 门槛从 30 掉到 10 → 已练的答对数照样算 → 下次直接打卡成功"（反向也一样：家长中途把 1 分改成 5 分会让当天已练的全作废）；⑥**补签闸门的判据是「那天确实没练够」**（`dayFellShort` 重算那天的门槛），**不是**「那天有没有 session 行」——`saveSession` 只在计时走满时落库，所以"有行"其实等价于"完整走完过一节"，拿它当判据会把"练到 40 个（门槛 30）却被收走 iPad、没点完成键"那天永久堵死（最该补的一天），同时放过"点开就退出"的日子（一行都不落）。配套堵住**补签卡这条 50 分的付费绕过通道**（`canRepair` 新增 `'attempted'`）。「再练一轮」**刻意不用 `key` 重挂载**（会 stop 掉 vosk 再重启，正是 89743b6 那条换眼白屏的内存尖峰路径），改为 `restartRound()` 手工复位 15 项 state/ref，清单由 `src/training/goal-gate.test.ts` 源文本契约逐条锚定。另外**一轮训练锁定一个日期**（`roundDateRef`，左眼节开始时定）：原先 `saveSession` / `doCheckIn` 各自现算日期，跨零点会把左右两节裂到两天，加门槛后可能整轮判 below-goal 且两天都没打卡行、连续天数直接断。右眼末按钮从「完成并打卡 🎊」改成中性的「完成这一轮 →」（`train.finishRound`），因为它点下去可能落到不达标页。顺带把 `TrainingPage` 私有的无校验 `readDurationSec` 收进 `goal.ts` 并加钳制——原实现 `Number('abc')=NaN` 会让门槛所有比较为 false（**静默全放行**）、`'0'` 会让门槛变 0。首页加「今天练了 12/30」第三态（消除"首页说没练、统计说练了 1 次"的观感打架），设置页时长档位下显示当天门槛。明确不做：速率型门槛（会惩罚调节慢的孩子，与医学目标相反）、积分补发/重算（撞 `reconcile.ts` 的硬口径且积分是货币）、统计与周报口径改动（训练量是事实）、服务端门槛校验。**三条已知且明示接受**：①旧版本设备按老逻辑打的卡经云端 LWW / `restoreBackup` 拉回来仍会落库——**门槛是本地写入闸门，不是数据校验器**；②**发分不对称**：同样练两轮，"第一轮 29 差一个 → 第二轮 40"按当天累计 69 结算得 375 分，而"第一轮就 40 达标"只按 40 结算得 230 分（+63%），即"多练只有先失败才发分"。已与用户确认保留现状（要吃这个差价得先坐满 6 分钟故意不答再认真练 6 分钟），止损阀门是 `goal.ts` 的 `POINTS_CORRECT_CAP_FACTOR`（默认 0=不封顶；真要开取 **6**，**不要取 2**——3 分钟档门槛只有 30，封在 60 会把诚实一轮的当日积分从 430-830 砍到 330）；③孩子长期把「单眼时长」钉在 1 分钟每天只需 10 个答对——非本次引入（无门槛时改档就已把训练量砍到 1/3），要堵得把时长挪进家长折叠区或加家长口令，留待后续。设计见 [spec](docs/superpowers/specs/2026-07-27-训练完成门槛-design.md)。
```

- [ ] **Step 7.2: CLAUDE.md「下一步」补一句。** 在「下一步：真机持续使用收集反馈（限时挑战的节奏松紧待闺女实测，…」这句里，`；迭代 3 按上述子迭代推进` **之前**插入：

```markdown
；**训练完成门槛需闺女实测校准**——若认真练却被拦，只改 `src/training/goal.ts` 的 `GOAL_CORRECT_PER_MIN`（5 → 3 或 2），**不要**改判定逻辑、不要加"正确率豁免"分支、更不要改成速率型
```

- [ ] **Step 7.3: 更新单测基线快照。** 把「开发命令」小节那行 `> 单测基线：**555 个 / 58 个文件**（2026-07-27 实测 …）` 改成本次实测的真实数字（= Step 0.1 的基线 + 52 个测试 / + 3 个文件），并保留"开工前自己跑一遍"那句提醒。

- [ ] **Step 7.4: 最终验证。** 依次跑：
  - `npm test` → 预期 **基线 + 52 个测试 / +3 个文件**，全绿
  - `npm run typecheck` → 零输出
  - `npm run typecheck:functions` → 零输出（本计划没碰 `functions/`，这一步是确认没有连带破坏）
  - `git status` → 只应看到本计划列出的那些文件

- [ ] **Step 7.5: 提交。**
  ```
  git add CLAUDE.md
  git commit -m "docs: CLAUDE.md 记入训练完成门槛（口径、止损开关与明确不做）"
  ```

- [ ] **Step 7.6: 合回 master 再 push（分支到此收束）。**
  Step 0.5 建分支的目的就是把 Task 2 那次 typecheck 红关在分支里。合并前**必须**确认 Step 7.4 全部通过（尤其 typecheck 零输出）——CI 的质量门跑的就是这几条。
  ```
  git switch master
  git merge --no-ff feat/training-goal-gate -m "feat: 训练完成门槛（挂机不再能打卡拿分续连续天数）"
  npm test && npm run typecheck && npm run typecheck:functions
  git push
  git branch -d feat/training-goal-gate
  ```
  用 `--no-ff` 让这一组提交在 master 上留一个可回滚的合并点（门槛是产品行为变更，实测不满意时整段回退比挑 commit 省事）。
  push 之后 `deploy-cf.yml` 会自动构建发布到 <https://fq.myacelw.top>；孩子那台 iPad 联网打开一次即更新（首次可能仍看到旧版本，刷新一次才由新 SW 接管，这是预期行为）。

---

## 自查对照表

| spec 小节 | 覆盖任务 |
|---|---|
| §3 门槛数值与脏时长钳制 | Task 1 |
| §3.5 门槛按真实练过的时长（防追溯改档） | Task 1（`goalForDay` + 单测）/ Task 2（`doCheckIn` 与 `getHomeStats` 都走它 + 集成单测） |
| §4.3 发分不对称（数值 + 止损阀门） | Step 0.6 决策点 / Task 1（`pointsCorrect`）/ Task 2（锚定 230 vs 375） |
| §8.5 一轮锁一个日期（跨零点） | Task 5（`roundDateRef` + 契约测试） |
| §7.5 右眼末按钮改中性文案 | Task 4（`train.finishRound`）/ Task 5（⑥） |
| §4 整次判定 / 判定顺序 / 当天累计 | Task 2（`doCheckIn` 实现 + 单测锚定三态顺序） |
| §5.1 session 照常落库 | 无改动（Task 2 的实现刻意不碰 `saveSession`） |
| §5.2 / §5.3 不写打卡行、积分与连续天数 | Task 2 |
| §5.4 勋章照常解锁不庆祝 | Task 5（`syncBadges` 在分流前 + 低调提示 + 契约测试锚定顺序） |
| §5.5 保底怪不发 / 彩蛋怪保留 | Task 5 |
| §5.6 补签闸门 | Task 3 |
| §5.7 同步（session 推、checkin 不推） | Task 2 单测「不达标不推云」锚定 |
| §5.8 统计零改动 + 首页第三态 | Task 6 |
| §5.9 限时挑战当天不解锁 | 零改动（`challengeUnlocked` 只看 `checkedInToday`） |
| §6 接口变更 | Task 1 / 2 / 3 |
| §7 UI 与文案 | Task 4（文案）/ Task 5（不达标页）/ Task 6（首页与设置页） |
| §8 重来的状态复位 | Task 5 |
| §9 风险与止损 | 各任务注释 + Task 7 的 CLAUDE.md 条目 |
| §10 明确不做 | Global Constraints + Task 7 |
| §11 测试策略 | Task 1 / 2 / 3 / 4 / 5 的测试步骤 |

---

## 评审意见处置

2026-07-27 评审 verdict `needs-fixes`，10 条逐条处置如下。

### 已修（blocker / major）

| # | 严重度 | 问题 | 怎么修的 |
|---|---|---|---|
| 1 | blocker | 门槛完全派生自 `fzp.durationSec`（= 设置页四个按钮，孩子随手可点）→ 练完不达标后调小档位可**追溯降低**当天门槛，直接打卡成功；反向调大又会让当天已练的轮次作废 | 新增 `goalForDay(durationSec, 各节 elapsedSec)`：**当天练过就按最长那节算，没练过才用当前设置**（spec §3.5 / Global Constraints）。`doCheckIn`、`getHomeStats`、补签闸门三处共用它。`goal.test.ts` 加了"5 分钟档练完后改 1 分钟仍是 50"与反向两条，`checkin.test.ts` 加了同名集成断言。**评审建议的 `Math.max(durationSec, ...elapsed)` 未照抄**：那个写法没解决"调大档位追溯抬高门槛"这一半，改成"练过就完全由练过的时长说话"两个方向一起修 |
| 2 | major | 补签闸门用"那天有没有 session 行"当判据，而 `saveSession` 只在计时走满时落库 → "练到 40 个却没点完成键"的日子**不可补**（最该补的一天），"点开就退出"的日子照样能花 50 分买回连续 | 判据改成**重算那天的门槛**：`dayFellShort(correctSum, elapsedSecs)`（无 session 行 → false）；服务层 `attemptedOn` 改名 `fellShortOn`、改成取行再喂纯函数。spec §5.6 里那句"恒等"与 Task 3 的同款注释已删除并写明它错在哪。`goal.test.ts` 加 3 条（没练过 / 练了没练够 / 练够却没打卡）、`ledger.test.ts` 加 1 条契约、Task 6 加手测 3b 反向验证 |
| 3 | major | Global Constraints 要求每任务收尾 typecheck 干净，而 Task 2 明写"带一条已知断裂提交"；本仓在 master 上逐任务提交，push 后 CI 必红、线上停在旧版本 | 三条护栏一起上：新增 **Step 0.5 建分支 `feat/training-goal-gate`**；Global Constraints 写明"**Task 2 是唯一被授权 typecheck 红的提交**"；Step 2.7 加 ⛔「到 Step 5.6 之前一律不许 push」，Step 5.6 标注"解除 push 禁令的那一步"，新增 **Step 7.6** 合并回 master 再 push（`--no-ff` 留可回滚合并点） |
| 4 | major | `goal.short` / `goal.kept` 承诺"再答对 N 个""这一轮已经记下"，但没走满一整节根本不落库、答对数全丢 | ZH/EN 双份改成「再练一轮（左右各一节），答对 {n} 个就完成啦」/「练完整节才会记下，答对数会累加」（spec §7.1 / §7.6、Task 4）。键数不变，`fill()` 单占位符约束仍满足 |
| 5 | major | 发分不对称：同样练两轮，"先失败再补够"375 分 vs"第一轮就达标"230 分（+63%），本次改动新引入，而积分是能换现实奖励与补签卡的货币 | ①具体数值与推导写进 spec §4.3（含决策表 row 11、风险表 #22）；②**新增 Step 0.6 决策点，要求开工前拿到用户明确答复**；③`goal.ts` 提供止损阀门 `POINTS_CORRECT_CAP_FACTOR` + `pointsCorrect()`（默认 0 = 不封顶 = 现状行为），`doCheckIn` 首次结算处已接上，`reconcile.ts` 一字未动；④`checkin.test.ts` 锚定 230 / 375 两个数，避免被当 bug"优化"掉。**评审建议的 `factor = 2` 明确驳回**：3 分钟档门槛只有 30，而一节现实产出 40-80 个（spec §3.2）、一轮 80-160，封在 60 会把诚实一轮的当日积分从 430-830 砍到 330，属于严重误伤——真要封顶 factor 应取 6，这条已写进常量注释与 Step 0.6 |

### 已修（minor）

| # | 问题 | 怎么修的 |
|---|---|---|
| 6 | `beforeEach` 与 `pushCheckin` 的 fire-and-forget 事务竞态，会偶发复活一条今日打卡行 | `beforeEach` 清表前 `await new Promise((r) => setTimeout(r, 0))` 让出一拍，并在每条走 `'checked-in'` 分支的用例结尾 `waitFor` 到 outbox 落地（照 `src/data/api.test.ts` 的稳法） |
| 7 | 跨零点的一轮被拆成两天 → 可能整轮判 below-goal 且两天都断连续 | 采纳：新增 `roundDateRef`（左眼节开始时定，`restartRound` 重定），`saveSession` / `doCheckIn` / `captureDailyOnCheckin` 共用（spec §8.5、Task 5 ②b），契约测试锚定 |
| 8 | `/<TrainingPage[^>]*\bkey=/` 会被 `=>` 里的 `>` 截断，闸门形同虚设 | 改成 `indexOf('<TrainingPage')` + `slice` 到 `'/>'` 截出整个标签再 `not.toContain('key=')`，并加 `expect(i).toBeGreaterThan(-1)`（Task 5 契约测试内附注释说明原因） |
| 9 | `durationSec / 60` 未 sanitize → 脏值时提示显示"× NaN 分钟"，四个档位也全不高亮 | `min:` 改走 `sanitizeDurationSec(durationSec) / 60`，且 `SettingsPage` 的 `useState` 初值统一成 `sanitizeDurationSec(Number(lsGet('fzp.durationSec')))`（Task 6 Step 6.2） |
| 10 | 右眼末按钮写「完成并打卡 🎊」却可能落到不达标页 | 采纳"改中性文案"这一路：新增 `train.finishRound`「完成这一轮 →」/「Finish this round →」，删 `train.finishCheckin`（全仓仅一处引用），`GOAL_KEYS` 14 条并同步数量断言。**未采纳另一路**（在单节完成页补"今日答对 {n}/{goal}"）：那一格需要在节末再查一次库、且它是"当天累计"而非"本节"，摆在单节统计里语义混乱；文案改中性已足够消除承诺-结果不一致 |

### 无需改动 / 部分驳回

- **#1 的后半「长期把档位钉在 1 分钟」——本计划不修**（已在 spec §3.5 末、决策表 row 12、风险表 #20 显式记为已知代价，并写进 Step 0.6 请用户拍板）。技术依据：这不是本次改动引入的洞。**在完全没有门槛的今天，孩子把单眼时长从 3 分改成 1 分就已经直接把训练量砍到 1/3**，门槛只是随之等比缩放（而"按时长等比缩放"本身是不可动摇的医学口径，见 spec §3.3——固定值会惩罚调节慢的孩子）。真正属于本次引入的是"**事后**改档能追溯放宽已经练过的那天"，那一半已按 #1 修死。要连"钉低"一起堵，得改设置页的可达性（挪进家长折叠区 / 家长口令），是独立的产品改动，不该塞进本计划的 diff。
- **#5 的具体改法（`POINTS_CORRECT_CAP_FACTOR = 2`）驳回**，理由见上表 #5：会把诚实训练的当日积分砍掉 22-60%。已改为"默认不封顶 + 用户拍板 + 若封顶取 6"，并把这条误伤写进常量注释，防后来者照抄 2。
- **#10 的第二方案（finished 页加一格「今日答对 n/goal」）未采纳**，理由见上表 #10。
