# 视标自适应 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让视标大小按训练表现自动收紧（或在开关关闭时给出一键可应用的建议），把「建议调小视标」从周报里一行没人执行的文案，变成真的会发生的事。

**Architecture:** 一个纯函数模块 `src/training/optotype-auto.ts` 吃 `SessionRow[]` 吐决策，不碰 DOM、不碰 db；`TrainingPage` 在一轮结束、sessions 落库后调用它一次；开关只决定决策是「直接执行」还是「摆成待办等家长按」。撤回与开关都在设置页已折叠的家长区。

**Tech Stack:** TypeScript + React + Vite；vitest + fake-indexeddb；localStorage（不进云同步）。

**来源：** [spec](../specs/2026-07-29-视标自适应-design.md)。所有阈值与口径已按闺女 2026-07-11~07-28 的真实数据定稿并逐节确认。

## Global Constraints

- **医学铁律：视标尺寸必须严格等于 `毫米设定 × 屏幕标定`。** 其祖先链上不许有任何 `transform: scale`。本计划改的是「毫米设定」这个数，不改渲染链路。
- **自动调整只朝「变难」方向。** 永远不自动调大。要放松只能家长手动。（spec §3.1）
- **自动下限 `AUTO_FLOOR_MM = 0.6`**（40cm 下等效视力 0.97）。手动滑块仍可到 0.3mm，下限只约束自动逻辑。（spec §3.2）
- **回退需要观察期内 2 个训练日都破线**，不是任一天。（spec §3.3，由 07-18 那个孤立疲劳日实证）
- **窗口/冷却/观察期一律按「训练日序号」算，不碰自然日。**（spec §5.3.1）
- **撤回按钮只在设置页家长区，不许出现在结算页。**（spec §4.3）
- **中英文案必须成对**——`src/i18n.test.ts` 用 `hasKey('zh'|'en', k)` 逐键校验。
- **开工第一步先跑 `npm test` 记真实基线**（写这份计划时是 720 个 / 74 文件）。

---

## File Structure

**新建**
- `src/training/optotype-auto.ts` — 常量、`OptotypeAdjust`/`AdjustDecision` 类型、`decideOptotypeAdjust`、`sanitizeSizeMm`、`readSizeMm`/`readLastAdjust`/`writeAdjust`/`readAutoEnabled`
- `src/training/optotype-auto.test.ts` — 纯函数单测，含真实数据回放

**修改**
- `src/SettingsPage.tsx` — 读取改走 `sanitizeSizeMm`；家长区加「自动跟随难度」开关 + 最近一次调整 + 撤回
- `src/training/TrainingPage.tsx` — 读取改走 `sanitizeSizeMm`；一轮结束调用决策；结算页两个分支各渲染一张卡
- `src/challenge/ChallengePage.tsx` — 读取改走 `sanitizeSizeMm`
- `src/stats/weekly-report.ts` 无需改逻辑；只改 `src/i18n.tsx` 里 `stats.suggest.highAccuracy` 的文案
- `src/i18n.tsx` / `src/i18n.test.ts` — 10 组中英文案

**为什么单独成文件**：`optotype-size.ts` 管的是 mm↔视力的几何换算（纯数学），本模块管的是「要不要调、调到多少」的策略与状态，职责不同。

---

## Task 1: 纯函数核心（判定 + 钳制）

**Files:**
- Create: `src/training/optotype-auto.ts`
- Create: `src/training/optotype-auto.test.ts`

**Interfaces:**
- Consumes: `SessionRow`（`src/data/db.ts`，字段 `date/eye/answered/correct/acuity/avgReactionMs?`）
- Produces:
  - `AUTO_WINDOW_DAYS=3` / `AUTO_MIN_ACCURACY=0.95` / `AUTO_MAX_REACTION_MS=2000` / `AUTO_COOLDOWN_DAYS=3` / `AUTO_STEP_MM=0.1` / `AUTO_FLOOR_MM=0.6` / `REVERT_MIN_ACCURACY=0.85` / `REVERT_REACTION_FACTOR=1.5` / `REVERT_COOLDOWN_DAYS=10` / `SIZE_MIN_MM=0.3` / `SIZE_MAX_MM=2` / `DEFAULT_SIZE_MM=1`
  - `interface OptotypeAdjust { from: number; to: number; atDate: string; kind: 'tighten'|'revert'|'manual'; baselineReactionMs: number }`
  - `type AdjustDecision = { action:'none' } | { action:'tighten'; from:number; to:number; baselineReactionMs:number } | { action:'revert'; from:number; to:number }`
  - `sanitizeSizeMm(mm: number): number`
  - `decideOptotypeAdjust(sessions, currentMm, last, todayStr): AdjustDecision`

- [ ] **Step 1: 记基线**

Run: `npm test`
记下「N passed / M files」。

- [ ] **Step 2: 写失败的测试**

新建 `src/training/optotype-auto.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import type { SessionRow } from '../data/db'
import {
  AUTO_FLOOR_MM, AUTO_STEP_MM, DEFAULT_SIZE_MM, SIZE_MAX_MM,
  sanitizeSizeMm, decideOptotypeAdjust, type OptotypeAdjust,
} from './optotype-auto'

/** 造一天的节次：给定日期、正确率、若干节的反应时间 */
function day(date: string, accuracy: number, reactions: number[]): SessionRow[] {
  return reactions.map((ms, i) => ({
    date,
    startedAtMs: 0,
    eye: (i % 2 === 0 ? 'left' : 'right') as 'left' | 'right',
    answered: 100,
    correct: Math.round(accuracy * 100),
    flips: 30,
    elapsedSec: 180,
    acuity: 0.7,
    avgReactionMs: ms,
  }))
}

/** 闺女 2026-07-11 ~ 07-28 的真实数据（12 个训练日，正确率与日中位反应） */
const REAL: Array<[string, number, number[]]> = [
  ['2026-07-11', 0.95, [3757, 4291]],
  ['2026-07-12', 0.97, [2374, 1954, 1937]],
  ['2026-07-13', 0.98, [2176, 2735, 2014, 6446, 1534]],
  ['2026-07-14', 1.00, [1667, 1884, 1439]],
  ['2026-07-15', 1.00, [1787, 1837]],
  ['2026-07-16', 1.00, [1644, 1794]],
  ['2026-07-17', 1.00, [1904, 1550]],
  ['2026-07-18', 0.96, [3868, 4814]],
  ['2026-07-21', 1.00, [2794, 2300]],
  ['2026-07-26', 0.98, [1664, 1647, 4916]],
  ['2026-07-27', 0.98, [1167, 1183]],
  ['2026-07-28', 0.97, [1562, 1545]],
]

describe('sanitizeSizeMm —— 三个读取点的唯一出口', () => {
  it('脏值回默认：NaN / 0 / 负数', () => {
    expect(sanitizeSizeMm(Number('abc'))).toBe(DEFAULT_SIZE_MM)
    expect(sanitizeSizeMm(0)).toBe(DEFAULT_SIZE_MM)
    expect(sanitizeSizeMm(-1)).toBe(DEFAULT_SIZE_MM)
  })

  it('越界钳回滑块范围', () => {
    expect(sanitizeSizeMm(99)).toBe(SIZE_MAX_MM)
    expect(sanitizeSizeMm(0.05)).toBe(0.3)
  })

  it('正常值原样通过（并消掉浮点毛刺）', () => {
    expect(sanitizeSizeMm(0.7)).toBe(0.7)
    expect(sanitizeSizeMm(0.7999999999)).toBe(0.8)
  })
})

describe('decideOptotypeAdjust —— 真实数据回放', () => {
  // 这是最强的一条锚：把 12 个训练日按序喂进去，断言完整轨迹。
  // 期望：07-14 / 07-17 / 07-27 三次收紧，零回退，终值 0.6mm。
  // 阈值一旦被改动，这条会立刻红。
  it('三次收紧、零回退、终值 0.6mm', () => {
    let mm = 0.9
    let last: OptotypeAdjust | null = null
    const fired: Array<[string, string, number]> = []
    const acc: SessionRow[] = []

    for (const [date, accuracy, reactions] of REAL) {
      acc.push(...day(date, accuracy, reactions))
      const d = decideOptotypeAdjust(acc, mm, last, date)
      if (d.action === 'none') continue
      fired.push([date, d.action, d.to])
      last = {
        from: d.from, to: d.to, atDate: date, kind: d.action,
        baselineReactionMs: d.action === 'tighten' ? d.baselineReactionMs : 0,
      }
      mm = d.to
    }

    expect(fired).toEqual([
      ['2026-07-14', 'tighten', 0.8],
      ['2026-07-17', 'tighten', 0.7],
      ['2026-07-27', 'tighten', 0.6],
    ])
    expect(mm).toBe(AUTO_FLOOR_MM)
  })

  it('07-21 是"擦边不回退"：2547ms 差一点点没到 1727×1.5≈2591 —— 别把它改成"任一天即回退"', () => {
    // 07-17 收紧后基线 1727。07-18 破线（4341），07-21 未破线（2547 < 2591）。
    // 规则是"两天都破线才回退"，所以不回退。若改成"任一天"，07-18 就会触发回退、
    // 把之后 4 个训练日锁进 10 天长冷却，而 07-18 只是个 22:43 开练的孤立疲劳日。
    const acc: SessionRow[] = []
    for (const [date, accuracy, reactions] of REAL.slice(0, 9)) acc.push(...day(date, accuracy, reactions))
    const last: OptotypeAdjust = {
      from: 0.8, to: 0.7, atDate: '2026-07-17', kind: 'tighten', baselineReactionMs: 1727,
    }
    expect(decideOptotypeAdjust(acc, 0.7, last, '2026-07-21').action).toBe('none')
  })
})

describe('decideOptotypeAdjust —— 边界', () => {
  const three = (r: number) => [
    ...day('2026-07-01', 1, [r]), ...day('2026-07-02', 1, [r]), ...day('2026-07-03', 1, [r]),
  ]

  it('到下限就不再收紧', () => {
    expect(decideOptotypeAdjust(three(1000), AUTO_FLOOR_MM, null, '2026-07-03').action).toBe('none')
  })

  it('有效训练日不足窗口 → none（宁可不调，不瞎调）', () => {
    const two = [...day('2026-07-01', 1, [1000]), ...day('2026-07-02', 1, [1000])]
    expect(decideOptotypeAdjust(two, 0.9, null, '2026-07-02').action).toBe('none')
  })

  it('缺 avgReactionMs 的节次不计入；整天都缺 → 该天不算有效训练日', () => {
    const rows = three(1000).map((r) => ({ ...r, avgReactionMs: undefined }))
    expect(decideOptotypeAdjust(rows, 0.9, null, '2026-07-03').action).toBe('none')
  })

  it('任一天正确率低于 0.95 → 不收紧', () => {
    const rows = [
      ...day('2026-07-01', 1, [1000]), ...day('2026-07-02', 0.9, [1000]), ...day('2026-07-03', 1, [1000]),
    ]
    expect(decideOptotypeAdjust(rows, 0.9, null, '2026-07-03').action).toBe('none')
  })

  it('反应中位数超阈值 → 不收紧', () => {
    expect(decideOptotypeAdjust(three(2500), 0.9, null, '2026-07-03').action).toBe('none')
  })

  it('冷却未满 → 不收紧（上次调整距今只有 2 个训练日）', () => {
    const last: OptotypeAdjust = {
      from: 1, to: 0.9, atDate: '2026-07-01', kind: 'tighten', baselineReactionMs: 1000,
    }
    expect(decideOptotypeAdjust(three(1000), 0.9, last, '2026-07-03').action).toBe('none')
  })

  it('手动改动也重置冷却（kind:manual）', () => {
    const last: OptotypeAdjust = {
      from: 0.7, to: 0.9, atDate: '2026-07-02', kind: 'manual', baselineReactionMs: 0,
    }
    expect(decideOptotypeAdjust(three(1000), 0.9, last, '2026-07-03').action).toBe('none')
  })

  it('收紧后连着两天都破线 → 回退，并且 from/to 反向', () => {
    const rows = [
      ...day('2026-07-01', 1, [1000]),   // 收紧当天
      ...day('2026-07-02', 0.5, [9000]), // 观察 1：破线
      ...day('2026-07-03', 0.5, [9000]), // 观察 2：破线
    ]
    const last: OptotypeAdjust = {
      from: 0.8, to: 0.7, atDate: '2026-07-01', kind: 'tighten', baselineReactionMs: 1000,
    }
    const d = decideOptotypeAdjust(rows, 0.7, last, '2026-07-03')
    expect(d).toEqual({ action: 'revert', from: 0.7, to: 0.8 })
  })

  it('只有一个观察日破线 → 继续观察，不回退', () => {
    const rows = [
      ...day('2026-07-01', 1, [1000]),
      ...day('2026-07-02', 0.5, [9000]),
      ...day('2026-07-03', 1, [1000]),
    ]
    const last: OptotypeAdjust = {
      from: 0.8, to: 0.7, atDate: '2026-07-01', kind: 'tighten', baselineReactionMs: 1000,
    }
    expect(decideOptotypeAdjust(rows, 0.7, last, '2026-07-03').action).toBe('none')
  })

  it('回退后进 10 个训练日的长冷却，不会立刻又收紧', () => {
    const rows: SessionRow[] = []
    for (let i = 1; i <= 6; i++) rows.push(...day(`2026-07-0${i}`, 1, [1000]))
    const last: OptotypeAdjust = {
      from: 0.7, to: 0.8, atDate: '2026-07-02', kind: 'revert', baselineReactionMs: 0,
    }
    expect(decideOptotypeAdjust(rows, 0.8, last, '2026-07-06').action).toBe('none')
  })

  it('currentMm 是唯一真相：localStorage 与 last.to 不一致时以 currentMm 为准', () => {
    // 备份恢复 / 手工改键会造成这种不一致，不报错、不纠正，照 currentMm 往下算。
    const last: OptotypeAdjust = {
      from: 1, to: 0.9, atDate: '2026-06-01', kind: 'tighten', baselineReactionMs: 1000,
    }
    const d = decideOptotypeAdjust(three(1000), 1.2, last, '2026-07-03')
    expect(d).toEqual({ action: 'tighten', from: 1.2, to: 1.1, baselineReactionMs: 1000 })
  })
})
```

- [ ] **Step 3: 跑测试确认它红**

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: FAIL —— 模块不存在。

- [ ] **Step 4: 实现**

新建 `src/training/optotype-auto.ts`：

```ts
import type { SessionRow } from '../data/db'
import { lsGet, lsSet } from '../data/storage'

/**
 * 视标自适应——按训练表现自动收紧视标，并在调过头时自愈回退（spec 2026-07-29）。
 *
 * 为什么不只看正确率：调节灵敏度训练的刺激来自**拍子的 ±2.00D**，与视标大小无关，
 * 所以正确率高恰恰说明她成功清晰化了、那正是训练目的。真正的疗效指标是**清晰化所需
 * 时间**。视标大小决定的是"看清"这个判据严不严——偏大时孩子还没完全调节到位就已经
 * 认得出方向，"清晰"被提前判定、强度打折。
 *
 * 实证：她 07-11 正确率 95%、07-18 正确率 96%，都能过现有周报那道"≥90%"的门，
 * 但当天中位反应是 4024ms 与 4341ms——答得对但很吃力，那时候调小视标是帮倒忙。
 * 所以必须两个信号同时成立。
 *
 * ⚠️ 本模块**只朝"变难"方向调**。要放松只能家长手动——自动调大等于给孩子一条
 * 把训练调水的通道，与"把视标大小折叠进家长区"那个决定直接冲突。
 */

/** 判定窗口：最近几个**训练日** */
export const AUTO_WINDOW_DAYS = 3
/** 窗口内每天的正确率都要达到 */
export const AUTO_MIN_ACCURACY = 0.95
/** 窗口的「日中位数的中位数」要低于 */
export const AUTO_MAX_REACTION_MS = 2000
/** 距上次调整至少要再练满几个训练日 */
export const AUTO_COOLDOWN_DAYS = 3
/** 一次调一档（与设置页滑块步进一致） */
export const AUTO_STEP_MM = 0.1

/**
 * 自动收紧的医学下限。40cm 下 0.6mm ≈ 等效视力 0.97，即正常视力上限。
 * 再往下她就是**受自己视力限制而不是受调节能力限制**，训练退化成猜。
 * ⚠️ 这是按通用值定的——`src/exams` 里一条验光记录都没有。
 * 有实测矫正视力后应按它复核本值。手动滑块仍可到 SIZE_MIN_MM，下限只约束自动逻辑。
 */
export const AUTO_FLOOR_MM = 0.6

/** 回退：观察日正确率低于此值即算该天破线 */
export const REVERT_MIN_ACCURACY = 0.85
/** 回退：观察日中位反应超过「调整前基线 × 此系数」即算该天破线 */
export const REVERT_REACTION_FACTOR = 1.5
/** 回退后的长冷却（训练日），防止调下去→退回来→再调下去来回震荡 */
export const REVERT_COOLDOWN_DAYS = 10

/** 设置页滑块的范围与默认值，sanitizeSizeMm 按它钳制 */
export const SIZE_MIN_MM = 0.3
export const SIZE_MAX_MM = 2
export const DEFAULT_SIZE_MM = 1

export const LS_SIZE = 'fzp.optotypeSizeMm'
export const LS_LAST_ADJUST = 'fzp.optotypeLastAdjust'
export const LS_AUTO_ENABLED = 'fzp.optotypeAuto'

export interface OptotypeAdjust {
  from: number
  to: number
  /** 调整发生的**训练日**（YYYY-MM-DD） */
  atDate: string
  kind: 'tighten' | 'revert' | 'manual'
  /** 收紧时记下调整前窗口的中位反应；回退判据要拿它做基线。manual/revert 置 0 */
  baselineReactionMs: number
}

export type AdjustDecision =
  | { action: 'none' }
  | { action: 'tighten'; from: number; to: number; baselineReactionMs: number }
  | { action: 'revert'; from: number; to: number }

/**
 * 把脏 localStorage 值钳成可用毫米数——**三个读取点的唯一出口**。
 *
 * 原先 ChallengePage / SettingsPage / TrainingPage 各写一份裸 `Number(...)`：
 * `Number('abc')` 是 NaN，训练页 `heightPx = NaN × pxPerMm` 会让视标直接渲染不出来。
 * 与 goal.ts 的 sanitizeDurationSec 是同一类问题、同一种处理。本迭代要程序化写入
 * 这个键，脏值后果更严重，故收口。
 */
export function sanitizeSizeMm(mm: number): number {
  if (!Number.isFinite(mm) || mm <= 0) return DEFAULT_SIZE_MM
  const clamped = Math.min(Math.max(mm, SIZE_MIN_MM), SIZE_MAX_MM)
  // 步进是 0.1，浮点减法会留下 0.7999999999 这种毛刺，落库/显示前统一收敛到一位小数
  return Math.round(clamped * 10) / 10
}

export function readSizeMm(): number {
  const v = lsGet(LS_SIZE)
  return sanitizeSizeMm(v === null ? NaN : Number(v))
}

export function readAutoEnabled(): boolean {
  // 默认开：用户选的就是"自动调、事后告知"。只有显式写过 '0' 才算关。
  return lsGet(LS_AUTO_ENABLED) !== '0'
}

export function writeAutoEnabled(on: boolean): void {
  lsSet(LS_AUTO_ENABLED, on ? '1' : '0')
}

export function readLastAdjust(): OptotypeAdjust | null {
  const raw = lsGet(LS_LAST_ADJUST)
  if (!raw) return null
  try {
    const o = JSON.parse(raw) as Partial<OptotypeAdjust>
    if (typeof o.from !== 'number' || typeof o.to !== 'number' || typeof o.atDate !== 'string') return null
    if (o.kind !== 'tighten' && o.kind !== 'revert' && o.kind !== 'manual') return null
    return {
      from: sanitizeSizeMm(o.from),
      to: sanitizeSizeMm(o.to),
      atDate: o.atDate,
      kind: o.kind,
      baselineReactionMs: Number.isFinite(o.baselineReactionMs) ? Number(o.baselineReactionMs) : 0,
    }
  } catch {
    // 手工改坏了就当没有——丢一条审计记录远好于让训练页崩掉
    return null
  }
}

export function writeLastAdjust(a: OptotypeAdjust): void {
  lsSet(LS_LAST_ADJUST, JSON.stringify(a))
}

/** 一天的聚合：正确率 + 该天所有节次反应的中位数 */
interface DayStat { date: string; accuracy: number; medianMs: number }

function median(xs: readonly number[]): number {
  const s = [...xs].sort((a, b) => a - b)
  const m = s.length >> 1
  return s.length % 2 ? s[m] : (s[m - 1] + s[m]) / 2
}

/**
 * 把 sessions 折成按日期升序的每日统计。
 *
 * ⚠️ 口径：**先按天取中位、再对天取中位**（下面 windowMedian），不是把所有节次
 * 混在一起——单节异常（如她 07-26 那节 4916ms）不该带偏整天。
 * ⚠️ 口径：缺 avgReactionMs 的节次不计入；整天都缺则该天不算有效训练日。
 * ⚠️ 口径：**不看是否打卡、不看门槛是否达标**。与 goal.ts 那条「门槛只决定今天算不算
 * 完成，不决定训练事实是否被记录」一致——她 07-13 那天没打卡但有 5 节有效数据。
 */
export function dayStats(sessions: readonly SessionRow[]): DayStat[] {
  const byDate = new Map<string, { answered: number; correct: number; ms: number[] }>()
  for (const s of sessions) {
    let d = byDate.get(s.date)
    if (!d) { d = { answered: 0, correct: 0, ms: [] }; byDate.set(s.date, d) }
    d.answered += s.answered
    d.correct += s.correct
    if (typeof s.avgReactionMs === 'number' && Number.isFinite(s.avgReactionMs)) d.ms.push(s.avgReactionMs)
  }
  return [...byDate.entries()]
    .filter(([, d]) => d.ms.length > 0 && d.answered > 0)
    .map(([date, d]) => ({ date, accuracy: d.correct / d.answered, medianMs: median(d.ms) }))
    .sort((a, b) => a.date.localeCompare(b.date))
}

/** 该天是否"破线"（回退判据）：正确率塌了，或清晰化明显变吃力 */
function breached(d: DayStat, baselineMs: number): boolean {
  return d.accuracy < REVERT_MIN_ACCURACY || d.medianMs > baselineMs * REVERT_REACTION_FACTOR
}

/**
 * 判定今天要不要调视标。纯函数——不读 localStorage、不写任何东西。
 *
 * ⚠️ 三条写死的口径：
 *  ① 窗口/冷却/观察期一律按**训练日序号**算差值，不碰自然日。她出去玩 4 天，
 *    按自然日算冷却会白白流逝，窗口还会跨过断档去比两周前的数据。
 *  ② **回退判据优先于收紧判据**。顺序反了的话，刚回退完可能立刻又满足收紧条件。
 *  ③ **currentMm 是唯一真相**，last 只用于冷却与观察期。备份恢复/手工改键导致
 *    currentMm !== last.to 时，照 currentMm 往下算，不纠正、不报错。
 */
export function decideOptotypeAdjust(
  sessions: readonly SessionRow[],
  currentMm: number,
  last: OptotypeAdjust | null,
  todayStr: string,
): AdjustDecision {
  const days = dayStats(sessions)
  const todayIdx = days.findIndex((d) => d.date === todayStr)
  if (todayIdx < 0) return { action: 'none' }          // 今天没有有效数据
  const mm = sanitizeSizeMm(currentMm)

  const lastIdx = last ? days.findIndex((d) => d.date === last.atDate) : -1
  const since = lastIdx >= 0 ? todayIdx - lastIdx : Infinity

  // ── ② 回退优先 ──────────────────────────────────────────────
  if (last && last.kind === 'tighten' && lastIdx >= 0) {
    const observed = days.slice(lastIdx + 1, todayIdx + 1)
    // 观察期不足 2 个训练日 → 继续观察（spec §5.3.1）
    if (observed.length >= 2) {
      const [a, b] = observed
      // ⚠️ **两天都破线**才回退，不是任一天。07-18 那个 22:43 开练的孤立疲劳日
      // （4341ms）若能单独触发回退，会把之后 4 个训练日全锁进 10 天长冷却。
      if (breached(a, last.baselineReactionMs) && breached(b, last.baselineReactionMs)) {
        return { action: 'revert', from: mm, to: sanitizeSizeMm(mm + AUTO_STEP_MM) }
      }
    }
  }

  // ── 收紧 ────────────────────────────────────────────────────
  if (mm - AUTO_STEP_MM < AUTO_FLOOR_MM - 1e-9) return { action: 'none' }   // 已到下限

  const cooldown = last?.kind === 'revert' ? REVERT_COOLDOWN_DAYS : AUTO_COOLDOWN_DAYS
  if (since < cooldown) return { action: 'none' }

  const win = days.slice(Math.max(0, todayIdx - AUTO_WINDOW_DAYS + 1), todayIdx + 1)
  if (win.length < AUTO_WINDOW_DAYS) return { action: 'none' }
  if (win.some((d) => d.accuracy < AUTO_MIN_ACCURACY)) return { action: 'none' }

  const windowMedian = median(win.map((d) => d.medianMs))
  if (windowMedian > AUTO_MAX_REACTION_MS) return { action: 'none' }

  return {
    action: 'tighten',
    from: mm,
    to: sanitizeSizeMm(mm - AUTO_STEP_MM),
    baselineReactionMs: windowMedian,
  }
}
```

- [ ] **Step 5: 跑测试确认绿**

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: PASS，16 条。

Run: `npm test`
Expected: PASS，比基线多 16 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 6: 加源文本契约，锚住医学下限**

在 `src/training/optotype-auto.test.ts` 追加（顶部加 `import src from './optotype-auto.ts?raw'`）：

```ts
describe('源文本契约', () => {
  it('自动逻辑不许出现比 AUTO_FLOOR_MM 更小的毫米字面量', () => {
    // 这条防的是"顺手把下限写死成 0.4 试试"这类绕过。SIZE_MIN_MM=0.3 是滑块的
    // 手动下限、允许出现；除它之外不该有 0.1~0.5 之间的裸小数（AUTO_STEP_MM=0.1 除外）。
    const allowed = new Set(['0.3', '0.1'])
    const found = [...src.matchAll(/(?<![\d.])0\.[0-5]\d*/g)].map((m) => m[0])
    expect(found.filter((f) => !allowed.has(f))).toEqual([])
  })

  it('回退必须是两天都破线（&&），不是任一天（||）', () => {
    expect(src).toMatch(/breached\(a, last\.baselineReactionMs\) && breached\(b, last\.baselineReactionMs\)/)
  })
})
```

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: PASS，18 条。

⚠️ 若第一条红了，说明实现里真有裸小数——**改实现让它引用常量，不要放宽这条断言**。

- [ ] **Step 7: 提交**

```bash
git add src/training/optotype-auto.ts src/training/optotype-auto.test.ts
git commit -m "feat(视标): 自适应判定的纯函数核心，按真实数据回放锚死轨迹"
```

---

## Task 2: 三个读取点收口到 sanitizeSizeMm

**Files:**
- Modify: `src/SettingsPage.tsx:27-31`、`src/training/TrainingPage.tsx:459`、`src/challenge/ChallengePage.tsx:192`
- Modify: `src/training/optotype-auto.test.ts`（加一条源文本契约）

**Interfaces:**
- Consumes: `sanitizeSizeMm` / `readSizeMm`（Task 1）
- Produces: 无

- [ ] **Step 1: 写失败的测试**

在 `src/training/optotype-auto.test.ts` 的「源文本契约」describe 里追加（顶部补三个 `?raw` import）：

```ts
import settingsSrc from '../SettingsPage.tsx?raw'
import trainingSrc from '../training/TrainingPage.tsx?raw'
import challengeSrc from '../challenge/ChallengePage.tsx?raw'
```

```ts
  it('三个读取点全部走 sanitize，不许再有裸 Number(lsGet(...))', () => {
    // Number('abc') 是 NaN，训练页 heightPx = NaN × pxPerMm 会让视标直接渲染不出来。
    // 本迭代开始程序化写入这个键，脏值后果更严重，所以收口成唯一出口。
    for (const [name, s] of [['Settings', settingsSrc], ['Training', trainingSrc], ['Challenge', challengeSrc]] as const) {
      expect(s.includes('optotypeSizeMm'), `${name} 应改为从 optotype-auto 取值`).toBe(false)
    }
    expect(trainingSrc).toMatch(/readSizeMm\(\)/)
    expect(challengeSrc).toMatch(/readSizeMm\(\)/)
    expect(settingsSrc).toMatch(/readSizeMm\(\)/)
  })
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: FAIL —— 三个文件里都还有 `optotypeSizeMm` 字面量。

- [ ] **Step 3: 改 `src/challenge/ChallengePage.tsx`**

把第 192 行

```tsx
  const sizeMm = Number(lsGet('fzp.optotypeSizeMm') ?? '1')
```

改成

```tsx
  const sizeMm = readSizeMm()
```

import 里加 `import { readSizeMm } from '../training/optotype-auto'`；若 `lsGet` 在该文件其他地方不再使用就一并删掉这个导入。

- [ ] **Step 4: 改 `src/training/TrainingPage.tsx`**

把第 459 行

```tsx
  const sizeMm = Number(lsGet('fzp.optotypeSizeMm') ?? '1')
```

改成

```tsx
  const sizeMm = readSizeMm()
```

import 里加 `readSizeMm`（Task 3 还会从同一模块引入更多符号，先只加这个）。

- [ ] **Step 5: 改 `src/SettingsPage.tsx`**

把第 27-31 行

```tsx
  const [sizeMm, setSizeMm] = useState(() => {
    const v = lsGet('fzp.optotypeSizeMm')
    return v ? Number(v) : 1
  })
```

改成

```tsx
  const [sizeMm, setSizeMm] = useState(readSizeMm)
```

第 57 行的写回改为走同一个键常量：

```tsx
  useEffect(() => { lsSet(LS_SIZE, String(sizeMm)) }, [sizeMm])
```

import 里加 `import { readSizeMm, LS_SIZE } from './training/optotype-auto'`。

- [ ] **Step 6: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，与 Task 1 结束时同数（本任务只加了 1 条契约，故 +1）。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 7: 提交**

```bash
git add src/SettingsPage.tsx src/training/TrainingPage.tsx src/challenge/ChallengePage.tsx src/training/optotype-auto.test.ts
git commit -m "fix(视标): 三个读取点收口到 sanitizeSizeMm，堵住 NaN 让视标渲染不出来"
```

---

## Task 3: 文案

**Files:**
- Modify: `src/i18n.tsx`（zh 与 en 两处字典）
- Modify: `src/i18n.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: 10 个文案键，供 Task 4/5 使用

- [ ] **Step 1: 写失败的测试**

`src/i18n.test.ts` 顶部加常量：

```ts
/** 视标自适应文案 */
const OPTOTYPE_AUTO_KEYS = [
  'optoAuto.tightened', 'optoAuto.tightenedHint',
  'optoAuto.suggest', 'optoAuto.suggestHint', 'optoAuto.apply',
  'optoAuto.reverted', 'optoAuto.revertedHint',
  'optoAuto.switch', 'optoAuto.switchHint',
  'optoAuto.lastAdjust', 'optoAuto.undo',
]
```

并加一条 it：

```ts
  it('视标自适应文案在 zh 与 en 都成对存在', () => {
    expect(OPTOTYPE_AUTO_KEYS.length).toBe(11)
    for (const k of OPTOTYPE_AUTO_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/i18n.test.ts`
Expected: FAIL —— 11 个键都不存在。

- [ ] **Step 3: 加 zh 文案**

`src/i18n.tsx` 的 zh 字典，在 `'settings.optotypeNeedCalib'` 之后加：

```ts
  'optoAuto.tightened': '👁️ 视标已调小',
  'optoAuto.tightenedHint': '最近三天又快又准，给你加点难度 💪',
  'optoAuto.suggest': '👁️ 建议调小视标',
  'optoAuto.suggestHint': '最近三天又快又准，可以加点难度了',
  'optoAuto.apply': '应用',
  'optoAuto.reverted': '👁️ 视标已退回',
  'optoAuto.revertedHint': '这两天有点跟不上，先退回一档',
  'optoAuto.switch': '🤖 自动跟随难度',
  'optoAuto.switchHint': '练得又快又准时自动把视标调小一档（最小 0.6mm），跟不上会自动退回。关掉则只在训练结束时给建议。',
  'optoAuto.lastAdjust': '最近一次调整：{date} 从 {from}mm 到 {to}mm',
  'optoAuto.undo': '↩️ 撤回这一步',
```

- [ ] **Step 4: 加 en 文案**

en 字典对应位置：

```ts
  'optoAuto.tightened': '👁️ Optotype made smaller',
  'optoAuto.tightenedHint': 'Fast and accurate for three days running — here comes a bit more challenge 💪',
  'optoAuto.suggest': '👁️ Time for a smaller optotype',
  'optoAuto.suggestHint': 'Fast and accurate for three days running — ready for more challenge',
  'optoAuto.apply': 'Apply',
  'optoAuto.reverted': '👁️ Optotype rolled back',
  'optoAuto.revertedHint': 'The last two days were a struggle — stepping back one notch',
  'optoAuto.switch': '🤖 Auto-adjust difficulty',
  'optoAuto.switchHint': 'Shrinks the optotype one step (down to 0.6mm) when sessions are fast and accurate, and rolls back if they are not. Turn it off to only get a suggestion after training.',
  'optoAuto.lastAdjust': 'Last change: {from}mm → {to}mm on {date}',
  'optoAuto.undo': '↩️ Undo this step',
```

- [ ] **Step 5: 改写周报那条会误导的建议**

`stats.suggest.highAccuracy` 现在写的是"可以把视标调小一点"，功能上线后 app 已经自己调了，这句会让家长以为还要手动做一遍。zh 改成：

```ts
  'stats.suggest.highAccuracy': '正确率很棒 👍 视标会按表现自动调小（设置页可关）；视标变小后反应变慢是正常的，趋势图跨调整点不宜直接比',
```

en 改成：

```ts
  'stats.suggest.highAccuracy': '👍 Great accuracy! The optotype now shrinks automatically as she improves (switch it off in Settings). Reaction times naturally rise after each step, so the trend line is not directly comparable across changes.',
```

- [ ] **Step 6: 跑测试**

Run: `npm test`
Expected: PASS，比 Task 2 结束时多 1 条。

- [ ] **Step 7: 提交**

```bash
git add src/i18n.tsx src/i18n.test.ts
git commit -m "feat(视标): 自适应的中英文案，并改写会误导的周报建议"
```

---

## Task 4: 设置页家长区（开关 + 撤回）

**Files:**
- Modify: `src/SettingsPage.tsx`（`Collapsible` 内、视标滑块之后）
- Modify: `src/training/optotype-auto.test.ts`（源文本契约）

**Interfaces:**
- Consumes: `readAutoEnabled` / `writeAutoEnabled` / `readLastAdjust` / `writeLastAdjust` / `OptotypeAdjust` / `LS_SIZE`（Task 1）、Task 3 的文案
- Produces: 无

- [ ] **Step 1: 写失败的测试**

`src/training/optotype-auto.test.ts` 的「源文本契约」describe 里追加：

```ts
  it('撤回按钮只出现在设置页，绝不出现在结算页', () => {
    // 结算页主要是孩子在看。撤回按钮摆在那里等于给她一条每天一按、把训练调回容易的
    // 通道——正是"把视标大小折叠进家长区"要防的那件事，而且比展开折叠更好按。
    expect(settingsSrc).toMatch(/optoAuto\.undo/)
    expect(trainingSrc.includes('optoAuto.undo'), 'TrainingPage 不许有撤回按钮').toBe(false)
  })

  it('开关与撤回都在已折叠的家长区内（settings.trainingLoad 之后）', () => {
    const foldAt = settingsSrc.indexOf('settings.trainingLoad')
    expect(foldAt).toBeGreaterThan(-1)
    expect(settingsSrc.indexOf('optoAuto.switch')).toBeGreaterThan(foldAt)
    expect(settingsSrc.indexOf('optoAuto.undo')).toBeGreaterThan(foldAt)
  })
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: FAIL —— `settingsSrc` 里还没有 `optoAuto.switch` / `optoAuto.undo`。

- [ ] **Step 3: 加 state 与手动改动记账**

`src/SettingsPage.tsx` 组件顶部（`sizeMm` 那个 state 附近）加：

```tsx
  const [autoOn, setAutoOn] = useState(readAutoEnabled)
  const [lastAdjust, setLastAdjust] = useState(readLastAdjust)
```

把第 57 行那个写回 effect 改成同时记账手动改动：

```tsx
  // 手动改动要写一条 kind:'manual' 的记录——它不参与判据，只起"重置冷却"的作用：
  // 家长刚把视标调大（比如孩子那天状态不好），自动逻辑第二天又压回去就是跟家长对着干。
  const firstSizeRender = useRef(true)
  useEffect(() => {
    lsSet(LS_SIZE, String(sizeMm))
    if (firstSizeRender.current) { firstSizeRender.current = false; return }
    const rec: OptotypeAdjust = {
      from: lastAdjust?.to ?? sizeMm, to: sizeMm,
      atDate: toDateStr(new Date()), kind: 'manual', baselineReactionMs: 0,
    }
    writeLastAdjust(rec)
    setLastAdjust(rec)
  }, [sizeMm])
```

import 补 `useRef`、`toDateStr`（来自 `./data/date-utils`）、以及 `readAutoEnabled, writeAutoEnabled, readLastAdjust, writeLastAdjust, type OptotypeAdjust`。

⚠️ `firstSizeRender` 那道闸门是必须的：不加的话组件一挂载就会写一条 manual 记录，等于每次打开设置页都把冷却重置一次，自动逻辑永远不会触发。

- [ ] **Step 4: 加 UI**

在 `Collapsible` 里、视标滑块那个 `</div>` 之后、时长那段 `<div style={{ marginTop: 14, ...`  之前插入：

```tsx
        <div style={{ marginTop: 14, paddingTop: 14, borderTop: '1px solid var(--line)' }}>
          <label style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 10, cursor: 'pointer' }}>
            <span style={{ fontSize: 14, fontWeight: 700 }}>{t('optoAuto.switch')}</span>
            <input
              type="checkbox"
              checked={autoOn}
              onChange={(e) => { setAutoOn(e.target.checked); writeAutoEnabled(e.target.checked) }}
              style={{ width: 20, height: 20, accentColor: 'var(--violet)' }}
            />
          </label>
          <p style={{ fontSize: 12, color: 'var(--muted)', marginTop: 6, lineHeight: 1.6 }}>{t('optoAuto.switchHint')}</p>

          {lastAdjust && lastAdjust.kind !== 'manual' && (
            <div style={{ marginTop: 10, display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 8, flexWrap: 'wrap' }}>
              <span style={{ fontSize: 12, color: 'var(--muted)' }}>
                {t('optoAuto.lastAdjust', {
                  date: lastAdjust.atDate, from: lastAdjust.from.toFixed(1), to: lastAdjust.to.toFixed(1),
                })}
              </span>
              <button
                onClick={() => { setSizeMm(lastAdjust.from) }}
                style={{
                  fontSize: 12, fontWeight: 700, padding: '5px 12px', borderRadius: 99,
                  border: '1px solid var(--line)', background: 'transparent',
                  color: 'var(--violet)', cursor: 'pointer',
                }}
              >
                {t('optoAuto.undo')}
              </button>
            </div>
          )}
        </div>
```

⚠️ 撤回按钮只调 `setSizeMm(lastAdjust.from)`——写回与记账由 Step 3 那个 effect 统一处理（它会写一条 `manual` 记录，从而重置冷却）。**不要在这里再写一次 `writeLastAdjust`**，两处写会打架。

⚠️ `kind !== 'manual'` 这个门控是刻意的：手动改动不需要"撤回"入口（滑块就在正上方），只有自动改动才需要。

- [ ] **Step 5: 跑测试与 typecheck**

Run: `npm test`
Expected: PASS，比 Task 3 结束时多 2 条。

Run: `npm run typecheck`
Expected: 无输出。

- [ ] **Step 6: 提交**

```bash
git add src/SettingsPage.tsx src/training/optotype-auto.test.ts
git commit -m "feat(视标): 设置页家长区的自动开关与撤回，手动改动重置冷却"
```

---

## Task 5: 训练结算页接入

**Files:**
- Modify: `src/training/TrainingPage.tsx`
- Modify: `src/training/optotype-auto.test.ts`（源文本契约）

**Interfaces:**
- Consumes: `decideOptotypeAdjust` / `readSizeMm` / `readAutoEnabled` / `readLastAdjust` / `writeLastAdjust` / `LS_SIZE` / `AdjustDecision`（Task 1）、Task 3 的文案
- Produces: 无

- [ ] **Step 1: 写失败的测试**

`src/training/optotype-auto.test.ts` 的「源文本契约」describe 里追加：

```ts
  it('结算页两个分支都渲染自适应卡（不达标页也要判——数据就是数据）', () => {
    // 与 goal.ts 那条「门槛只决定今天算不算完成，不决定训练事实是否被记录」一致。
    const hits = [...trainingSrc.matchAll(/<OptotypeAdjustCard/g)]
    expect(hits.length, '不达标页与完成页各一处').toBe(2)
  })

  it('开关关闭时才渲染「应用」按钮，打开时是既成事实', () => {
    expect(trainingSrc).toMatch(/optoAuto\.apply/)
    expect(trainingSrc).toMatch(/optoAuto\.tightened/)
  })
```

- [ ] **Step 2: 跑测试确认它红**

Run: `npx vitest run src/training/optotype-auto.test.ts`
Expected: FAIL —— `<OptotypeAdjustCard` 出现 0 次。

- [ ] **Step 3: 加 state 与判定调用**

`src/training/TrainingPage.tsx` 顶部 state 区加：

```tsx
  const [optoDecision, setOptoDecision] = useState<AdjustDecision>({ action: 'none' })
```

在 `finishRound`（sessions 落库、`syncBadges` 之后，与捕获判定同一位置）追加：

```tsx
    // 视标自适应：一轮结束后判一次。⚠️ 放在这里而不是 checked-in 分支里——
    // 不达标的日子照样有有效训练数据，判据不看打卡（spec §4.1）。
    const rows = await db.sessions.toArray()
    const decision = decideOptotypeAdjust(rows, readSizeMm(), readLastAdjust(), roundDateRef.current)
    if (decision.action !== 'none' && readAutoEnabled()) {
      applyOptotypeDecision(decision, roundDateRef.current)
    }
    setOptoDecision(decision)
```

并在文件内加一个模块级辅助（放在组件外，紧挨 `CapturedMonsterReveal` 那类私有组件旁）：

```tsx
/** 把决策写进 localStorage：视标值 + 一条审计记录。开关关闭时由「应用」按钮调同一个函数 */
function applyOptotypeDecision(d: Exclude<AdjustDecision, { action: 'none' }>, dateStr: string): void {
  lsSet(LS_SIZE, String(d.to))
  writeLastAdjust({
    from: d.from, to: d.to, atDate: dateStr, kind: d.action,
    baselineReactionMs: d.action === 'tighten' ? d.baselineReactionMs : 0,
  })
}
```

import 补 `decideOptotypeAdjust, readSizeMm, readAutoEnabled, readLastAdjust, writeLastAdjust, LS_SIZE, type AdjustDecision`。

⚠️ `TrainingPage.tsx` 目前**没有** import `db` 与 `lsSet`（实测确认），两个都要新加：`import { db } from '../data/db'`、`lsSet` 来自 `'../data/storage'`。

- [ ] **Step 4: 加卡片组件**

在文件内加（与 `CapturedMonsterReveal` 同级）：

```tsx
/**
 * 视标自适应的结算页告知。
 *
 * ⚠️ 这里**只告知或提供「应用」，绝不提供「撤回」**。结算页主要是孩子在看，撤回按钮
 * 摆在这儿等于给她一条每天一按、把训练调回容易的通道。方向不对称是安全的关键：
 * 「应用」只会让训练变难，孩子不会去按；撤回只会让训练变容易，所以只在设置页家长区。
 */
function OptotypeAdjustCard({ decision, dateStr }: { decision: AdjustDecision; dateStr: string }) {
  const t = useT()
  const [applied, setApplied] = useState(false)
  if (decision.action === 'none') return null

  const auto = readAutoEnabled()
  const isRevert = decision.action === 'revert'
  const done = auto || applied
  // ⚠️ 回退也有两种呈现，别只按 isRevert 选文案：开关关闭时回退**没有被执行**（下面
  // 那个 if 里 readAutoEnabled() 为假就不写 localStorage），此时若标题用过去式的
  // 「视标已退回」，就会和同一张卡上的「应用」按钮自相矛盾——文案说已经发生、按钮说还没。
  const titleKey = isRevert
    ? (done ? 'optoAuto.reverted' : 'optoAuto.suggestRevert')
    : (done ? 'optoAuto.tightened' : 'optoAuto.suggest')
  const hintKey = isRevert
    ? (done ? 'optoAuto.revertedHint' : 'optoAuto.suggestRevertHint')
    : (done ? 'optoAuto.tightenedHint' : 'optoAuto.suggestHint')

  return (
    <div className="fq-card fq-rise" style={{ marginTop: 14, textAlign: 'left', borderLeft: '4px solid var(--violet)' }}>
      <div style={{ fontSize: 15, fontWeight: 800 }}>
        {t(titleKey)}
        <span style={{ marginLeft: 8, color: 'var(--violet)' }}>
          {decision.from.toFixed(1)} → {decision.to.toFixed(1)}mm
        </span>
      </div>
      <p style={{ fontSize: 13, color: 'var(--muted)', marginTop: 6, lineHeight: 1.6 }}>{t(hintKey)}</p>
      {!done && (
        <button
          className="fq-btn"
          style={{ marginTop: 10 }}
          onClick={() => { applyOptotypeDecision(decision, dateStr); setApplied(true) }}
        >
          {t('optoAuto.apply')}
        </button>
      )}
    </div>
  )
}
```

- [ ] **Step 5: 在两个分支各渲染一处**

不达标页（约 386 行那段 `capturedThisSession.map(...)` 所在的容器内、其后）与完成页（约 448 行同结构处、其后），各插入：

```tsx
              <OptotypeAdjustCard decision={optoDecision} dateStr={roundDateRef.current} />
```

- [ ] **Step 6: 跑测试、typecheck 与构建**

Run: `npm test`
Expected: PASS，比 Task 4 结束时多 2 条。

Run: `npm run typecheck`
Expected: 无输出。

Run: `npm run build`
Expected: 成功。

- [ ] **Step 7: 浏览器里真验一遍**

Run: `npm run dev`，控制台造数据：

```js
const db = (await import('/src/data/db.ts')).db
await db.sessions.clear()
const mk = (date, acc, ms, eye) => ({ date, startedAtMs: Date.now(), eye, answered: 100,
  correct: Math.round(acc*100), flips: 30, elapsedSec: 180, acuity: 0.73, avgReactionMs: ms })
// 三个连续训练日，又快又准 —— 应触发收紧
for (const [d, ms] of [['2026-07-26', 1500], ['2026-07-27', 1400], ['2026-07-28', 1300]]) {
  await db.sessions.add(mk(d, 0.98, ms, 'left')); await db.sessions.add(mk(d, 0.98, ms, 'right'))
}
localStorage.setItem('fzp.optotypeSizeMm', '0.8')
localStorage.removeItem('fzp.optotypeLastAdjust')
localStorage.removeItem('fzp.optotypeAuto')
location.reload()
```

逐项确认（练一轮到结算页，或把 `fzp.durationSec` 设成最小值加快）：

1. 结算页出现「👁️ 视标已调小　0.8 → 0.7mm」，**没有**撤回按钮
2. `localStorage['fzp.optotypeSizeMm']` 变成 `0.7`
3. 设置页展开「📐 视标与时长（家长设置）」→ 滑块显示 0.7mm、下方有「最近一次调整…」与「↩️ 撤回这一步」
4. 点撤回 → 滑块回到 0.8mm，且「最近一次调整」那一行消失（因为记账变成了 `manual`）
5. 把开关关掉、重跑上面的脚本 → 结算页变成「👁️ 建议调小视标」+「应用」按钮，localStorage 未变；点「应用」后才变
6. DevTools Console 无 error

- [ ] **Step 8: 提交**

```bash
git add src/training/TrainingPage.tsx src/training/optotype-auto.test.ts
git commit -m "feat(视标): 结算页接入自适应，两个分支各一张卡，撤回不下放给孩子"
```

---

## 收尾检查

- [ ] `npm test` 全绿，总数 = 基线 720 + **24**（Task 1 加 18、Task 2 加 1、Task 3 加 1、Task 4 加 2、Task 5 加 2）→ **744 个 / 75 文件**
- [ ] `npm run typecheck` 与 `npm run typecheck:functions` 都无输出
- [ ] `npm run build` 成功
- [ ] **医学铁律复验**：`src/skins/` 与视标渲染链路零改动（`git diff --stat` 里不该出现 `src/skins/`）；`optotypeHeightPx` 的调用方式未变
- [ ] **同步层零改动**——`git diff --stat` 里不该出现 `src/sync/`（视标大小刻意不进云同步，spec §3.7）
- [ ] 按 [CLAUDE.md](CLAUDE.md) 惯例写进「当前状态」，含四条口径：只调难不调简单、下限 0.6mm 且缺验光锚点、两天都破线才回退、三个读取点已收口
