# 迭代 3d 手机竖屏适配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 FocalQuest 在手机竖屏（375×812 起）上完整可用——训练页三段（顶栏 / 视标舞台 / 十字方向盘）全部落在首屏、方向盘拇指可达、三个皮肤的怪兽不出界、标定页不再静默写坏 px/mm——同时 iPad 横竖屏观感与视标物理尺寸一字不变。

**Architecture:** 纯 CSS 响应式，零 JS 尺寸监听。①把 App 外壳改成 `height:100dvh` 的一列 flex（导航固定 + 内容区自己滚），一举干掉全站三处写死的 `calc(100vh - 57px)` 魔法数；②手机窄屏用单一断点 `@media (max-width:560px)`（导航只留图标、方向盘放大、语音提示改整行）＋矮屏断点 `@media (max-height:560px)`；③皮肤方形画布用**容器查询单位** `width: min(100%, 420px, 100cqh)` 自适应舞台剩余高度，画布内所有装饰改 `cqmin` 等比——**视标 E 始终是 `毫米设定 × 屏幕标定` 的绝对像素，不在任何祖先链上叠 transform scale**；④CSS 里出现的每个数字都由 `src/layout/layout-budget.ts` 的常量派生，并有一个读 `index.css` 文本的契约测试锁死，防 TS 与 CSS 漂移。

**Tech Stack:** React 18 + TypeScript + Vite 6；纯 CSS（媒体查询 + `dvh` + `container-type: size` / `cqh` / `cqmin` + `env(safe-area-inset-*)`）；vitest（`environment: 'node'`，无 jsdom，故布局只能靠纯函数单测 + 浏览器手测）；手测用 `mcp__Claude_Browser__*`（preview_start / resize_window / javascript_tool / read_page / find / computer），**禁止截图**（本项目已知 preview 截图必超时）。

## Global Constraints

- **医学：视标物理尺寸 = `sizeMm × pxPerMm`，任何屏幕都不缩放。** 链路 `TrainingPage.tsx:346 const heightPx = sizeMm * pxPerMm` → `CurrentSkin.Stage` → `TumblingE.tsx:22 <svg width={heightPx} height={heightPx} viewBox="0 0 5 5">`。屏幕小只能改"周围怎么排"，**禁止**给舞台/画布/怪兽容器套 `transform: scale()` 去"整体缩一下"。
- **医学：视标祖先链上的 transform scale 乘积必须为 1。** 现状 `ShrineStage.tsx:118` 的 `transform: 'translate(-50%,-50%) scale(0.8)'` 让神庙皮肤的 E 只有标定值的 80%（既有偏差），本迭代把 0.8 折进容器的 `width/height` 并从 `:180-181` 的 `fzpFloat` / `fzpShakeG` 每一帧里删掉 scale。**装饰（sprite 层、爆炸、勇者）与 E 是兄弟节点，可以单独 transform，不受此限。**
- **医学：Tumbling E 的 5×5 网格必须等比**（笔画宽 = 缺口宽 = 字高/5）。禁止改 `viewBox="0 0 5 5"`、禁止非等比拉伸 / `objectFit` 变形。
- **医学：视标必须高对比**（`PlainStage` 的 `color:'#111'`；`SpaceStage` 的暗化层 `rgba(4,6,16,0.42)` + `drop-shadow(0 0 3px #000)`；`ShrineStage` 的 `background:'rgba(0,0,0,0.6)'` 圆底 + `padding: Math.max(6, heightPx * 0.4)`）。空间紧张时**牺牲装饰，不牺牲对比**，也不得让 E 被 `overflow:hidden` 裁到。
- **医学：节奏参数不得为排版改动**——`DURATION_SEC = 180`、`TRANSITION_MS = 900`（设置页档位 600/900/1500）、`TRAIN_DISTANCE_MM = 400`、拍子 ±2.00D。
- **医学：标定语义不得改。** `CARD_WIDTH_MM = 85.6`、`cssPxPerMm(px) = px / 85.6`、`mmToCssPx(mm, r) = mm * r` 四条单测锚定，**只能新增函数，不能改这三个的语义**。窄屏方案是新增"卡片短边 53.98mm"模式，长边模式仍走 `cssPxPerMm`。
- **医学：竖屏标定的静默破坏必须堵住。** 现状 `CalibrationPage.tsx:36 const w = Math.min(Math.max(cardPx, minPx), maxPx)` + `:59 save()` 无条件写入：手机竖屏上 `maxPx = 345` → 显示 4.03 px/mm，而真值约 6.05，一次误触"保存"就把 px/mm 低估 33%，此后所有视标偏小 33% 且家长完全无感。本迭代必须让"放不下参照物"时**保存按钮禁用 + 明确警告**。
- **交互：D-pad 四向必须与 E 开口方位一一对应。** `DPAD = [{up,col2,row1},{left,col1,row2},{right,col3,row2},{down,col2,row3}]`、`.fq-dpad` 3×3 grid、`directionToRotation`（right=0/down=90/left=180/up=270，有单测）、键盘 `KEY_MAP`（arrow / 1-4 / asdf / jkl;）。**禁止**为省高度改成一排或换序。
- **交互：翻拍引导是训练命脉**——`@media (prefers-reduced-motion: reduce)` 里 `.fzp-flip-icon` 不转但**进度条必须继续走**（是节奏提示），改布局时保留这条契约。
- **产品边界变更（须用户确认）**：`CLAUDE.md` 关键决策 1「只做 iPad Web，不做手机」、`README.md:183` 同口径、`docs/需求文档.md:81`「手机小屏与 40cm 视标空间冲突 → 远期再议」、`:161`「iPad 适配：横竖屏择一固定（建议横屏）」——本迭代显式推翻这四处，Task 7 一并改文档。**神庙视标由 80% 修正为 100%** 亦属行为变更（换皮肤不再偷偷改训练强度，但神庙历史数据与新数据不可比），须在收尾时向用户点明。
- **手机横屏不是目标形态**：目标是手机竖屏 + iPad 横竖屏。手机横屏（矮屏）做到"不破版、方向盘不出界"即可，并用纯 CSS 提示「竖屏更好用」。
- **技术：纯 CSS 优先，不新增任何 JS 尺寸监听 / resize 事件 / matchMedia**。全仓现有的视口读取只有 `CalibrationPage.tsx:26` 的 `document.documentElement.clientWidth` + ResizeObserver（保留）与 `useCountUp.ts:12` 的 reduced-motion matchMedia。
- **技术：`viewport` meta 保留 `maximum-scale=1.0, user-scalable=no`**——双指缩放会让"银行卡对齐"整体放大，直接毁掉物理标定；a11y 折损为既有的、已接受的取舍。本迭代只补 `viewport-fit=cover`。
- **技术：manifest 不锁方向**（现无 `orientation` 字段）。本迭代显式写 `orientation: 'any'` 以示"故意不锁"，且 iOS standalone 本就不吃该字段。
- **测试：`vitest.config.ts` 是 `environment: 'node'`、无 jsdom / @testing-library、`include: ['src/**/*.test.ts', 'functions/**/*.test.ts']`**（`.test.tsx` 不会被跑）。故不写任何需要 document/window 的测试；布局靠纯函数 + CSS 文本契约 + 浏览器手测。
- **测试基线：`npm test` = 400 passed / 44 files**（2026-07-25 实测）。本计划完工应为 **436 passed / 47 files**。首次跑 40-70 秒属正常（Windows IO 慢）。
- 每个改 CSS / 组件的任务都必须以"跑全量测试确认既有 400 个仍绿"收尾。
- 交流、注释、提交信息一律简体中文；技术术语与代码标识符保留英文。

---

### Task 1: 布局预算纯函数（TDD）

把"给定视口宽高，导航多高 / 方向盘多大 / 舞台正方形边长多少 / 三段是否都放得下"抽成纯函数，作为后续 CSS 数值的唯一出处与回归闸门。

**Files**
- Create: `E:\developer\fzp\src\layout\layout-budget.ts`
- Test: `E:\developer\fzp\src\layout\layout-budget.test.ts`

**Interfaces**
- Consumes: 无（纯算术，不 import 任何模块）
- Produces:
  - `export const PHONE_MAX_W_PX = 560`
  - `export const SHORT_MAX_H_PX = 560`
  - `export const NAV_ONE_LINE_MIN_PX = 531`
  - `export const CANVAS_MAX_PX = 420`
  - `export const DPAD_MAX_PX_PHONE = 240`
  - `export const DPAD_MAX_PX_WIDE = 210`
  - `export const DPAD_VW_PHONE = 66`
  - `export const DPAD_VW_WIDE = 58`
  - `export const DPAD_DVH_PHONE = 30`
  - `export const DPAD_DVH_SHORT = 34`
  - `export const TOP_BAR_PX = 45`
  - `export const ANSWER_BAND_EXTRA_PX = 34`
  - `export const NAV_H_PHONE_PX = 60`
  - `export const NAV_H_WIDE_PX = 57`
  - `export const MIN_STAGE_PX = 96`
  - `export const TRAIN_WARN_MAX_H_PX = 359`
  - `export type LayoutMode = 'phone' | 'short' | 'wide'`
  - `export function layoutMode(vw: number, vh: number): LayoutMode`
  - `export function navHeightPx(vw: number): number`
  - `export function dpadSidePx(vw: number, vh: number): number`
  - `export function answerBandPx(vw: number, vh: number): number`
  - `export function stageHeightPx(vw: number, vh: number): number`
  - `export function stageSidePx(vw: number, vh: number): number`
  - `export function everythingFits(vw: number, vh: number): boolean`

- [ ] **Step 1: 先写失败测试** —— 创建 `src/layout/layout-budget.test.ts`，完整内容：

```ts
import { describe, it, expect } from 'vitest'
import {
  PHONE_MAX_W_PX, SHORT_MAX_H_PX, NAV_ONE_LINE_MIN_PX, CANVAS_MAX_PX,
  DPAD_MAX_PX_PHONE, DPAD_MAX_PX_WIDE, MIN_STAGE_PX, TRAIN_WARN_MAX_H_PX,
  layoutMode, navHeightPx, dpadSidePx, answerBandPx, stageHeightPx, stageSidePx, everythingFits,
} from './layout-budget'

/** 真机视口表（CSS px）：手机竖屏是本迭代目标，iPad 两向是不许回退的基线 */
const DEVICES: { name: string; w: number; h: number }[] = [
  { name: 'iPhone SE1 竖', w: 320, h: 568 },
  { name: 'iPhone SE2 竖', w: 375, h: 667 },
  { name: 'iPhone X/13mini 竖', w: 375, h: 812 },
  { name: 'iPhone 12/13/14 竖', w: 390, h: 844 },
  { name: 'iPhone 11/XR 竖', w: 414, h: 896 },
  { name: 'iPhone 14 Pro Max 竖', w: 430, h: 932 },
  { name: 'iPad 竖', w: 768, h: 1024 },
  { name: 'iPad 横', w: 1024, h: 768 },
  { name: 'iPhone 横（非目标形态，仅要求不破版）', w: 812, h: 375 },
]

describe('layout-budget 布局预算', () => {
  it('手机断点不小于英文导航单行所需宽度（否则文字导航会折行、57px 假设再被打破）', () => {
    expect(PHONE_MAX_W_PX).toBeGreaterThanOrEqual(NAV_ONE_LINE_MIN_PX)
  })

  it('layoutMode：宽屏 wide、手机竖屏 phone、矮屏 short（矮屏优先，与 CSS 里 max-height 规则后置一致）', () => {
    expect(layoutMode(1024, 768)).toBe('wide')
    expect(layoutMode(768, 1024)).toBe('wide')
    expect(layoutMode(375, 812)).toBe('phone')
    expect(layoutMode(812, 375)).toBe('short')
    expect(layoutMode(400, SHORT_MAX_H_PX)).toBe('short')
  })

  it('navHeightPx：窄屏图标导航 60px、宽屏文字导航 57px', () => {
    expect(navHeightPx(375)).toBe(60)
    expect(navHeightPx(PHONE_MAX_W_PX)).toBe(60)
    expect(navHeightPx(PHONE_MAX_W_PX + 1)).toBe(57)
    expect(navHeightPx(1024)).toBe(57)
  })

  it('dpadSidePx：iPad 横竖屏都保持今天的 210px（不许因适配缩小）', () => {
    expect(dpadSidePx(1024, 768)).toBe(DPAD_MAX_PX_WIDE)
    expect(dpadSidePx(768, 1024)).toBe(DPAD_MAX_PX_WIDE)
  })

  it('dpadSidePx：手机竖屏放大到 240px 上限（拇指可达）', () => {
    expect(dpadSidePx(390, 844)).toBe(DPAD_MAX_PX_PHONE)
    expect(dpadSidePx(375, 812)).toBe(DPAD_MAX_PX_PHONE)
  })

  it('dpadSidePx：矮屏按高度收缩，不再吃满 210px', () => {
    expect(dpadSidePx(812, 375)).toBeCloseTo(127.5, 1)
    expect(dpadSidePx(375, 667)).toBeCloseTo(200.1, 1)
  })

  it('stageSidePx：iPad 横竖屏都等于画布上限 420（与今天的 maxWidth:420 一致）', () => {
    expect(stageSidePx(1024, 768)).toBe(CANVAS_MAX_PX)
    expect(stageSidePx(768, 1024)).toBe(CANVAS_MAX_PX)
  })

  it('stageSidePx 永不超过画布上限，且永不为负', () => {
    for (const d of DEVICES) {
      expect(stageSidePx(d.w, d.h)).toBeLessThanOrEqual(CANVAS_MAX_PX)
      expect(stageSidePx(d.w, d.h)).toBeGreaterThan(0)
    }
  })

  it('真机表全部放得下：舞台边长不小于 MIN_STAGE_PX，三段之和不超视口高', () => {
    for (const d of DEVICES) {
      expect(everythingFits(d.w, d.h), `${d.name} 放不下`).toBe(true)
      expect(stageSidePx(d.w, d.h)).toBeGreaterThanOrEqual(MIN_STAGE_PX)
      const total = navHeightPx(d.w) + 45 + stageHeightPx(d.w, d.h) + answerBandPx(d.w, d.h)
      expect(total).toBeLessThanOrEqual(d.h)
    }
  })

  it('提示阈值是保守的：在 TRAIN_WARN_MAX_H_PX 高度上其实还放得下（先提示再破版）', () => {
    for (const w of [320, 375, 812]) {
      expect(everythingFits(w, TRAIN_WARN_MAX_H_PX), `w=${w}`).toBe(true)
    }
  })

  it('提示阈值不是白设的：再矮 30px 就真放不下了', () => {
    for (const w of [320, 375, 812]) {
      expect(everythingFits(w, TRAIN_WARN_MAX_H_PX - 30), `w=${w}`).toBe(false)
    }
  })

  it('stageSidePx 对视口高单调不减（屏越高舞台不该变小）', () => {
    let prev = 0
    for (const h of [400, 500, 600, 700, 800, 900, 1000]) {
      const cur = stageSidePx(375, h)
      expect(cur).toBeGreaterThanOrEqual(prev)
      prev = cur
    }
  })
})
```

- [ ] **Step 2: 跑到失败** —— `npm test -- src/layout/layout-budget.test.ts`，预期报模块不存在（`Failed to resolve import "./layout-budget"`）。确认是"模块缺失"而不是别的错。
- [ ] **Step 3: 实现** —— 创建 `src/layout/layout-budget.ts`，完整内容：

```ts
/**
 * 竖屏布局预算：训练页三段（导航 / 顶栏 / 舞台 / 答题带）在给定视口里各占多少。
 *
 * 为什么要有这个文件：src/index.css 里的响应式数字（断点、方向盘 min() 三档、画布上限、
 * 太矮提示阈值）必须有唯一出处，否则 CSS 与代码会各自漂移。这里的常量是权威，
 * src/layout/css-contract.test.ts 会读 index.css 文本核对两边一致。
 *
 * 铁律：这里算的都是"周围怎么排"。视标 E 的物理尺寸恒为 毫米设定 × 屏幕标定，
 * 与视口无关、不参与任何缩放（见 CLAUDE.md 关键医学参数）。
 */

/** 手机窄屏断点：≤560px 走"图标导航 + 紧凑排布"。
 *  560 的由来：5 项文字导航单行需 461px(中文) / 531px(英文)，低于此必折行；
 *  折行让导航从 57px 变 99px，正是"↓ 按钮掉出首屏"的根因。 */
export const PHONE_MAX_W_PX = 560
/** 矮屏断点（手机横屏 / iPad 分屏）：≤560px 高时方向盘按高度收缩。 */
export const SHORT_MAX_H_PX = 560
/** 实测：5 项文字导航单行所需最小宽度（英文最长；中文 461px）。 */
export const NAV_ONE_LINE_MIN_PX = 531

/** 皮肤方形画布上限（与今天 SpaceStage/ShrineStage 的 maxWidth:420 一致）。 */
export const CANVAS_MAX_PX = 420

/** 方向盘边长的三个上限档（对应 CSS 的 min() 三参数）。 */
export const DPAD_MAX_PX_PHONE = 240
export const DPAD_MAX_PX_WIDE = 210
export const DPAD_VW_PHONE = 66
export const DPAD_VW_WIDE = 58
export const DPAD_DVH_PHONE = 30
export const DPAD_DVH_SHORT = 34

/** 训练页顶栏（眼别 chip + 进度条 + 计时 + 暂停/静音）实测高度。 */
export const TOP_BAR_PX = 45
/** 答题带 = 方向盘 + 语音提示行 + 上下内边距。 */
export const ANSWER_BAND_EXTRA_PX = 34
/** 导航实测高度：窄屏图标一行 60px；宽屏文字一行 57px。 */
export const NAV_H_PHONE_PX = 60
export const NAV_H_WIDE_PX = 57

/** 舞台正方形边长下限：低于此，皮肤装饰无法承载视标（此时提示家长，绝不缩视标）。 */
export const MIN_STAGE_PX = 96
/** CSS 里"屏幕太矮"提示的媒体查询阈值（@media (max-height: 359px)）。
 *  刻意比真正放不下的高度保守——先提示，再破版。 */
export const TRAIN_WARN_MAX_H_PX = 359

export type LayoutMode = 'phone' | 'short' | 'wide'

/** 与 CSS 一致：max-height 规则写在 max-width 之后，故矮屏优先。 */
export function layoutMode(vw: number, vh: number): LayoutMode {
  if (vh <= SHORT_MAX_H_PX) return 'short'
  if (vw <= PHONE_MAX_W_PX) return 'phone'
  return 'wide'
}

export function navHeightPx(vw: number): number {
  return vw <= PHONE_MAX_W_PX ? NAV_H_PHONE_PX : NAV_H_WIDE_PX
}

/** 对应 CSS：.fq-dpad { width: min(210px, 58vw) }，手机/矮屏各有覆盖档。 */
export function dpadSidePx(vw: number, vh: number): number {
  const mode = layoutMode(vw, vh)
  if (mode === 'phone') {
    return Math.min(DPAD_MAX_PX_PHONE, (vw * DPAD_VW_PHONE) / 100, (vh * DPAD_DVH_PHONE) / 100)
  }
  if (mode === 'short') {
    return Math.min(DPAD_MAX_PX_WIDE, (vw * DPAD_VW_WIDE) / 100, (vh * DPAD_DVH_SHORT) / 100)
  }
  return Math.min(DPAD_MAX_PX_WIDE, (vw * DPAD_VW_WIDE) / 100)
}

export function answerBandPx(vw: number, vh: number): number {
  return dpadSidePx(vw, vh) + ANSWER_BAND_EXTRA_PX
}

/** 舞台可用高度 = 视口高 − 导航 − 顶栏 − 答题带。 */
export function stageHeightPx(vw: number, vh: number): number {
  return vh - navHeightPx(vw) - TOP_BAR_PX - answerBandPx(vw, vh)
}

/** 舞台正方形边长——对应 CSS 的 width: min(100%, 420px, 100cqh)。 */
export function stageSidePx(vw: number, vh: number): number {
  return Math.min(vw, CANVAS_MAX_PX, Math.max(0, stageHeightPx(vw, vh)))
}

/** 三段是否都放得下（false → 训练页显示"屏幕太矮"提示，视标仍不缩）。 */
export function everythingFits(vw: number, vh: number): boolean {
  return stageSidePx(vw, vh) >= MIN_STAGE_PX
}
```

- [ ] **Step 4: 跑到通过** —— `npm test -- src/layout/layout-budget.test.ts`，预期 `12 passed`。
- [ ] **Step 5: 全量回归** —— `npm test`，预期 **412 passed / 45 files**（400 基线 + 12）。再跑 `npm run typecheck`，预期无输出。
- [ ] **Step 6: 提交** —— `git add src/layout && git commit`，信息：`feat(layout): 抽出竖屏布局预算纯函数（断点/方向盘/舞台边长，12 单测）`。

---

### Task 2: 应用外壳 flex 化 + 响应式基础设施（干掉 57px 魔法数）

**Files**
- Modify: `E:\developer\fzp\src\index.css`
- Modify: `E:\developer\fzp\src\App.tsx`
- Modify: `E:\developer\fzp\index.html`
- Modify: `E:\developer\fzp\vite.config.ts`
- Test: `E:\developer\fzp\src\layout\css-contract.test.ts`（Create）

**Interfaces**
- Consumes: `src/layout/layout-budget.ts` 的全部常量（仅测试 import；CSS 里是字面量，由契约测试锁死）
- Produces: CSS 类 `.fq-app`、`.fq-app-main`、`.fq-nav-label`；`.fq-home` 改为 `min-height: 100%`

- [ ] **Step 1: index.css —— 加应用外壳** —— 在 `h1, h2, h3, h4, p { margin: 0; }`（第 31 行）之后插入：

```css
/* ---------- 应用外壳：一列 flex，导航固定 + 内容区自己滚 ---------- */
/* 干掉全站三处写死的 calc(100vh - 57px)：内容区高度交给 flex 算，导航折不折行都对。
   100dvh 让移动 Safari 地址栏收放时不再抖（旧内核回退到前一条 100%）。 */
.fq-app {
  display: flex;
  flex-direction: column;
  height: 100%;
  height: 100dvh;
}
.fq-app-main {
  flex: 1 1 auto;
  min-height: 0;
  overflow-y: auto;
  overscroll-behavior-y: contain;
  -webkit-overflow-scrolling: touch;
}
```

- [ ] **Step 2: index.css —— 导航改造（安全区 + 不再 sticky）** —— 把 `.fq-nav { ... }`（第 75-81 行）整块替换为：

```css
/* 顶部导航（在滚动容器之外，故不再需要 sticky） */
.fq-nav {
  position: relative; z-index: 20; flex: 0 0 auto;
  display: flex; gap: 6px; flex-wrap: wrap;
  padding: max(10px, env(safe-area-inset-top, 0px)) max(12px, env(safe-area-inset-right, 0px)) 10px max(12px, env(safe-area-inset-left, 0px));
  background: rgba(255, 255, 255, 0.88);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line);
}
```

- [ ] **Step 3: index.css —— 首页去魔法数** —— 把第 163 行的
  `.fq-home { position: relative; overflow: hidden; min-height: calc(100vh - 57px); }`
  替换为
  `.fq-home { position: relative; overflow: hidden; min-height: 100%; }`
- [ ] **Step 4: index.css —— 训练页骨架 + 皮肤画布 + 断点块（一次写全）** —— 在文件末尾 `@media (prefers-reduced-motion: reduce) { ... }`（第 176 行）之**前**插入下面这一整段。（Task 3 / Task 4 只往里填 `.fzp-rotate-hint` 与组件侧的类名引用，尺寸规则在此一次落地，才能被本任务的契约测试锁死。）

```css
/* ---------- 训练页三段布局（顶栏 / 舞台 / 答题带） ---------- */
/* 高度由 .fq-app-main 给定，故用 100%，不再算 100vh 减导航 */
.fzp-train { position: relative; display: flex; flex-direction: column; height: 100%; overflow: hidden; }
.fzp-train-top { flex: 0 0 auto; display: flex; align-items: center; gap: 10px; padding: 12px 16px; }
/* 舞台是尺寸容器：画布用 100cqh 拿到"还剩多少高"，纯 CSS 无需 JS 测量 */
.fzp-stage {
  flex: 1 1 auto; min-height: 0; position: relative;
  display: flex; align-items: center; justify-content: center; padding: 8px 0;
  container-type: size;
}
.fzp-answer {
  flex: 0 0 auto; display: grid; grid-template-columns: 1fr auto 1fr;
  align-items: center; gap: 8px;
  padding: 4px 14px calc(18px + env(safe-area-inset-bottom, 0px));
}
.fzp-voice-hint { color: var(--muted); font-size: 12px; font-weight: 600; text-align: right; line-height: 1.4; }

/* ---------- 皮肤舞台画布：正方形，且永不超出舞台剩余高度 ---------- */
/* 视标 E 仍是 mm×标定的绝对像素，只有"承载它的装饰"跟着画布等比（cqmin）。
   禁止给画布或其祖先加 transform: scale —— 那会改变视标物理尺寸（医学参数）。 */
.fzp-skin-canvas {
  position: relative;
  width: min(100%, 420px, 100cqh);
  aspect-ratio: 1 / 1;
  margin: 0 auto;
  border-radius: 16px;
  overflow: hidden;
  container-type: size;
}

/* 屏幕太矮（低于 layout-budget 的 MIN_STAGE_PX 预算）：明确提示，绝不缩视标 */
.fzp-tiny-warn { display: none; }
@media (max-height: 359px) {
  .fzp-tiny-warn {
    display: block; position: absolute; top: 4px; left: 50%; transform: translateX(-50%);
    z-index: 8; max-width: 92%; text-align: center;
    font-size: 12px; font-weight: 700; color: #fff; line-height: 1.35;
    background: rgba(199, 60, 60, 0.92); padding: 5px 12px; border-radius: 12px;
  }
}

/* ---------- 手机窄屏（≤560px）：导航只留图标，腾出高度给训练 ---------- */
/* 560 的由来见 src/layout/layout-budget.ts：5 项文字导航单行需 461px(中文)/531px(英文)，
   低于此必折行；折行让导航从 57px 变 99px，正是"↓ 按钮掉出首屏"的根因。 */
@media (max-width: 560px) {
  .fq-nav { flex-wrap: nowrap; justify-content: space-around; gap: 2px; }
  .fq-nav button { font-size: 22px; padding: 7px 10px; gap: 0; }
  .fq-nav-label { display: none; }
  .fq-page { padding: 16px 14px 32px; }
  /* 手机竖屏：方向盘放大到 240（拇指可达），间距 7→10px 降误触 */
  .fq-dpad { width: min(240px, 66vw, 30dvh); gap: 10px; }
  .fq-dpad-btn { font-size: 32px; border-radius: 18px; }
  /* 语音提示改为方向盘上方整行居中：窄屏挤成多行会顶掉方向盘 */
  .fzp-answer { grid-template-columns: 1fr; justify-items: center; gap: 4px; padding: 2px 10px calc(14px + env(safe-area-inset-bottom, 0px)); }
  .fzp-voice-hint { text-align: center; }
  .fzp-answer-spacer { display: none; }
}

/* ---------- 矮屏（手机横屏 / 分屏）：方向盘按高度收缩，保证三段都在首屏 ---------- */
/* 刻意写在手机块之后：两者同时命中时以高度为准（layout-budget 的 layoutMode 同序） */
@media (max-height: 560px) {
  .fq-dpad { width: min(210px, 58vw, 34dvh); gap: 6px; }
  .fq-dpad-btn { font-size: 24px; }
}
```

- [ ] **Step 5: App.tsx —— 外壳与导航文案节点** —— 把 `return (` 之后的整个 JSX（第 41-82 行）替换为：

```tsx
  return (
    <div className="fq-app">
      {showOnboard && (
        <Onboarding onDone={() => { lsSet('fzp.onboarded', '1'); setShowOnboard(false) }} />
      )}
      <nav className="fq-nav">
        {NAV.map((n) => (
          <button
            key={n.key}
            className={view === n.key ? 'on' : ''}
            aria-label={t(`nav.${n.key}`)}
            onClick={() => { setBadgeTab('badges'); setView(n.key) }}
          >
            <span aria-hidden style={{ position: 'relative' }}>
              {n.icon}
              {n.key === 'settings' && pendingCount > 0 && (
                <span style={{ position: 'absolute', top: -2, right: -6, width: 8, height: 8, borderRadius: '50%', background: '#ff4d4f' }} />
              )}
            </span>
            {/* 手机窄屏隐藏文案只留图标（aria-label 兜住无障碍），导航才不折行 */}
            <span className="fq-nav-label">{t(`nav.${n.key}`)}</span>
          </button>
        ))}
      </nav>
      <main className="fq-app-main">
        {view === 'home' && (
          <HomePage
            onStart={() => setView('train')}
            onOpenDex={() => { setBadgeTab('dex'); setView('badges') }}
            onOpenRewards={() => setView('rewards')}
          />
        )}
        {view === 'train' && <TrainingPage />}
        {view === 'stats' && <StatsPage />}
        {view === 'badges' && <BadgeWall initialTab={badgeTab} />}
        {view === 'calib' && <CalibrationPage />}
        {view === 'speech' && <SpeechTestPage />}
        {view === 'rewards' && <RewardsPage />}
        {view === 'privacy' && <PrivacyPage onBack={() => setView('settings')} />}
        {view === 'settings' && (
          <SettingsPage
            onReplayGuide={() => setShowOnboard(true)}
            onOpenSpeech={() => setView('speech')}
            onOpenCalib={() => setView('calib')}
            onOpenPrivacy={() => setView('privacy')}
          />
        )}
      </main>
    </div>
  )
```

- [ ] **Step 6: index.html —— 补 viewport-fit** —— 把第 5-8 行的 meta 替换为：

```html
    <!-- viewport-fit=cover：让内容延伸到刘海/Home 指示条区域，配合 env(safe-area-inset-*) 自己留白。
         maximum-scale / user-scalable=no 刻意保留：双指缩放会整体放大"银行卡对齐"，直接毁掉物理标定。 -->
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"
    />
```

- [ ] **Step 7: vite.config.ts —— manifest 显式不锁方向** —— 把第 55 行 `start_url: '.',` 替换为：

```ts
          start_url: '.',
          // 刻意不锁方向：iPad 横竖屏与手机竖屏都要能用（iOS standalone 本就不吃这个字段，
          // 写出来是为了让后来人知道"没锁"是决定，不是遗漏）
          orientation: 'any',
```

- [ ] **Step 8: 写 CSS 契约测试** —— 创建 `src/layout/css-contract.test.ts`，完整内容：

```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import {
  PHONE_MAX_W_PX, SHORT_MAX_H_PX, CANVAS_MAX_PX,
  DPAD_MAX_PX_PHONE, DPAD_MAX_PX_WIDE, DPAD_VW_PHONE, DPAD_VW_WIDE,
  DPAD_DVH_PHONE, DPAD_DVH_SHORT, TRAIN_WARN_MAX_H_PX,
} from './layout-budget'

const css = readFileSync(new URL('../index.css', import.meta.url), 'utf8')
const training = readFileSync(new URL('../training/TrainingPage.tsx', import.meta.url), 'utf8')

/** CSS 不可单测，但"CSS 里的数字必须等于 layout-budget 的常量"可以。
 *  这个文件是防 TS 与 CSS 漂移的闸门——改了常量忘改 CSS（或反之）立刻红。 */
describe('index.css 与布局预算常量的契约', () => {
  it('声明了手机窄屏断点', () => {
    expect(css).toContain(`@media (max-width: ${PHONE_MAX_W_PX}px)`)
  })

  it('声明了矮屏断点', () => {
    expect(css).toContain(`@media (max-height: ${SHORT_MAX_H_PX}px)`)
  })

  it('方向盘三档宽度与常量一致', () => {
    expect(css).toContain(`width: min(${DPAD_MAX_PX_WIDE}px, ${DPAD_VW_WIDE}vw)`)
    expect(css).toContain(`width: min(${DPAD_MAX_PX_PHONE}px, ${DPAD_VW_PHONE}vw, ${DPAD_DVH_PHONE}dvh)`)
    expect(css).toContain(`width: min(${DPAD_MAX_PX_WIDE}px, ${DPAD_VW_WIDE}vw, ${DPAD_DVH_SHORT}dvh)`)
  })

  it('皮肤画布用容器查询单位兜住舞台剩余高度', () => {
    expect(css).toContain(`width: min(100%, ${CANVAS_MAX_PX}px, 100cqh)`)
    expect(css).toContain('container-type: size')
  })

  it('"屏幕太矮"提示的阈值与常量一致', () => {
    expect(css).toContain(`@media (max-height: ${TRAIN_WARN_MAX_H_PX}px)`)
  })

  it('应用外壳存在且内容区可滚（导航高度不再靠猜）', () => {
    expect(css).toMatch(/\.fq-app\s*\{[^}]*height:\s*100dvh/)
    expect(css).toMatch(/\.fq-app-main\s*\{[^}]*overflow-y:\s*auto/)
  })

  it('全站不再出现写死的导航高度 calc(100vh - 57px)', () => {
    expect(css).not.toContain('calc(100vh - 57px)')
    expect(training).not.toContain('calc(100vh - 57px)')
  })

  it('每个 env(safe-area-inset-*) 都带兜底值（无兜底会让整条声明在旧内核失效）', () => {
    const bare = css.match(/env\(safe-area-inset-[a-z]+\s*\)/g)
    expect(bare).toBeNull()
    expect(css).toContain('env(safe-area-inset-top, 0px)')
  })
})
```

- [ ] **Step 9: 确认 `.fq-dpad` 基础定义未被动过** —— 打开 `src/index.css` 第 113-116 行，确认它仍是下面这个字面量（契约测试第 3 条断言的就是这串，且这也是 iPad 行为"一像素不动"的保证）：

```css
.fq-dpad {
  display: grid; grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(3, 1fr);
  gap: 7px; width: min(210px, 58vw); aspect-ratio: 1; margin: 0 auto;
}
```
- [ ] **Step 10: 跑测试到通过** —— `npm test -- src/layout/css-contract.test.ts`，预期 `8 passed`。（若"方向盘三档"那条红，说明 Step 4 的两个断点块没落全；若"无 57px"那条红，说明 Task 3 还没改 TrainingPage —— 此时先把 `TrainingPage.tsx` 第 361、423 两处 `calc(100vh - 57px)` 分别换成 `'100%'`，其余 JSX 改动仍归 Task 3。）
- [ ] **Step 11: 全量回归 + typecheck** —— `npm test` 预期 **420 passed / 46 files**；`npm run typecheck` 与 `npm run typecheck:functions` 均无输出。
- [ ] **Step 12: 手测外壳（iPad 基线不回退）** ——
  1. `mcp__Claude_Browser__preview_start` 传 `{ "name": "focal-quest-dev" }`。
  2. `mcp__Claude_Browser__resize_window` 传 `{ "width": 1024, "height": 768 }`。
  3. `mcp__Claude_Browser__javascript_tool`（action `javascript_exec`）执行种子脚本：
     `localStorage.setItem('fzp.cssPxPerMm','5.2');localStorage.setItem('fzp.optotypeSizeMm','0.7');localStorage.setItem('fzp.onboarded','1');localStorage.setItem('fzp.skinId','plain');localStorage.setItem('fzp.lang','zh');location.reload();'seeded'`
  4. 再执行量尺脚本：
     ```js
     (() => { const r=s=>{const e=document.querySelector(s); if(!e) return null; const b=e.getBoundingClientRect(); return {w:Math.round(b.width),h:Math.round(b.height),top:Math.round(b.top),bottom:Math.round(b.bottom)}}; const m=document.querySelector('.fq-app-main'); return JSON.stringify({vw:innerWidth,vh:innerHeight,docScrollH:document.documentElement.scrollHeight,docScrollW:document.documentElement.scrollWidth,nav:r('.fq-nav'),app:r('.fq-app'),main:r('.fq-app-main'),mainScrollH:m&&m.scrollHeight,mainClientW:m&&m.clientWidth,mainScrollW:m&&m.scrollWidth,navLabelDisplay:getComputedStyle(document.querySelector('.fq-nav-label')).display}) })()
     ```
     **预期**：`nav.h` 在 55-60 之间（单行文字导航）、`navLabelDisplay === "inline"`（宽屏文案可见）、`docScrollH === vh`（文档层不滚）、`mainScrollW <= mainClientW`（无横向溢出）。
  5. `resize_window` 到 `{ "width": 375, "height": 812 }`，重跑量尺脚本。**预期**：`nav.h <= 62`（**关键**：不再是勘察记录的 99px）、`navLabelDisplay === "none"`、`docScrollH === 812`、`mainScrollW <= mainClientW`。
  6. `mcp__Claude_Browser__read_console_messages` 传 `{ "onlyErrors": true }`，预期空数组。
- [ ] **Step 13: 提交** —— `git add -A && git commit`，信息：`feat(layout): App 外壳改 flex+dvh、导航窄屏只留图标，干掉三处 calc(100vh - 57px)（8 契约单测）`。

---

### Task 3: 训练页竖屏三段布局与 D-pad 可用性

**Files**
- Modify: `E:\developer\fzp\src\training\TrainingPage.tsx`
- Modify: `E:\developer\fzp\src\index.css`（追加 `.fzp-rotate-hint` 与 `@keyframes fzpRevealFloat`；三段骨架/断点已在 Task 2 落地）
- Modify: `E:\developer\fzp\src\i18n.tsx`
- Test: `E:\developer\fzp\src\i18n.test.ts`（Modify，加 1 个 `it`）

**Interfaces**
- Consumes: Task 2 落地的 `.fzp-train` / `.fzp-train-top` / `.fzp-stage` / `.fzp-answer` / `.fzp-voice-hint` / `.fzp-answer-spacer` / `.fzp-tiny-warn` / `.fq-dpad`
- Produces: i18n 键 `train.rotateHint`、`train.screenTooSmall`（zh + en）；CSS 类 `.fzp-rotate-hint`（本任务在 index.css 追加）

- [ ] **Step 1: index.css 加"横屏提示"** —— 在 Task 2 新增的 `.fzp-tiny-warn` 规则之前插入：

```css
/* 手机横屏（矮屏）提示竖屏更好用；iPad 横屏（高 768）不触发 */
.fzp-rotate-hint { display: none; }
@media (orientation: landscape) and (max-height: 560px) {
  .fzp-rotate-hint {
    display: block; position: absolute; top: 4px; left: 50%; transform: translateX(-50%);
    z-index: 7; white-space: nowrap; pointer-events: none;
    font-size: 12px; font-weight: 700; color: #fff;
    background: rgba(51, 40, 90, 0.72); padding: 4px 12px; border-radius: 999px;
  }
}
```

- [ ] **Step 2: TrainingPage.tsx —— preparing 分支去魔法数** —— 把第 361 行 `minHeight: 'calc(100vh - 57px)',` 替换为 `minHeight: '100%',`。（若 Task 2 Step 10 的兜底已改过，此步只需确认现值是 `'100%'`。）
- [ ] **Step 3: TrainingPage.tsx —— 三段骨架换成 CSS 类** —— 把第 422-424 行

```tsx
  return (
    <div style={{ position: 'relative', display: 'flex', flexDirection: 'column', minHeight: 'calc(100vh - 57px)' }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '12px 16px' }}>
```
  替换为
```tsx
  return (
    <div className="fzp-train">
      {/* 手机横屏才显示（纯 CSS 媒体查询控制）：竖屏方向盘更大、单手可达 */}
      <div className="fzp-rotate-hint">{t('train.rotateHint')}</div>
      {/* 屏幕矮到布局预算兜不住时明确提示——视标绝不缩小（那等于偷偷改训练强度） */}
      <div className="fzp-tiny-warn">{t('train.screenTooSmall')}</div>
      <div className="fzp-train-top">
```
- [ ] **Step 4: TrainingPage.tsx —— 舞台换类** —— 把第 436 行
  `      <div style={{ flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '8px 0', position: 'relative' }}>`
  替换为
  `      <div className="fzp-stage">`
- [ ] **Step 5: TrainingPage.tsx —— 答题带换类** —— 把第 508-510 行

```tsx
      {/* 语音提示放左侧（次要信息，不占整行）；十字方向盘居中且紧凑 */}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr auto 1fr', alignItems: 'center', gap: 8, padding: '4px 14px 18px' }}>
        <span style={{ color: 'var(--muted)', fontSize: 12, fontWeight: 600, textAlign: 'right', lineHeight: 1.4 }}>{voskHint}</span>
```
  替换为
```tsx
      {/* 宽屏：语音提示在方向盘左侧（不占整行）。手机窄屏：CSS 改成方向盘上方整行居中 */}
      <div className="fzp-answer">
        <span className="fzp-voice-hint">{voskHint}</span>
```
  并把第 526 行 `        <span aria-hidden />` 替换为 `        <span className="fzp-answer-spacer" aria-hidden />`
- [ ] **Step 6: TrainingPage.tsx —— 顺手修既有 bug（同文件 2 行）** —— 第 577 行用了从未定义的 `fzpFloat`（index.css 里是 `fqFloat`，皮肤里的 `fzpFloat` 只在皮肤内联 style 内），故结算页未揭晓的开箱盒其实不浮动。把
  `          animation: revealed ? 'none' : 'fzpFloat 2.6s ease-in-out infinite',`
  替换为
  `          animation: revealed ? 'none' : 'fzpRevealFloat 2.6s ease-in-out infinite',`
  并在 index.css 的 `@keyframes fzpDexToast ...`（第 160 行）之后插入：
```css
/* 结算页未揭晓的开箱盒：浮动 + 略缩。原先误用了未定义的 fzpFloat，动画不生效；
   这里把 scale(0.85) 写进每一帧，才不会与元素上的 inline transform 打架。 */
@keyframes fzpRevealFloat { 0%, 100% { transform: translateY(0) scale(0.85) } 50% { transform: translateY(-8px) scale(0.85) } }
```
- [ ] **Step 7: i18n.tsx 加两条文案（zh + en）** —— 在 zh 字典的 `'train.pause': '暂停',`（第 92 行）之后插入：

```ts
  'train.rotateHint': '📱 竖屏更好用哦',
  'train.screenTooSmall': '⚠️ 屏幕太矮，视标区放不下。视标不会被缩小（那会改变训练强度）——请把设备横过来，或换大屏设备。',
```
  在 en 字典的 `'train.pause': 'Pause',`（第 500 行）之后插入：
```ts
  'train.rotateHint': '📱 Portrait works better',
  'train.screenTooSmall': '⚠️ The screen is too short for the optotype area. The optotype is never scaled down (that would change training intensity) — rotate the device or use a bigger screen.',
```
- [ ] **Step 8: i18n.test.ts 加键齐全测试** —— 在 `const OTHER_KEYS = ['reward.overspend']` 之后插入：

```ts
/** 竖屏适配新增文案（迭代 3d） */
const PORTRAIT_KEYS = ['train.rotateHint', 'train.screenTooSmall']
```
  并在 `describe` 块末尾（最后一个 `it` 之后）插入：
```ts
  it('竖屏适配新增文案在两种语言都存在', () => {
    for (const k of PORTRAIT_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```
- [ ] **Step 9: 全量回归** —— `npm test` 预期 **421 passed / 46 files**；`npm run typecheck` 无输出。
- [ ] **Step 10: 手测——方向盘必须整只在首屏（本迭代核心验收）** ——
  1. `preview_start { "name": "focal-quest-dev" }`；`resize_window { "width": 375, "height": 812 }`。
  2. `javascript_tool` 种子（同 Task 2 Step 12.3）。
  3. `read_page { "filter": "interactive" }` 找导航里"🎯 训练"按钮的 `ref_N`，`computer { "action": "left_click", "ref": "ref_N" }`。
  4. `computer { "action": "wait", "duration": 3 }`（准备页 1.5s 自动开始 + 首题渲染）。
  5. `javascript_tool` 量尺：
     ```js
     (() => { const r=s=>{const e=document.querySelector(s); if(!e) return null; const b=e.getBoundingClientRect(); return {w:Math.round(b.width*100)/100,h:Math.round(b.height*100)/100,top:Math.round(b.top),bottom:Math.round(b.bottom)}}; const m=document.querySelector('.fq-app-main'); return JSON.stringify({vw:innerWidth,vh:innerHeight,nav:r('.fq-nav'),train:r('.fzp-train'),top:r('.fzp-train-top'),stage:r('.fzp-stage'),dpad:r('.fq-dpad'),svg:r('.fzp-stage svg'),btns:[...document.querySelectorAll('.fq-dpad-btn')].map(b=>{const x=b.getBoundingClientRect();return {label:b.getAttribute('aria-label'),w:Math.round(x.width),h:Math.round(x.height),bottom:Math.round(x.bottom)}}),mainScrollH:m&&m.scrollHeight,mainClientH:m&&m.clientHeight,docScrollH:document.documentElement.scrollHeight}) })()
     ```
     **预期**：
     - `dpad.bottom <= 812` 且 `dpad.w` ≈ 240（勘察时是 `bottom:836`，↓ 键有 24px 在屏外——必须消失）
     - 四个 `btns` 每个 `w >= 70 && h >= 70`（拇指可达，远超 44px 下限）
     - `btns` 里 `label` 为"下/Down"的那个 `bottom <= 812`
     - `mainScrollH === mainClientH`（训练页不滚）、`docScrollH === 812`
     - `svg.w` ≈ `0.7 × 5.2 = 3.64`（**医学红线**：视标像素与 iPad 上完全一致）
  6. `resize_window` 依次到 `{390,844}`、`{414,896}`、`{320,568}`，每次重跑量尺，断言同样的四条（`dpad.bottom <= vh`、按钮 ≥ 60×60、不滚、`svg.w ≈ 3.64`）。
  7. `resize_window { "width": 1024, "height": 768 }`（iPad 横屏基线）：断言 `dpad.w === 210`、`svg.w ≈ 3.64`、`stage.h >= 400`。
  8. `resize_window { "width": 812, "height": 375 }`（手机横屏）：断言 `dpad.bottom <= 375`、`.fzp-rotate-hint` 可见（`getComputedStyle(document.querySelector('.fzp-rotate-hint')).display === 'block'`）。
  9. `read_console_messages { "onlyErrors": true }` 预期空。
  10. 清理：`javascript_tool` 执行 `['fzp.cssPxPerMm','fzp.optotypeSizeMm','fzp.onboarded','fzp.skinId','fzp.lang'].forEach(k=>localStorage.removeItem(k));'cleaned'`。
- [ ] **Step 11: 提交** —— `git add -A && git commit`，信息：`feat(train): 训练页三段竖屏布局 + 手机方向盘放大到 240/间距 10（修 ↓ 键掉出首屏）`。

---

### Task 4: 三个皮肤 Stage 竖屏适配（cq 等比 + 神庙视标 scale 归一）

画布用 `100cqh` 兜住舞台剩余高度后，装饰若还写死 px 就会在小画布里出界（神庙 224px 守护者在 300px 画布上会被 `overflow:hidden` 切头）。本任务把装饰全部换成 `cqmin`（画布短边百分比），并顺带把神庙压在视标上的 `scale(0.8)` 折进容器尺寸——**视标从此等于标定值，不再暗自小 20%**。

**Files**
- Modify: `E:\developer\fzp\src\skins\space\SpaceStage.tsx`
- Modify: `E:\developer\fzp\src\skins\shrine\ShrineStage.tsx`
- Test（既有，必须保持绿）: `E:\developer\fzp\src\skins\space\SpaceStage.test.ts`、`E:\developer\fzp\src\skins\shrine\ShrineStage.test.ts`、`E:\developer\fzp\src\skins\registry.test.ts`

**Interfaces**
- Consumes: `.fzp-skin-canvas`（Task 2 落地）；`StageProps { target, heightPx, phase, lastAnswer, isEgg, capturedReserveIds }` 保持不变
- Produces: 不改任何导出函数签名（`buildEnemyPool` / `enemyForSeq` / `buildGuardianPool` / `guardianForSeq` 一字不动，单测入口不动）

- [ ] **Step 1: SpaceStage —— 画布换类** —— 把第 70-81 行的根 div 替换为：

```tsx
    <div
      className="fzp-skin-canvas"
      style={{ background: `url(${asset('/skins/space/bg-nebula.jpg')}) center / cover, #070a16` }}
    >
```
- [ ] **Step 2: SpaceStage —— 删掉像素写死的 enemySize，敌舰改 cqmin** —— 删除第 65 行 `const enemySize = Math.max(120, heightPx * 2.6)`（正常标定下它恒为 120px 下限，改成 30cqmin ≈ 126px@420 画布，行为等价且能随画布缩），并把第 104-126 行整块替换为：

```tsx
      {/* 敌舰（E 印在核心）——showing 且未被击碎时。
          承载体尺寸用 cqmin（画布短边%）等比；视标 E 仍是 mm×标定的绝对像素，不跟着缩。 */}
      {phase === 'showing' && target && !hit && (
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '50%',
            width: '30cqmin',
            height: '30cqmin',
            transform: 'translate(-50%,-50%)',
            animation: miss ? 'fzpShake 0.3s' : 'fzpFloat 2.8s ease-in-out infinite',
            filter: isEgg ? 'drop-shadow(0 0 14px gold)' : 'none',
          }}
        >
          <img src={enemy.src} alt="" style={{ display: 'block', width: '100%', height: '100%' }} />
          <div style={{ position: 'absolute', inset: 0, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <span style={{ color: '#ffffff', filter: 'drop-shadow(0 0 3px #000) drop-shadow(0 0 1px #000)' }}>
              <TumblingE direction={target} heightPx={heightPx} />
            </span>
          </div>
          <div style={{ position: 'absolute', top: '-5cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(9px, 2.6cqmin)', letterSpacing: 1, color: '#bfe4ff', whiteSpace: 'nowrap', textShadow: '0 0 4px #001' }}>{t(`space.enemy.${enemy.name}`)}</div>
          {isEgg && <div style={{ position: 'absolute', top: '-3.5cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(14px, 4.8cqmin)' }}>✨</div>}
        </div>
      )}
```
- [ ] **Step 3: SpaceStage —— 其余装饰改 cqmin** —— 逐处替换（都是"E 的兄弟节点"，允许单独 transform）：
  - 第 130 行翻拍提示：`fontSize: 16` → `fontSize: 'max(12px, 3.8cqmin)'`
  - 第 137 行激光：`width: 5` → `width: 'max(3px, 1.2cqmin)'`
  - 第 140 行白闪：`width: 200, height: 200` → `width: '48cqmin', height: '48cqmin'`
  - 第 142-158 行爆炸序列帧，整块替换为：

```tsx
      {hit && (
        <div
          key={`b${fx!.key}`}
          style={{
            position: 'absolute',
            top: '40%',
            left: '50%',
            width: '38cqmin',
            height: '38cqmin',
            marginLeft: '-19cqmin',
            marginTop: '-19cqmin',
            backgroundImage: `url(${asset('/skins/space/explosion-strip9.png')})`,
            backgroundRepeat: 'no-repeat',
            // 整条 9 帧等比放大到"每帧宽 = 盒宽"，否则画布一缩 background-position 就错帧
            backgroundSize: 'calc(9 * 38cqmin) 38cqmin',
            animation: 'fzpBoomStrip 0.55s steps(9) forwards',
            transform: 'scale(1.4)',
          }}
        />
      )}
```
  - 第 162-165 行战机，整块替换为：
```tsx
      {/* 战机：两帧引擎火焰交替 */}
      <div style={{ position: 'absolute', bottom: '2%', left: '50%', width: '17cqmin', height: '17cqmin', transform: 'translateX(-50%)', animation: miss ? 'fzpShakeShip 0.3s' : 'none' }}>
        <img src={asset('/skins/space/ship-1.png')} alt="" style={{ position: 'absolute', inset: 0, width: '100%', height: '100%' }} />
        <img src={asset('/skins/space/ship-2.png')} alt="" style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', animation: 'fzpEngine 0.32s steps(1) infinite' }} />
      </div>
```
  - 第 172 行 keyframes：`@keyframes fzpBoomStrip { from { background-position: 0 0 } to { background-position: -1440px 0 } }` → `@keyframes fzpBoomStrip { from { background-position-x: 0 } to { background-position-x: calc(9 * -38cqmin) } }`
- [ ] **Step 4: ShrineStage —— 画布换类 + HUD 等比** —— 把第 85-96 行的根 div 替换为：

```tsx
    <div
      className="fzp-skin-canvas"
      style={{ background: `url(${asset('/skins/shrine/bg.png')}) center / cover, #0b1a18`, imageRendering: 'pixelated' }}
    >
```
  并把第 102 行计数条的 `top: 10, left: 12, fontSize: 12` 改为 `top: '2.4cqmin', left: '2.9cqmin', fontSize: 'max(9px, 2.9cqmin)'`（其余属性不动）。
- [ ] **Step 5: ShrineStage —— 守护者：scale(0.8) 折进尺寸（医学修正）+ sprite 帧随画布等比** —— 在组件里 `const miss = fx?.correct === false`（第 82 行）之后插入：

```tsx
  // sprite 帧数交给 CSS 变量，让 keyframes 能按帧数算位移（帧宽 = 盒宽 = 37cqmin）
  const spriteVars = { '--fzp-g-frames': guardian.kind === 'sprite' ? guardian.frames : 0 } as CSSProperties
```
  并把文件第 1 行 `import { useEffect, useState } from 'react'` 改为 `import { useEffect, useState, type CSSProperties } from 'react'`。（`as CSSProperties` 是 React 里给自定义 CSS 变量赋值的常规写法；若这台机器的 TS 版本报"两种类型无重叠"，改成 `as unknown as CSSProperties`，不要改成 `any`。）
  然后把第 110-138 行整块替换为：
```tsx
      {/* 守护者（每题轮换）+ 核心符文 E。
          原实现是 192×224 + transform: scale(0.8)，而 CSS transform 会作用于后代 ——
          等于让视标 E 只有标定值的 80%（换皮肤就偷偷改训练强度）。这里把 0.8 折进
          容器尺寸（192*0.8/420≈37%、224*0.8/420≈43%）并改用 cqmin 随画布等比，
          祖先链上的 scale 归一为 1，E 从此严格等于 毫米设定 × 屏幕标定。 */}
      {phase === 'showing' && target && !hit && (
        <div
          style={{
            position: 'absolute',
            top: '36%',
            left: '50%',
            width: '37cqmin',
            height: '43cqmin',
            transform: 'translate(-50%,-50%)',
            animation: miss ? 'fzpShakeG 0.35s' : 'fzpFloat 2.6s ease-in-out infinite',
            filter: isEgg ? 'drop-shadow(0 0 14px gold)' : 'drop-shadow(0 0 10px rgba(255,120,40,0.5))',
          }}
        >
          {guardian.kind === 'sprite' ? (
            <div
              style={{
                ...spriteVars,
                position: 'absolute',
                inset: 0,
                backgroundImage: `url(${guardian.src})`,
                backgroundRepeat: 'no-repeat',
                backgroundSize: 'calc(var(--fzp-g-frames) * 37cqmin) 43cqmin',
                animation: `fzpGuardian 0.9s steps(${guardian.frames}) infinite`,
              }}
            />
          ) : (
            <img src={guardian.src} alt="" style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', objectFit: 'contain', imageRendering: 'pixelated' }} />
          )}
          {/* 怪兽名牌，强化"每题变形"的代入感 */}
          <div style={{ position: 'absolute', top: '-6.7cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(9px, 2.6cqmin)', letterSpacing: 1, color: '#ffd9a0', whiteSpace: 'nowrap', textShadow: '0 0 4px #000' }}>{t(`shrine.guardian.${guardian.name}`)}</div>
          {/* 符文 E：深色圆底保证对比（铁律一：看清优先）。padding 跟着 heightPx 走，
              绝不能换成 cqmin —— 圆底必须与视标同比例，而不是与画布同比例。 */}
          <div style={{ position: 'absolute', top: '52%', left: '50%', transform: 'translate(-50%,-50%)', background: 'rgba(0,0,0,0.6)', borderRadius: '50%', padding: Math.max(6, heightPx * 0.4), display: 'flex' }}>
            <span style={{ color: '#ffffff' }}>
              <TumblingE direction={target} heightPx={heightPx} />
            </span>
          </div>
          {isEgg && <div style={{ position: 'absolute', top: '-2.9cqmin', left: '50%', transform: 'translateX(-50%)', fontSize: 'max(14px, 4.8cqmin)' }}>✨</div>}
        </div>
      )}
```
- [ ] **Step 6: ShrineStage —— 其余装饰 + keyframes 去 scale** ——
  - 第 142 行翻拍文案：`fontSize: 16` → `fontSize: 'max(12px, 3.8cqmin)'`
  - 第 149 行剑气：`height: 5` → `height: 'max(3px, 1.2cqmin)'`
  - 第 152 行爆散：`width: 190, height: 190` → `width: '45cqmin', height: '45cqmin'`
  - 第 155 行精灵：`fontSize: 28` → `fontSize: 'max(16px, 6.7cqmin)'`
  - 第 161-174 行勇者，整块替换为：

```tsx
      {/* 勇者：idle 循环；答对时播攻击动画。两条 strip 的帧宽 = 盒宽，故 background-size 也走 cqmin */}
      <div
        style={{
          position: 'absolute',
          bottom: '4%',
          left: hit ? '30%' : '22%',
          width: hit ? '46cqmin' : '18cqmin',
          height: '23cqmin',
          transition: 'left 0.15s',
          backgroundImage: hit ? `url(${HERO.attack})` : `url(${HERO.idle})`,
          backgroundRepeat: 'no-repeat',
          backgroundSize: hit ? 'calc(6 * 46cqmin) 23cqmin' : 'calc(4 * 18cqmin) 23cqmin',
          animation: hit ? 'fzpHeroAtk 0.5s steps(6) forwards' : 'fzpHeroIdle 0.8s steps(4) infinite',
          zIndex: 3,
        }}
      />
```
  - 第 176-185 行内联 keyframes 块，前四条替换为：
```
        @keyframes fzpGuardian { from { background-position-x: 0 } to { background-position-x: calc(var(--fzp-g-frames) * -37cqmin) } }
        @keyframes fzpHeroIdle { from { background-position-x: 0 } to { background-position-x: calc(4 * -18cqmin) } }
        @keyframes fzpHeroAtk { from { background-position-x: 0 } to { background-position-x: calc(6 * -46cqmin) } }
        @keyframes fzpFloat { 0%,100% { transform: translate(-50%,-50%) } 50% { transform: translate(-50%,-56%) } }
        @keyframes fzpShakeG { 25% { transform: translate(-56%,-50%) } 75% { transform: translate(-44%,-50%) } }
```
  （后三条 `fzpSlash` / `fzpSpirit` / `fzpFade` 保持原样。**注意**：`fzpFloat` / `fzpShakeG` 里的 `scale(0.8)` 必须删净，否则动画期间视标又缩回 80%。）
- [ ] **Step 7: PlainStage 复核（不改代码）** —— 打开 `src/skins/PlainStage.tsx` 确认：无写死宽高、无 absolute 定位视标、无 scale，只有 `padding: 16` + `TumblingE heightPx`。**结论应为"无需改动"**，把这一结论写进提交信息。
- [ ] **Step 8: 全量回归** —— `npm test` 预期 **421 passed / 46 files**（皮肤纯函数单测未动，必须仍绿）；`npm run typecheck` 无输出（重点看 `spriteVars` 的 `as CSSProperties` 是否通过）。
- [ ] **Step 9: 手测两个方形皮肤（需先解锁）** ——
  1. 临时把 `src/skins/registry.ts:32` 的 `SKIN_UNLOCK_COST` 三个值都改成 `0`（否则积分不够时 `TrainingPage.tsx:349-351` 会回退 plain，测不到方形画布）。
  2. `preview_start { "name": "focal-quest-dev" }`；`resize_window { "width": 375, "height": 812 }`；`javascript_tool` 种子并把皮肤设为 shrine：
     `localStorage.setItem('fzp.cssPxPerMm','5.2');localStorage.setItem('fzp.optotypeSizeMm','0.7');localStorage.setItem('fzp.onboarded','1');localStorage.setItem('fzp.skinId','shrine');localStorage.setItem('fzp.lang','zh');location.reload();'seeded'`
  3. 点导航"🎯 训练"→ `computer wait 3` → `javascript_tool` 量尺：
     ```js
     (() => { const c=document.querySelector('.fzp-skin-canvas'); const cb=c&&c.getBoundingClientRect(); const svg=document.querySelector('.fzp-skin-canvas svg'); const sb=svg&&svg.getBoundingClientRect(); const kids=[...(c?c.children:[])].map(e=>{const b=e.getBoundingClientRect();return {tag:e.tagName,left:Math.round(b.left-cb.left),top:Math.round(b.top-cb.top),w:Math.round(b.width),h:Math.round(b.height)}}); return JSON.stringify({canvas:cb&&{w:Math.round(cb.width),h:Math.round(cb.height)},svgW:sb&&Math.round(sb.width*100)/100,kids}) })()
     ```
     **预期**：`canvas.w === canvas.h`（正方形）且 ≈ 375；`svgW ≈ 3.64`（**医学修正验证**：改前会是 `2.91 = 3.64×0.8`）；`kids` 里每一项 `top >= -8 && left >= -8 && top + h <= canvas.h + 8`（守护者/勇者/名牌不出界，名牌允许 8px 内的负偏移）。
  4. `resize_window { "width": 375, "height": 667 }` 重跑，断言 `canvas.w === canvas.h` 且 ≈ 328（`100cqh` 生效、画布随舞台缩），`svgW` 仍 ≈ 3.64（**视标不随画布缩——这是本迭代最关键的红线**）。
  5. `localStorage.setItem('fzp.skinId','space');location.reload()` → 重复 3/4 两步，断言同样四条（space 的 kids 含敌舰/战机）。
  6. `resize_window { "width": 1024, "height": 768 }`：断言 `canvas.w === 420`（iPad 基线不回退）。
  7. `read_console_messages { "onlyErrors": true }` 预期空。
  8. **必做还原**：把 `SKIN_UNLOCK_COST` 改回 `{ plain: 0, space: 1000, shrine: 2500 }`；`git diff -- src/skins/registry.ts` 必须为空输出；清理 localStorage（同 Task 3 Step 10.10）。
- [ ] **Step 10: 提交** —— `git add -A && git commit`，信息：`feat(skins): 皮肤画布 100cqh 自适应 + 装饰改 cqmin；修正神庙 scale(0.8) 让视标等于标定值（PlainStage 无需改动）`。

---

### Task 5: 标定页窄屏安全——短边模式 + 保存闸门（TDD）

手机竖屏视口物理宽约 62-72mm，比银行卡长边（85.6mm）还窄：勘察实测 375 视口下滑块上限只能到 4.03 px/mm，真值约 6.05 —— 在竖屏保存会把 px/mm 低估 33%，此后所有视标偏小 33% 且家长完全无感。本任务给窄屏换参照边（卡片短边 53.98mm，375 屏最大可达 6.39 px/mm），并给"放不下参照物"的情形加**保存禁用 + 明确警告**。

**Files**
- Create: `E:\developer\fzp\src\calibration\calibration-fit.ts`
- Test: `E:\developer\fzp\src\calibration\calibration-fit.test.ts`（Create）
- Modify: `E:\developer\fzp\src\calibration\CalibrationPage.tsx`
- Modify: `E:\developer\fzp\src\i18n.tsx`
- Test: `E:\developer\fzp\src\i18n.test.ts`（Modify，加 1 个 `it`）
- 不动: `E:\developer\fzp\src\calibration\calibration-math.ts` 与其 4 条单测（语义红线）

**Interfaces**
- Consumes: `CARD_WIDTH_MM = 85.6`、`cssPxPerMm(cardCssPx)`（来自 `calibration-math.ts`）
- Produces:
  - `export const CARD_SHORT_MM = 53.98`
  - `export const HANDLE_RESERVE_PX = 30`
  - `export const MIN_CARD_PX = 150`
  - `export const LONG_EDGE_MIN_PX = 520`
  - `export type CardEdge = 'long' | 'short'`
  - `export function maxCardPx(availPx: number): number`
  - `export function pickCardEdge(availPx: number): CardEdge`
  - `export function edgeMm(edge: CardEdge): number`
  - `export function ratioFromCardPx(cardPx: number, edge: CardEdge): number`
  - `export function cardPxFromRatio(ratio: number, edge: CardEdge): number`
  - `export function canSave(availPx: number, edge: CardEdge, savedRatio: number | null): boolean`

- [ ] **Step 1: 先写失败测试** —— 创建 `src/calibration/calibration-fit.test.ts`，完整内容：

```ts
import { describe, it, expect } from 'vitest'
import { cssPxPerMm, CARD_WIDTH_MM } from './calibration-math'
import {
  CARD_SHORT_MM, HANDLE_RESERVE_PX, MIN_CARD_PX, LONG_EDGE_MIN_PX,
  maxCardPx, pickCardEdge, edgeMm, ratioFromCardPx, cardPxFromRatio, canSave,
} from './calibration-fit'

/** 真机常量：375 CSS px 的 iPhone 竖屏，物理屏宽约 62mm → 约 6.05 px/mm */
const PHONE_TRUE_RATIO = 6.05

describe('标定的窄屏适配', () => {
  it('maxCardPx 给右侧拖柄留出空间', () => {
    expect(maxCardPx(1024)).toBe(1024 - HANDLE_RESERVE_PX)
    expect(maxCardPx(375)).toBe(375 - HANDLE_RESERVE_PX)
  })

  it('maxCardPx 有下限，不会小于最小卡宽', () => {
    expect(maxCardPx(100)).toBe(MIN_CARD_PX + 10)
    expect(maxCardPx(0)).toBe(MIN_CARD_PX + 10)
  })

  it('pickCardEdge：iPad 竖屏 / 横屏都用长边', () => {
    expect(pickCardEdge(768)).toBe('long')
    expect(pickCardEdge(1024)).toBe('long')
  })

  it('pickCardEdge：手机竖屏放不下长边，改用短边', () => {
    expect(pickCardEdge(375)).toBe('short')
    expect(pickCardEdge(430)).toBe('short')
  })

  it('pickCardEdge：手机横屏又能用长边（标定值与方向无关，横屏标一次最准）', () => {
    expect(pickCardEdge(812)).toBe('long')
  })

  it('pickCardEdge 的分界就是 LONG_EDGE_MIN_PX', () => {
    expect(pickCardEdge(LONG_EDGE_MIN_PX + HANDLE_RESERVE_PX)).toBe('long')
    expect(pickCardEdge(LONG_EDGE_MIN_PX + HANDLE_RESERVE_PX - 1)).toBe('short')
  })

  it('edgeMm：长边 85.6、短边 53.98', () => {
    expect(edgeMm('long')).toBe(CARD_WIDTH_MM)
    expect(edgeMm('short')).toBe(CARD_SHORT_MM)
  })

  it('长边模式与既有 cssPxPerMm 完全等价（不改既有语义）', () => {
    expect(ratioFromCardPx(856, 'long')).toBeCloseTo(cssPxPerMm(856), 10)
    expect(ratioFromCardPx(518, 'long')).toBeCloseTo(cssPxPerMm(518), 10)
  })

  it('短边模式按 53.98mm 折算', () => {
    expect(ratioFromCardPx(539.8, 'short')).toBeCloseTo(10, 6)
  })

  it('非正卡宽抛错（和 cssPxPerMm 一致的防呆）', () => {
    expect(() => ratioFromCardPx(0, 'short')).toThrow()
    expect(() => ratioFromCardPx(-5, 'long')).toThrow()
  })

  it('cardPxFromRatio 与 ratioFromCardPx 互逆', () => {
    for (const edge of ['long', 'short'] as const) {
      const px = cardPxFromRatio(PHONE_TRUE_RATIO, edge)
      expect(ratioFromCardPx(px, edge)).toBeCloseTo(PHONE_TRUE_RATIO, 10)
    }
  })

  it('短边模式在 375 屏上够得到真机 6.05 px/mm（长边模式够不到，这就是换边的理由）', () => {
    expect(ratioFromCardPx(maxCardPx(375), 'short')).toBeGreaterThan(PHONE_TRUE_RATIO)
    expect(ratioFromCardPx(maxCardPx(375), 'long')).toBeLessThan(PHONE_TRUE_RATIO)
  })

  it('canSave：没有存档、屏幕够宽 → 允许保存', () => {
    expect(canSave(1024, 'long', null)).toBe(true)
    expect(canSave(375, 'short', null)).toBe(true)
  })

  it('canSave：已存 6.05 但当前屏够不到（长边模式 + 375 宽）→ 禁止保存，保护既有正确值', () => {
    expect(canSave(375, 'long', PHONE_TRUE_RATIO)).toBe(false)
    expect(canSave(375, 'short', PHONE_TRUE_RATIO)).toBe(true)
  })
})
```

- [ ] **Step 2: 跑到失败** —— `npm test -- src/calibration/calibration-fit.test.ts`，预期报 `Failed to resolve import "./calibration-fit"`。
- [ ] **Step 3: 实现纯函数** —— 创建 `src/calibration/calibration-fit.ts`，完整内容：

```ts
import { cssPxPerMm, CARD_WIDTH_MM } from './calibration-math'

/**
 * 窄屏标定：手机竖屏视口物理宽约 62-72mm，比银行卡长边（85.6mm）还窄，
 * 长边模式在 375 屏上最多只能到 4.03 px/mm，而真值约 6.05 —— 在竖屏保存
 * 会把 px/mm 低估 33%，此后所有视标偏小 33%，家长完全察觉不到。
 *
 * 对策：窄屏改用卡片短边（53.98mm）当参照物（375 屏可达 6.39 px/mm），
 * 并在"当前屏够不到已存比值"时禁止保存（见 canSave）。
 * CSS px/mm 是设备常量、与屏幕方向无关，所以"横屏标定 + 竖屏训练"完全合法。
 *
 * 注意：既有 cssPxPerMm / mmToCssPx / CARD_WIDTH_MM 的语义一字未动（有单测锚定），
 * 长边模式仍然直接委托 cssPxPerMm。
 */

/** 银行卡 ISO/IEC 7810 ID-1 短边 */
export const CARD_SHORT_MM = 53.98
/** 右侧拖柄预留宽度（与页面里 maxPx 的算法一致） */
export const HANDLE_RESERVE_PX = 30
/** 参照带最小宽度（太小了对不准） */
export const MIN_CARD_PX = 150
/** 走长边模式所需的最小卡宽：520px ≈ 6.07 px/mm × 85.6mm，覆盖现代手机/平板真值。
 *  够不到就说明这块屏放不下整张卡的长边，必须换短边。 */
export const LONG_EDGE_MIN_PX = 520

export type CardEdge = 'long' | 'short'

/** 当前视口能给参照带的最大宽度 */
export function maxCardPx(availPx: number): number {
  return Math.max(MIN_CARD_PX + 10, Math.floor(availPx - HANDLE_RESERVE_PX))
}

/** 这块屏该用卡片的哪条边做参照 */
export function pickCardEdge(availPx: number): CardEdge {
  return maxCardPx(availPx) >= LONG_EDGE_MIN_PX ? 'long' : 'short'
}

export function edgeMm(edge: CardEdge): number {
  return edge === 'long' ? CARD_WIDTH_MM : CARD_SHORT_MM
}

/** 参照带宽度（CSS px）→ px/mm */
export function ratioFromCardPx(cardPx: number, edge: CardEdge): number {
  if (edge === 'long') return cssPxPerMm(cardPx)
  if (!(cardPx > 0)) throw new Error('cardPx 必须为正数')
  return cardPx / CARD_SHORT_MM
}

/** px/mm → 参照带应有的宽度（CSS px），用于旋转设备后还原滑块位置 */
export function cardPxFromRatio(ratio: number, edge: CardEdge): number {
  if (!(ratio > 0)) throw new Error('ratio 必须为正数')
  return ratio * edgeMm(edge)
}

/**
 * 当前屏是否允许保存标定。
 * 已有存档时必须"够得到"它——否则页面显示的是被 clamp 截断的小值，
 * 家长顺手一点保存就把正确值覆盖成偏小 33% 的坏值（静默破坏医学参数）。
 */
export function canSave(availPx: number, edge: CardEdge, savedRatio: number | null): boolean {
  const max = maxCardPx(availPx)
  if (max < MIN_CARD_PX + 10) return false
  if (savedRatio === null) return true
  return cardPxFromRatio(savedRatio, edge) <= max
}
```

- [ ] **Step 4: 跑到通过** —— `npm test -- src/calibration/calibration-fit.test.ts`，预期 `14 passed`。
- [ ] **Step 5: i18n 加 6 条文案** —— zh 字典在 `'calib.save': '💾 保存标定',`（第 350 行）之前插入：

```ts
  'calib.instructionShort': '把银行卡**竖着**贴在色块上，让卡的短边（{mm} mm）与色块左右两边对齐。',
  'calib.edgeLong': '长边模式（85.6 mm）',
  'calib.edgeShort': '短边模式（53.98 mm）',
  'calib.shortEdgeWhy': '这块屏太窄，放不下整张卡的长边（85.6 mm），改用短边同样能算出 px/mm。想更准可以把设备**横过来**再标一次——标定值是屏幕属性，与横竖屏无关。',
  'calib.cantSave': '⚠️ 当前屏幕放不下参照物，这里不能保存（避免把已有的正确值覆盖成偏小的值）。请把设备横过来，或在 iPad 上标定。',
  'calib.savedMismatch': '⚠️ 已保存的 {v} px/mm 在这块屏上放不下，上面显示的是被截断的值——**不要保存**。',
```
  en 字典在 `'calib.save': '💾 Save calibration',`（第 753 行）之前插入：
```ts
  'calib.instructionShort': 'Hold the card **upright** against the bar and align the card’s short edge ({mm} mm) with the left and right edges.',
  'calib.edgeLong': 'Long edge (85.6 mm)',
  'calib.edgeShort': 'Short edge (53.98 mm)',
  'calib.shortEdgeWhy': 'This screen is too narrow for the card’s long edge (85.6 mm); the short edge gives the same px/mm. For best accuracy **rotate the device** and calibrate again — the value is a screen property, independent of orientation.',
  'calib.cantSave': '⚠️ The reference does not fit on this screen, so saving is disabled here (it would overwrite a correct value with a too-small one). Rotate the device, or calibrate on the iPad.',
  'calib.savedMismatch': '⚠️ The saved {v} px/mm does not fit on this screen; the value above is clamped — **do not save**.',
```
- [ ] **Step 6: i18n.test.ts 加键齐全测试** —— 在 `PORTRAIT_KEYS` 之后插入：

```ts
/** 标定页窄屏新增文案（迭代 3d） */
const CALIB_PORTRAIT_KEYS = [
  'calib.instructionShort', 'calib.edgeLong', 'calib.edgeShort',
  'calib.shortEdgeWhy', 'calib.cantSave', 'calib.savedMismatch',
]
```
  并在 describe 末尾插入：
```ts
  it('标定页窄屏新增 6 条文案在两种语言都存在', () => {
    expect(CALIB_PORTRAIT_KEYS.length).toBe(6)
    for (const k of CALIB_PORTRAIT_KEYS) {
      expect(hasKey('zh', k), `zh 缺 ${k}`).toBe(true)
      expect(hasKey('en', k), `en 缺 ${k}`).toBe(true)
    }
  })
```
- [ ] **Step 7: 改 CalibrationPage** —— 把 `src/calibration/CalibrationPage.tsx` **整文件**替换为：

```tsx
import { useState, useEffect } from 'react'
import { mmToCssPx, CARD_WIDTH_MM } from './calibration-math'
import {
  CARD_SHORT_MM, MIN_CARD_PX, maxCardPx, pickCardEdge, edgeMm,
  ratioFromCardPx, cardPxFromRatio, canSave,
} from './calibration-fit'
import { lsGet, lsSet } from '../data/storage'
import { useT, Rich } from '../i18n'

const STORAGE_KEY = 'fzp.cssPxPerMm'
/** 银行卡 ISO/IEC 7810 ID-1：85.6 × 53.98 mm，宽高比约 1.586 */
const CARD_ASPECT = CARD_WIDTH_MM / CARD_SHORT_MM
/** 短边模式的参照带高度：卡片竖放比屏幕还高，只需对齐左右两边，故不画整张卡 */
const SHORT_BAND_H = 190

export function CalibrationPage() {
  const t = useT()
  // 可用宽度（视口宽，排除滚动条）——滑块/拖拽上限随之，杜绝溢出裁切
  const [avail, setAvail] = useState(360)
  const [cardPx, setCardPx] = useState(300)
  const [saved, setSaved] = useState<number | null>(() => {
    const v = lsGet(STORAGE_KEY)
    return v ? Number(v) : null
  })

  useEffect(() => {
    const measure = () => setAvail(document.documentElement.clientWidth)
    measure()
    const ro = new ResizeObserver(measure) // 比 window.resize 更可靠（旋转/视口变化都触发）
    ro.observe(document.documentElement)
    window.addEventListener('resize', measure)
    return () => { ro.disconnect(); window.removeEventListener('resize', measure) }
  }, [])

  // 窄屏用卡片短边当参照（长边根本放不下，硬凑会把 px/mm 低估 33%）
  const edge = pickCardEdge(avail)

  // 已有存档时按当前参照边还原参照带宽度；旋转设备切换长/短边也会重算
  useEffect(() => {
    if (saved === null) return
    setCardPx(Math.round(cardPxFromRatio(saved, edge)))
  }, [edge, saved])

  const minPx = MIN_CARD_PX
  const maxPx = maxCardPx(avail)
  const w = Math.min(Math.max(cardPx, minPx), maxPx)
  const ratio = ratioFromCardPx(w, edge)
  const saveOk = canSave(avail, edge, saved)
  // 已存值在这块屏上够不到 → 页面显示的是被截断的值，必须警告且不许保存
  const mismatch = saved !== null && !saveOk

  function setW(px: number) {
    setCardPx(Math.min(Math.max(px, minPx), maxPx))
  }

  // 直接拖右下角手柄：卡框右边缘跟随手指/指针
  function onDragStart(e: React.PointerEvent<HTMLDivElement>) {
    e.preventDefault()
    const cardEl = e.currentTarget.parentElement as HTMLElement
    const move = (ev: PointerEvent) => {
      const rect = cardEl.getBoundingClientRect()
      setW(Math.round(ev.clientX - rect.left))
    }
    const up = () => {
      window.removeEventListener('pointermove', move)
      window.removeEventListener('pointerup', up)
    }
    window.addEventListener('pointermove', move)
    window.addEventListener('pointerup', up)
  }

  function save() {
    if (!saveOk) return
    lsSet(STORAGE_KEY, String(ratio))
    setSaved(ratio)
  }

  const bandH = edge === 'long' ? Math.round(w / CARD_ASPECT) : SHORT_BAND_H

  return (
    <div className="fq-page fq-rise">
      <h2 className="fq-h2">{t('calib.title')}</h2>
      <p className="fq-sub">
        {edge === 'long'
          ? t('calib.instruction', { mm: CARD_WIDTH_MM })
          : <Rich text={t('calib.instructionShort', { mm: CARD_SHORT_MM })} />}
      </p>

      {/* 全宽参照带：冲破 fq-page 的 460px 上限、用测得视口宽（非 100vw，避开滚动条溢出），卡框永不溢出/滚动 */}
      <div
        style={{ width: avail, position: 'relative', left: '50%', transform: 'translateX(-50%)', marginTop: 16, padding: '4px 0 4px 16px', boxSizing: 'border-box' }}
      >
        <div style={{ position: 'relative', width: w, height: bandH }}>
          <div
            style={{
              position: 'absolute', inset: 0,
              background: 'linear-gradient(135deg, var(--violet), var(--violet-2))',
              borderRadius: 10, boxShadow: 'var(--shadow)',
              display: 'grid', placeItems: 'center', color: '#fff', fontSize: 13, fontWeight: 700,
              textAlign: 'center', padding: '0 10px',
            }}
          >
            💳 {edge === 'long' ? t('calib.cardHint') : t('calib.edgeShort')}
          </div>
          {/* 右边缘拖拽手柄（触控友好，touchAction none 防误滚） */}
          <div
            onPointerDown={onDragStart}
            style={{ position: 'absolute', top: 0, right: -12, width: 24, height: '100%', cursor: 'ew-resize', touchAction: 'none', display: 'grid', placeItems: 'center' }}
          >
            <div style={{ width: 8, height: 44, borderRadius: 6, background: 'var(--violet)', boxShadow: 'var(--shadow)' }} />
          </div>
        </div>
      </div>

      {edge === 'short' && (
        <div className="fq-card" style={{ marginTop: 14, fontSize: 13, color: 'var(--muted)', lineHeight: 1.6 }}>
          <Rich text={t('calib.shortEdgeWhy')} />
        </div>
      )}

      <div className="fq-card" style={{ marginTop: 16 }}>
        <input
          type="range"
          min={minPx}
          max={maxPx}
          value={w}
          onChange={(e) => setW(Number(e.target.value))}
          style={{ width: '100%', accentColor: 'var(--violet)' }}
        />
        <p style={{ fontSize: 13, color: 'var(--muted)', marginTop: 4 }}>
          {t('calib.barWidth', { px: w })}<b style={{ color: 'var(--violet)' }}>{ratio.toFixed(3)}</b> px/mm
          <span className="fq-chip" style={{ marginLeft: 8, fontSize: 11 }}>{t(edge === 'long' ? 'calib.edgeLong' : 'calib.edgeShort')}</span>
        </p>
        {mismatch && (
          <p style={{ fontSize: 13, color: 'var(--coral)', marginTop: 8, lineHeight: 1.6 }}>
            <Rich text={t('calib.savedMismatch', { v: saved!.toFixed(3) })} />
          </p>
        )}
        <button
          className="fq-cta"
          style={{ width: '100%', marginTop: 8, opacity: saveOk ? 1 : 0.45 }}
          onClick={save}
          disabled={!saveOk}
        >
          {t('calib.save')}
        </button>
        {!saveOk && (
          <p style={{ fontSize: 12, color: 'var(--muted)', marginTop: 8, lineHeight: 1.6 }}>{t('calib.cantSave')}</p>
        )}
        {saved !== null && (
          <div style={{ marginTop: 12, textAlign: 'center' }}>
            <span className="fq-chip" style={{ background: '#e8f9f0', color: '#1d9e75' }}>
              {t('calib.saved', { v: saved.toFixed(3) })}
            </span>
          </div>
        )}
      </div>

      {saved !== null && (
        <div className="fq-card" style={{ marginTop: 14, textAlign: 'center' }}>
          <div className="fq-card-title" style={{ justifyContent: 'center' }}>{t('calib.verifyTitle')}</div>
          <p style={{ fontSize: 13, color: 'var(--muted)', marginBottom: 12 }}>
            <Rich text={t('calib.verifyBody')} />
          </p>
          <div
            style={{
              width: mmToCssPx(20, saved),
              height: mmToCssPx(20, saved),
              background: 'linear-gradient(135deg, #ff8a5b, #ff5c86)',
              borderRadius: 8,
              margin: '0 auto',
              boxShadow: 'var(--shadow-coral)',
            }}
          />
        </div>
      )}
    </div>
  )
}
```
  **注意**：`calib.instruction` 的返回值以前直接放在 `<p>` 里，现在长边分支仍是纯字符串、短边分支是 `<Rich>`，两者都合法（`fq-sub` 的 `<p>` 可以放元素）。
- [ ] **Step 8: 全量回归** —— `npm test` 预期 **436 passed / 47 files**（421 + 14 + 1）；`npm run typecheck` 无输出。
- [ ] **Step 9: 手测标定页三种情形** ——
  1. `preview_start`；`resize_window { "width": 1024, "height": 768 }`；`javascript_tool`：
     `localStorage.setItem('fzp.onboarded','1');localStorage.setItem('fzp.lang','zh');localStorage.removeItem('fzp.cssPxPerMm');location.reload();'seeded'`
  2. 进入设置页（点 ⚙️）→ 点"去标定 / 重新标定"按钮（`read_page` + `find` 取 ref）。
  3. `javascript_tool`：
     ```js
     (() => { const b=document.querySelector('.fq-cta'); const r=document.querySelector('input[type=range]'); return JSON.stringify({avail:document.documentElement.clientWidth,mode:document.body.innerText.includes('长边模式')?'long':'short',rangeMax:r&&r.max,saveDisabled:b&&b.disabled}) })()
     ```
     **预期（iPad 横屏）**：`mode === 'long'`、`rangeMax === '994'`、`saveDisabled === false`。
  4. 用 `javascript_tool` 模拟保存一个真值：`localStorage.setItem('fzp.cssPxPerMm','6.05');location.reload();'set'`，再回到标定页，断言 `saveDisabled === false`（1024 屏放得下 518px）。
  5. `resize_window { "width": 375, "height": 812 }`，重跑 Step 3 脚本。**预期**：`mode === 'short'`、`rangeMax === '345'`、`saveDisabled === false`（短边模式够得到 6.05），且页面出现"短边模式"chip 与 `calib.shortEdgeWhy` 卡片。
  6. 把参照带拉到最大验证可达比值：`javascript_tool` 执行
     `(()=>{const r=document.querySelector('input[type=range]');r.value=r.max;r.dispatchEvent(new Event('input',{bubbles:true}));return 'moved'})()`
     然后读页面文本里的 px/mm 数值（`document.body.innerText.match(/([\d.]+)\s*px\/mm/)`），**预期 ≥ 6.39**（覆盖真机 6.05 —— 这正是短边模式的意义；改造前长边模式在此只能到 4.03）。
  7. 验证覆盖闸门：`javascript_tool` 执行 `localStorage.setItem('fzp.cssPxPerMm','9.5');location.reload();'set'`（模拟一台高密度屏上标好、拿到窄屏打开），回标定页断言 `saveDisabled === true` 且页面含 `calib.cantSave` 与 `calib.savedMismatch` 文案（`document.body.innerText.includes('不要保存')`）。
  8. `read_console_messages { "onlyErrors": true }` 预期空；清理 `fzp.cssPxPerMm` / `fzp.onboarded` / `fzp.lang`。
- [ ] **Step 10: 提交** —— `git add -A && git commit`，信息：`feat(calib): 窄屏改用卡片短边标定 + 够不到已存值时禁止保存（堵住静默低估 33% 的路径，14 单测）`。

---

### Task 6: 其余页面竖屏修补（首页 / 统计 / 勋章墙 / 图鉴 / 设置）

**Files**
- Modify: `E:\developer\fzp\src\HomePage.tsx`
- Modify: `E:\developer\fzp\src\stats\StatsPage.tsx`
- Modify: `E:\developer\fzp\src\badges\BadgeCard.tsx`
- Modify: `E:\developer\fzp\src\dex\DexWall.tsx`
- Test（既有，必须保持绿）: `E:\developer\fzp\src\badges\BadgeCard.test.ts`（`spritePos` 未动）

**Interfaces**
- Consumes: `.fq-page` 的手机内边距（Task 2）
- Produces: 无新导出；只把三处"写死列宽/不换行"的 flex 改成可伸缩

- [ ] **Step 1: 首页两列入口允许换行** —— `src/HomePage.tsx` 第 143 行
  `        <div className="fq-rise" style={{ display: 'flex', gap: 12, animationDelay: '0.16s' }}>`
  →
  `        <div className="fq-rise" style={{ display: 'flex', flexWrap: 'wrap', gap: 12, animationDelay: '0.16s' }}>`
  并把这两个按钮的 `flex: 1,` 都改成 `flex: '1 1 150px',`（第一个在 `border: '1.5px solid var(--violet)'` 那行的对象里，第二个在 `border: '1.5px solid var(--coral)'` 那行的对象里）。窄到 320px 时两列会自动堆成一列，而不是各挤到 140px。
- [ ] **Step 2: 统计页周报三列允许换行** —— `src/stats/StatsPage.tsx` 第 77 行
  `        <div style={{ display: 'flex', gap: 12, marginBottom: 12 }}>`
  →
  `        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 12, marginBottom: 12 }}>`
  并把该块内三处 `<div style={{ flex: 1 }}>`（分别紧跟 `report.thisWeekCount`、`report.avgReactionSec`、`report.accuracy` 三段）都改成 `<div style={{ flex: '1 1 88px' }}>`。
- [ ] **Step 3: 勋章卡宽度可伸缩** —— `src/badges/BadgeCard.tsx` 第 57 行 `        width: 132,` →

```tsx
        // 在 flexWrap 容器里可伸缩：iPad 一行 3 张、手机竖屏一行 2 张且填满宽度
        flex: '1 1 132px',
        maxWidth: 168,
```
  **不要动**下面 96×96 的图标区（`backgroundSize: '400% 400%'` 的 4×4 切片与正方形容器绑定，改尺寸会错位）。
- [ ] **Step 4: 图鉴卡宽度可伸缩** —— `src/dex/DexWall.tsx` 第 162 行 `        width: 108,` →

```tsx
        flex: '1 1 108px',
        maxWidth: 132,
```
  **不要动** 80×80 的图片区与放大 modal（`position:'fixed'` + `maxWidth:300` + 160×160 图，窄屏本就够）。
- [ ] **Step 5: 全量回归** —— `npm test` 预期 **436 passed / 47 files**；`npm run typecheck` 无输出。
- [ ] **Step 6: 手测——全站 375×812 零横向溢出** ——
  1. `preview_start`；`resize_window { "width": 375, "height": 812 }`；`javascript_tool` 种子（同 Task 2 Step 12.3）。
  2. 对 5 个导航页（🏠 首页 / 📊 统计 / 🏅 勋章 / ⚙️ 设置；训练页已在 Task 3 测过）逐个：`read_page` 取 ref → `computer left_click` → `computer wait 1` → `javascript_tool`：
     ```js
     (() => { const m=document.querySelector('.fq-app-main'); const over=[...document.querySelectorAll('.fq-app-main *')].filter(e=>e.getBoundingClientRect().right > innerWidth + 1).map(e=>e.className||e.tagName).slice(0,8); return JSON.stringify({mainScrollW:m.scrollWidth,mainClientW:m.clientWidth,docScrollW:document.documentElement.scrollWidth,vw:innerWidth,overflowing:over}) })()
     ```
     **预期每页**：`mainScrollW <= mainClientW + 1`、`docScrollW <= vw + 1`、`overflowing` 为空数组。
  3. 勋章页额外断言列数与填充：`(()=>{const c=[...document.querySelectorAll('.fq-app-main section > div > div')].slice(0,2).map(e=>Math.round(e.getBoundingClientRect().width));return JSON.stringify(c)})()` —— 预期两张卡宽度相等且都在 150-168 之间（一行 2 张、填满 347px 内容宽）。
  4. 图鉴页（首页"📖 图鉴"卡进入）同样断言无溢出，且每行 3 张（卡宽 105-115）。
  5. `resize_window { "width": 768, "height": 1024 }` 与 `{ "width": 1024, "height": 768 }` 各重跑一遍 2 的脚本，确认 iPad 两向也零溢出；勋章页应回到一行 3 张（卡宽 130-140）。
  6. `read_console_messages { "onlyErrors": true }` 预期空；清理 localStorage。
- [ ] **Step 7: 设置页只做复核（不改代码）** —— 375×812 下打开设置页，确认：4 个 `.fq-seg` 分段器（时长 60/120/180/300、翻拍 600/900/1500、拍子度数、语言）都不溢出（既有 `flexWrap: 'wrap'` 已兜住）、视标预览 E 的 `svg` 宽仍是 `sizeMm × pxPerMm`、皮肤预览框 200×200 正常。**结论应为"无需改动"**，写进提交信息。
- [ ] **Step 8: 提交** —— `git add -A && git commit`，信息：`fix(ui): 首页/统计/勋章/图鉴窄屏可伸缩排布，375 宽零横向溢出（设置页复核无需改动）`。

---

### Task 7: 收尾——全量验证、文档口径更新、边界变更报备

**Files**
- Modify: `E:\developer\fzp\CLAUDE.md`
- Modify: `E:\developer\fzp\README.md`
- Modify: `E:\developer\fzp\docs\需求文档.md`

**Interfaces**
- Consumes: 前 6 个任务的全部产出
- Produces: 文档口径一致（平台边界从"只做 iPad"改为"iPad + 手机竖屏"）

- [ ] **Step 1: 全量验证四连** —— 依次跑并记录输出：
  1. `npm test` → 预期 `Test Files 47 passed (47)` / `Tests 436 passed (436)`
  2. `npm run typecheck` → 无输出
  3. `npm run typecheck:functions` → 无输出
  4. `npm run build` → 成功，且末尾 PWA 汇总里 `precache` 条目数与改造前同量级（本迭代没加静态资源）
- [ ] **Step 2: 生产构建下再手测一遍关键页** —— `preview_start { "name": "focal-quest-static" }`（裸静态服务器，端口 4173，真实模拟线上）→ `resize_window { "width": 375, "height": 812 }` → 种子 localStorage → 进训练页 → 重跑 Task 3 Step 10.5 的量尺脚本，断言同样四条（方向盘在首屏、不滚、`svg.w ≈ 3.64`）。另 `javascript_tool` 读 `crossOriginIsolated`，预期 `true`（确认本迭代没碰坏 SW 补头机制）。测完清理 localStorage 并 `preview_stop`。
- [ ] **Step 3: CLAUDE.md —— 关键决策 1 改口径** —— 把第 54 行
  `1. **平台：只做 iPad Web**。不做手机 / 电脑 / 原生 App / Flutter（远期才议）。`
  替换为
  `1. **平台：iPad Web 为主，手机竖屏已适配**（迭代 3d：断点 560px、导航窄屏只留图标、皮肤画布 100cqh 自适应、标定窄屏走卡片短边）。仍不做电脑 / 电视 / 原生 App / Flutter（远期才议）。**手机横屏不是目标形态**，只保证不破版。`
- [ ] **Step 4: CLAUDE.md —— 状态条目** —— 在第 50 行 `  - 待办：3c 多档案 → 3d 手机竖屏 → 3e 管理后台。` 之前插入：

```
  - **3d 手机竖屏适配 ✅ 完成**：①App 外壳改 `height:100dvh` 一列 flex（导航固定 + `.fq-app-main` 自己滚），**干掉全站三处写死的 `calc(100vh - 57px)`**——那个 57 在手机上实际是 99（导航折两行），正是"↓ 方向键掉出首屏 24px"的根因；②单一手机断点 `@media (max-width:560px)`（560 = 英文导航单行所需 531px 的上取整余量）：导航只留图标（文案 `aria-label` 兜无障碍）、方向盘 210→240px/间距 7→10px、语音提示从方向盘左侧改成上方整行；矮屏断点 `@media (max-height:560px)` 让方向盘按 34dvh 收缩；③皮肤方形画布改 `width: min(100%, 420px, 100cqh)`（**容器查询单位拿到舞台剩余高度，纯 CSS 无需 JS 测量**），画布内所有装饰改 `cqmin` 等比，sprite 逐帧动画的 `background-size`/`background-position` 也改 cqmin 才不错帧；④**顺带修正神庙皮肤的医学偏差**——原 `scale(0.8)` 套在视标祖先上，E 只有标定值的 80%，现把 0.8 折进容器 `width/height` 并从 `fzpFloat`/`fzpShakeG` 每帧删掉 scale，视标从此严格等于 `毫米 × 标定`（⚠️ 神庙历史数据与新数据不可比）；⑤**标定页堵住一条静默破坏医学参数的路**——手机竖屏放不下银行卡长边（85.6mm，375 屏最多只到 4.03 px/mm 而真值约 6.05），现窄屏自动改用**卡片短边 53.98mm**（可达 6.39），且"当前屏够不到已存值"时**禁用保存 + 明确警告**，不再一次误触把 px/mm 低估 33%；⑥`viewport` 补 `viewport-fit=cover` + 全站 `env(safe-area-inset-*)`（都带 `, 0px` 兜底），manifest 显式 `orientation: 'any'`；`user-scalable=no` 刻意保留（双指缩放会毁掉物理标定）。⑦新增 `src/layout/layout-budget.ts` 作为 CSS 数字的唯一出处 + `css-contract.test.ts` **读 index.css 文本核对**（防 TS/CSS 漂移，含"`calc(100vh - 57px)` 不许回来"这条闸门）。**436 单测绿 / 47 文件**。
```
- [ ] **Step 5: README.md —— 关键决策 1** —— 把第 183 行
  `1. **只做 iPad Web**——不做手机/电脑/原生 App/Flutter。`
  替换为
  `1. **iPad Web 为主，手机竖屏已适配**（迭代 3d）——不做电脑/电视/原生 App/Flutter；手机横屏只保证不破版。`
- [ ] **Step 6: 需求文档 —— 排除项表格** —— 把第 81 行
  `| 手机 / 电脑 / 电视端 | 聚焦降低复杂度；手机小屏与 40cm 视标空间冲突 | 远期再议 |`
  替换为
  `| ~~手机~~ / 电脑 / 电视端 | 电脑电视：聚焦降低复杂度。**手机已在迭代 3d 支持竖屏**——原顾虑"小屏与 40cm 视标空间冲突"实测不成立：视标本体只有约 3.6 CSS px（0.7mm × 5.2 px/mm），挤的是它周围的装饰舞台，靠 `100cqh` 自适应即可，视标物理尺寸一像素未动 | 电脑/电视远期再议 |`
- [ ] **Step 7: 需求文档 —— iPad 适配条** —— 把第 161 行
  `- **iPad 适配**：横竖屏择一固定（建议横屏），防误触，支持"添加到主屏"以全屏 PWA 方式运行。`
  替换为
  `- **多形态适配**（迭代 3d 修订，原为"横竖屏择一固定"）：iPad 横竖屏 + 手机竖屏都完整可用，不锁方向（manifest `orientation: 'any'`）；手机横屏只保证不破版并提示"竖屏更好用"。防误触靠方向盘尺寸/间距（手机 240px / 间距 10px，单键约 73×73），支持"添加到主屏"以全屏 PWA 方式运行。`
- [ ] **Step 8: 文档一致性自检** —— `grep -rn "只做 iPad" CLAUDE.md README.md docs/需求文档.md` 预期零命中；`grep -rn "calc(100vh - 57px)" src/` 预期零命中。
- [ ] **Step 9: 提交** —— `git add -A && git commit`，信息：`docs: 平台边界口径改为「iPad + 手机竖屏」，记录迭代 3d 完成（436 单测绿）`。
- [ ] **Step 10: 向用户报备两处行为变更（不要自行决定）** —— 在最终汇报里明确列出，请用户确认：
  1. **平台边界推翻**：`CLAUDE.md` 关键决策 1 / `README.md:183` / `docs/需求文档.md:81,161` 四处原本都写"只做 iPad、手机远期再议"，本迭代显式改口径。
  2. **神庙皮肤视标变大 25%**（原 `scale(0.8)` 是既有 bug，修正后 E 等于设定值）——同一孩子换皮肤不再偷偷改训练强度，但**神庙皮肤的历史训练数据与今后的不可比**；若父女实测觉得"神庙突然变简单"，可在设置页把视标毫米数下调一档（0.7 → 0.6mm）而**不要**把 scale 加回去。
  3. 建议真机复测：手机竖屏走一遍完整流程（首页 → 自动开始 → 答题 → 翻拍 → 换眼 → 打卡 → 勋章/开箱），以及**在 iPad 上重新标定一次**确认 px/mm 未被之前的竖屏误触写坏（设置页顶部卡片会显示当前值）。
