# 迭代 2 勋章墙 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加 30 个勋章的勋章墙：从 sessions/checkins 判定达成、Dexie 持久化解锁、勋章墙点亮/置灰/进度/稀有度边框、训练完成时检测新解锁并庆祝。

**Architecture:** 勋章定义是纯数据；判定（衍生指标 + 阈值比对）是无副作用纯函数 TDD；`badge-service` 编排读写 Dexie（v2 加 badges 表）；勋章墙与训练页消费 service。emoji 占位，图片后续替换不动逻辑。

**Tech Stack:** TypeScript · React 18 · Vite 6 · Vitest 2 · Dexie（升级 v2）· 复用 1a/1b 的 cpm/db/sfx

## Global Constraints

- 30 个勋章，6 类（连续/节数/时长/CPM/正确率/答对），稀有度四级（bronze/silver/gold/rainbow）。数据见 Task 1（源自 spec 第 3 节）。
- 判定：`evaluate` 返回 `stats[metric] >= threshold` 的 badge id。
- 衍生指标口径：`maxStreak`=max(checkins.streak)；`maxAccuracy` **仅统计 answered≥5 的节**；`maxCpm`=max `cpm(flips,elapsedSec)`。
- Dexie **version(2)** 加 `badges` 表（主键 `id`），不动 sessions/checkins。
- 训练完成打卡后 + 打开勋章墙都调 `syncBadges`（检测+补发）。
- 稀有度边框配色：铜 `#cd7f32`、银 `#9aa4ad`、金 `#e0a92c`、彩虹渐变描边。
- emoji 占位，图片后续替换。
- 纯逻辑 TDD；DB/UI/解锁流程手动验证。
- **执行 git 用 PowerShell**；commit 末尾附 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

---

## 文件结构

```
src/badges/
  badge-defs.ts / .test.ts   # Task1  30勋章数据 + Rarity/Metric 类型（数据 sanity 测试）
  stats-derive.ts / .test.ts # Task2  deriveStats（纯函数）
  evaluate.ts / .test.ts     # Task3  evaluate（纯函数）
  badge-service.ts           # Task4  syncBadges/getUnlockedIds（编排）
  BadgeCard.tsx              # Task5  单格
  BadgeWall.tsx              # Task5  勋章墙
src/data/db.ts               # Task4  升级 v2 加 badges 表（modify）
src/training/sfx.ts          # Task6  加 'badge'（modify）
src/training/TrainingPage.tsx # Task6 打卡后 syncBadges + 庆祝（modify）
src/App.tsx                  # Task5  加勋章墙导航（modify）
```

---

### Task 1: 勋章定义（badge-defs，数据 + sanity 测试）

**Files:**
- Create: `src/badges/badge-defs.ts`
- Test: `src/badges/badge-defs.test.ts`

**Interfaces:**
- Produces:
  - `type Rarity = 'bronze' | 'silver' | 'gold' | 'rainbow'`
  - `type Metric = 'maxStreak' | 'totalSessions' | 'totalSec' | 'maxCpm' | 'maxAccuracy' | 'totalCorrect'`
  - `interface BadgeDef { id: string; name: string; emoji: string; metric: Metric; threshold: number; rarity: Rarity }`
  - `BADGES: BadgeDef[]`（30 个）

- [ ] **Step 1: 写实现 `src/badges/badge-defs.ts`**

```ts
export type Rarity = 'bronze' | 'silver' | 'gold' | 'rainbow'
export type Metric = 'maxStreak' | 'totalSessions' | 'totalSec' | 'maxCpm' | 'maxAccuracy' | 'totalCorrect'

export interface BadgeDef {
  id: string
  name: string
  emoji: string
  metric: Metric
  threshold: number
  rarity: Rarity
}

export const BADGES: BadgeDef[] = [
  { id: 'streak-2', name: '小火苗', emoji: '🌟', metric: 'maxStreak', threshold: 2, rarity: 'bronze' },
  { id: 'streak-3', name: '闪闪萤火虫', emoji: '✨', metric: 'maxStreak', threshold: 3, rarity: 'bronze' },
  { id: 'streak-5', name: '暖暖小太阳', emoji: '🔆', metric: 'maxStreak', threshold: 5, rarity: 'bronze' },
  { id: 'streak-7', name: '一周小火龙', emoji: '🔥', metric: 'maxStreak', threshold: 7, rarity: 'bronze' },
  { id: 'streak-10', name: '十日星星侠', emoji: '⭐', metric: 'maxStreak', threshold: 10, rarity: 'silver' },
  { id: 'streak-15', name: '半月月亮猫', emoji: '🎖️', metric: 'maxStreak', threshold: 15, rarity: 'silver' },
  { id: 'streak-21', name: '习惯小水晶', emoji: '💠', metric: 'maxStreak', threshold: 21, rarity: 'silver' },
  { id: 'streak-30', name: '月度小皇冠', emoji: '👑', metric: 'maxStreak', threshold: 30, rarity: 'gold' },
  { id: 'streak-60', name: '双月钻石龙', emoji: '💎', metric: 'maxStreak', threshold: 60, rarity: 'gold' },
  { id: 'streak-100', name: '百日彩虹凤凰', emoji: '🏵️', metric: 'maxStreak', threshold: 100, rarity: 'rainbow' },
  { id: 'sessions-1', name: '破土小芽', emoji: '🌱', metric: 'totalSessions', threshold: 1, rarity: 'bronze' },
  { id: 'sessions-5', name: '冒险小苗', emoji: '🌿', metric: 'totalSessions', threshold: 5, rarity: 'bronze' },
  { id: 'sessions-10', name: '大力小勇士', emoji: '💪', metric: 'totalSessions', threshold: 10, rarity: 'silver' },
  { id: 'sessions-25', name: '机灵小工匠', emoji: '🔧', metric: 'totalSessions', threshold: 25, rarity: 'silver' },
  { id: 'sessions-50', name: '半百小冠军', emoji: '🏆', metric: 'totalSessions', threshold: 50, rarity: 'gold' },
  { id: 'sessions-100', name: '百炼小铁人', emoji: '⚙️', metric: 'totalSessions', threshold: 100, rarity: 'gold' },
  { id: 'sessions-200', name: '训练大师石像', emoji: '🗿', metric: 'totalSessions', threshold: 200, rarity: 'rainbow' },
  { id: 'time-30m', name: '小闹钟卫士', emoji: '⏱️', metric: 'totalSec', threshold: 1800, rarity: 'bronze' },
  { id: 'time-2h', name: '沙漏小精灵', emoji: '⌛', metric: 'totalSec', threshold: 7200, rarity: 'silver' },
  { id: 'time-10h', name: '时光老爷爷钟', emoji: '🕰️', metric: 'totalSec', threshold: 36000, rarity: 'gold' },
  { id: 'cpm-6', name: '稳稳小乌龟', emoji: '🐢', metric: 'maxCpm', threshold: 6, rarity: 'bronze' },
  { id: 'cpm-8', name: '蹦跳小兔子', emoji: '🐰', metric: 'maxCpm', threshold: 8, rarity: 'bronze' },
  { id: 'cpm-10', name: '疾风小猎豹', emoji: '🐆', metric: 'maxCpm', threshold: 10, rarity: 'silver' },
  { id: 'cpm-12', name: '冲天小火箭', emoji: '🚀', metric: 'maxCpm', threshold: 12, rarity: 'gold' },
  { id: 'cpm-15', name: '闪电小超人', emoji: '⚡', metric: 'maxCpm', threshold: 15, rarity: 'rainbow' },
  { id: 'acc-90', name: '神箭小射手', emoji: '🎯', metric: 'maxAccuracy', threshold: 0.9, rarity: 'silver' },
  { id: 'acc-100', name: '火眼金睛', emoji: '💯', metric: 'maxAccuracy', threshold: 1.0, rarity: 'rainbow' },
  { id: 'correct-100', name: '百题小学霸', emoji: '✅', metric: 'totalCorrect', threshold: 100, rarity: 'silver' },
  { id: 'correct-500', name: '五百智慧星', emoji: '📗', metric: 'totalCorrect', threshold: 500, rarity: 'gold' },
  { id: 'correct-1000', name: '千题大博士', emoji: '📚', metric: 'totalCorrect', threshold: 1000, rarity: 'rainbow' },
]
```

- [ ] **Step 2: 写 sanity 测试 `src/badges/badge-defs.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { BADGES } from './badge-defs'

describe('BADGES', () => {
  it('has 30 badges', () => {
    expect(BADGES).toHaveLength(30)
  })
  it('all ids are unique', () => {
    expect(new Set(BADGES.map((b) => b.id)).size).toBe(30)
  })
  it('every threshold is positive', () => {
    expect(BADGES.every((b) => b.threshold > 0)).toBe(true)
  })
  it('exactly 5 rainbow badges', () => {
    expect(BADGES.filter((b) => b.rarity === 'rainbow')).toHaveLength(5)
  })
})
```

- [ ] **Step 3: 跑测试确认通过**

Run: `npm run test -- "badge-defs"`
Expected: PASS（4 tests）。

- [ ] **Step 4: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/badges/badge-defs.ts src/badges/badge-defs.test.ts
git -C "E:\developer\fzp" commit -m @'
feat(2): 30 勋章定义（名字/emoji/指标/阈值/稀有度）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 2: 衍生指标（stats-derive，纯函数 TDD）

**Files:**
- Create: `src/badges/stats-derive.ts`
- Test: `src/badges/stats-derive.test.ts`

**Interfaces:**
- Consumes: `SessionRow`/`CheckinRow`（`src/data/db.ts`）、`cpm`（`src/training/cpm.ts`）。
- Produces:
  - `interface BadgeStats { maxStreak: number; totalSessions: number; totalSec: number; maxCpm: number; maxAccuracy: number; totalCorrect: number }`
  - `deriveStats(sessions: SessionRow[], checkins: CheckinRow[]): BadgeStats`

- [ ] **Step 1: 写失败测试 `src/badges/stats-derive.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { deriveStats } from './stats-derive'
import type { SessionRow, CheckinRow } from '../data/db'

function mk(over: Partial<SessionRow>): SessionRow {
  return { date: '2026-07-07', startedAtMs: 0, eye: 'left', answered: 10, correct: 8, flips: 20, elapsedSec: 120, acuity: 0.8, ...over }
}
function ck(over: Partial<CheckinRow>): CheckinRow {
  return { date: '2026-07-07', streak: 1, dailyPoints: 30, totalPoints: 30, ...over }
}

describe('deriveStats', () => {
  it('empty → all zero', () => {
    expect(deriveStats([], [])).toEqual({
      maxStreak: 0, totalSessions: 0, totalSec: 0, maxCpm: 0, maxAccuracy: 0, totalCorrect: 0,
    })
  })

  it('maxStreak is the max checkin streak', () => {
    expect(deriveStats([], [ck({ streak: 3 }), ck({ date: '2026-07-08', streak: 7 })]).maxStreak).toBe(7)
  })

  it('totals and maxCpm', () => {
    const out = deriveStats([mk({ flips: 20, elapsedSec: 60, correct: 8 }), mk({ flips: 10, elapsedSec: 120, correct: 5 })], [])
    expect(out.totalSessions).toBe(2)
    expect(out.totalSec).toBe(180)
    expect(out.totalCorrect).toBe(13)
    // cpm(20,60)=10 vs cpm(10,120)=2.5 → max 10
    expect(out.maxCpm).toBeCloseTo(10, 6)
  })

  it('maxAccuracy only counts sessions with answered>=5', () => {
    // a perfect 1/1 session must NOT count; the 8/10 one should
    const out = deriveStats([mk({ answered: 1, correct: 1 }), mk({ answered: 10, correct: 8 })], [])
    expect(out.maxAccuracy).toBeCloseTo(0.8, 6)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- "stats-derive"`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/badges/stats-derive.ts`**

```ts
import type { SessionRow, CheckinRow } from '../data/db'
import { cpm } from '../training/cpm'

export interface BadgeStats {
  maxStreak: number
  totalSessions: number
  totalSec: number
  maxCpm: number
  maxAccuracy: number
  totalCorrect: number
}

export function deriveStats(sessions: SessionRow[], checkins: CheckinRow[]): BadgeStats {
  return {
    maxStreak: checkins.reduce((m, c) => Math.max(m, c.streak), 0),
    totalSessions: sessions.length,
    totalSec: sessions.reduce((a, s) => a + s.elapsedSec, 0),
    maxCpm: sessions.reduce((m, s) => Math.max(m, cpm(s.flips, s.elapsedSec)), 0),
    maxAccuracy: sessions.reduce((m, s) => (s.answered >= 5 ? Math.max(m, s.correct / s.answered) : m), 0),
    totalCorrect: sessions.reduce((a, s) => a + s.correct, 0),
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- "stats-derive"`
Expected: PASS。

- [ ] **Step 5: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/badges/stats-derive.ts src/badges/stats-derive.test.ts
git -C "E:\developer\fzp" commit -m @'
feat(2): 勋章衍生指标 deriveStats（正确率仅算答满5题的节）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 3: 判定（evaluate，纯函数 TDD）

**Files:**
- Create: `src/badges/evaluate.ts`
- Test: `src/badges/evaluate.test.ts`

**Interfaces:**
- Consumes: `BADGES`（Task1）、`BadgeStats`（Task2）。
- Produces: `evaluate(stats: BadgeStats): string[]`（已达成 badge id）。

- [ ] **Step 1: 写失败测试 `src/badges/evaluate.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { evaluate } from './evaluate'
import type { BadgeStats } from './stats-derive'

const ZERO: BadgeStats = { maxStreak: 0, totalSessions: 0, totalSec: 0, maxCpm: 0, maxAccuracy: 0, totalCorrect: 0 }

describe('evaluate', () => {
  it('zero stats → nothing achieved', () => {
    expect(evaluate(ZERO)).toEqual([])
  })
  it('1 session unlocks sessions-1 only (among sessions)', () => {
    const out = evaluate({ ...ZERO, totalSessions: 1 })
    expect(out).toContain('sessions-1')
    expect(out).not.toContain('sessions-5')
  })
  it('threshold is inclusive (>=)', () => {
    expect(evaluate({ ...ZERO, maxStreak: 7 })).toContain('streak-7')
  })
  it('maxCpm 12 unlocks cpm-6/8/10/12 but not cpm-15', () => {
    const out = evaluate({ ...ZERO, maxCpm: 12 })
    expect(out).toEqual(expect.arrayContaining(['cpm-6', 'cpm-8', 'cpm-10', 'cpm-12']))
    expect(out).not.toContain('cpm-15')
  })
  it('perfect accuracy unlocks acc-90 and acc-100', () => {
    const out = evaluate({ ...ZERO, maxAccuracy: 1.0 })
    expect(out).toEqual(expect.arrayContaining(['acc-90', 'acc-100']))
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test -- "src/badges/evaluate"`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 写实现 `src/badges/evaluate.ts`**

```ts
import { BADGES } from './badge-defs'
import type { BadgeStats } from './stats-derive'

/** 返回已达成（stats[metric] >= threshold）的 badge id 列表 */
export function evaluate(stats: BadgeStats): string[] {
  return BADGES.filter((b) => stats[b.metric] >= b.threshold).map((b) => b.id)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test -- "src/badges/evaluate"`
Expected: PASS。

- [ ] **Step 5: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/badges/evaluate.ts src/badges/evaluate.test.ts
git -C "E:\developer\fzp" commit -m @'
feat(2): 勋章判定 evaluate（指标>=阈值）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 4: Dexie v2 + 勋章编排（db + badge-service，手动验证）

**Files:**
- Modify: `src/data/db.ts`
- Create: `src/badges/badge-service.ts`

**Interfaces:**
- Consumes: `db`（升级后含 badges）、`deriveStats`（Task2）、`evaluate`（Task3）、`BADGES`/`BadgeDef`（Task1）。
- Produces:
  - `interface BadgeRow { id: string; unlockedAt: number }`（db.ts）
  - `syncBadges(now: number): Promise<BadgeDef[]>`（新解锁的勋章定义）
  - `getUnlockedIds(): Promise<Set<string>>`

- [ ] **Step 1: 修改 `src/data/db.ts` 升级 v2 加 badges 表**

在 `CheckinRow` 之后加接口：

```ts
export interface BadgeRow {
  id: string
  unlockedAt: number
}
```

在 `FocalQuestDB` 类里，`checkins` 字段后加：

```ts
  badges!: Table<BadgeRow, string>
```

在构造函数里，`this.version(1).stores({...})` 之后加一行：

```ts
    this.version(2).stores({
      badges: 'id',
    })
```
（Dexie 累积 schema：v2 只声明新增的 badges 表，sessions/checkins 自动保留。）

- [ ] **Step 2: 写 `src/badges/badge-service.ts`**

```ts
import { db } from '../data/db'
import { deriveStats } from './stats-derive'
import { evaluate } from './evaluate'
import { BADGES, type BadgeDef } from './badge-defs'

/** 检测并写入新解锁的勋章，返回新解锁的定义列表 */
export async function syncBadges(now: number): Promise<BadgeDef[]> {
  const [sessions, checkins, existing] = await Promise.all([
    db.sessions.toArray(),
    db.checkins.toArray(),
    db.badges.toArray(),
  ])
  const achieved = new Set(evaluate(deriveStats(sessions, checkins)))
  const have = new Set(existing.map((b) => b.id))
  const newIds = [...achieved].filter((id) => !have.has(id))
  if (newIds.length > 0) {
    await db.badges.bulkPut(newIds.map((id) => ({ id, unlockedAt: now })))
  }
  const byId = new Map(BADGES.map((b) => [b.id, b]))
  return newIds.map((id) => byId.get(id)).filter((b): b is BadgeDef => b !== undefined)
}

export async function getUnlockedIds(): Promise<Set<string>> {
  const rows = await db.badges.toArray()
  return new Set(rows.map((b) => b.id))
}
```

- [ ] **Step 3: 全量测试 + 类型检查**

Run: `npm run test`
Expected: PASS（含 badge-defs/stats-derive/evaluate 新测试）。
Run: `npx tsc --noEmit`
Expected: exit 0。

- [ ] **Step 4: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/data/db.ts src/badges/badge-service.ts
git -C "E:\developer\fzp" commit -m @'
feat(2): Dexie v2 加 badges 表 + 解锁编排 syncBadges/getUnlockedIds

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 5: 勋章墙 UI（BadgeCard + BadgeWall + 导航）

**Files:**
- Create: `src/badges/BadgeCard.tsx`
- Create: `src/badges/BadgeWall.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `BadgeDef`/`Rarity`/`BADGES`（Task1）、`deriveStats`/`BadgeStats`（Task2）、`syncBadges`/`getUnlockedIds`（Task4）、`db`（读 sessions/checkins）。
- Produces: `BadgeWall(): JSX.Element`、`BadgeCard(props)`。

- [ ] **Step 1: 写 `src/badges/BadgeCard.tsx`**

```tsx
import type { BadgeDef, Rarity } from './badge-defs'

const RARITY_BORDER: Record<Rarity, string> = {
  bronze: '#cd7f32',
  silver: '#9aa4ad',
  gold: '#e0a92c',
  rainbow: '',
}

const METRIC_SUFFIX: Record<BadgeDef['metric'], string> = {
  maxStreak: '天',
  totalSessions: '节',
  totalSec: '秒',
  maxCpm: 'CPM',
  maxAccuracy: '',
  totalCorrect: '题',
}

/** 未解锁时的进度文案，如 "5/7 天" */
function progressText(def: BadgeDef, current: number): string {
  if (def.metric === 'maxAccuracy') {
    return `${Math.round(current * 100)}/${Math.round(def.threshold * 100)}%`
  }
  const cur = def.metric === 'totalSec' ? Math.round(current / 60) : Math.floor(current)
  const thr = def.metric === 'totalSec' ? Math.round(def.threshold / 60) : def.threshold
  const suffix = def.metric === 'totalSec' ? '分' : METRIC_SUFFIX[def.metric]
  return `${cur}/${thr}${suffix}`
}

export function BadgeCard({ def, unlocked, current }: { def: BadgeDef; unlocked: boolean; current: number }) {
  const rainbow = def.rarity === 'rainbow'
  const borderStyle: React.CSSProperties = rainbow
    ? { borderImage: 'linear-gradient(135deg,#ff5f6d,#ffc371,#47cf73,#5b8def,#c86dd7) 1', borderWidth: 3, borderStyle: 'solid' }
    : { border: `3px solid ${RARITY_BORDER[def.rarity]}` }

  return (
    <div
      style={{
        width: 92,
        padding: 8,
        borderRadius: 12,
        textAlign: 'center',
        background: '#fff',
        opacity: unlocked ? 1 : 0.45,
        filter: unlocked ? 'none' : 'grayscale(1)',
        ...borderStyle,
      }}
      title={def.name}
    >
      <div style={{ fontSize: 34, lineHeight: 1.1 }}>{def.emoji}</div>
      <div style={{ fontSize: 12, marginTop: 4 }}>{def.name}</div>
      {!unlocked && <div style={{ fontSize: 10, color: '#888', marginTop: 2 }}>{progressText(def, current)}</div>}
    </div>
  )
}
```

- [ ] **Step 2: 写 `src/badges/BadgeWall.tsx`**

```tsx
import { useEffect, useState } from 'react'
import { db } from '../data/db'
import { BADGES } from './badge-defs'
import { deriveStats, type BadgeStats } from './stats-derive'
import { syncBadges, getUnlockedIds } from './badge-service'
import { BadgeCard } from './BadgeCard'

export function BadgeWall() {
  const [unlocked, setUnlocked] = useState<Set<string> | null>(null)
  const [stats, setStats] = useState<BadgeStats | null>(null)

  useEffect(() => {
    ;(async () => {
      await syncBadges(Date.now())
      const [ids, sessions, checkins] = await Promise.all([
        getUnlockedIds(),
        db.sessions.toArray(),
        db.checkins.toArray(),
      ])
      setUnlocked(ids)
      setStats(deriveStats(sessions, checkins))
    })()
  }, [])

  if (unlocked === null || stats === null) return <div style={{ padding: 24 }}>加载中…</div>

  return (
    <div style={{ padding: 24 }}>
      <h2>勋章墙</h2>
      <p style={{ color: '#666' }}>已解锁 {unlocked.size}/{BADGES.length}</p>
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 12 }}>
        {BADGES.map((def) => (
          <BadgeCard key={def.id} def={def} unlocked={unlocked.has(def.id)} current={stats[def.metric]} />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: 修改 `src/App.tsx` 加勋章墙导航**（替换整个文件）

```tsx
import { useState } from 'react'
import { HomePage } from './HomePage'
import { CalibrationPage } from './calibration/CalibrationPage'
import { SpeechTestPage } from './speech/SpeechTestPage'
import { TrainingPage } from './training/TrainingPage'
import { StatsPage } from './stats/StatsPage'
import { BadgeWall } from './badges/BadgeWall'

type View = 'home' | 'train' | 'stats' | 'badges' | 'calib' | 'speech'

export function App() {
  const [view, setView] = useState<View>('home')
  return (
    <div>
      <nav style={{ display: 'flex', gap: 8, padding: 12, borderBottom: '1px solid #ccc', flexWrap: 'wrap' }}>
        <button onClick={() => setView('home')}>首页</button>
        <button onClick={() => setView('train')}>训练</button>
        <button onClick={() => setView('stats')}>统计</button>
        <button onClick={() => setView('badges')}>勋章墙</button>
        <button onClick={() => setView('calib')}>标定</button>
        <button onClick={() => setView('speech')}>语音</button>
      </nav>
      {view === 'home' && <HomePage />}
      {view === 'train' && <TrainingPage />}
      {view === 'stats' && <StatsPage />}
      {view === 'badges' && <BadgeWall />}
      {view === 'calib' && <CalibrationPage />}
      {view === 'speech' && <SpeechTestPage />}
    </div>
  )
}
```

- [ ] **Step 4: 类型检查**

Run: `npx tsc --noEmit`
Expected: exit 0。

- [ ] **Step 5: 手动验证勋章墙**（本机 `npm run dev`，用之前造的训练数据）

1. 点「勋章墙」：看到 30 格；已达成的彩色+稀有度边框，未达成置灰+进度（如"3/7天"）。
2. 顶部"已解锁 X/30"数字合理。
3. 彩虹级（凤凰/石像/博士/超人/火眼金睛）边框是渐变。

- [ ] **Step 6: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/badges/BadgeCard.tsx src/badges/BadgeWall.tsx src/App.tsx
git -C "E:\developer\fzp" commit -m @'
feat(2): 勋章墙 UI（30格点亮/置灰/进度/稀有度边框）+ 接入导航

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 6: 训练解锁庆祝（sfx + TrainingPage 集成 + 走查）

**Files:**
- Modify: `src/training/sfx.ts`
- Modify: `src/training/TrainingPage.tsx`

**Interfaces:**
- Consumes: `syncBadges`/`BadgeDef`（Task4）、`playSfx('badge')`。

- [ ] **Step 1: 修改 `src/training/sfx.ts` 加 'badge'**

把 `SfxKind` 类型改为：

```ts
type SfxKind = 'correct' | 'wrong' | 'flip' | 'finish' | 'checkin' | 'badge'
```

在 `playSfx` 的 switch 中 `case 'checkin'` 之后加：

```ts
    case 'badge':
      ;[659, 988, 1319, 1760].forEach((f, i) => tone(f, i * 0.09, 0.2))
      break
```

- [ ] **Step 2: 修改 `src/training/TrainingPage.tsx` 打卡后检测新勋章**

在 import 区加：

```tsx
import { syncBadges } from '../badges/badge-service'
import type { BadgeDef } from '../badges/badge-defs'
```

在组件里 `checkin` state 附近加新 state：

```tsx
  const [newBadges, setNewBadges] = useState<BadgeDef[]>([])
```

把 `nextEyeOrFinish` 的 else 分支（右眼完成）改为：

```tsx
    } else {
      const result = await doCheckIn(toDateStr(new Date()))
      const unlocked = await syncBadges(Date.now())
      playSfx(unlocked.length > 0 ? 'badge' : 'checkin')
      setNewBadges(unlocked)
      setCheckin(result)
    }
```

在 `checkin` 结算画面（`if (checkin) { return ... }`）里，`累计 {checkin.totalPoints} 分` 那段之后、`</div>` 之前，插入新解锁展示：

```tsx
        {newBadges.length > 0 && (
          <div style={{ marginTop: 16 }}>
            <p style={{ fontWeight: 700 }}>🎉 解锁新勋章！</p>
            <div style={{ display: 'flex', gap: 12, justifyContent: 'center', flexWrap: 'wrap' }}>
              {newBadges.map((b) => (
                <div key={b.id} style={{ textAlign: 'center' }}>
                  <div style={{ fontSize: 32 }}>{b.emoji}</div>
                  <div style={{ fontSize: 12 }}>{b.name}</div>
                </div>
              ))}
            </div>
          </div>
        )}
```

- [ ] **Step 3: 全量测试 + 类型检查**

Run: `npm run test`
Expected: PASS。
Run: `npx tsc --noEmit`
Expected: exit 0。

- [ ] **Step 4: 手动走查解锁流程**（本机 `npm run dev`）

1. 清掉 badges 表（DevTools→IndexedDB→focalquest→badges 清空）或用新库。
2. 走完一轮训练（临时把 `DURATION_SEC` 调小），右眼「完成并打卡」。
3. 应看到打卡画面 + "🎉 解锁新勋章"（至少 破土小芽 sessions-1、可能 cpm/streak 等）+ 听到勋章音效。
4. 去「勋章墙」确认这些已点亮。
5. 再练一轮，已解锁的不重复弹（只弹这次新达成的）。

- [ ] **Step 5: Commit**（PowerShell）

```powershell
git -C "E:\developer\fzp" add src/training/sfx.ts src/training/TrainingPage.tsx
git -C "E:\developer\fzp" commit -m @'
feat(2): 训练完成检测并庆祝新解锁勋章 + 勋章音效

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

## Self-Review（计划作者自检）

**1. Spec 覆盖：**
- 30 勋章定义 + 稀有度 → Task1 ✅
- 衍生指标（含 maxAccuracy answered≥5）→ Task2 ✅
- 判定 evaluate → Task3 ✅
- Dexie v2 badges 表 → Task4 ✅
- 解锁编排 syncBadges/补发 → Task4 + Wall/Training 触发 ✅
- 勋章墙点亮/置灰/进度/稀有度边框 → Task5 ✅
- 训练完成检测+庆祝+音效 → Task6 ✅
- 打开墙也检测 → Task5 BadgeWall useEffect syncBadges ✅

**2. Placeholder 扫描：** 无 TBD/TODO；每个 code step 完整代码；修改用精确片段；手动验证含具体操作。

**3. 类型一致性：** `Metric`=`keyof BadgeStats`（evaluate 的 `stats[b.metric]` 类型安全）；`BadgeDef`/`Rarity`/`BADGES`（Task1）→ evaluate/service/UI；`BadgeStats`/`deriveStats`（Task2）→ evaluate/service/Wall；`BadgeRow`/`db.badges`（Task4）→ service；`syncBadges`/`getUnlockedIds`（Task4）→ Wall/Training。签名前后一致。

**4. 方法论：** 纯逻辑（Task1 数据 sanity、Task2-3 TDD）；db/编排/UI/集成（Task4-6）类型检查 + 手动验证。
